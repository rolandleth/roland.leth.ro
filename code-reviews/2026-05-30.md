# Code Review — 2026-05-30

## Issues and Observations

- **next-env.d.ts** — Manually edited an auto-generated file that explicitly says "This file should not be edited." Import path changed from `./.next/types/routes.d.ts` to `./.next/dev/types/routes.d.ts` (Next.js 16 dev-mode path). Risk: a production `yarn build` may regenerate the file pointing back to `./.next/types/...`, or CI's type-check may fail to find routes at the dev-only path. Verify the production build emits typed routes at the same path before relying on this edit; otherwise this is a working-tree workaround that doesn't survive a clean rebuild.
- **next.config.ts — `images.remotePatterns`** — `http://localhost` added inside the `NODE_ENV !== "production"` block alongside picsum hosts. Scoping to non-production is correct; no port restriction is fine since Next.js dev typically runs on a single port. Confirm no environment ever runs Next.js with `NODE_ENV=development` while serving production traffic (preview/staging on Vercel sets `NODE_ENV=production`, so this is safe).

## Risk Assessment

**Low.** A single "minor improvement" commit: a 1-line dev-only image domain (`localhost`), a 1-line margin-bottom (`mb-4`) on the project tablist, an auto-generated `code-reviews/2026-05-24.md` file, and a manual edit to `next-env.d.ts`. Only the last carries any forward risk — and only if Next.js 16 emits the typed-routes file at a different path during production build. No data-model, control-flow, security, or contract changes; nothing touches the cache wrap set or platform-picker mock surface flagged in recent dev-journal watch-outs.

## Notes

- Mode: inline (1 commit, 4 files, +14/-2; one file is the auto-emitted code-reviews artifact).
- Dev-journal watch-outs in `2026-05-22.md` (`MockPlatformPicker.autoFill` union drift, `cacheWrapsAtLoad` snapshot pinning) are untouched.
- `next-env.d.ts` is normally regenerated by Next.js on every `dev`/`build`; if the manual edit is needed because Next.js 16's generator is wrong for this project's TS config, that's worth a one-line note in the project CLAUDE.md so a future agent doesn't "fix" it back.
