# Code Review Monthly Rollup — 2026-06

_Generated 2026-06-10._

Reviews: 1 across 1 day.

## Recurring flags

- **Import-tooling resilience (`scripts/import-projects.ts`)** — the dominant cluster of the month: the "reuse blobs on re-run" guarantee is fragile from several angles at once — `listExistingBlobs` ignores `@vercel/blob` pagination (`cursor`/`hasMore`), a transient `list()` failure downgrades to warn-and-overwrite-everything, and the reuse heuristic keys on `(key, byte size)` rather than a content hash/`etag`. All latent at current data volume; real once a project carries enough blobs or the network blips mid-CI. [source](code-reviews/2026-06-09.md)
- **Duplicated script bootstrap** — `makePrisma` / `DATABASE_URL` guard / per-project try-catch error formatting exist in three near-identical copies across `scripts/` (`import-projects.ts`, `seed-projects.ts`, `seed-posts.ts`). The `localhost:300` seed typo is exactly the failure mode shared scaffolding would prevent. Centralize when a fourth script appears. [source](code-reviews/2026-06-09.md)
- **Error swallowing in scripts** — `processProject` catch-all turns programmer errors (`TypeError`/`ReferenceError`) into indistinguishable `status: "failed"`; both seed scripts string-interpolate errors (`"...: ${error}"`), dropping `.stack`. Rethrow non-`Error`/non-validation cases; use `console.error("...", error)`. [source](code-reviews/2026-06-09.md)
- **AI-tone em-dash cadence in user-facing privacy prose** — single mid-sentence em-dashes in `PrivacyPageLayout` boilerplate and stacked across the three per-app privacy pages (uniform "...PII — no name..." rhythm). Per `~/.claude/rules/ai-tone.md`, vary to a period/colon. [source](code-reviews/2026-06-09.md)

## Test coverage gaps

- **Import shell has zero integration tests** — `loadImages`/`listExistingBlobs`/`resolveImageUrls`/`writeProject`/`processProject` are untested; the load-bearing "second run with same files = zero `put` calls" claim, `--dry-run`, `--reupload`, and the `deleteMany`+`create` transaction all live in the untested shell. First flagged 2026-06-09; open. [source](code-reviews/2026-06-09.md)
- **`projectImport.ts` boundary cases** — no coverage for Windows backslash path separators (`sanitizePathSegment` regex includes `\\` but split is `/`-only), `images: undefined` vs `[]`, mixed hosted/local icon+hero, or a throwing `resolve` callback. First flagged 2026-06-09; open. [source](code-reviews/2026-06-09.md)
- **`projectMappers` empty-array case** — `sections: []` / `links: []` (produces `{ create: [] }`, distinct from `undefined`) untested. First flagged 2026-06-09; open. [source](code-reviews/2026-06-09.md)
- **`PrivacyPageLayout` ordering + degenerate cases** — no test pins boilerplate ("Last update"/"Contact") rendering after page `sections`, nor the `sections: []` boilerplate-only case; `AppleLegalLink` has no test pinning href + accessible name. First flagged 2026-06-09; open. [source](code-reviews/2026-06-09.md)

## Follow-ups

### Still standing at month-end

- **`AppleLegalLink` missing `rel="noopener"`** — external Apple legal `<a>` lacks `target`/`rel`; without `rel="noopener"` the linked page can navigate the opener. Security-relevant but trivial. First flagged 2026-06-09. [source](code-reviews/2026-06-09.md)
- **Untracked Continuum privacy routes** — `src/app/privacy/continuum/dpia/page.tsx` and `responsible-use/page.tsx` exist on disk, uncommitted; decide whether they route through `PrivacyPageLayout` (consistency) or are removed. First flagged 2026-06-09. [source](code-reviews/2026-06-09.md)
- **Tablist keyboard-scroll affordance** — `ProjectContent.tsx` arrow-key nav doesn't `scrollIntoView` an off-screen active tab on narrow viewports; user can select an invisible tab. First flagged 2026-06-09. [source](code-reviews/2026-06-09.md)
- **`seed-projects.ts` `localhost:300` typo** — three entries (`:104`, `:179`, `:228`) use `localhost:300` instead of `localhost:3000`; broken anchors. Trivial. First flagged 2026-06-09. [source](code-reviews/2026-06-09.md)

## One-off findings

- **2026-06-09 — unknown-flag message drift** — `KNOWN_FLAGS` includes `--reupload` but the error string lists only `--dry-run, --cleanup`; derive the message from `KNOWN_FLAGS`. [source](code-reviews/2026-06-09.md)
- **2026-06-09 — `writeProject` id churn** — `deleteMany`+`create` rotates the integer `id` each re-run; fine since public surface keys on `slug`, but worth a comment so a future caller doesn't assume id stability. [source](code-reviews/2026-06-09.md)
- **2026-06-09 — seed scripts `DATABASE_URL!` assertion** — both seeds assert non-null; should mirror `import-projects.ts`'s explicit guard (or share a helper). [source](code-reviews/2026-06-09.md)
- **2026-06-09 — `loadImages` no size cap** — reads every image fully into a `Buffer` with no per-file ceiling; a stray large file OOMs before hitting Blob. Documented soft cap (~50 MB) that throws. [source](code-reviews/2026-06-09.md)
- **2026-06-09 — `ProjectManifest` vs `projectCreateSchema` drift** — two independent structural defs; a schema field added without updating the manifest type type-checks then drops at write. Derive from `z.input<typeof projectCreateSchema>` if field count grows. [source](code-reviews/2026-06-09.md)
- **2026-06-09 — import summary not debuggable** — final `N imported, N failed` line doesn't break down by reason; `result.detail` is built but never re-printed. [source](code-reviews/2026-06-09.md)

## Notes

- **Risk at 2026-06-09: Medium** — import tooling is now load-bearing (publishes Reckon to prod, stages Continuum) but its reuse promise is one transient `list()` failure from a silent re-upload storm, with no integration coverage. Privacy refactor clean (all four pages route through `PrivacyPageLayout`). [source](code-reviews/2026-06-09.md)
