# Code Review Monthly Rollup — 2026-07

_Generated 2026-07-13._

Reviews: 1 across 1 day. Source files: `code-reviews/2026-07-07.md`.

## Recurring flags

- **Dual cache layer staleness** — `s-maxage=3600` on the `.md` route outlives the `post-<section>-<slug>` data cache bust by up to an hour; mirrors the `llms.txt` open thread in `dev-journal/2026-07-05.md`. Both should be resolved together. [source](code-reviews/2026-07-07.md)

## Test coverage gaps

- **`.md` route doesn't assert `loadPost` draft/future-datetime filters** — `loadPost` is mocked to `null` for the miss case, proving the 404 path but not that the route honours `published: false` or future `datetime`. A call-assertion on the happy path would catch a future raw-prisma refactor that exposes drafts. [source](code-reviews/2026-07-07.md)
- **No round-trip body test in the route test** — `postMarkdown.test.ts` covers the writer, but the route test is where an integration regression (e.g. body beginning with a blank line breaking `parseFrontmatter`) would surface. Also no `---` thematic-break body test. [source](code-reviews/2026-07-07.md)
- **`proxy.test.ts` — no blog `.md` rewrite + admin auth gate interaction test** — the suite covers admin protection and `.md` rewrite in isolation but not the combination; the ordering comment ("Rewrites don't re-run middleware") is the invariant a targeted test could pin. [source](code-reviews/2026-07-07.md)

## Follow-ups

### Still standing at month-end

- **`.md` route `s-maxage=3600` dual cache layer** — either drop the route cache in favour of the data cache tag, or set `s-maxage=0, stale-while-revalidate=86400`. [first flagged: 2026-07-07; matches llms.txt open thread]
- **`matchLegacyRedirect` mutual-exclusion invariant untested** — a future overlapping pattern would win by declaration order with no test to notice. A single property test ("no two patterns match the same path") would lock this in. [first flagged: 2026-07-07]

## One-off findings

- **2026-07-07 — `buildPostMarkdownFile` frontmatter round-trip claim is stronger than enforced** — `PostDetail.body` is arbitrary DB content; a body beginning with a blank line would silently break re-import (`parseFrontmatter` strips `^[\r\n]+`). Low probability (importer already strips), but worth noting. [source](code-reviews/2026-07-07.md)
- **2026-07-07 — `.md` 404 logging** — invalid-section and missing-post cases return the same generic `Not Found` with no log line; a spike of `.md` 404s (renamed post crawled) would be indistinguishable from ordinary bot noise. Consistent with the feed-route shape (intentional). [source](code-reviews/2026-07-07.md)
- **2026-07-07 — `content → import` dependency** — `lib/content/postMarkdown.ts` imports `escapeYamlDoubleQuoted` from `lib/import/frontmatter.ts`; already tracked in `dev-journal/2026-07-06.md`. No new action. [source](code-reviews/2026-07-07.md)
- **2026-07-07 — Risk: Low** — `.md` export is additive; middleware widened its contract but test suite covers all previously-passing paths plus new edge cases. [source](code-reviews/2026-07-07.md)
