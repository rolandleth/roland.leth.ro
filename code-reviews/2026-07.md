# Code Review Monthly Rollup — 2026-07

_Generated 2026-07-27._

Reviews: 9 across 9 days.

## Recurring flags

- **Reporting that doesn't match what happened** — flagged repeatedly across three features. 2026-07-19 found the revalidate route reporting pasted guide slugs as "revalidated" without checking they matched a row, and per-resource loops discarding partial results on a mid-batch throw. 2026-07-20 found the identical class in IndexNow: `submitted` counted attempted URLs, so an all-403 run still claimed success. The IndexNow instances were fixed same day (`attempted`/`accepted` split) and the panel's failure-detail loss was closed 2026-07-20–21; the guides/revalidate-route instances from 2026-07-19 were never revisited. [strongest source](code-reviews/2026-07-20.md)
- **Admin panel/hook scaffolding duplication, peeled back in layers** — 2026-07-20 flagged ~25 lines of fetch/abort/supersede scaffolding copied verbatim into a second panel (fixed same day via `useAdminAction`); 2026-07-21 found the outcome-region markup still duplicated one layer down (fixed same day via `adminOutcomeClass`); 2026-07-22 confirmed no further duplication remained. Three passes to fully close one duplication chain. [strongest source](code-reviews/2026-07-21.md)
- **Middleware as the sole admin-auth boundary** — flagged 2026-07-20 as a real gap against vendor guidance (not exposed to the framework CVE on this Next version, but a design smell), explicitly deferred 2026-07-20/21/22 as out of scope for the branch in front of it. Closed 2026-07-26 with a `requireAdmin()` handler-level second lock — but 2026-07-27's review caught a leftover: the `proxy.ts` comment still asserts middleware is the *only* gate, now contradicted by the new second lock. A five-review arc from flagged, to deferred three times, to fixed, to a stale doc trailing the fix. [strongest source](code-reviews/2026-07-27.md)
- **Bulk-script test coverage gaps** — 2026-07-17 flagged `init-post-slugs.ts` at zero coverage, resolved same day by extracting the matching/planning core into `slugInit.ts` (13 unit tests). 2026-07-18 repeated the flag on what the extraction left behind — the imperative shell (`initFolder`/`stampFile`/`resolveSection`) — still untested at month-end. [strongest source](code-reviews/2026-07-18.md)
- **Comments/docstrings asserting more than the code delivers** — 2026-07-19 found a `proxy.ts` comment claiming a change "can only ever ADD protection" when one arm removed it, plus a `revalidateGuideSlugs` docstring describing a return its only caller drops. 2026-07-20 found `isSubmittableOrigin`'s comment claiming IP literals were excluded while the code passed any dotted host (fixed same day). 2026-07-24 found a `proxy.ts` rationale comment about `/api/` being robots-blocked that robots.ts itself contradicts (fixed same day). The pattern recurs because each fix session narrates intent in a comment that the next reviewer has to re-verify against the code — two of four instances were caught wrong. [strongest source](code-reviews/2026-07-24.md)
- **Fixes that introduce the regression they were meant to prevent** — 2026-07-18's case-insensitivity fix for the admin-gate (`/ADMIN` bypass) applied `toLowerCase()` to the *exemption* arm too, silently un-gating `/admin/Login` case variants — caught by 2026-07-19's dedicated regression audit of the fix commits. No later review re-touched this code path to confirm a fix. [strongest source](code-reviews/2026-07-19.md)
- **Swallowed/generic error handling** — 2026-07-17 (a per-file write failure aborting an entire batch instead of isolating it) and 2026-07-18 (nine call sites doing `error instanceof Error ? error.message : String(error)`, dropping the stack) both resolved same-day via per-file try/catch and a shared `errorMessage` util.

## Test coverage gaps

- **`init-post-slugs.ts` imperative shell** (`initFolder`/`stampFile`/`resolveSection`) — untested; the write-failure→problem conversion and dry-run branching aren't pinned, only the extracted pure core is. First flagged 2026-07-17 (whole file), narrowed 2026-07-18 (shell only). Still open at month-end. [source](code-reviews/2026-07-18.md)
- **`.md` route detail-filter and round-trip coverage** — no test that the route honors `loadPost`'s draft/future-datetime filters, no route-level round-trip test, no `matchLegacyRedirect` property test pinning patterns can't overlap. First flagged 2026-07-07. Still open. [source](code-reviews/2026-07-07.md)
- **`useFormState`/`useAdminResource` hook gaps (guides/posts/projects admin forms)** — `useFormState` (backs all four admin forms) has zero coverage; `GuideForm`/`GuideTopicForm` have no test files at all, including the one non-trivial piece of logic (`handleTopicChange`'s topic-adopts-project behavior). First flagged 2026-07-19. Still open — distinct from the unrelated `useAdminAction` hook (RevalidatePanel/IndexNowPanel), whose equivalent gaps were closed 2026-07-21. [source](code-reviews/2026-07-19.md)
- **Guide import edge cases** — no test for `publishedAt` when a filename carries no `-HHmm` time and the DB row was previously admin-stamped at a different time; grouped→ungrouped and ungrouped→grouped folder-move transitions are untested (only grouped A→B is). First flagged 2026-07-18, repeated 2026-07-19. Still open. [source](code-reviews/2026-07-19.md)
- **Revalidate route multi-entry / multi-resource failure paths** — both isolation tests use single-entry arrays, so a mid-loop throw and a two-of-three-resources-erroring response are uncovered. First flagged 2026-07-19. Still open. [source](code-reviews/2026-07-19.md)
- **Concurrent-miss cache dedupe unverified** — `createBoundedWrapperCache`'s behavior under two concurrent misses on the same slug isn't pinned; a test was explicitly skipped as it would validate the test double, not production `unstable_cache` semantics. First flagged 2026-07-18. Still open (deliberately). [source](code-reviews/2026-07-18.md)
- **Admin body search seq-scan** (`listGuidesForAdmin`/`listPostsForAdmin`) — no trigram/GIN index; deferred as premature at current volume, revisit at ~thousands of rows. First flagged 2026-07-17, repeated 2026-07-18. Deferred, open. [source](code-reviews/2026-07-18.md)
- **`src/app/layout.tsx` site-wide feed alternate** — was untested with no `layout.test.tsx`; a later edit replacing `alternates` could silently drop site-wide autodiscovery. Flagged and resolved same day 2026-07-24 (`layout.test.tsx` + `page.test.tsx` added). [source](code-reviews/2026-07-24.md)

## Follow-ups

### Resolved during the month

- **2026-07-17 → 2026-07-17 — Slug-tooling hardening**: byte-canonical drift healing, `matchRow` ambiguous-match refusal, unknown-flag guard, atomic writes, blank-vs-missing `slug:` distinction, plus several smaller test/observability items — all ~10 findings fixed same day. [source](code-reviews/2026-07-17.md)
- **2026-07-18 → 2026-07-18 — Guides feature review (~39 findings, ~30 fixed)**: guide/topic cache-tag namespace collision; `useAdminResource` save/remove race; revalidate endpoint's missed parent-hub bust and discarded partial results (207 + per-resource isolation); bot-probe extension check bypassable via a nested segment; `atomicWrite` same-process race + mode reset; admin-form/detail-wrapper/tag-literal duplication (`wrapNullableDetail`, `@apply` + `useFormState`, single-sourced tag constants); a latent bug where a pure guide folder-move silently failed to update `topicId`. Roughly 9 more findings investigated and deliberately deferred or skipped with recorded reasons (false-positive P2002 message, premature GIN index and parallel file reads, unnecessary `atomicWrite` fsync, among others). [source](code-reviews/2026-07-18.md)
- **2026-07-18 → 2026-07-19 — `createBoundedWrapperCache` concurrent-miss concern**: verified clean on inspection (the factory is synchronous between `get`/`set`, no interleaving window on Node's single thread) — no code change needed. [source](code-reviews/2026-07-19.md)
- **2026-07-20 → 2026-07-20/21 — IndexNow reporting-honesty and hardening pass (~34 of 36 findings)**: `attempted`/`accepted` split; `isSubmittableOrigin` rejecting IP literals; `?dryRun=false` actually disabling dry-run; 400→503/422 status realignment; unguarded `sitemap()` await wrapped; panel failure-detail loss closed; double-body-read bug fixed; `INDEXNOW_KEY` regex moved out of the aggregate env schema; `useAdminAction` extracted, replacing ~25 duplicated lines; `IndexNowPanel` went from zero tests to a full suite. Submit throttle investigated and declined (buttons already disable in flight). [source](code-reviews/2026-07-20.md)
- **2026-07-21 → 2026-07-21 — Regression-audit fixes for the 07-20 IndexNow work**: `INDEXNOW_KEY` trimmed before validation (was served correctly but rejected as "malformed"); outcome-region markup deduplicated via `adminOutcomeClass`; `reset` moved into `useAdminAction.run` as a parameter (avoiding an RSC-serialization trip and a `react-hooks/refs` violation); superseded-request failures now logged at `debug`; per-batch `errorName`/`message` now reach server logs; `aria-controls` now names both the outcome and error regions with an always-mounted error wrapper. All 11 findings from the pass resolved same day. [source](code-reviews/2026-07-21.md)
- **2026-07-24 → 2026-07-24 — Feed autodiscovery review**: a `proxy.ts` comment incorrectly claiming `/api/feed/` is robots-blocked was corrected; the dual crawlable feed URLs (`/api/feed/:section` vs. the pretty `/blog/:section/feed.xml`) were confirmed deliberate rather than an oversight; the untested site-wide feed-alternate default in `layout.tsx` gained a test file. [source](code-reviews/2026-07-24.md)
- **2026-07-20 → 2026-07-26 — Middleware-as-sole-auth-boundary**: deferred through three review cycles (07-20/21/22) as out of scope for the branch in front of it, then closed with a `requireAdmin()` second lock in all 21 admin handlers plus an anchored, explicit-namespace middleware matcher. Confirmed by the 2026-07-27 review, which also caught a stale comment left behind (see Still standing). [source](code-reviews/2026-07-27.md)

### Still standing at month-end

- **`code-reviews/2026-07-19.md` was never actioned** — 26 findings across `proxy.ts` case-handling, revalidate-reporting honesty, a `"none"` sentinel colliding with a real project slug, ICU-vs-Postgres ordering drift, and five test gaps. Tracked as an open thread in the 2026-07-20 and 2026-07-21 journals, then dropped from tracking with no resolution note. First flagged 2026-07-19. [source](code-reviews/2026-07-19.md)
- **`proxy.ts` comment now contradicts the actual auth posture** — asserts middleware is the sole gate with no handler-level re-check; the 2026-07-26 work added exactly that re-check (`requireAdmin()`), making the comment false. First flagged 2026-07-27. [source](code-reviews/2026-07-27.md)
- **Case-insensitivity fix over-widened a login exemption (regression)** — `toLowerCase()` was applied to the exemption arm as well as the gated-prefix arm, silently un-gating `/admin/Login`/`/ADMIN/LOGIN`. Not exploitable today (Next routing is case-sensitive), but no later session revisited this specific code path. First flagged 2026-07-19. [source](code-reviews/2026-07-19.md)
- **`.md` route CDN cache outlives its tag bust** — `Cache-Control: s-maxage=3600` is independent of the `unstable_cache` tag bust it should track; same shape as the pre-existing `llms.txt` open thread. First flagged 2026-07-07. [source](code-reviews/2026-07-07.md)
- **`postMarkdown.ts` round-trip guarantee overstated** — assumes no leading blank line in the body; nothing trims one before emission, so a body starting with a blank line would silently break re-import. First flagged 2026-07-07. [source](code-reviews/2026-07-07.md)
- **Guides admin UX gaps** — `GuidesTab.tsx` never surfaces `sortOrder` (admins can't spot collisions without opening each row); the guide-topics `[id]` route splits its cascade log from the primary audit line and omits `isProjectChanged`/`guideCount`; its preflight runs before the existence check, wasting queries on the 404 path. First flagged 2026-07-18. [source](code-reviews/2026-07-18.md)
- **`import-guides.ts` cross-table slug pre-check runs outside the transaction** — a concurrent admin create in the other table can slip through between check and write (no cross-table DB constraint exists). First flagged 2026-07-18. [source](code-reviews/2026-07-18.md)
- **"Sweeps that stopped short"** — an `@apply` extraction left `ProjectForm`'s delete button and `PostBulkImport`'s submit button inline instead of using the shared class; an `aria-describedby` pass wired one help text of eight across `GuideForm`/`GuideTopicForm`. First flagged 2026-07-19, never revisited. [source](code-reviews/2026-07-19.md)
- **`guideFormOptions.ts`'s `NONE` sentinel collides with a real project slug** — a project literally slugged `none` would silently write `null` for its guide association. First flagged 2026-07-19. [source](code-reviews/2026-07-19.md)
- **Miss-stampede negative cache deferred** — detail-page misses are deliberately never cached (the stale-404 incident fix), but a slug-enumerating crawler still hits Postgres per request. Waiting on prod DB metrics before adding a short-TTL negative cache. First flagged 2026-07-17, repeated 2026-07-18. [source](code-reviews/2026-07-18.md)

## One-off findings

- **2026-07-07 — Featured-projects 3-up grid + `Image` `sizes` update**: clean two-line change with an aligned hint update, no issues raised in the same review as the `.md` export work. [source](code-reviews/2026-07-07.md)
- **2026-07-18 — `GuideProjectCta.tsx` deletion verified fully clean** — confirmed no dangling references remain in `src/` or `prisma/` after the component was removed in favor of authored prose. [source](code-reviews/2026-07-18.md)
- **2026-07-20 — A review subagent produced a confidently wrong finding** (claimed unauthenticated `/api/admin/*` redirects to the HTML login page) — verified false against `proxy.ts` and dropped before reaching the review file, rather than being written up as a defect. [source](code-reviews/2026-07-20.md)
- **2026-07-27 — Duplicated `requireAdmin` guard block across ~20 handlers** — flagged only per DRY discipline; low priority since a contract test already fails if any handler omits the guard, so the repetition is test-caught rather than a live risk. [source](code-reviews/2026-07-27.md)
