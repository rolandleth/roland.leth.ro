# Code Review Monthly Rollup — 2026-07

_Generated 2026-07-07._

Reviews: 1 across 1 day.

## Recurring flags

- **Dual cache layers (CDN `s-maxage` vs Next data-cache tag)** — the `.md` route sets `Cache-Control: s-maxage=3600` but freshness is tag-driven (`post-<section>-<slug>`), so the CDN serves stale for up to an hour after an edit; mirrors the open `llms.txt` staleness pattern. Fix: drop the route cache for the tag, or `s-maxage=0, stale-while-revalidate=…`. [source](code-reviews/2026-07-07.md)

## Test coverage gaps

- **`.md` route draft/future filtering** — no test that the route actually asks `loadPost` for the published-and-past view; only the `null` miss path is covered, so a raw-prisma refactor could silently expose drafts. [source](code-reviews/2026-07-07.md)
- **`.md` frontmatter round-trip at the route layer** — round-trip is unit-tested in `postMarkdown.test.ts` but not at the route; no test for a body containing `---` (thematic breaks) against non-greedy frontmatter parsing. [source](code-reviews/2026-07-07.md)
- **proxy `.md` rewrite × admin-auth ordering** — the `.md` rewrite and admin protection are each tested in isolation, not their interaction (the "rewrites don't re-run middleware" invariant). [source](code-reviews/2026-07-07.md)

## Follow-ups

### Still standing at month-end

- **`.md` route CDN-cache staleness** — first flagged 2026-07-07; cross-cuts the `llms.txt` dual-cache open thread; the route itself is unaddressed. [source](code-reviews/2026-07-07.md)
- **`buildPostMarkdownFile` round-trip claim > enforcement** — a stored body with a leading blank line would break re-import (`parseFrontmatter` strips `^[\r\n]+`); low probability (importer already strips), but the claim is stronger than the code enforces. [source](code-reviews/2026-07-07.md)

## One-off findings

- **2026-07-07 — `proxy.ts matchLegacyRedirect` mutual-exclusivity invariant** — extraction is clean, but the helper now silently owns "no two patterns match the same path"; a property test would lock it in. [source](code-reviews/2026-07-07.md)
- **2026-07-07 — `.md` 404s undifferentiated** — invalid-section and missing-post both return a generic `Not Found` with no log line; a crawl spike is indistinguishable from bot noise (same gap the feed route inherits). [source](code-reviews/2026-07-07.md)
- **2026-07-07 — `content → import` layer dependency** — `postMarkdown.ts` imports `escapeYamlDoubleQuoted` from `lib/import/frontmatter`; fine while `frontmatter.ts` stays free of `next/*`/IO deps. [source](code-reviews/2026-07-07.md)
