# Code Review — 2026-05-31

## Issues and Observations

- [x] **[src/app/privacy/continuum/page.tsx, src/app/privacy/reckon/page.tsx, src/app/privacy/body-tracking/page.tsx, src/app/privacy/page.tsx — `LAST_UPDATED` + "Last update"/"Contact" sections]** — every privacy page redeclares a `LAST_UPDATED` const and renders the same "Last update" copy and a near-identical "Contact" section (only the mailto differs). With four call sites now, the pattern warrants moving `lastUpdated` and `contactEmail` onto `PrivacyPageLayout` so the boilerplate sections render once and a copy tweak doesn't have to land in four files. **Resolved 2026-06-08**: `PrivacyPageLayout` now takes `lastUpdated`/`contactEmail`/`contactPronoun?` and auto-renders both sections; all 4 pages dropped the const and the two entries.
- [x] **[src/app/privacy/reckon/page.tsx, src/app/privacy/continuum/page.tsx, src/app/privacy/body-tracking/page.tsx — Apple Privacy Policy anchor]** — the `<a href="https://www.apple.com/legal/privacy/">Apple&apos;s Privacy Policy</a>` block is duplicated six times across three files (3 in reckon, 2 in continuum, 1 in body-tracking). Extract a small `AppleLegalLink` component or shared constant so the URL and label have a single source of truth. **Resolved 2026-06-08**: added `src/components/privacy/AppleLegalLink.tsx`; all 6 anchors replaced with `<AppleLegalLink />`.
- [x] **[src/app/privacy/continuum/page.tsx — Purchases section]** — copy reads "Continuum may offer paid features through the App Store". The hedge ("may") is vague; either commit to the current monetisation state or drop the section until paid features ship — soft-claim copy in a privacy policy is unusual. **Resolved 2026-06-08**: committed to "Continuum offers paid features through the App Store" (user confirmed IAP/subscription); kept only the model-agnostic privacy fact (Apple processes payment).

## Architecture & Coupling

- [x] **[src/app/privacy/* — voice drift]** — root `/privacy` uses first person ("I do not collect…", "contact me"); the per-app pages use first person plural ("We do not collect…", "contact us"). Likely intentional (personal site vs per-app boilerplate), but worth deciding once and documenting so the next app page doesn't drift again. **Resolved 2026-06-08**: the contact line is now decided in one place — `contactPronoun` defaults to `"us"`, root overrides to `"me"`, documented on the prop; body-copy voice stays per-page (intentional).

## Missing Tests

- [x] No new tests for `continuum/page.tsx` or `reckon/page.tsx`. Acceptable in isolation — both are pure data + `PrivacyPageLayout` (already covered by `PrivacyPageLayout.test.tsx`). If the DRY refactor above lands (lifting `lastUpdated`/`contactEmail` to the layout), add a layout-level assertion that the contact mailto and last-updated string both render. **Resolved 2026-06-08**: added layout-level assertions for the auto-rendered last-updated string and the `mailto:` contact link (both `"us"` default and `"me"` override); 6 tests pass.

## Risk Assessment

**Low.** Additive: two new static privacy pages plus a one-line email change in the existing body-tracking page (`roland+hi@leth.ro` → `roland+bodytracking@leth.ro`, aligning with the per-app inbox pattern the new pages adopt). No routing, data, or auth surface touched; the legacy `[slug]` catch-all is unaffected since the new pages live under `/privacy/*`. The only durable concern is the growing duplication across the four privacy pages — small now, but the next app added will copy the same shape.

## Notes

- Orchestration: inline (1 commit, 4 files, +192/−1 LOC).
- No prior dev-journal watch-outs apply to the privacy area; the most recent open watch-outs (`MockPlatformPicker.autoFill`, `cacheWrapsAtLoad`) are unrelated.
