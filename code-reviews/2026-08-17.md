# Code Review — 2026-08-17

## Issues and Observations

- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:82 — `GET`]** — `Promise.all` makes two independent checks all-or-nothing: a transient failure in the guide query discards a good post result and revalidates nothing. `Promise.allSettled` with per-branch revalidation (still returning 500 so the run is marked failed) degrades to partial work instead of no work. **Resolved 2026-08-17**: `Promise.allSettled`, per-branch revalidation, still a 500 so the run is marked failed. New tests cover a guide-only failure, partial work surviving a guide-side outage, and the log naming which half broke.
- [x] **[src/lib/routing/legacyRoutes.ts:52-57 + src/app/blog/[section]/p/[page]/page.tsx:69 — `?page=` redirect]** — the redirect's `\d+` accepts `0`, `02`, `007`; the route's `String(page) !== pageParam` round-trip rejects all of them. So `/blog/tech?page=02` now 308s into a 404 where it used to render page 2 — the redirect exists to carry SEO signal from indexed `?page=N` URLs and instead carries it into a dead end. **Resolved 2026-08-17**: pattern tightened to `[1-9]\d*`, so those values no longer match the rule and fall through to `/blog/:section` — page 1, HTTP 200, a real page. The route keeps its strict round-trip check, so one page still has exactly one URL. Parameterised test covers `0`/`02`/`007` rejecting and `1`/`2`/`12` matching.
- [x] **[src/lib/db/posts.ts:133 — `getPostsBySection`]** — `parsePageParam` clamps to `MAX_PAGE = 10_000` and the round-trip check passes every integer 2–10000, so a crawler walking `/blog/tech/p/2…10000` triggers ~10k on-demand renders, each a billed invocation running `findMany` with `skip` up to 99,990 plus a `count`, each minting a distinct on-disk cache key. `createBoundedWrapperCache` (max 256) bounds only in-memory wrapper objects, not the work or the disk entries — the docblock at `posts.ts:71-73` claims it bounds probe damage, which is half true. **Resolved 2026-08-17**: two bounds, cheapest first. `MAX_PAGE` drops from 10_000 to 30 (300 posts), enforced by the existing round-trip check so anything above it 404s before a query runs; then the route checks the page against the section's real `totalPages` via a new count-only cache. The corpus outgrowing `MAX_PAGE` throws in `generateStaticParams`, so the ceiling is a build failure with a named fix rather than real pages quietly 404ing. The `posts.ts` docblock now says the map bounds itself and nothing else.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:135-140 — duplicated comment]** — the singular-form comment block was left in place above its plural rewrite; two near-identical paragraphs now sit back to back. A leftover from the `61929b8` edit. **Resolved 2026-08-17**: dropped the stale singular paragraph, kept the plural one that covers posts and guides.
- [x] **[src/lib/db/posts.ts:455 / src/lib/db/guides.ts:542 — `findPostsBecameLive`, `findGuidesBecameLive`]** — the count→rows change dropped O(1) memory for an unbounded `findMany` with no `take`. A bulk import of backdated posts (`admin/(protected)/posts/bulk` exists) landing inside the 49h window loads every row and fires one `revalidateTag` per row. Low likelihood, trivial cap. **Resolved 2026-08-17**: both take a required `limit`. The cron asks for `DUE_ROW_CAP + 1` and switches to `revalidateAllPosts` / `revalidateAllGuides` on overflow, so the bound is a switch rather than a silent truncation — the blanket bust also covers rows the capped query never returned.
- [x] **[src/app/admin/adminPageContract.test.ts:94-104 — `describe.each(editPages())`]** — assertions are raw source-text matches, so the file's stated guarantee ("a fifth edit page can't ship without the guard") is not delivered: an import line plus the literal `tag: ADMIN_EDIT_TAGS.x` anywhere in the file passes, with no `generateMetadata` export and no call to it. The API-side mirror it claims parity with (`src/app/api/admin/adminAuthContract.test.ts:85-86`) imports each module and calls every exported handler. **Resolved 2026-08-17**: rewritten behaviourally — each page module is imported and its `generateMetadata` called with no session, asserting the fallback title and the tagged bypass line. Also asserts the export exists, which the source-text form never did.
- [x] **[src/app/admin/adminPageContract.test.ts:73-124 — tag↔route correspondence]** — `route` is computed but used only as a `describe.each` label; only global uniqueness is asserted. Two pages transposing tags (guides page taking `ADMIN_EDIT_TAGS.posts`, posts taking `.guides`) passes every assertion here, and the data to catch it is already in hand. **Resolved 2026-08-17**: the expected tag per route is declared in the test's own list rather than scraped from the page, so the pairing is checked against an independent statement. Verified by transposing the guides page's tag to `.posts` — the old test passed, the new one fails.
- [x] **[src/app/admin/adminPageContract.test.ts:75 — `editPages`]** — the predicate is `/edit/page.tsx`, but the invariant it enforces is about any admin page reading a row in `generateMetadata`. A `[id]/page.tsx` or `[id]/preview/page.tsx` gets no tag assertion and does not perturb the count test at `:91`, which makes the gap look closed. **Resolved 2026-08-17**: discovery keys on `export async function generateMetadata` rather than `/edit/page.tsx`, and compares the discovered set to the declared list by equality. Any admin page that reads a row in `generateMetadata` now has to be listed.
- [x] **[src/lib/api/cronAuth.ts:40 + src/lib/auth/env.ts:95-97 — `getCronSecret`/`nonEmpty`]** — no trim, so `CRON_SECRET=" "` or a value with a trailing newline counts as configured and every cron run 401s at `console.warn`. That is exactly the silent-cron-stop the doc block says the logging exists to prevent, presented as routine scanner noise. **Resolved 2026-08-17**: `nonEmpty` trims before testing and returns the trimmed value, so a dashboard-pasted trailing newline no longer counts as configured — and no longer breaks the compare if it does slip in.
- [ ] **[eslint.config.mjs:44-57 — `pageMetadataRestrictions`]** — the selectors match the literal object shape only. A same-file helper (`export const metadata = buildMeta()`) or a spread (`{...ogBlock}`) sidesteps all six and reintroduces the dropped `siteName`/`locale`/`card`/`creator` bug the `:188-193` comment calls invisible in review and in the rendered page. That is an ordinary refactor, not an evasion. **Not doing, 2026-08-17**: catching `{...ogBlock}` or `buildMeta()` means writing a real rule with scope analysis, not an AST selector — the selector language cannot follow a value to its definition. Left as-is; the finding's own reading (an ordinary refactor sidesteps it) is the accurate one, and the cost of the fix is out of proportion to a bug that has happened once.
- [x] **[src/lib/auth/adminMetadata.ts:11 — `ADMIN_EDIT_TAGS` doc]** — says `adminAuthContract.test.ts` walks the edit-page directory; the walker is `src/app/admin/adminPageContract.test.ts`. A file by the named path exists and does something else, so the reader is sent to the wrong enforcement mechanism rather than to no file at all. **Resolved 2026-08-17**: named the right file, and said explicitly that the other one exists and covers `/api/admin`.
- [x] **[src/lib/routing/legacyRoutes.ts:9-10 — module doc]** — claims the middleware keeps "the admin auth gate and the bot-probe short-circuit". The bot-probe filter was deleted in `4c00c73`; `src/proxy.ts` is now the auth gate alone. The comment describes a mechanism that no longer exists. **Resolved 2026-08-17**: dropped the bot-probe clause and named the commit that removed it.
- [x] **[src/lib/db/posts.ts:290-294, :332-333 — `allPublishedPostSlugsCache`, `makeArchiveCache`]** — both docblocks say scheduled posts "auto-surface … without waiting for a cache bust". `src/app/sitemap.ts:8-17` and `src/app/blog/[section]/archive/page.tsx:10-18` state the opposite (the read-time filter only re-runs on regeneration, hence the cron). A reader landing in `posts.ts` first would conclude the cron is redundant. **Resolved 2026-08-17**: both now say "read time" means generation time and name the cron as what surfaces a due post. Two more instances of the same claim were found and fixed alongside them — `getPostBySlug`'s inline comment and `revalidatePostDetails`' "padded superset" line.
- [x] **[src/lib/content/metadata.ts:72 — `defaultOgImageDescriptor.alt`]** — the alt string is hand-composed from the OG script's `NAME` + `TAGLINE` literals in a different file. Dimensions are single-sourced by import; the alt is not, and `yarn og:card --check` compares rendered PNG against committed PNG so it cannot see alt drift. **Resolved 2026-08-17**: `OG_CARD_NAME` and `OG_CARD_TAGLINE` moved into `metadata.ts` beside the dimensions, and the script imports them. The alt is now composed from the same two constants the card draws, so a copy change moves the picture and its description together.
- [ ] **[scripts/generate-og-card.tsx:249-268 — `--check`]** — compares a render using freshly-fetched Google Fonts against committed bytes. A Newsreader or Inter revision upstream changes the render and fails with "the committed card is stale", blaming the author for someone else's change. Relevant because wiring `--check` into CI is an open thread. **Not doing, 2026-08-17**: the honest fix is vendoring the two font files so the render stops depending on an upstream that can change under it. That is a separate call about repo weight and licence terms, not a review cleanup. Flagging it here as the blocker for the CI thread: wiring `--check` in as it stands will eventually fail a build for someone else's font revision.
- [x] **[src/app/blog/[section]/p/[page]/page.tsx:39-55 — `generateMetadata`]** — validates only the section, then uses the *clamped* `parsePageParam` value with no round-trip check, so `/blog/tech/p/abc` and `/p/999999` produce "page 1" metadata and `path: /blog/tech/p/1` before the component 404s. The guard at `:69` sits 15 lines below and is not shared. **Resolved 2026-08-17**: extracted `resolvePage` (returns `null` for junk, padding, page 1, and out-of-range) plus `isRealPage` (checks the section's actual page count). Both the metadata and the body run the same pair, so they can no longer disagree.
- [x] **[src/lib/api/cronAuth.ts:48,57 vs src/lib/auth/middlewareBypass.ts:57 — tag convention]** — `requireCronAuth` brackets the tag itself and takes unbracketed input; `logMiddlewareBypass`/`requireAdmin` take pre-bracketed tags and interpolate raw. Neither is enforced (`tag: string` in both), so `requireCronAuth("[api:cron:x]")` yields `[[api:cron:x]]` — and log shape is precisely what `middlewareBypass.ts` was extracted to single-source. **Resolved 2026-08-17**: `requireCronAuth` now takes a pre-bracketed tag like its siblings, and both cron routes pass one. Left as `tag: string` rather than a union — the two cron tags aren't a closed set the way `ADMIN_EDIT_TAGS` is, and the convention is now stated on the parameter.
- [x] **[eslint.config.mjs:33-35 — `restrictedSyntaxBeyondInnerHtml`]** — the JsonLdScript/ThemeScript exemption keys on a substring test of `entry.selector`, not on intent. A future raw-HTML ban worded without the literal `dangerouslySetInnerHTML` stays enforced in the two files that need the exemption; one that merely mentions the string is silently dropped there. **Resolved 2026-08-17**: each entry carries an explicit `exemptForInnerHtmlFiles` marker and the filter reads that instead of testing the selector string. The marker is stripped before the options reach ESLint, which rejects unknown keys. Verified the exemption still applies — both exempt files still use the syntax and lint stays clean.
- [x] **[src/lib/utils/format.ts:45-47 — `MAX_PAGE` comment]** — states "PAGE_SIZE=12" (it is 10) and justifies the bound against `?page=999999999`, a query param no route reads any more. Both halves are wrong after this change. **Resolved 2026-08-17**: rewritten around the actual cost (a URL-segment probe is a billed render with its own `skip`, `count`, and cache entry) and the actual bound (30 pages = 300 posts), naming this the outer guard and `totalPages` the exact one.
- [x] **[src/lib/db/guides.ts:520-524 — orphaned docblock]** — `revalidateGuides`'s docblock was left in place and `findGuidesBecameLive` inserted below it, so `findGuidesBecameLive` carries two stacked docblocks and `revalidateGuides` has none. Survived the `61929b8` rewrite. **Resolved 2026-08-17**: moved the docblock down onto `revalidateGuides`, leaving `findGuidesBecameLive` with only its own.
- [x] **[src/lib/routing/legacyRoutes.ts:52-64 — bare `:section`]** — the two blog pagination rules use `:section` while every other rule in the file pins `(${SECTION_PATTERN})`. `/blog/bogus?page=2` 308s to `/blog/bogus/p/2` and then 404s, a redirect into a dead end instead of a direct 404. **Resolved 2026-08-17**: both rules pin the section. A test asserts it for each, and the existing "builds section rules from SECTIONS" sweep now covers them too.
- [x] **[src/lib/utils/format.ts:211 — `isFutureDatetime` doc]** — still lists "getPostsBySection filter" as a caller; that filter moved into SQL (`publishedWhere`) in this range. **Resolved 2026-08-17**: caller list is now bulk import + the admin PostsTab marker, with an explicit note that the list filter moved to SQL rather than going away.
- [x] **[src/components/blog/Pagination.tsx:8 / src/lib/utils/pagination.ts:14 — `section: string`]** — both widen `section` to `string` where every caller has a `Section`. `blogPagePath` is the single source of the URL contract; typing it `Section` costs nothing and stops a future caller generating `/blog/typo/p/2`. **Resolved 2026-08-17**: both take `Section`. The import into `pagination.ts` is type-only, so the module's stated "no domain pulls another into its dependency graph" note still holds at runtime.

## Architecture & Coupling

- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:41 + vercel.json:2-11 — `WINDOW_HOURS`]** — the `WINDOW_HOURS ≥ 2 × interval + 1` invariant lives in two files with no code or test linking them, enforced only by prose. `61929b8` edited the schedule (`0 0` → `0 1`) with nothing to check the relation — harmless here since both are daily, but it demonstrates the path is unguarded. A test parsing `vercel.json` is cheap insurance. **Resolved 2026-08-17**: `src/app/api/cron/windowInvariant.test.ts` parses `vercel.json`, scrapes `WINDOW_HOURS` from the route source, and asserts the relation in both directions — wide enough to survive a missed run plus jitter plus the DST hour, and not so wide the overlap is unbounded. It throws on a cron shape it cannot compute from, so an unparseable schedule fails loudly rather than silently skipping the relation it guards.
- [x] **[src/lib/db/posts.ts:102 vs :316, :360; src/lib/db/guides.ts:171 — scheduled-content filter]** — the filter lives in SQL for the list and search, and in JS-after-cache for the archive, sitemap, feed, and guides. Four surfaces answering "is this live yet?" two ways, correct only because the cron busts every relevant tag together. Worth one comment naming the split, since the list's SQL form is what makes the cron mandatory. **Resolved 2026-08-17**: named the split in a header comment on `publishedWhere` — which surfaces use which form, that neither re-runs per request, and why the SQL form is not a drop-in for the JS one (it also fixes the page boundaries).
- [x] **[src/app/admin/adminPageContract.test.ts:15-17 — invariant 1]** — encodes "sits under `(protected)/`" as equivalent to "guarded", but Next does not re-execute a layout on client-side navigation within the same segment, so `guides/new/page.tsx:11-14` and `guide-topics/new/page.tsx` can run `getProjectsForAdmin()` on a request where the layout check did not. Bounded by the `/admin/:path*` matcher, so an accuracy problem in an enforced invariant, not an open hole. **Resolved 2026-08-17**: the docblock now states that placement is not the same as guarded, names the client-side-navigation case and the two `new/page.tsx` files it affects, and says the matcher is what bounds it.
- [ ] **[src/app/blog/[section]/p/[page]/page.tsx:18-21 — `dynamicParams` default]** — the docblock's guarantee ("`/p/4` renders once on demand and then caches") assumes a previously-cached `notFound()` render carries the `blog-{section}` tag. This is the same stale-404 class that `61929b8` just fixed on the detail routes; cheap to pin with a test, expensive to rediscover. **Partially resolved 2026-08-17**: the route now reads `getSectionPageCount` — an `unstable_cache` tagged `blog-{section}` — on the 404 path itself, which is what rides a bustable tag onto the cached not-found entry, and a test asserts that read happens before the 404. The propagation itself is Next's behaviour, so it stays unverified here for the same reason as the `.md` route's identical open item below.

## Concurrency & Thread Safety

- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:71-84 — `GET`]** — three clock reads for one conceptual window: `Date.now()`, `currentDatetimeString()`, and a fresh `new Date()` for the guide query. The post and guide windows end at different instants, contradicting the parity `guides.test.ts:757` asserts in prose. Derive one `const at = new Date()` and pass it everywhere. **Resolved 2026-08-17**: one `const at = new Date()` feeds both windows, with a test asserting the two halves close at the same instant through the shared formatter.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:71-75 — window bounds]** — the lower bound is an absolute offset flattened to a *local wall-clock* string for the post query, while the guide query keeps the absolute `Date`. Under a non-UTC runtime a spring-forward makes the post window 48h — exactly 2× the interval, erasing the jitter hour `WINDOW_HOURS = 49` buys — while the guide window stays 49h. Inert on Vercel, but nothing pins TZ anywhere in the repo. **Resolved 2026-08-17**: `WINDOW_HOURS` is 50, not 49, and the derivation now names all three hours it buys back (missed run, Hobby jitter, DST shift). Kept the wall-clock frame rather than pinning `TZ` — `Post.datetime` is an authored wall-clock string, so the compare has to happen in that frame; the extra hour is what makes the invariant survive a DST zone.

## Observability

- [x] **[src/lib/api/cronAuth.ts:57 — `requireCronAuth`]** — the `[tag] unauthorized` warn carries no request metadata at all: no path, method, header presence, or correlation id, unlike the sibling `logMiddlewareBypass` payload. A cron firing with a stale secret is indistinguishable from a port scan, which defeats the reason the doc gives for logging this branch. **Resolved 2026-08-17**: the warn carries `path`, `method`, and `hasAuthorizationHeader` — a scanner sends none, a misconfigured cron sends a wrong one. The header VALUE is deliberately excluded (it is a near-miss of the real secret) and a test asserts it never reaches the log.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:90-93 — failure log]** — carries `windowStart` and the error but not `now`, and — because of `Promise.all` — not which of the two queries failed. Both success paths log more context than the one path that needs it. **Resolved 2026-08-17**: the line now carries `requestId`, `now`, `postsFailed`, `guidesFailed`, and each side's error separately. The success log gained the two booleans as well, since a half that failed otherwise reads identically to a half that found nothing — both report a count of 0.
- [x] **[src/lib/auth/middlewareBypass.ts:29 — `bypassIdForRequest`]** — the shared-id claim rests on React `cache()` scope spanning `generateMetadata` and the layout render. If that stops holding across a Next major the ids diverge and nothing fails, logs, or tests; the module's whole reason to exist degrades silently. **Partially resolved 2026-08-17**: not by asserting the invariant — it cannot be asserted from a unit test. `cache` memoizes per request, and a request scope only exists under React's server dispatcher, which Next establishes and Vitest does not; outside one it calls straight through. The new test file pins that fact explicitly so the absence doesn't read as an oversight, and adds a structural assertion that the module still routes the id through `cache(...)`, which catches the realistic regression (someone drops the wrapper). The React-major scenario the finding describes stays undetectable here.
- [x] **[src/lib/auth/adminMetadata.ts:94 — `loadName` catch]** — carries neither `bypassId` nor the shared bypass text, and is a three-argument shape where every other line in the group is two. A DB outage on an edit page produces a line no existing alert grep matches. **Resolved 2026-08-17**: two arguments carrying `bypassId`, matching the rest of the layer. `bypassIdForRequest` is exported for this, so the line joins the same request as any bypass line from the same render. Left the message text distinct — this is a loader failure, not a bypass, and folding it into the shared bypass string would make a DB outage alert as a security event.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:95 — hand-rolled 500]** — bypasses `respondInternalError` (`src/lib/api/apiErrors.ts:59`), so the response carries no `requestId` and cannot be correlated with the logged line. Defensible (the helper would drop the window context), but the two should be reconciled rather than quietly diverging. **Resolved 2026-08-17**: reconciled by matching the contract rather than the call — a `randomShortId` goes into both the error log and the 500 body, so the response correlates to the line, while the window bounds `respondInternalError` would have dropped are kept. A test asserts the two ids are the same value.

## Performance

- [x] **[prisma/schema.prisma:35 + src/lib/db/posts.ts:455 — `findPostsBecameLive`]** — `where { published, datetime range }` omits `section`, so the `[section, published, datetime]` composite's leftmost column is missing and the query degrades to a scan. Same for `findGuidesBecameLive`: no index on `publishedAt` at all. Both run daily; negligible at current row counts, worth knowing before either grows. **Resolved 2026-08-17**: added `@@index([published, datetime])` on `Post` and `@@index([published, publishedAt])` on `Guide`, each with a comment saying why the existing composite cannot serve the query — its leftmost column is absent because the due-check is deliberately scope-less. Needs a `yarn db:push` to reach the database.
- [x] **[src/app/blog/[section]/p/[page]/page.tsx:22-35 — `generateStaticParams`]** — calls `getPostsBySection(section)` for `totalPages` alone, which runs the full page-1 `findMany` with `postListItemSelect` (carrying `body`) plus the `count`, then discards `posts`. A count-only helper avoids pulling two full pages of post bodies at build. **Resolved 2026-08-17**: added `getSectionPageCount` — one cached `count`, no rows, its own cache key, sharing the `blog-{section}` tag. Used by `generateStaticParams` and by the route's new out-of-range check, which is exactly when the rows are least wanted.

## Missing Tests

- [x] **[src/app/api/cron/revalidate-scheduled/route.test.ts:218-226 — "looks back further than the widest gap"]** — the window test does not test the window. `windowStart < now` is a trivially-passing lexicographic compare, and `> 25h` passes for any `WINDOW_HOURS` from 26 to infinity while the constant is 49. The one number the whole design hinges on is unlocked, and it survived `61929b8` untouched. Use fake timers and assert the bound to the minute. **Resolved 2026-08-17**: fake timers pin the instant and the window is asserted to equal `WINDOW_HOURS` exactly, plus a second test that the upper bound is `now` rather than drifted forward. The relation to the cron schedule moved to `windowInvariant.test.ts`.
- [x] **[src/lib/api/cronAuth.ts:24 — `isAuthorized`]** — no test file, and the only module in `src/lib/api/` without one. The equal-length branch — the only place a real byte comparison decides a negative — is never executed: every fixture differs in length from the 18-byte `Bearer test-secret` and short-circuits at `:18`, so inverting `:24` leaves every test green. One 18-byte fixture closes it. **Resolved 2026-08-17**: new `cronAuth.test.ts` with a same-length wrong credential — asserted same-length, so it can't silently stop exercising the byte comparison — plus prefix-match, scheme, and configuration cases. Turned up one surprise worth pinning: a trailing-whitespace header authorizes, because the Headers API strips optional whitespace per RFC 9110 before this code sees it.
- [x] **[src/app/blog/[section]/page.tsx — no test file]** — the page-1 route has no test at all: section validation, `generateStaticParams`, `generateMetadata`, and the hardcoded `page={1}` are uncovered. The `searchParams` removal is the point of the whole change and has no regression pin, though `sitemap.test.ts:483` already uses `expect(module).not.toHaveProperty(...)` for exactly this concern. **Resolved 2026-08-17**: new test file at the route's post-`df990a4` path, `(list)/page.test.tsx`. Pins the `searchParams` absence, `dynamicParams = false` (the soft-404 fix that landed with the move), the generated param set, section validation, and that the canonical URL and title carry no page number.
- [x] **[src/components/blog/Pagination.tsx — no test file]** — both hrefs were rewired to `blogPagePath` and nothing covers them. The load-bearing case is prev-from-page-2 resolving to the bare `/blog/:section` rather than `/p/1`; that is the entire duplicate-content contract, resting on one un-asserted ternary. **Resolved 2026-08-17**: new test file. Prev-from-page-2 resolving to the bare `/blog/tech` is asserted directly, with the other three href cases, both edges, past-the-end, and the nav landmark's accessible name.
- [x] **[src/lib/auth/middlewareBypass.ts — no test file]** — the shared-`bypassId`-per-request invariant is asserted nowhere; all three consumer tests only check `bypassId: expect.any(String)`. Coverage reports the module green because the consumers exercise its lines, which makes the untested contract look covered. **Resolved 2026-08-17**: new test file covering the log shape, the surface field, the greppable invariant text, and id presence/omission. The shared-id contract itself is not assertable from a unit test — see the `:29` entry above for why, and what stands in for it.
- [x] **[vitest.config.ts:55 — `coverage.exclude`]** — `src/app/api/**` stays excluded after `fbbc610` removed the `.tsx` exclusion for exactly the reason the comment gives. The cron route and the newly-static `.md` route — the largest new logic in this range — sit outside the report entirely, in the commit whose stated purpose was to stop hiding logic from it. **Resolved 2026-08-17**: `src/app/api/**` dropped from the exclusions, with a comment naming why it was wrong for the same reason the broad `.tsx` glob was.
- [x] **[src/app/admin/adminPageContract.test.ts — no negative fixture]** — a hand-rolled walker plus five source-text assertions, with nothing proving the walk fails on a violating tree. The two non-vacuity guards only check `length > 0`, so a predicate typo that silently narrows the set still passes. **Resolved 2026-08-17**: the walker's one piece of real logic (`routeFor`) is now a pure function tested on synthetic paths, including the `[id]/page.tsx` and `[id]/preview/page.tsx` shapes the old predicate missed and a nested route group. The list-vs-tree check is an equality rather than a `length > 0`.
- [x] **[src/app/api/cron/revalidate-scheduled/route.test.ts:200-212 — failure path]** — only the post-side query is made to reject; the guide side of the aggregate rejection is untested, and nothing asserts the 500 body shape or the error log. Status-only assertions leave the response contract free to drift. **Resolved 2026-08-17**: added guide-side rejection, partial-work-survives, error-log shape, and `requestId` body-matching tests.
- [x] **[src/lib/db/posts.test.ts:174-204 — page numbers 91/92/93]** — arbitrary page numbers dodge module-lifetime wrapper memoization, so any future test reusing 91–93 silently breaks them. The uniqueness assertion at `:203` also only checks `new Set(keys).size === keys.length` and never asserts the key contains the page number. **Resolved 2026-08-17**: the comment now says 91-93 are reserved and that reuse breaks these tests rather than the new one, and the assertion checks the keys contain the page numbers rather than merely differing.
- [x] **[src/lib/content/metadata.ts:72 — alt text]** — nothing couples `defaultOgImageDescriptor.alt` to the OG script's `NAME`/`TAGLINE`. The dimensions have a PNG-header test; the alt has no equivalent, and it is the string a screen reader announces for every shared link on the site. **Resolved 2026-08-17**: the alt is asserted against `OG_CARD_NAME`/`OG_CARD_TAGLINE`, plus a structural test that the OG script still imports them rather than restating its own copies — which is the drift a runtime assertion alone would miss.
- [x] **[src/lib/utils/randomShortId.ts — `randomShortId`]** — no test file, the only module in `src/lib/utils/` without one. Nothing asserts length 12, the hex alphabet, or non-repetition; a bad refactor of the `slice` would silently shrink the entropy of every log correlation id. **Resolved 2026-08-17**: new test file covering length, the hex alphabet, non-repetition across 1000 draws, and that the UUID's dashes don't survive — which would halve the entropy while the length assertion still passed.
- [x] **[src/app/blog/[section]/p/[page]/page.test.tsx — clamp path]** — covers `abc`, `2abc`, `2.5`, `""`, `" 2"` but not `"999999"` (clamp makes the round-trip check reject) or `"10000"` (passes validation, falls through to `BlogPostList`'s empty-page 404). Those two are the boundary between the two different 404 mechanisms. **Resolved 2026-08-17**: both mechanisms pinned — `999999` dies on the round-trip check with an assertion that no query ran, `29` survives parsing and dies on the real page count, and `3` (the last real page) renders, so the bound can't go off by one. Added `02`/`007` to the segment cases and a metadata mirror for each.
- [x] **[src/app/admin/(protected)/layout.test.tsx:74 — "logs before redirecting"]** — asserts only `toHaveBeenCalled()`, which the `:54` test already establishes given the mocked `redirect` throws. It adds no constraint the preceding test does not. **Resolved 2026-08-17**: removed, replaced by a comment saying why there is deliberately no such test — the mocked `redirect` throws, so the preceding test already proves the ordering.

## Risk Assessment

**Medium.** Sixteen commits reshaped load-bearing plumbing: legacy routing moved off billed compute into `next.config.ts`, blog pagination moved from `?page=` to path segments and went static, the hourly `revalidate = 3600` backstop was replaced by a single daily cron, the middleware matcher narrowed to two explicit namespaces, and the `/og` route became a build script. Contracts mostly narrowed rather than widened, which is the right direction, and the work is unusually well documented. Type-check is clean and 2048 of 2049 tests pass.

Two things hold the rating at Medium rather than Low. First, the cron is now the sole mechanism surfacing scheduled content, and its correctness rests on a `WINDOW_HOURS ≥ 2 × interval + 1` relation that lives in prose across two files — a schedule edit landed *during this review* with nothing to check it, and the one test guarding the window accepts any value from 26 hours upward. Second, `61929b8` found and fixed a genuine production defect in this same mechanism (detail pages and `.md` routes pinning a 404 rendered while a post was still future-dated, invisible because the aggregates listed it correctly); that class of prerendered-404 staleness is not yet pinned by a test, and the paginated blog route carries the same shape.

The admin guard work is a real improvement in enforceability, but `adminPageContract.test.ts` asserts source text where its API-side sibling asserts behaviour, so it delivers less than its own docblock claims. No finding here is an open security hole — the `/admin/:path*` matcher remains the primary gate and covers every case.

## Notes

- **Orchestration**: split. Five partitions — routing/proxy, auth/admin/lint, content metadata + OG, blog pagination + Prisma, cron/revalidation — each a gatherer (`Explore`) → analyzer (`general-purpose`, opus) pair. The routing and metadata/OG gatherers were killed mid-run; those two partitions were covered inline by the main agent instead. Consolidation was done in main (≤5 partitions, no synth subagent).
- **Scope drift**: the review baselined on `5615d47..8ad4434`. Commit `61929b8` ("Serve post .md variants statically and heal scheduled-content 404s") landed while the agents were running and was reviewed as a follow-up pass, so the range reported here is `5615d47..61929b8`. Its `count*BecameLive` → `find*BecameLive` rename invalidated nothing in the cron findings above; each was re-verified against the current file.
- **Test run**: 133 of 134 files pass, 2048 of 2049 tests. The single failure is a vitest pool worker-start timeout on `src/components/ui/PresetOrFreeformInput.test.tsx` — infrastructure, not an assertion, and the file is outside this change set. Setup time was 1088s against 544s of test time, consistent with sandbox resource contention.
- **Generated client is stale**: `src/generated/prisma/internal/class.ts` still carries `@@index([slug])` in its `inlineSchema` snapshot. Consistent with the open thread in `dev-journal/2026-08-15.md` to run `yarn db:push`; not a finding, but the drift is real until that runs.
- **Journal watch-outs this range touches**: "Cron is now a single point of failure for scheduled content" and "`WINDOW_HOURS` is coupled to the cron schedule in `vercel.json`" are both live and now have concrete instances above. The palette-drift and stable-`og-card.png`-URL watch-outs are unchanged. "`yarn og:card --check` is not wired into CI" gains a caveat: wiring it as-is would fail CI on an upstream Google Fonts revision.
- **Open threads possibly closed by this range**: the `/og` 404 verification thread is moot (route deleted). The `bypassId` join thread remains open and is still unverified in a real request.
- No file content in any partition contained instructions directed at the reviewing agent.

## Review 2

Scoped to `61929b8` alone (the only commit in the last 24h). Review 1 already folded it in as a follow-up pass, so this block is the delta — findings already listed above are not repeated.

### Issues and Observations

- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:28-29 — `WINDOW_HOURS` docblock]** — still derives the 49h bound from `0 0 * * *` "fires anywhere inside the midnight hour"; `vercel.json:9` is now `0 1 * * *`. The same commit changed the schedule and left the prose, so a reader re-checking the derivation checks it against an expression the project no longer uses. **Resolved 2026-08-17**: derivation now quotes `0 1 * * *` and the 01:00 hour.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:34-37, :57-58 — cost argument]** — "Both queries are counts, so a wider window costs the same to run", "the count is 0 either way", "busting three tags a second time", and "two count queries per day" are all stale after count→rows. A wider window now returns more rows and fires one extra `revalidateTag` per row, so the "overlap is cheap" reasoning `WINDOW_HOURS = 49` rests on is no longer self-evident from its own comment. **Resolved 2026-08-17**: rewritten as "bounded rather than free" — each extra row costs one redundant `revalidateTag`, the empty path costs nothing, and the ceiling is one extra day of scheduled content. `:57` now says "due-content queries".
- [ ] **[src/app/api/blog/[section]/[slug]/md/route.ts:18-19 — tag ride-up claim]** — the commit's central claim (`getPostBySlug`'s tags land on the route-cache entry, so `revalidatePost` regenerates the markdown) is asserted only in prose. It matters most on the miss path: `wrapNullableDetail` throws `CacheMissError` (`src/lib/db/cacheMiss.ts:63-68`) so nothing is stored, and whether the tags still register on the surrounding render decides if a 404 pinned before a post existed can ever be healed. Verify against a real deploy. **Still open, 2026-08-17**: unchanged, and not closeable here. The mechanism is Next's own tag propagation from `unstable_cache` onto a prerendered route entry; no unit test can observe it, and the miss path (`CacheMissError`, nothing stored) is exactly where a test would be least informative. Needs the deploy check the finding asks for. The paginated blog route carries the identical open question — see Review 1's `:34`.
- [x] **[src/lib/db/guides.ts:542 — `findGuidesBecameLive`]** — selects `slug` only, while the post side deliberately selects the full `postTag` identity and pins it with a test (`posts.test.ts` — "selects the identity the caller needs to bust a detail tag"). Correct today, but the asymmetry is undocumented and the select has to change again the moment a guide's topic needs busting. **Resolved 2026-08-17**: documented on `findGuidesBecameLive` — each side selects exactly the identity its own tag helper keys on (`guideTag` is slug-only, `postTag` is section+slug), and the select grows if a due guide ever has to bust its topic hub.

### Architecture & Coupling

- [x] **[src/lib/db/guides.ts:324-350, :362 — `getGuideTopicBySlug` vs the cron]** — the topic hub caches under `guide-topic-{slug}` + `guide-pages` and filters scheduled guides at read time, but the cron busts only `GUIDES_TAG` and `guide-detail-{slug}`. A guide coming due would not appear on its hub. Latent only because no `/guides/topics` route exists yet — `getGuideTopicBySlug` and `revalidateGuideTopic` are exported and tested but unrouted. **Resolved 2026-08-17**: documented as a known latent gap on `getGuideTopicBySlug`, naming the tags involved, why it is harmless today, and that routing the hub means adding a topic bust to the cron. Not fixed — adding a bust for an unrouted page is work for a page that does not exist.
- [x] **[src/app/api/cron/revalidate-scheduled/route.ts:119-133 vs src/lib/db/posts.ts:484 — missed-window residue]** — a cron gap wider than 49h strands both halves, but they recover unevenly: the next admin save busts the section aggregates for everyone, while `revalidatePost` busts only the *saved* post's detail tag. The stranded post ends up listed everywhere and 404ing on its own URL — precisely the state this commit set out to remove — with `revalidateAllPosts` (`post-pages`) the only thing that heals it. Worth naming in the docblock, since the journal already flags the cron as a single point of failure. **Resolved 2026-08-17**: named on the `revalidatePostDetails` call, including `revalidateAllPosts` as the recovery lever if a run is ever confirmed missed.

### Missing Tests

- [x] **[src/app/api/cron/revalidate-scheduled/route.test.ts — log payload]** — the new `duePostSlugs`/`dueGuideSlugs` fields are unasserted, though their own comment calls them the one lever for tracing a stranded post after the fact. Every other behaviour in the file (bust calls, JSON body, window shape) is pinned. **Resolved 2026-08-17**: both slug lists asserted, plus the overflow case where they are replaced by a marker rather than silently reported as empty.
- [ ] **[src/app/api/blog/[section]/[slug]/md/route.test.ts — tag propagation]** — the suite mocks `@/lib/db/posts` wholesale, so `force-static` and `generateStaticParams` are pinned but the tag ride-up that makes the static switch safe is not exercised at all. Same untested-mechanism shape as the stale-404 class flagged at Review 1's `:34`. **Still open, 2026-08-17**: same reason as `route.ts:18-19` above — the propagation is Next's, and mocking `@/lib/db/posts` is what makes the rest of the suite possible. A test that unmocked it would assert Prisma call shapes, not tag ride-up.

### Risk Assessment

**Low.** `61929b8` is a well-scoped fix to a real defect, and the tests that landed with it are strong — the detail-tag busts, the empty-list no-op, the half-open window, and the `select` shape are all pinned by assertions that would fail on a regression. The 126 tests across the four changed files pass. What holds it short of trivial is that the mechanism the fix depends on (tags propagating from `unstable_cache` onto a prerendered route entry) is documented but never exercised, and the same commit edited the cron schedule without updating the prose that derives `WINDOW_HOURS` from it. Neither is a defect today; both are the kind of drift that makes the next incident harder to reason about.

### Notes

- **Orchestration**: inline (1 commit, 10 files, 474 LOC delta — under both split thresholds).
- **Overlap with Review 1**: the duplicated comment at `route.ts:135-140`, the unbounded `findMany`, the `WINDOW_HOURS`/`vercel.json` unguarded invariant, the missing indexes on both due-checks, the orphaned docblock at `guides.ts:520-524`, and the `src/app/api/**` coverage exclusion were all already filed above and are not repeated here.
- **Unrelated change in the same commit**: `package.json` `start` now pins `NODE_ENV=production`. Not mentioned in the commit message and unrelated to its subject; it matches a known local-build footgun, so it reads as deliberate.
- **Journal drift**: `dev-journal/2026-08-15.md:9` open thread says to watch for the "hourly `nothing due` heartbeat". The cadence is daily, so the check as written will not fire.
- **Out of scope**: the working tree carries uncommitted changes (`src/app/blog/[section]/page.tsx` and `.../archive/page.tsx` modified, both `projects` `loading.tsx` files deleted). Not reviewed — no commit covers them.
- No file content read during this pass contained instructions directed at the reviewing agent.
