# Code Review — 2026-05-11 — Full codebase

## Issues and Observations

- [x] **`src/lib/posts.ts:141 — getPostBySlug`** — no `published: true` or `datetime <= now` filter; drafts and future-scheduled posts are served via the canonical `/blog/:section/:slug` URL, even though listings/feed hide them. Same shape via `loadPost` and `generateStaticParams`. **Resolved 2026-05-11**: switched to `findFirst` with `where: { section, slug, published: true, datetime: { lte: now } }`; tests pin the new filter.
- [x] **`src/app/sitemap.ts:36 — sitemap` + `src/app/blog/[section]/[slug]/page.tsx:19 — generateStaticParams`** — both pull from `getAllPublishedPostSlugs`, which only filters `published: true`. Future-dated posts land in sitemap.xml and are prerendered at build, exposing the same draft-leak as above to search engines. Carried watch-out from 2026-05-07. **Resolved 2026-05-11**: `getAllPublishedPostSlugs` adds `datetime: { lte: now }`; the prior "does NOT filter" sitemap test was flipped to assert the new filter.
- [x] **`src/app/api/cron/ping/route.ts:34-44 — GET`** — when `CRON_SECRET` is unset, the env-config check runs before the auth check, so an unauthenticated probe gets a 500 with `{ error: "CRON_SECRET not configured" }` naming the env var. Move auth above the config check. **Resolved 2026-05-11**: missing-`CRON_SECRET` now returns 401 with generic `{ error: "Unauthorized" }` while keeping the server-side `console.error` for operator visibility; test pins the no-leak contract.
- [x] **`src/app/api/upload/route.ts:78-83 — POST MIME allowlist`** — relies on the client-supplied `file.type`; an authed admin with a hijacked session can upload `text/html` masquerading as `image/png` to a public Blob. Sniff magic bytes before `put`. **Resolved 2026-05-11**: `detectImageMime` (PNG/JPEG/GIF/WebP/AVIF magic bytes) runs after the allowlist; mismatch returns 415 with a `console.warn` tagged `mime mismatch`. Unit tests cover every accepted format + HTML payload rejection.
- **`src/app/api/auth/logout/route.ts — POST`** — no CSRF protection; a logged-in admin visiting a hostile page can be force-logged-out. Confirm `sameSite=strict` on the session cookie in `auth.ts`, or require a header check.
- [x] **`src/lib/format.ts:122 — createSlug`** — `createSlug("!!!???")` and similar all-punctuation titles return `""`; `Post.slug` is then either rejected at the DB or stored empty. Add a fallback (`"untitled"`) or a Zod `.refine` against empty slug. **Resolved 2026-05-11**: `producesNonEmptySlug` refine on `postCreateSchema.title` and `projectCreateSchema.name`; admin form returns clean 400 instead of letting it surface as a DB constraint error.
- **`src/lib/format.ts:48 — formatDate`** — local-time `new Date(y, m-1, d)` still renders the wrong day at the Vercel-region TZ boundary; flagged 2026-05-07, still open. Pin to UTC via `Date.UTC` + `toLocaleDateString("en-US", { timeZone: "UTC" })`.
- [x] **`src/lib/format.ts:126 — createSlug` punctuation regex** — `[\s.‐-―]+` covers `‐` (U+2010) through `―` (U+2015) but not U+2212 minus, soft hyphen, or NBSP-only separators; a slug from a title containing those silently fails to collapse. **Resolved 2026-05-11**: dash class extended to `[\s.‐-―−­]+` (added U+2212 + U+00AD); tests pin `createSlug("−−−") === ""` and `createSlug("­­­") === ""`. NBSP is already covered by `\s` in JS regex.
- **`src/lib/loanCalculator.ts:122-129 — extraPayments loop`** — `durationOfRepay % frequency === 0` applies the extra payment on month 0 (`frequency: 12` → first-day extra payment, UX surprise); and the final extra is not capped to `remainingLoan`, so `valueOfPaidExtraPayments` overcounts when the last payment overshoots zero.
- **`src/lib/reorder.ts — moveAndReorder` OOB** — JSDoc claims "every `sortOrder` reassigned by position … contiguous starting at 0," but both OOB branches early-return without reassignment. Carried from 2026-05-07; still no test pins the actual contract.
- **`src/lib/auth.ts:118 — dummyHash` module-load**: `bcrypt.hashSync("dummy", 10)` blocks ~80-100ms on cold start for every route that transitively imports `auth.ts` (including unauthenticated reads). Lazily memoize behind a function.
- [x] **`src/app/api/admin/posts/route.ts:48-53 + projects/route.ts:96-100 — 409 unique-slug no log**, and **`upload/route.ts:78-83 — 415 disallowed MIME no log**, and **`upload/route.ts:43-71 — 413 paths no log`** — adversarial/abusive signals collapse into silent responses; sustained probing is invisible. Add `console.warn` with route tag + relevant context (slug / mime / content-length). **Resolved 2026-05-11**: all five paths now log at warn with structured payloads; route tests pin the format.
- [x] **`src/app/api/cron/ping/route.ts:48 + auth/login/route.ts:54 — `console.error` for routine adversarial hits** — bot scans and rate-limit-hits log at error level; under any sustained probe these dominate the log. Demote to `warn`; keep credential-misconfig at error. **Resolved 2026-05-11**: both demoted to `warn`; cron-unauthorized test pins `console.error` is NOT called for wrong-secret. Credential-misconfig + invalid-credentials stay at error.
- [x] **`src/app/api/admin/*/route.ts — no audit log on writes`** — `login`/`logout` emit `console.info` audit lines but admin create/update/delete on posts and projects are silent. With no Vercel paid log retention, "did someone delete a post at 3am" has no answer. **Resolved 2026-05-11**: every POST/PUT/DELETE on posts and projects emits a structured `console.info` audit line (id + slug + section/previousSection/previousSlug as relevant); tests pin the payload shape.
- [x] **`src/app/api/admin/posts/[id]/route.ts:92-100 — PUT previous-section read outside `$transaction`** — `findUnique` for the prior section runs separately from the `update`; two concurrent PUTs see the same prior section. Single-admin makes the race theoretical; the asymmetry vs. the project PUT (Serializable txn) is the bigger smell. Same shape on `projects/[id]/route.ts:84-92` for `previousSlug`. **Resolved 2026-05-11**: both PUTs wrap the prior-state read and the update in a single `prisma.$transaction`; project PUT keeps Serializable isolation, post PUT stays at default (no sortOrder shift). Regression tests pin the in-txn contract.
- **`src/app/api/admin/projects/[id]/route.ts:140-154 — PUT sections/links destructive replace** — `deleteMany` + nested-create on every PUT that includes `sections`, even for a one-character description tweak; mutates `updatedAt` on every child row. Diff at the helper boundary, or document the destructive-by-design contract.
- **`src/app/api/admin/posts/route.ts:8 + projects/route.ts:14 + upload/route.ts:24 — `Request` vs `NextRequest`** — five admin handlers type the arg as `Request`; `login`/`cron` use `NextRequest`. Pick one.
- **API routes have no `export const maxDuration`** — default 10s on Vercel Hobby; upload (~10 MiB) and feed (cold-cache `Promise.all` of 20 `markdownToHtml`) can run close to or past it. Set explicitly per route so future regressions surface at deploy.
- **`src/app/blog/[section]/search/page.tsx:40 — UX threshold mismatch`** — `searchPosts` short-circuits at `term.length < 2`, but the page just shows "Nothing found" for `?q=a`. Surface "type at least 2 characters" gated on `MIN_SEARCH_TERM_LENGTH`.
- **`src/app/error.tsx + global-error.tsx — no error id surfaced`** — `apiErrors.ts` already issues 12-char request ids; mirror for the boundary so users can hand back an id matching `console.error`'s.
- **`src/app/error.tsx:21 + not-found.tsx:11 — `aria-hidden` status number`** — large `500`/`404` is `aria-hidden`; screen readers hear "Something went wrong" with no status. Include the code in the visible heading or as accessible text.
- **`src/app/[slug]/page.tsx:26 — `console.error` tag**: `"[page:[slug]] lookupLegacySlug failed for"` — square brackets nested inside square brackets are awkward for grep. Standardize on `[legacy-slug:db-failure]` (carried from 2026-05-07).
- **`src/proxy.ts:51-56 — admin guard via blocklist`** — `isAdminPage` matches `/admin` and excludes `/admin/login`; any future intermediate path (e.g. `/admin/forgot-password`) is gated by default. Invert to an allowlist `PUBLIC_ADMIN_PATHS = new Set([...])` so intent is visible.
- [x] **`src/components/admin/LoginForm.tsx:27 — `router.push` without `refresh()`** — `useAdminResource` pairs `push` + `refresh()` (lines 70-73); login is the one admin mutation that doesn't. On slow networks, RSC tree lags behind the cookie. **Resolved 2026-05-11**: `router.refresh()` added after `router.push("/admin")`; test asserts both fire on successful login.
- [x] **`src/components/admin/LoginForm.tsx + AdminNav.tsx — no `AbortController`** — every other admin mutation got abort wiring in the 2026-05-07 sweep; login and logout are the holdouts. **Resolved 2026-05-11**: both got `abortRef` + `useEffect` cleanup; `AbortError` swallowed silently; tests pin the abort-on-unmount contract for both.
- [x] **`src/components/admin/LoginForm.tsx:33 — bare `catch {}` swallows context**: collapses network rejections and JSON-parse errors to the generic message with no `console.warn`. Add a tagged log; missing logs here mean a flapping login is invisible. **Resolved 2026-05-11**: tagged `console.warn("[admin:LoginForm] submit failed", err)`; same pattern applied to `AdminNav` logout. Tests pin both warn calls.
- [x] **`src/components/admin/IsFeaturedToggle.tsx:50-54 — only generic "Failed to save"**: ignores the server's error body; a 409 / 413 / 500 render identical messages. Reuse `readErrorMessage(response, "Failed to save")` like the rest. **Resolved 2026-05-11**: routed through `readErrorMessage` (server's `data.error` plus `(HTTP NNN)` suffix). Same change applied to `ImageUpload` for consistency; tests pin both surfaces.
- **`src/components/admin/ProjectSortOrderInput.tsx:44 — `parseInt(value, 10)` accepts trailing garbage**: `"3abc" → 3`; clamp hides it but a user typing `"3.7"` silently saves as 3 with no indication. Use `Number(value)` + `Number.isInteger`.
- **`src/components/admin/PlatformPicker.tsx:28-30 — derived state in `useState` init`** — `useState(validParsedKeywords)` seeds only from the first prop value; parent prefill drift after mount is invisible. Same derived-state class as `ExpandableSearch.tsx:25 — useState(initialValue.length > 0)`. Carried from 2026-05-07.
- **`src/components/admin/PlatformPicker.tsx:105-113 — hidden `required` mirror input** — `required` on an `sr-only readOnly` input means the browser focuses an off-screen element on validation. Carried from 2026-05-07.
- **`src/components/Typewriter.tsx:33 — `phrases[phraseIndex]`** — no guard against empty `phrases` or a shrinking array; `undefined.length` crashes. Closure also reads `phrases` without it in the dep array (only `phrases.length`), so a parent that swaps phrase content keeps painting the old character.
- **`src/components/Typewriter.tsx:97-109 — caret blink** — `repeat: Infinity` with no `useReducedMotion()` opt-out. The CSS `prefers-reduced-motion` rule on `[data-blob-bg]` only covers `LandingBackground`.
- **`src/components/ThemeProvider.tsx:39-43 — `systemIsDark` SSR seed`** — `useState` initializer runs on SSR with `typeof window === "undefined"` → `false`, and React doesn't re-run initializers on hydration. First paint for a `theme=system` visitor with system-dark and no prior cookie renders light until the `change` effect fires.
- **`src/components/loan-calculator/LoanCalculatorClient.tsx:22-25 — `parseNum`**: an empty input snaps to `fallback` (the previous numeric value), so the user can't clear-to-retype. Treat `""` as a transient state or uncontrol.
- **`src/components/loan-calculator/LoanCalculatorClient.tsx:198-202 — `computeLoan` memoized twice`**: parent runs `useMemo(computeLoan, [params])` and the inner `Calculator` runs it again over the same params. Per-keystroke duplicate work.
- **`src/components/loan-calculator/LoanCalculatorSummary.tsx:17-23 — hardcoded `text-[coral]`/`text-[lightseagreen]`** — carried from 2026-05-07; still raw CSS color names rather than design tokens.
- **`src/components/projects/ProjectContent.tsx:43-45 — `<style>` `:root` injection** — carried from 2026-05-07; per-project accent enters the global cascade, fast SPA navigation can briefly compose conflicting `:root` rules. Move to inline `style` on a wrapper.
- **`src/components/projects/FeaturedProjectCard.tsx:65-67 — `bg-black/60 text-white/80`** — the "Discontinued" pill ignores theme tokens; contrasts oddly in light mode atop already-darker hero art.

## Architecture & Coupling

- **Admin auth single source** — three layers still enforce admin gating independently (`src/proxy.ts`, `src/app/admin/(protected)/layout.tsx`, per-`generateMetadata` calls). Carried from 2026-05-07 open thread; extract `assertAdminRequest()` shared by middleware and every admin `generateMetadata`.
- **`src/lib/posts.ts` + `src/lib/projects.ts` — wrapper-cache template duplicated**: `createBoundedWrapperCache` + `unstable_cache` factory + `React.cache` dedupe with structurally identical bodies. Carried from 2026-05-07; extract `bySlugLoader<T>(buildFetch, tagFn)`.
- **`src/lib/format.ts` — concept density** — id parsing, page parsing, datetime, slug, reading time, body truncation in one file. Carried; split into `format/datetime.ts`, `format/slug.ts`, `format/text.ts`.
- **`src/lib/auth.ts:21 — `getSessionSecret` name collision** with `env.ts`'s `getSessionSecret` (re-imported as `getRawSessionSecret`). Rename to `getSessionSecretBytes` or inline.
- **`src/lib/adminPageUrl.ts:5 — `parseTab` allowlist cast` and `src/lib/sections.ts:6 — `isValidSection` cast`** — same `(ARR as readonly string[]).includes(value)` shape twice; extract `isMember<T>(arr, v): v is T`.
- **`src/components/admin/PostForm.tsx + ProjectForm.tsx — `setField` + `useAdminResource` boilerplate** — ~50 lines of identical structure (state object, isEditing flag, submit/delete chrome, error surface). Extract `useAdminForm<TInitial, TPayload>(initialData, mapToPayload)`.
- **`src/components/admin/IsFeaturedToggle.tsx + ProjectSortOrderInput.tsx — `useFieldMutation`** — both reimplement the same `AbortController` + `useEffect` cleanup + saving-state + finally-aborts dance around a one-field PUT. Extract.
- **`src/components/admin/AdminSearch.tsx:14-19 — URL builder** — hand-rolled `?` vs `&` and `tab=projects` branching; the dashboard already centralizes this in `lib/adminPageUrl.ts`. Delegate to `buildAdminPageUrl`.
- **`src/app/api/admin/projects/route.ts:42-90 + [id]/route.ts:94-159 — sortOrder shift logic`** — `shiftForInsert` and `shiftForReorder` live inline in route handlers; `src/lib/reorder.ts` exists. Pull into the lib so cache-bust + shift can be tested in isolation.
- **`src/app/api/cron/ping/route.ts:14-29 — `isAuthorized`** — generic constant-time bearer-compare; belongs in `src/lib/auth.ts` next to the JWT helpers.
- **`src/app/api/auth/login/route.ts:27-39 — `clientBucketKey`** — x-forwarded-for parsing belongs in `src/lib/request.ts` next to `siteBase()`; the next rate-limited endpoint will need the same helper.
- **`src/components/AnimatedCard.tsx` vs `src/components/about/FadeIn.tsx`** — near-identical thin wrappers around `motion[as] {...fadeUp(delay)}`; `FadeIn` already supports `as`. Collapse (carried from 2026-05-07).
- **`src/components/blog/BlogSectionHeader.tsx:17-31 — reimplements `useClickOutside`** — `ExpandableSearch` already consumes the hook. Carried from 2026-05-07.
- **`src/app/blog/[section]/{page,archive/page,search/page,[slug]/page}.tsx — `isValidSection` guard repeats 4× plus matching `generateMetadata` short-circuit** — extract `assertValidSection(section)` and `sectionMetadata(section, kind)`.
- **`src/app/error.tsx + not-found.tsx + global-error.tsx — duplicated chrome and copy** — same giant-number + heading + description + back-home shape three times; voice drift across them is hard to spot. Extract `<ErrorPageScaffold code title description action />`; keep `global-error`'s inline-style variant (the rationale stands).

## Concurrency & Thread Safety

- [x] **`src/app/api/admin/posts/[id]/route.ts:92-100 + projects/[id]/route.ts:84-92 — prior-state reads outside transaction`** — already in Issues; restating because the contract asymmetry vs. the Serializable project-PUT is the load-bearing point. **Resolved 2026-05-11**: see the Issues entry; both reads now run inside their PUT's `$transaction`.
- **`src/components/admin/IsFeaturedToggle.tsx:50 + ProjectSortOrderInput.tsx:78 — optimistic-revert reads `initial*` prop`** — sort-order toggle re-mounts via `key={…-${sortOrder}}` in `ProjectAdminControls.tsx:23` so the revert sees fresh state; `IsFeaturedToggle` has no such key. Concurrent saves can write the wrong value back.
- **`src/components/Typewriter.tsx:35-83 — `phrases` not in deps** — closure reads `phrases[phraseIndex]` but the `useEffect` only depends on `phrases.length`. A parent that swaps phrase content mid-cycle leaks the prior text into the next frame.
- **`src/lib/markdown.ts:27 — module-scope unified processors** — still no comment marking the "do not call `.use(...)` mid-render" constraint (carried from 2026-05-07). Now relevant because `projects/[slug]/page.tsx:42` calls `markdownToReact` in parallel across all project sections via `Promise.all`.
- **`src/app/api/admin/projects/route.ts:42-90 — Serializable POST with no retry** — carried from 2026-05-07; document the contract or add a single retry for `P2034`.

## Observability

- **`src/app/api/upload/route.ts:93-104 — duplicates `respondInternalError`** — inlines `randomUUID().slice(0,12)` and emits `{ error: "Upload failed", requestId }` instead of going through the shared helper. Two slightly different conventions for "internal error" now coexist.
- **`src/lib/apiErrors.ts:14-27 — `handlePrismaError` 404 body has no `requestId`** — but `respondInternalError` does. A 404 surfaced through the helper gives the user no correlation handle.
- [x] **`src/lib/apiErrors.ts:84 — `parseJsonBody` schema-validation`** — returns 400 with raw zod issues in the body and does NOT log; login's hand-rolled equivalent logs paths-only at warn. The shared helper is the wrong way around — any admin POST/PUT with a malformed body is invisible to logs. **Resolved 2026-05-11**: paths-only `console.warn` added in the helper (mirrors login); test pins the format AND that submitted values never appear in any log call.
- **`src/app/[slug]/page.tsx:25 — `lookupLegacySlug` catch** — DB outage vs DB miss collapse into one `notFound()`; carried from 2026-05-07.
- **`src/app/sitemap.ts — DB failure`** — uncaught `getAllPublishedPostSlugs` reject bubbles to a 500 sitemap. Wrap in try/catch and serve static-routes-only with a tagged warn; search engines penalize 500 sitemaps harder than partial ones.
- **`src/proxy.ts — six legacy-pattern redirects fire silently** — no aggregate signal for "how much legacy traffic remains." Sampled `console.info` would tell you when to retire the rewrite.
- **`src/components/admin/IsFeaturedToggle.tsx + ImageUpload.tsx + LoginForm.tsx + AdminNav.tsx — silent throw paths** — non-ok branches set error string but never `console.warn`; an admin can't distinguish "network glitch" from "API rejected the payload" from logs.
- **`src/components/admin/MarkdownEditor.tsx:51-62 — preview render failure**: surfaces in the DOM but never to console; a flaky `markdownToReact` is invisible during local dev.

## Performance

- **`src/lib/posts.ts:115 — `getPostsBySection` page > 1`** — page 1 cached, page 2+ runs raw `findMany`+`count` per request. With the new `MAX_PAGE = 10_000` clamp, `?page=9999` crawler traffic spends 10k uncached query pairs in the worst case. Either tag-cache page 2+ (`revalidate: 60`) or document.
- **`src/lib/posts.ts:74-96 — `blogPage1Cache` snapshots `now` inside the cached function** — a post scheduled for a future `datetime` and published won't auto-surface at its time; only a mutation bus or organic eviction makes it appear. Set `revalidate: 60` or schedule a tag-bust.
- **`src/app/api/feed/[section]/route.ts:115-120 — `<updated>`** — `Math.max(...posts.map(...))` recomputed per request; move inside the cached fetcher and return alongside posts.
- **`src/app/projects/[slug]/page.tsx:42 — `Promise.all` `markdownToReact`** — uncached per-section parse; carried from 2026-05-07. Wrap in `cache()` keyed `(slug, sectionId, updatedAt)` or precompute on write.
- **`src/components/Typewriter.tsx:35-83 — re-render churn** — every char triggers full effect teardown + re-arm; carried from 2026-05-07. Replace with ref-stored timer or `requestAnimationFrame`.
- **`src/components/admin/PostForm.tsx + ProjectForm.tsx — non-memoized children`** — carried from 2026-05-07; every Title keystroke re-renders `MarkdownEditor`, `ImageUpload`, `LinkManager`, `SectionManager`.
- **`src/components/admin/MarkdownEditor.tsx:30-67 — preview parse on toggle`** — full unified pipeline blocks the Preview toggle for long posts. Pre-warm via `requestIdleCallback` or `useDeferredValue`.
- **`src/components/projects/ProjectSectionCarousel.tsx:46-66 — `loading="eager"` on every slide** — overrides Next/Image lazy loading for off-screen slides; if a project gets 10+ images this defeats the optimization. `priority` on index 0 only.
- **`src/components/loan-calculator/LoanCalculatorClient.tsx — keystroke recompute** — carried from 2026-05-07; consider `useDeferredValue` or debounce.
- **`src/lib/auth.ts:118 — `dummyHash` cold-start** — repeats the issue under Issues; the cost is paid by every route that transitively imports `auth.ts`.

## Missing Tests

- **`src/app/api/auth/login/route.test.ts — rate-limit branch + fail-open**: highest-priority gap; the most security-sensitive code path in the partition is untested. Cover 429, Upstash-error fail-open, per-IP key derivation from `x-forwarded-for` (leftmost-of-comma-separated, missing-header → `"unknown"`).
- **`src/proxy.test.ts — `/api/admin/*` and `/api/upload` 401**: the load-bearing security gate has no unauth-rejection coverage; the route tests assume the middleware ran. Also missing: privacy redirect query-string preservation; `/adminx` adjacent-prefix not gated; `/api/admin` bare-path with valid token (symmetric pair to the existing 401 case).
- [x] **`src/app/api/cron/ping/route.test.ts — `isAuthorized` equal-length-wrong-secret**: constant-time path not pinned for a same-length wrong secret. Also: auth must run before env-var check (locks the leak fix). **Resolved 2026-05-11**: the env-var-leak fix is locked by a dedicated "401 not 500, env var name not in body" test. The same-length wrong-secret constant-time test remains a deferred carryover.
- **`src/app/api/feed/[section]/route.test.ts — future-`datetime` and `published: false` exclusion**: the `where: { datetime: { lte: now }, published: true }` guard is unverified.
- **`src/app/api/feed/[section]/route.test.ts — `Cache-Control` header**: regression to `no-store` would silently kill CDN caching.
- **`src/app/api/admin/posts/route.test.ts + [id]/route.test.ts — `revalidatePostSection` tag coverage**: only `blog-${section}` is asserted; `feed-${section}` and `posts` (which `legacySlug.ts` depends on) are not.
- **`src/app/api/admin/projects/route.test.ts + [id]/route.test.ts — Serializable isolation**: a regression to drop `{ isolationLevel: Serializable }` would silently relax the contract. Assert the txn's second arg.
- **`src/app/api/admin/projects/[id]/route.test.ts — destructive `sections != null` triggers `projectSection.deleteMany`**: contract unpinned.
- **`src/app/api/auth/logout/route.test.ts — `destroySession` failure** — silent 500 vs. caller-handled isn't pinned.
- **`src/app/api/upload/route.test.ts — missing `Content-Length` header** (precheck branch absent) **+ non-numeric `Content-Length`** (NaN > max is false, silently skips precheck).
- **`src/app/sitemap.test.ts — future-`datetime` post + DB outage**: locks the bug so the fix can flip the assertion; pins the failure contract.
- **`src/app/blog/[section]/{page,archive/page,search/page}.tsx — no `page.test.tsx`**: minimum invalid-section + threshold/empty-state behavior.
- **`src/app/blog/[section]/[slug]/page.test.tsx — `generateStaticParams` shape**: drift breaks the build silently in CI.
- **`src/app/projects/page.tsx + projects/[slug]/page.tsx — no tests**: stagger-offset math + per-section `markdownToReact` call shape both untested.
- [x] **`src/lib/apiErrors.test.ts — `parseJsonBody`**: no tests for the helper extracted in the prior sweep (malformed JSON, schema fail, success). **Resolved 2026-05-11**: full coverage added — valid payload, malformed JSON 400, schema mismatch 400 (paths-only warn), no-value-leak guarantee.
- **`src/lib/apiErrors.test.ts — `parseIdParam("12abc")`** trailing-garbage at the route boundary still untested (carried).
- **`src/lib/apiErrors.test.ts — `handlePrismaError` tag-log**: the `console.warn` path on tag-passed 404 is not pinned.
- **`src/lib/reorder.test.ts — OOB `sortOrder`** doc-vs-impl contract.
- **`src/lib/markdown.test.ts — `stripMarkdown` GFM tables + nested lists + raw HTML + link-with-title** (carried).
- **`src/lib/posts.test.ts — `searchPosts` `MIN_SEARCH_TERM_LENGTH = 2` short-circuit** unpinned.
- **`src/lib/posts.test.ts + projects.test.ts — wrapper-cache reuse path** (`unstable_cache` wrapper shared across two `getPostBySlug` calls).
- [x] **`src/lib/format.test.ts — `createSlug` Unicode dashes** (U+2212, soft hyphen, NBSP) **+ empty-output downstream contract**. **Resolved 2026-05-11**: tests cover U+2212 + soft hyphen folding into the dash class and reduction to `""`; schema tests pin the downstream `producesNonEmptySlug` refine rejecting them.
- **`src/lib/format.test.ts — `currentDatetimeString` zero-padding** (a regression dropping `.padStart(2, "0")` would slip through).
- **`src/lib/legacySlug.test.ts — DB-throw path**: outage observability is unpinned.
- **`src/lib/loanCalculator.test.ts — final-month over-application + month-0 extra payment**: surprising UX behaviors are unpinned.
- **`src/components/admin/useAdminResource.test.tsx — AbortController fires on unmount + supersession** (carried from 2026-05-07; still acknowledged as skipped).
- **`src/components/admin/PostForm.test.tsx + ProjectForm.test.tsx — request body shape**: tests assert URL and method but not the payload (e.g. `summary: ""` → `summary: undefined`, `_key` stripped from sections).
- **`src/components/admin/PlatformPicker.test.tsx — parent prop sync** (carried).
- **`src/components/admin/ProjectSortOrderInput.test.tsx — `"3abc"`/`"3.7"` handling + abort-on-rapid-blur**.
- **`src/components/admin/MarkdownEditor.test.tsx — cache invalidation on text change** (covers reuse-on-identical; doesn't cover edit-then-re-preview).
- [x] **`src/components/admin/LoginForm.test.tsx — `router.refresh()` (once added) + network-throw path**. **Resolved 2026-05-11**: both tests added — success-path asserts `refresh()` fires alongside `push("/admin")`, network-rejection test asserts the tagged warn log and the fallback message.
- **`src/components/admin/AdminNav.test.tsx — 401 fast-path**: status 401 → push-without-error branch is uncovered.
- **`src/components/admin/AdminSearch.test.tsx — page reset on submit**: query submit drops `?page=N`, relying on `parsePageParam` default; not pinned.
- **`src/components/admin/AdminPagination.tsx — no test**; **`ProjectsTab.tsx + PostsTab.tsx` — no tests** (server-component JSX-props pattern works).
- **`src/components/admin/ProjectAdminControls.tsx — no test`** — the `key={…-${sortOrder}}` re-mount contract is load-bearing for the revert-race.
- **`src/components/admin/SectionManager.test.tsx + LinkManager.test.tsx — move/reorder + collapse/expand** uncovered (only add/remove/update covered).
- **`src/app/admin/(protected)/{layout,page}.test.tsx — no tests**; **`posts/new/page.test.tsx + projects/new/page.test.tsx — no tests**.
- **`src/components/{Typewriter,Header,Footer,FooterYear}.test.tsx — no tests`** (phase machine, segment-boundary active-link, archive-link, current-year).
- **`src/components/blog/{Pagination,PostCard,SearchForm,PostContent}.tsx — no tests`** (page=2-Newer-no-`?page=1`, truncation, escape-to-cancel, conditional `time` element).
- **`src/components/projects/ProjectSectionCarousel.test.tsx — paginate direction + dot-click + wraparound + caption toggle** (carried).
- **`src/components/projects/{CompactProjectCard,FeaturedProjectCard}.test.tsx — no tests`** (accent fallback, initial-letter fallback, discontinued chrome).
- **`src/components/loan-calculator/{LoanCalculatorInput,LoanCalculatorSummary}.test.tsx — no tests`** (a11y `<label>` regression guard, comparison-color inversion).
- **`src/components/loan-calculator/LoanCalculatorClient.test.tsx — comparison diff math** (carried).
- **`src/components/ui/{useClickOutside,EmptyState,ReorderControls}.test.tsx — no tests`**.
- **`src/components/ThemeProvider.test.tsx — `change`-listener cleanup on system→dark switch** (carried).
- **`src/components/privacy/{PrivacyPageLayout,PrivacySection}.test.tsx — no tests`** (`headingLevel` mapping).
- **`src/app/error.test.tsx — repeat-error log invocation**: a regression dropping the `useEffect` dep array would spam; one rerender-with-new-error test pins it.
- **`src/next-config.test.ts — "every required directive present in both modes"** assertion missing.

## Risk Assessment

**Medium-Low.** The codebase has improved materially since the prior `--all` sweep on 2026-05-07: most input-validation, observability, and concurrency findings from that review closed cleanly. The current sweep surfaces four themes worth acting on, in priority order:

1. **Draft/future-dated post leakage** — `getPostBySlug`, `sitemap`, and `generateStaticParams` all skip the `published: true` + `datetime <= now` filter; the canonical URL serves drafts to anyone with the slug, and search engines see future content via sitemap. The watch-out has been carried for two cycles; it's the most user-visible correctness gap. Tightening at `getPostBySlug` is one filter clause.
2. **Security tests for the auth surface are still mostly absent** — the per-IP login rate-limit, fail-open, and key-derivation are unverified; the proxy has no 401 contract test for `/api/admin/*` or `/api/upload`. The handlers themselves don't authenticate, so without proxy tests, the load-bearing security gate is unverified end-to-end. Same status as 2026-05-07; the fix is a small batch of route tests.
3. **Two genuinely new findings worth fixing this week** — `cron/ping` returns `{ error: "CRON_SECRET not configured" }` to unauthenticated probes when the env var is unset (move auth above the config check; 3 lines); upload's MIME allowlist trusts client `file.type` so an authed-but-hijacked session can write `text/html` to a public Blob (sniff magic bytes; one library call).
4. **Architecture debt is stable** — admin auth still split across three layers (open thread from 2026-05-07); `posts.ts`/`projects.ts` wrapper-cache template still duplicated; `format.ts` still mixed-concern. None block work but they're now four cycles old. Worth bundling into one architecture-refactor session.

Nothing landed that touches load-bearing plumbing destructively. Contract surface has narrowed since the prior review (parsing got stricter, env validation tightened, `<ErrorMessage>` consolidated 7 callers). The error-boundary tests added 2026-05-09 close a regression-watch gap. The remaining surface is concentrated in (a) the small auth + content-visibility holes above, (b) test coverage for paths whose behavior is documented but unverified.

## Notes

- Mode: split, 5 partitions (`src/lib/`, `src/app/api/`, `src/app/` non-API non-admin + `proxy.ts`, admin UI `src/app/admin/` + `src/components/admin/`, public UI `src/components/` non-admin). Each partition spawned a combined gatherer+analyzer agent (opus); synth pass inline by main (≤5 partitions). All five returned cleanly on the first attempt.
- Dev-journal watch-outs reaffirmed in findings: sitemap future-dated posts (Issues, twice now), admin auth single source (Architecture open thread), `<Link>` for non-route hrefs (no new violations found this sweep), `error.digest` re-introduction paired with aggregation (not violated; tests still pass), per-IP login rate-limit fallback to `"unknown"` (Risk note + Missing Tests), `vitest.config.ts` glob broadened to `src/**/*.test.tsx` (verified — no `.test.tsx` outside intended dirs), status suffix `(HTTP NNN)` on surfaced errors (no strict-equality assertions found that would break), `postCreateSchema.datetime` regex (pinned), per-request `env.ts` `safeParse` (still deferred), module-load bcrypt cost (re-flagged under Issues + Performance), `AnimatePresence` under happy-dom (no new test-suite uses).
- Cross-cut: **error-extraction path inconsistency** — `useAdminResource.readErrorMessage` is the canonical reader, but `IsFeaturedToggle`, `ImageUpload`, and `LoginForm` each handle the response body differently (generic message / silent `.catch(() => ({}))` / no-op). One shared helper would close it.
- Cross-cut: **`AbortController` discipline** — applied in the 2026-05-07 sweep, but `LoginForm` and `AdminNav` (logout) are still un-aborted. Both client mutations, both potentially long-running, both miss the pattern.
- Cross-cut: **routine-adversarial logs at error level** — `cron/ping` unauthorized and `login` rate-limit-hit both log at `console.error`; under any sustained probe they dominate the log. Demote to `warn`.
- Cross-cut: **destructive-replace-on-PUT** — `projects/[id]/route.ts:140-154` deletes-and-recreates all sections/links on every PUT; combined with the lack of `maxDuration` exports, a large project edit could blow past the Hobby 10s default. Both worth fixing in the same touch.
- No web URLs ingested or retained.
