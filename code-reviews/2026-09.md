# Code Review Monthly Rollup — 2026-09

_Generated 2026-09-14._

Reviews: 4 across 4 days.

## Recurring flags

- **Stale comments/docblocks surviving a refactor or reversed decision** — flagged twice: 2026-09-02 found two docblocks in `src/lib/db/posts.ts` (`:718` `revalidateAllPosts`, `:412` `loadPostForAdmin`) still naming the deleted `loadPost`, still present at month end; 2026-09-03 found three comments still arguing for a session gate removed from `override-preview` earlier the same session, resolved same day. Named explicitly in the 2026-09-03 review as "a recurring class, not a one-off." [strongest source](code-reviews/2026-09-03.md)
- **Hand-duplicated logic / hand-kept field lists** — flagged 2026-09-01 (`formatDate`/`formatDayMonth` byte-identical except one option, resolved same day via a shared `formatCalendarDay` helper) and again 2026-09-14, worse: the "not discontinued" rule is written three times across `storeLink`/`badgeStorefront`/`ctaLabel`, and one new boolean (`isOwnApp`) touched roughly eight hand-kept field lists (Prisma, Zod, `ProjectDetail`, `ProjectManifest`, the POST route, `writeProject`, the seed type, the form state) with no compiler check binding `ProjectManifest` to the Zod schema it's cast against. Still present at month end. [strongest source](code-reviews/2026-09-14.md)
- **Billed-compute cost on unauthenticated/scanner-reachable routes** — flagged 2026-09-02 (404 probes under `/blog/:section/:slug` are deliberately uncached, so scanner traffic on nonexistent slugs costs one function invocation plus one Postgres query per hit) and again 2026-09-03 on the new `override-preview` route (same uncached-miss shape, plus no rate limit on the path), both citing the same precedent — the root-level legacy-slug route removed for exactly this cost profile. Both left open as a decision to make rather than a defect to fix. [strongest source](code-reviews/2026-09-02.md)

## Test coverage gaps

- **Scheduled-branch OG/Twitter metadata unasserted** — nothing binds `og:type`, `og:url`, or the default `og:image` for a scheduled post after the assertion pinning the old title-only shape was deleted. First flagged 2026-09-02, open at month end. [source](code-reviews/2026-09-02.md)
- **`buildScheduledPostMarkdownFile` has no dedicated test** — coverage is indirect via the route test only; missing the malformed-`datetime` case its live twin already has, and an equivalence test that both builders escape a `"` in the title identically. First flagged 2026-09-02, open at month end. [source](code-reviews/2026-09-02.md)
- **`posts.test.ts:419` is a lost assertion, not a duplicate** — slug `"scheduled-draft"` shows intent to prove the draft filter applies to a future-dated row, but it mocks `findFirst` to `null` and re-tests the missing-row case instead. First flagged 2026-09-02, open at month end. [source](code-reviews/2026-09-02.md)
- **`isOwnApp` / App Store badge coverage** — the `next/image` mock drops `className`, so a swapped `dark:hidden`/`hidden dark:block` would still pass all tests; no test covers an own app with both an iOS and a Mac link (the exact case `storefrontFor` can't handle); no test asserts `isOwnApp: true` is saved on create/update or defaults to `false`; the import script (`writeProject`) has no test file at all. First flagged 2026-09-14, open at month end. [source](code-reviews/2026-09-14.md)

## Follow-ups

### Resolved during the month

- **2026-09-01 → 2026-09-01** — All seven findings from that day's review resolved same day: `max-w-m` typo corrected to `max-w-lg`; `formatDate`/`formatDayMonth` duplication collapsed into `formatCalendarDay`; scheduled `.md` stub given real frontmatter; scheduled metadata routed through `buildPageMetadata`; the double clock-read race fixed via `resolvePost`; `ScheduledPostNotice.test.tsx` and a `datetime === now` boundary pair added. [source](code-reviews/2026-09-01.md)
- **2026-09-03 → 2026-09-03** — Stale session-gate comments in `loadingBoundaries.test.ts` and `posts.test.ts` rewritten; the `force-dynamic` comment's wrong body-freshness claim corrected; a route-config test pinning `dynamic === "force-dynamic"` and a test asserting the auth module is never called both added, each mutation-tested against a deliberately broken implementation before being called done. [source](code-reviews/2026-09-03.md)

### Still standing at month-end

- **`posts.ts:718` / `:412` docblocks name the deleted `loadPost`** — first flagged 2026-09-02; the text a future editor reads before touching cache invalidation is still wrong about which symbol backs the tag. [source](code-reviews/2026-09-02.md)
- **`page.test.tsx:173` test name claims coverage it no longer has** — asserts nothing after its `openGraph` pin was deleted, but the name still says "title-only." First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **`getPostBySlug` bypasses the `loadPostResolution` memo** — an uncached twin of the production path; no test binds the memoized entry point to a single call per render. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **`detailCacheMiss.test.ts` anchored to a non-production accessor** — miss/hit/error-rethrow/scheduled-caching all asserted through `getPostBySlug`, not the accessor production uses. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **No admin affordance for the `override-preview` URL** — reaching a preview link means knowing and typing the suffix; left open deliberately as a design call (where the affordance goes), not a defect. First flagged 2026-09-03. [source](code-reviews/2026-09-03.md)
- **`override-preview` enumeration cost** — `force-dynamic` plus an uncached miss on every `[section]/[slug]` pair, with no rate limit; deliberately left as a trigger to watch (traffic showing up in logs) rather than fixed. First flagged 2026-09-03. [source](code-reviews/2026-09-03.md)
- **Guides have no scheduled-notice equivalent** — `src/app/guides/[slug]/page.tsx:130` still `notFound()`s a scheduled guide; a UX asymmetry, not a defect. First flagged 2026-09-01, unchanged as of 2026-09-02. [source](code-reviews/2026-09-01.md)
- **`writeProject` resets `isOwnApp` to `false` on re-import** — the Reckon and Continuum manifests don't carry the field, so re-importing either clears an admin-set flag with no CLI output noting it happened. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)
- **"Not discontinued" rule duplicated three times; ~8 hand-kept field lists for one boolean** — see Recurring flags above. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)
- **`prose-img:mx-auto` not applied to project-section or FAQ prose wrappers** — images in `ProjectContent`/`ProjectFaq` still sit flush left, unlike posts and guides; no project content has images yet. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)
- **`storefrontFor`'s host rule lives in a client component** — `STORE_HOSTNAMES`/`isStoreUrl` sit in `"use client"` `ProjectContent`, so a server consumer (e.g. JSON-LD) can't reuse the same rule. Low severity while `ProjectContent` is the only consumer. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)
- **Badge shows the wrong store's name and artwork on a multi-storefront own app** — a Mac-bucket own app that also lists an iOS link renders "Download on the Mac App Store" on the iOS link, in both the image and its `aria-label` — worse than the same-day watch-out ("one badge per project") states, since it names the wrong store rather than only repeating one. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)
- **App Store badge SVGs committed as executable (mode `100755`)** — harmless at runtime, likely inherited from the downloaded Apple kit; the next asset drop from that kit will repeat it. First flagged 2026-09-14. [source](code-reviews/2026-09-14.md)

## One-off findings

- **2026-09-02 — Blog posts emit no `canonical` link, guides do** — `canonicalPath` is opt-in and neither the live nor scheduled branch uses it, despite blog posts being the surface most likely shared with UTM params; pre-existing, not a regression from that day's commit. [source](code-reviews/2026-09-02.md)
- **2026-09-14 — `AppStoreBadge` images are lazy-loaded with no priority hint** — the hero badge is above the fold and can render blank briefly; the theme variant hidden via `display:none` likely isn't fetched until the first toggle. Effect is plausible from the `lazy` attribute, not directly observed. [source](code-reviews/2026-09-14.md)

<!-- end -->
