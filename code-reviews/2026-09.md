# Code Review Monthly Rollup — 2026-09

_Generated 2026-09-13._

Reviews: 3 across 3 days.

## Recurring flags

- **Hand-rolled objects that skip a shared builder** — flagged twice in one review, both in the scheduled-post branch: `generateMetadata` returned a bare `{ title, robots }` instead of calling `buildPageMetadata` (dropping `alternates`, so the page advertised the wrong section feed), and the `.md` route assembled its stub body inline instead of calling into `postMarkdown.ts` (dropping frontmatter and YAML escaping). Both resolved the same day by routing through the shared builder. [source](code-reviews/2026-09-01.md)
- **Prose that outlives a refactor** — flagged on two consecutive days, five instances. 2026-09-02: `revalidateAllPosts` and `loadPostForAdmin` docblocks in `src/lib/db/posts.ts` name the deleted `loadPost` (the first is wrong twice — `post-pages` now rides on `fetchPostRow`, which backs two entry points). 2026-09-03: three comments in `loadingBoundaries.test.ts`, `posts.test.ts` and the `override-preview` page still argued for the session gate reversed mid-session. The three were fixed the same day; the two docblocks are still standing as of 2026-09-13. [source](code-reviews/2026-09-02.md), [source](code-reviews/2026-09-03.md)
- **Invariants enforced by convention, with nothing asserting them** — 2026-09-02: `getPostBySlug` calls `resolvePost` directly and bypasses the `loadPostResolution` memo, reopening the split-verdict door the resolver refactor closed, and nothing binds one render to one resolution call. 2026-09-03: `dynamic = "force-dynamic"` on the preview route was its top documented watch-out and unasserted, and the "serves the body with no session" test was green only because an unmocked helper would throw. The 2026-09-03 pair was pinned the same day (route-config test; hostile auth mocks with not-called assertions); the 2026-09-02 pair is still standing. [source](code-reviews/2026-09-02.md), [source](code-reviews/2026-09-03.md)
- **Uncached misses on public detail routes cost a function invocation plus a live Prisma query per probe** — `cacheMiss.ts` throws misses rather than caching them, so 404 probes under `/blog/:section/:slug` re-render every time (2026-09-02), and the `override-preview` route adds a guessable suffix on every section/slug pair with no rate limit (2026-09-03). Same cost profile the root-level legacy slug route was deleted for. Both left as decisions or triggers, not defects: negative caching has an unbounded-keys downside, and nothing advertises the preview suffix. [source](code-reviews/2026-09-02.md), [source](code-reviews/2026-09-03.md)

## Test coverage gaps

- **`ScheduledPostNotice` component** — no dedicated test, unlike every sibling in `src/components/blog/`. First flagged 2026-09-01, resolved the same day. [source](code-reviews/2026-09-01.md)
- **Publish-boundary predicates** — nothing pinned `datetime === now`. First flagged 2026-09-01, resolved the same day with a fake-timer pair. [source](code-reviews/2026-09-01.md)
- **Scheduled-branch metadata shape** — `og:type`, `og:url` and the default `og:image` for a scheduled post are unasserted in either direction after the one pinning assertion was deleted; the surviving test is still named "title-only"; `path`/`og:url` is section-covered but not slug-covered; the scheduled page's 200 is inferred from a throwing `notFound` mock, never bound. First flagged 2026-09-02, still standing at 2026-09-13. [source](code-reviews/2026-09-02.md)
- **`buildScheduledPostMarkdownFile`** — no direct test: the malformed-`datetime` case its live twin has, and an equivalence test that both builders escape a `"` in the title identically (the guarantee `postFrontmatterLines` exists to give). First flagged 2026-09-02, still standing. [source](code-reviews/2026-09-02.md)
- **`src/lib/db/posts.test.ts:419` is a lost assertion** — slug `"scheduled-draft"` shows it meant to prove the draft filter applies to a future-dated row, but it mocks `findFirst` to `null` and re-tests the missing case; the neighbouring comment still says the canonical URL 404s until publish. First flagged 2026-09-02, still standing. [source](code-reviews/2026-09-02.md)
- **`override-preview` route config and no-session guard** — `dynamic === "force-dynamic"` unasserted; the no-session test pinned nothing. First flagged 2026-09-03, resolved the same day and mutation-tested (each mutation failed exactly one test). [source](code-reviews/2026-09-03.md)

## Follow-ups

### Resolved during the month

- **2026-09-01 → 2026-09-01** — `max-w-m` typo in `ScheduledPostNotice`: corrected by hand to `max-w-lg`. Tailwind v4 drops an unknown `max-w-*` name silently, so nothing mechanical caught it. [source](code-reviews/2026-09-01.md)
- **2026-09-01 → 2026-09-01** — `formatDayMonth` duplicated `formatDate` verbatim: both now call a private `formatCalendarDay(datetime, options)`. [source](code-reviews/2026-09-01.md)
- **2026-09-01 → 2026-09-01** — scheduled `.md` stub had no frontmatter: `buildScheduledPostMarkdownFile` shares `postFrontmatterLines` with the live export and appends `scheduled: true`. [source](code-reviews/2026-09-01.md)
- **2026-09-01 → 2026-09-01** — scheduled metadata advertised the tech feed on life posts: the branch now routes through `buildPageMetadata` with `feed: feedLinkForSection(section)`. [source](code-reviews/2026-09-01.md)
- **2026-09-01 → 2026-09-01** — two independent clock reads could pin a 404 at a post's publish minute: `resolvePost` reads the row and the clock once. Verified resolved by the 2026-09-02 pass. [source](code-reviews/2026-09-01.md)
- **2026-09-03 → 2026-09-03** — three comments justifying the removed session gate (`loadingBoundaries.test.ts:44`, `posts.test.ts:593`, the `force-dynamic` docblock): rewritten; the `force-dynamic` comment now credits the flag with the verdict only, since the body's freshness comes from `revalidatePost` busting `fetchPostRow`'s tag. [source](code-reviews/2026-09-03.md)
- **2026-09-03 → 2026-09-03** — `OverridePreviewPage` emitted nothing, so the public-URL tripwire had no line to trip: a tagged `console.info` on the served-body case only, matching the `[blog:p]` convention rather than `auditLog` (typed to admin writes). A second test pins that the live redirect logs nothing. [source](code-reviews/2026-09-03.md)
- **2026-09-03 → 2026-09-03** — `dynamic === "force-dynamic"` and the absence of any auth call now asserted; auth modules mocked with a hostile default so the not-called assertions, not an incidental throw, do the catching. [source](code-reviews/2026-09-03.md)

### Still standing at month-end

- **Guides have no scheduled-post equivalent** — `src/app/guides/[slug]/page.tsx` still `notFound()`s a scheduled guide while posts render a notice. Correctness unaffected; UX asymmetry deliberately not widened into scope. First flagged 2026-09-01. [source](code-reviews/2026-09-01.md)
- **`getPostBySlug` bypasses the memo it sits beside** — still calls `resolvePost` directly at 2026-09-13; routing it through `loadPostResolution` keeps the accessor and closes the split-verdict door. `detailCacheMiss.test.ts` stays anchored to it, so repoint before any cleanup. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **Two `posts.ts` docblocks name the deleted `loadPost`** — `revalidateAllPosts` (wrong symbol and wrong "only") and `loadPostForAdmin`; both still present at 2026-09-13. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **Scheduled-branch coverage drift** — the seven missing-test items from 2026-09-02 (metadata shape, `buildScheduledPostMarkdownFile`, the lost `:419` assertion, the inferred 200, one-call-per-render, slug coverage, the stale comment) are all still open; the "title-only" test name is unchanged. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **`buildPageMetadata` has opt-in alternates but forced OG** — `canonicalPath`, `markdownPath` and `feed` are opt-in while `openGraph` and `twitter` are unconditional, which is why the scheduled branch picked up a card as a side effect. Also: blog posts emit no `canonical` while guides do, on the surface most likely shared with UTM params. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **404 probes under `/blog/:section/:slug` cost a function plus a query each** — misses are deliberately uncached; negative caching has a real downside, so this needs a decision rather than a change. First flagged 2026-09-02. [source](code-reviews/2026-09-02.md)
- **No admin affordance for the preview URL** — reaching a preview link means knowing the suffix and typing it. Left open deliberately: a feature and a design call (row action vs editor header), and per the 2026-09-03 journal the appended-path shape makes typing the suffix the intended workflow. First flagged 2026-09-03. [source](code-reviews/2026-09-03.md)
- **`override-preview` enumeration cost** — the only public page not prerendered, uncached misses, no rate limit. A trigger to watch (hit-log traffic Roland didn't send), not a defect. First flagged 2026-09-03. [source](code-reviews/2026-09-03.md)

## One-off findings

- **2026-09-01 — Withdrawn: stale notices are invisible in analytics** — the finding was wrong. The cron already logs a heartbeat on its quiet path for exactly this purpose (`src/app/api/cron/revalidate-scheduled/route.ts:266`), and a pinned prerendered page runs no code, so the cron is the only layer where detection can live. [source](code-reviews/2026-09-01.md)
- **2026-09-02 — `MarkdownPostIdentity.section` widened to `string`** — both call sites pass a `Section`; the widening drops the compile-time guard on the field that builds the `canonical:` segment. [source](code-reviews/2026-09-02.md)
- **2026-09-02 — `ScheduledPostNotice` back link announces the arrow glyph** — `← Back to the blog` puts `←` inside the accessible name, and the new test pins that exact string. [source](code-reviews/2026-09-02.md)
- **2026-09-02 — Malformed `datetime` handled two ways in one `.md` document, and `formatCalendarDay` returns its raw input with no trace** — defence-in-depth only: `postDatetime` in `src/lib/api/schemas.ts` gates every write path including the importer, so reaching either needs a direct DB edit. [source](code-reviews/2026-09-02.md)
- **2026-09-02 — Orchestration split at ~710 LOC across 10 files** — two partitions, one Explore gatherer → opus analyzer pair each, consolidated inline; under the 5-partition synth-subagent threshold. The 2026-09-03 review ran inline at 433 LOC. [source](code-reviews/2026-09-02.md), [source](code-reviews/2026-09-03.md)
