# Code Review — 2026-07-18

## Issues and Observations

- [ ] **[src/lib/db/guides.ts:236-242 — `guideTag` / `guideTopicTag`]** — Tag namespace collision: a guide with slug `topic-foo` and a topic with slug `foo` produce the same cache tag (`guide-topic-foo`). Cross-table slug uniqueness prevents same-slug collisions but not this prefix overlap; busting one silently busts the other. Harmless today (both over-invalidate), but breaks any per-slug operation that assumes tag isolation. Use `guide-detail-` / `guide-topic-` or a delimiter Postgres slugs can't contain.
- [ ] **[src/components/admin/useAdminResource.ts:53-153 — `save`/`remove` race]** — A second `save()` aborts the first and sets `isSubmitting=true`, but the first call's `finally` still runs and resets `isSubmitting=false` while the second is in flight — button re-enables mid-flight and can be double-submitted. Same shape in `remove()`. Gate the `finally` on `abortRef.current === controller`.
- [ ] **[src/app/api/admin/revalidate/route.ts — guides branch]** — Endpoint fires `revalidateGuide(slug) + revalidateGuideTopic(slug)` per pasted slug but never busts the *parent topic hub* for those guides. The admin form path covers this via `revalidateTopicsById`; the RevalidatePanel path (the intended post-import surface) leaves hubs stale. Import-script users can't fix stale hubs without pasting the topic slug too, and the panel doesn't tell them.
- [ ] **[src/lib/db/guideValidation.ts:64-99 — `describeGuideRefProblem`]** — Does not verify the referenced topic is `published`. Admin can attach a live guide to a draft/unpublished topic without warning; the read path silently drops the parent link, so the incoherent state is invisible in the UI.
- [ ] **[src/proxy.ts — `isBotProbe` extension check]** — Extension fast-path only inspects the last path segment. `/foo.php/bar` skips the short-circuit and still costs the function invocation the fix was meant to skip; trivially bypassed by appending a segment. Scan every dotted segment (or match on any `.<ext>/` boundary too).
- [ ] **[src/components/admin/RevalidatePanel.tsx — stale response flash]** — An in-flight response from an older click can call `setResult`/`setWarning` after the new click already reset state. The `abortRef.current === controller` guard only protects `setPending(null)`; extend it to result/warning setters (or short-circuit after the `json()` await).
- [ ] **[src/app/llms.txt/route.ts:22-29 — `linkLine`]** — `oneLine(text)` strips whitespace but does not escape markdown control chars. A guide title or description containing `]`, `[`, or `)` breaks the emitted `[Title](url)` link silently for a machine-parsed file. Titles are freeform (schema allows any string).
- [ ] **[scripts/import-guides.ts:245-281 — cross-table pre-check outside transaction]** — Cross-table slug collision is checked before `$transaction` opens; between check and write, a concurrent admin create in the other table slips through (no cross-table DB constraint). Single-author volume caveat applies, but the source comment currently only says "Postgres can't enforce this" — call this out at the pre-check too.
- [ ] **[scripts/import-guides.ts:360-401 — sequential awaits inside serializable transaction]** — Each topic is `findUnique`'d in a loop *inside* the serializable `$transaction`, then every create/update is sequentially awaited. N round-trips holding a serializable lock block any other guides/topics write for the duration. Batch topic lookups with one `findMany`, and parallelize independent writes where safe.
- [ ] **[src/app/api/admin/revalidate/route.ts — partial-failure discards `applied`]** — On a mid-batch throw (posts succeed, projects `revalidateTag` throws), response is a bare 500 with no payload; retry double-busts posts. `revalidateTag` isn't transactional. Collect per-resource results + errors and return a 207-shaped body.

## Architecture & Coupling

- [ ] **[src/components/admin/{GuideForm,GuideTopicForm,PostForm,ProjectForm}.tsx — form skeleton dup]** — `FormState` + `Payload` + `useState` + memoized `setField` + submit/delete button classNames + `admin-input` layout are duplicated four times (~80 lines per form). Extract an `AdminForm` shell or at minimum `SubmitBar`/`DeleteButton` + a `useFormState` hook. Long identical Tailwind chains for submit/delete should move to `@apply` in `globals.css` next to `.admin-input` (memory: `feedback_tailwind_reuse`).
- [ ] **[src/lib/db/cacheMiss.ts — `wrapNullableDetail` helper missing]** — Every detail wrapper (posts/projects/guides/guide-topics) re-implements `unstable_cache(...) → throw CacheMissError on null → nullOnCacheMiss(wrapper)`. Same wording pasted four times shows drift risk. A `wrapNullableDetail(fetch, keyParts, tags)` would collapse them.
- [ ] **[src/lib/db/{posts,projects,guides}.ts — tag helpers triplicated]** — `postTag`/`projectTag`/`guideTag`/`guideTopicTag` + `*_PAGES_TAG` constants + raw `"projects"`/`"guides"` literals in `revalidateAllProjects` / `revalidateGuides` live per module. Extract `PROJECTS_TAG` / `GUIDES_TAG` alongside the `*_PAGES_TAG` constants and consider a companion `cacheTags.ts`.
- [ ] **[src/lib/db/guides.ts:194-197 — `compareGuides`]** — JS comparator reimplements `guideOrder`'s SQL semantics; the two definitions can drift silently (e.g. Prisma nulls placement). Derive one from the other or pin the invariant in a test.
- [ ] **[src/components/admin/useAdminResource.ts:10 — `resource` union]** — Hardcoded literal set with no compile-time link to the middleware admin-API check. Adding a fifth resource requires this union edit *and* a middleware update. Cheap fix: `const RESOURCES` array derives both.
- [ ] **[src/components/admin/GuideTopicForm.tsx:7 — cross-import]** — Imports `GuideFormProjectOption` from `GuideForm`, locking `GuideForm`'s public surface as a de-facto types module. Move both `*Option` types to `src/components/admin/types.ts`.
- [ ] **[src/proxy.ts — inline probe lists]** — 40 extensions + 20 prefixes inline in middleware; comment invites log-driven growth. Factor to `botProbes.ts` for isolated review and a dedicated test file.
- [ ] **[src/proxy.ts — `/api/admin/login` chicken-and-egg]** — Admin-API gate has no `/admin/login` exemption paralleling the page gate. A future `POST /api/admin/login` handler would be gated behind the JWT it's supposed to mint. Add the exemption (or an assertion) preemptively.
- [ ] **[src/app/api/admin/revalidate/route.ts — `revalidateGuides` name shadowing]** — Local helper collides in name with the exported `revalidateGuides()` from `@/lib/db/guides`. Not imported here today, but any future import will silently shadow. Rename to `applyGuidesRevalidation` or similar.
- [ ] **[src/app/api/admin/revalidate/route.ts — Outcome contract asymmetry]** — Only posts populate `skipped`; projects/guides always return `[]`. Panel's warning text is hardcoded client-side to "post entries" — if guides/projects gain validation the wrong reason renders. Return the human reason per resource so the panel echoes it verbatim.
- [ ] **[src/app/guides/[slug]/page.tsx:177 — `renderTopicHub`]** — Declared `async` but awaits nothing.

## Concurrency & Thread Safety

- [ ] **[src/lib/import/atomicWrite.ts — `writeFileAtomic` same-process race]** — Temp filename is `${filePath}.tmp-${process.pid}` with no in-process uniquifier; two concurrent calls to the same path race on the temp. Current callers loop sequentially but the primitive is exported and undefended. Add a monotonic counter or `crypto.randomBytes(6)` suffix.
- [ ] **[src/lib/import/atomicWrite.ts — no fsync]** — Docstring claims "a crash… mid-write — never sees a truncated file" but there's no fsync on the temp before `rename` and no parent-dir fsync after; holds for SIGKILL and out-of-disk, not for kernel panic / power loss. Either add the fsyncs or narrow the claim.
- [ ] **[src/lib/import/atomicWrite.ts — mode/owner reset]** — Temp is created with default umask; a pre-existing 0o600 or executable-bit file silently becomes 0o644 after any stamp. Non-issue for `.md`, real footgun for future reuse. Stat + `chmod` before rename, or document the mode-reset invariant.
- [ ] **[src/lib/db/guides.ts:251-253 — `createBoundedWrapperCache`]** — Bounded wrapper map is shared across requests. Two concurrent misses on the same slug could race the `get(slug, factory)` and both build a wrapper, depending on the bounded-cache internals. Verify it dedupes on write, not just lookup.
- [ ] **[src/proxy.ts — admin-gate case asymmetry]** — Bot filter lowercases pathname; admin gate uses raw `pathname === "/admin"` / `startsWith("/admin/")`. `/ADMIN` skips the gate. Next.js 404s anyway today, but the asymmetry isn't tested and would break if any route ever normalized case.
- [ ] **[src/app/api/admin/guide-topics/[id]/route.ts:200 / guides/route.ts:86 / guides/[id]/route.ts:168 — misleading race message]** — `isPrismaUniqueConstraint` fallback always says "A topic/guide with this slug already exists", but the race being caught is cross-table (the other kind could have inserted the slug between preflight and write). Message could mislead the admin.

## Observability

- [ ] **[scripts/init-post-slugs.ts:265 — summary counter misnamed]** — `problems.length` is labelled `"unmatched"`, but `problems` also collects per-file read failures (105) and write failures (124). An EACCES surfaces as "3 unmatched" — misleads about the failure class. Split the counter or relabel to something honest.
- [ ] **[scripts/init-post-slugs.ts — `matchedSlugs` bookkeeping on write-failure]** — A write failure (121-126) drops back to `problem` *after* the match was known, without carrying the resolved slug forward. The DB row surfaces in the "DB rows with no matching file" list even though a file exists — two signals for the same event, and the fileless list becomes a lying alarm on partial runs.
- [ ] **[scripts/init-post-slugs.ts:255-266 — "Init complete" tail after problems]** — When `process.exitCode = 1` is set for problems, the next line still prints "Init complete: …". An operator scanning the tail sees "complete" and can miss the loud section above. Prefix with `Init FAILED (partial)` when `problems.length > 0`.
- [ ] **[scripts/import-guides.ts:428-431 — top-level error handler]** — `error instanceof Error ? error.message : String(error)` drops the stack on every failure — Prisma errors and transaction aborts become bare messages, hard to diagnose from CI logs. Log the error object directly, or capture `.stack` explicitly.
- [ ] **[src/lib/import/applySlugRewrites.ts — `describeChange` fallback]** — `const { previous } = file.slugRewrite ?? { previous: null }` is dead defensive code — if the invariant ever regresses, the branch silently renders as `"slug: X (from title)"`, indistinguishable from a first-time stamp. Drop the `??` and enforce non-null via type guard, or throw.
- [ ] **[src/proxy.ts — silent bot 404]** — `isBotProbe` returns a bare 404 with zero log line. Fine under normal traffic; a wrongly listed prefix silently 404s real users forever. A debug-env-gated counter or sampled log would flag drift.
- [ ] **[src/app/api/admin/revalidate/route.ts — unbounded success log]** — `console.info(..., { applied, skipped })` dumps full arrays; a 500-slug payload swamps the line. Summarise (`applied.posts: 500 slugs`) for large batches.
- [ ] **[src/lib/db/guideRevalidation.ts:29-36 — silent zero result]** — If a caller passes topic IDs that no longer exist (stale UI cache), `findMany` returns empty and nothing is revalidated silently. Log or return a count so the caller can detect drift.
- [ ] **[src/app/api/admin/guide-topics/[id]/route.ts:117-158,188-196 — split cascade + audit log]** — Cascade `console.info` fires separately from the primary PUT audit line, and the audit line does not carry `isProjectChanged` / `guideCount`. Reconstructing a topic move needs two greps.
- [ ] **[src/components/admin/GuidesTab.tsx:107-134 — `sortOrder` invisible]** — Guide rows show published/draft/scheduled + `updatedAt` but never expose `sortOrder`. Since sort is authored via a numeric field and collisions are silent (Prisma just picks an order), admins can't spot conflicts without opening each row.

## Performance

- [ ] **[src/lib/db/{posts,projects,guides}.ts — miss stampede exposure]** — Detail miss deliberately not cached (self-heal correctness) is correct, but the bot-probe short-circuit only catches known-bad shapes. A crawler enumerating arbitrary slug-shaped strings still hits Prisma on every request. The plan mentions the short-TTL negative-cache alternative — worth revisiting once prod logs are in (already a watch-out in 2026-07-17).
- [ ] **[scripts/import-guides.ts:83-128 — sequential file reads]** — `await readFile` per file, no `Promise.all`. Fine for a few dozen, painful once the guide corpus grows.
- [ ] **[src/lib/db/guides.ts:426-464 — `listGuidesForAdmin`]** — Search over `title` + `body` uses `contains` + `mode: insensitive`. No trigram/GIN index; at 10K+ rows this is a seq-scan per search (same shape as `listPostsForAdmin`, so the concern is repo-wide).
- [ ] **[src/lib/db/guides.ts:148-192 — `getGuidesOverview`]** — Comment already flags the O(n) in-memory grouping as intentional. Small tweak: `publishedTopicIds` `Set` is built after the first loop even though `topics` is available up-front; reorder to avoid the second pass.
- [ ] **[src/app/api/admin/guide-topics/[id]/route.ts:56-91 — preflight before existence check]** — `preflight` runs `findSlugOwner` + `describeTopicRefProblem` (up to 2 DB queries) *before* the transaction discovers the topic doesn't exist. Only wasteful on the 404 path.

## Missing Tests

- [ ] **[src/lib/import/atomicWrite.ts — no dedicated tests]** — Primitive is exported and used by two callers, exercised only transitively. Untested: writeFile-phase failure with temp cleanup, EACCES on folder, CRLF/utf8 round-trip byte-equality, concurrent same-file same-process race. Multiple concurrency findings above would be caught here.
- [ ] **[scripts/init-post-slugs.ts — imperative shell still untested]** — Pure core was extracted with 13 tests, but `initFolder` / `stampFile` / `resolveSection` — including the write-failure → `problem` conversion, `matchedSlugs` bookkeeping, dry-run branching, and the unknown-flag guard — remain untested. Two observability findings above are silent failures the shell's tests would catch.
- [ ] **[src/components/admin/useAdminResource — no test file for guides/guide-topics variants]** — Existing test file predates the guides resource additions; save/remove race behavior and the new resource variants aren't exercised. The double-submit race above lands under this gap.
- [ ] **[src/lib/db/detailCacheMiss.test.ts — seams untested]** — Missing: concurrent misses on same slug (dedupe or race?); non-`CacheMissError` errors propagate AND aren't cached (only implicitly asserted); the scheduled-post outer datetime filter returning null while the row *is* cached (the "cache populated but caller gets null" seam).
- [ ] **[src/app/api/admin/revalidate/route.test.ts — partial-failure shape]** — Only "first call throws" covered. A "posts succeed → projects throw" case would document the current lose-applied behavior and force the decision on it.
- [ ] **[src/components/admin/RevalidatePanel.test.tsx — guides row unexercised]** — No test clicks the guides button or asserts guides-specific text. Deleting the guides `ResourceConfig` entry (or renaming its `key`) fails no test.
- [ ] **[src/app/api/admin/guides/route.test.ts / guide-topics/route.test.ts — P2002 race branch untested]** — `isPrismaUniqueConstraint` catch (routes.ts:86-93 and 72) is documented as load-bearing but only the `findSlugOwner` catch is exercised.
- [ ] **[src/app/api/admin/guides/[id]/route.test.ts / guide-topics/[id]/route.test.ts — DELETE/GET gaps]** — Guide DELETE not-found untested; guide-topic DELETE covers success + FK constraint but not P2025; GET is entirely untested for both `[id]` routes.
- [ ] **[src/proxy.test.ts — bot vs. admin interaction untested]** — `/admin/wp-login.php` and `/api/admin/wp-admin.php` are 404'd today via the bot filter (runs first). Nail the order down explicitly. Also add a POST/PUT test to lock method-agnostic gating.
- [ ] **[src/lib/db/guides.test.ts — module-load snapshot excludes per-slug wrappers]** — Snapshot only covers aggregates; a bug registering per-slug wrappers on module load (leaking a keyed cache per boot) would go unnoticed. Add a "no additional cache registrations after module load" assertion.
- [ ] **[src/lib/db/guides.test.ts:337-452 — `GuideDetail` field coverage]** — No assertion that returned `GuideDetail` carries fields the JSON-LD builder relies on (`readingTime`, `body`, `publishedAt`, `updatedAt`); a dropped SELECT column slips past.
- [ ] **[src/lib/import/guideImport.test.ts — `topicFolder` change on update]** — No test for a guide moving between folders; the plan carries `topicFolder` → `topicId` resolution to the shell, but no test asserts the plan carries the new folder through the update payload.
- [ ] **[src/lib/import/guideImport.test.ts — `publishedAt` filename-no-time edge]** — No case for filename `2026-07-17-…` (no `-HHmm`) where the DB row was previously stamped by admin at a different time; would re-sync silently to midnight.
- [ ] **[src/lib/content/guideTopicBody.test.ts:59-63 — em-dash false-positives]** — Only guards sentence-embedded dashes, not markdown table separators (`| --- | --- |`) or setext underlines. Setext is the closest realistic false-positive.
- [ ] **[src/lib/content/guideJsonLd.test.ts:69-105 — never-published branch]** — Doesn't assert `dateModified` is emitted when `publishedAt` is null (the whole point of the builder is that `dateModified` is always there).
- [ ] **[src/lib/db/guideRevalidation.test.ts — default-empty branch]** — Doesn't assert `revalidateGuideTopic` is called without `guideSlugs` (the documented default contract); a future maintainer adding `guideSlugs` here would balloon sibling-guide invalidation silently.
- [ ] **[src/lib/import/slugInit.test.ts — compound datetime+title case]** — Corruption-guard trio covers same-minute disambiguation, refused-title-at-different-datetime, and both-keys-ambiguous, but not: file datetime has 2+ hits (no datetime bind) and the sole title hit sits elsewhere. Current code correctly returns null but nothing pins it.
- [ ] **[src/lib/import/applySlugRewrites.test.ts — blank-slug log]** — No assertion that `previous: ""` renders as `slug: "" → x` (not `(from title)`). The blank-vs-missing distinction is load-bearing for the "distinguish blank slug fields" narrative.
- [ ] **[src/app/sitemap.test.ts — topic `lastModified`]** — Asserts topic hub URLs are emitted (379) but never asserts `lastModified` propagates from a topic's `updatedAt` (only checked for guides, 404).

## Accessibility

- [ ] **[src/components/admin/GuidesTab.tsx:68,137]** — Multiple identical "Edit" links per page with no context for screen readers. Add `aria-label={`Edit ${topic.title}`}` (also pre-existing drift in `PostsTab.tsx`).
- [ ] **[src/components/guides/GuideContent.tsx:30-34]** — Back-link text is `← {topic.title}`; screen readers announce "left-arrow, …". Wrap the arrow in `<span aria-hidden>← </span>` (project already uses this pattern in `Footer`).
- [ ] **[src/components/admin/GuideForm.tsx:211]** — Disabled + `opacity-50` project select has no `aria-describedby` linking to the "Set by the topic…" helper below.

## Risk Assessment

**Medium.** The guides feature is a large, mostly clean addition — schema + data layer + admin CRUD + public pages + importer + integrations, all landing behind existing auth gates, with tests that pin the load-bearing behaviors (cross-table slug uniqueness, scheduled-guide filtering, published-topic drop, `changeFrequency` distinction from posts). The plumbing changes (detail-miss no longer cached, single-sourced detail tags, RevalidatePanel applied/skipped, middleware bot short-circuit + admin-gate tightening) also read as deliberate and well-tested. Risk concentrates in three places: (1) the `guideTag`/`guideTopicTag` namespace overlap, which is dormant today but a subtle correctness bug the moment a guide slug starts with `topic-`; (2) the `useAdminResource` save/remove race, which double-enables the submit button under legitimate impatient-user behavior and creates duplicate rows on rapid double-clicks; (3) the revalidate endpoint's silence on parent topic hubs — the very use case the panel exists for (post-script-import hub freshness) is the one it doesn't cover. None are catastrophic, none block ship; all three deserve close before the next content-heavy import cycle. Cross-cutting: four near-identical admin forms and four near-identical detail cache wrappers are drift bait; extracting an `AdminForm` shell and a `wrapNullableDetail` helper would pay for itself the moment a fifth resource lands.

## Notes

- **Orchestration:** split, 4 partitions (guides data + import; guides UI + admin; import tooling + slug work; cache/revalidation + middleware), each reviewed by a parallel general-purpose analyzer on Opus. Main consolidated the four findings lists directly (≤5 partitions rule, no synth-pass agent needed).
- **Cross-cutting themes** that surfaced across partitions and would be one-shot cleanups: (a) admin form skeleton dup across four `*Form.tsx` files + duplicated submit/delete button Tailwind chains (per `feedback_tailwind_reuse` memory, `@apply` these); (b) `wrapNullableDetail` helper missing — four detail modules re-implement the same `unstable_cache + CacheMissError + nullOnCacheMiss` sandwich with drift-prone comment blocks; (c) tag literals repeated per module — `PROJECTS_TAG`/`GUIDES_TAG` should sit next to the `*_PAGES_TAG` constants; (d) `error instanceof Error ? error.message : String(error)` copied nine times across scripts — promote to `src/lib/utils/errorMessage.ts`; (e) two independent client-side race patterns (RevalidatePanel + useAdminResource) with the same root cause (finally block resetting state without checking it's still the active controller).
- **Watch-outs already flagged in 2026-07-17** (not re-flagged as findings): the FK-to-Project trap, dual `publishedAt` write paths for guides, `null publishedAt` = live for guides, `offers` JSON-LD-only, individual tag busts unproven, 404 probes hit DB (the miss-stampede finding above is scoped to that watch-out), `init-post-slugs` refuses re-dated files. Session 5's admin-gate over-match on `/administrator*` is fixed and covered.
- **All 11 items from 2026-07-17 review verified resolved on inspection** — no prior findings resurface.
- **`GuideProjectCta.tsx` deletion confirmed** — file gone; no references remain in `src/` or `prisma/`. `74d782f` cleanup is complete.
- **Auth boundaries verified**: `/api/admin/guides*` and `/api/admin/guide-topics*` are gated by the existing `isAdminApi` matcher (`/api/admin` exact + `startsWith("/api/admin/")`). No regression from the session-5 tightening; `useAdminResource` union addition is the only "add-a-resource" surface that lacks a compile-time link to middleware.
- No `TODO|FIXME|HACK|XXX|TEMP|unsafe` markers found in the reviewed diffs.

## Resolution — 2026-07-18 (branch `fix/code-review-2026-07-18`)

Worked in 8 committed phases; full suite 1810 → 1891 tests, tsc + lint clean throughout.

**Resolved (the bulk of the findings):**
- **Phase 1** — shared `errorMessage` util (9 sites; top-level script `main().catch` now logs the full error for stacks), `POSTS_TAG`/`PROJECTS_TAG`/`GUIDES_TAG` single-sourced, `wrapNullableDetail` helper collapsing the four detail-cache wrappers.
- **Phase 2** — guide vs topic cache-tag namespace collision fixed (`guide-detail-` / `guide-topic-`), `compareGuides` co-located with `guideOrder` + invariant test.
- **Phase 3** — revalidate endpoint now busts guide parent hubs (`revalidateGuideSlugs`), isolates per-resource failures (207 + `errors`), per-resource skip reasons, summarised logs, `revalidateTopicsById` returns a count + warns on drift; RevalidatePanel stale-flash guard + name-shadow rename.
- **Phase 4** — `useAdminResource` double-submit race fixed; `@apply` submit/delete button classes, `useFormState` hook, shared `guideFormOptions` (`NONE` + option types), char-counter + a11y (aria-labels, aria-hidden arrow, aria-describedby).
- **Phase 5** — `botProbes` module extracted, inner-segment probe bypass closed, case-insensitive admin gate.
- **Phase 6** — `atomicWrite` per-call temp uniquifier + mode preservation + narrowed docstring; `init-post-slugs` honest counters/status/bookkeeping; `import-guides` batch topic lookup; `applySlugRewrites` dead-code + `isDryRun` rename; `sortedMarkdownNames` DRY.
- **Phase 7** — `llms.txt` link-label escaping, `sortOrder` INT4 cap, code-aware product-link warning, draft-topic picker flag, single-pass `getGuidesOverview`, `renderTopicHub` desynced.
- **Phase 8** — new tests for cache-miss error/scheduling seams, route P2002/404/GET paths, jsonLd never-published `dateModified`, sitemap topic `lastModified`, guideImport edges, plus botProbes/atomicWrite/errorMessage unit suites.

**Deferred (environmental / premature — unchanged by design):**
- Miss-stampede negative cache — the 2026-07-17 watch-out says wait for prod bot-traffic metrics before adding a short-TTL miss cache.
- GIN/trigram index on admin body search — premature at single-user volume; a migration decision, not a bug.

**Skipped after investigation (with reasons):**
- `atomicWrite` fsync — narrowed the docstring instead; power-loss durability isn't worth an fsync-per-write on a dev-machine `.md` importer (SIGKILL/out-of-disk safety, what it needs, already holds).
- Misleading P2002 race messages — **false positive**: cross-table slug uniqueness is app-enforced (no cross-table DB constraint), so a P2002 only ever fires same-table; the "A guide/topic with this slug already exists" message is accurate for the race it catches.
- `describeGuideRefProblem` published-check — rejecting a guide→draft-topic attach would break the valid "publish guide before its topic" workflow, and the read path already drops the link gracefully; addressed the "invisible" concern via the draft-topic picker flag instead.
- `/api/admin/login` gate exemption — misleading: login lives at `/api/auth/login` (outside the gate) per convention; added a clarifying comment steering future login handlers away from `/api/admin/*` instead.
- App-level bot-404 logging — redundant with Vercel's platform request logs (path+status), and middleware is the hottest path the journal is CPU-conscious about.
- `import-guides` parallel file reads — unbounded `Promise.all` risks EMFILE at the "hundreds of files" scale the finding cited; a proper fix needs bounded concurrency, over-engineering for the current handful.
- `useAdminResource` RESOURCES-array — middleware gates all `/api/admin/*` generically, so there's no auth-drift risk and no clean single source across panel/hook.
- `loadGuide`/`loadGuideTopic` bare-form — kept the arrow form for consistency with `loadPost`/`loadProject`.
- module-load "no extra registrations" test — already covered by the existing exact `toEqual` snapshot.
- concurrent-miss cache test — would pin the test double's in-flight dedup, not production `unstable_cache` semantics.

**Discovered latent bug (documented in-code, NOT fixed — needs a design decision):**
- `src/lib/import/guideImport.ts — planGuide`: a guide file MOVED to a new folder with byte-identical content produces an empty `data`, is skipped as `UNCHANGED`, and its `topicId` is never re-resolved — the move silently doesn't take. The planner works in folder-space while the existing row is in topicId-space (mapping only known in the shell post-topic-write), so it can't diff membership. In practice a move usually rides an edit (which updates and carries the new folder). Fix options: thread the folder→id map into the planner, or have the shell re-apply `topicId` on skips too. Flagged for a follow-up decision.
