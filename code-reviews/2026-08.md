# Code Review Monthly Rollup — 2026-08

_Generated 2026-08-17._

Reviews: 5 across 4 days. Source files: `2026-08-12.md`, `2026-08-13.md`, `2026-08-14.md`, `2026-08-17.md` (contains two passes, Review 1 + Review 2).

## Recurring flags

- **Admin page-namespace invariant enforced only by prose/comments, not code** — flagged 4 times: 2026-08-12 (`proxy.ts` comment overstated the guarantee), 2026-08-13 (bypass events produced no log signal), 2026-08-14 (`adminEditMetadata`'s positional signature let tags transpose silently; a contract test was added as the structural fix), 2026-08-17 (that same contract test only string-matches source, doesn't execute the guard — the identical gap, one layer down). Still standing. [strongest source](code-reviews/2026-08-17.md)
- **OG/social-card fragility: font-CDN dependency, hand-copied palette, stable-URL caching** — flagged 3 times: 2026-08-13 (Google Fonts runtime dependency, Satori 8-digit-hex bug, hand-copied palette literals), 2026-08-14 (`og-card.png`'s URL never changing — explicitly left unresolved), 2026-08-17 (alt text has no drift guard; `--check` can false-fail on an upstream font revision). Two items (stable URL, palette drift) still unresolved at month end. [strongest source](code-reviews/2026-08-14.md)
- **Weak or vacuous test assertions ship alongside real fixes** — flagged in 3 of 4 review days: 2026-08-13 (root layout's `generateMetadata` missing the `openGraph`/`twitter` spread assertion), 2026-08-14 (`toMatchObject` instead of `toEqual` on `AggregateOffer`; a redundant console-spy layered on the test harness's own mock), 2026-08-17 (the cron's `WINDOW_HOURS` test accepts any value ≥26h though the constant is 49; `adminPageContract.test.ts` has no negative fixture proving the walk can actually fail). The 2026-08-17 instances remain open. [strongest source](code-reviews/2026-08-17.md)
- **Stale comments/docblocks describing mechanisms that changed or were removed** — flagged 5 times within the single 2026-08-17 review: `legacyRoutes.ts`'s module doc still claims a bot-probe filter deleted weeks earlier; two `posts.ts` docblocks claim scheduled content "auto-surfaces" when the opposite is true; a `MAX_PAGE` comment cites the wrong `PAGE_SIZE` and a dead query param; `WINDOW_HOURS`'s derivation comment still cites the old cron schedule after the same commit changed it. Two fixed same day; three remain open. [strongest source](code-reviews/2026-08-17.md)

## Test coverage gaps

- **`adminPageContract.test.ts` asserts source text, not behavior** — doesn't import or execute the pages it claims to guard, so a page missing `generateMetadata` entirely still passes. First flagged 2026-08-17, still standing.
- **`cronAuth.ts`'s `isAuthorized` has no test file** — the only module in `src/lib/api/` without one; every fixture differs in length from the reference secret, so the equal-length comparison branch never executes. First flagged 2026-08-17, still standing.
- **`middlewareBypass.ts`'s shared `bypassId` has no test file** — all three consumer tests only check `bypassId: expect.any(String)`, so coverage looks green while the shared-id-per-request contract is unverified (also an open thread in `dev-journal/2026-08-15.md`). First flagged 2026-08-17, still standing.
- **`src/app/blog/[section]/page.tsx` has no test file** — the page-1 static route (the entire point of the `searchParams` removal) has zero coverage: section validation, `generateStaticParams`, `generateMetadata` all untested. First flagged 2026-08-17, still standing.
- **`Pagination.tsx` has no test file** — the load-bearing prev-from-page-2 → bare `/blog/:section` case, the whole duplicate-content contract, rests on one un-asserted ternary. First flagged 2026-08-17, still standing.
- **`randomShortId.ts` has no test file** — the only module in `src/lib/utils/` without one; nothing pins length, alphabet, or non-repetition of log correlation ids. First flagged 2026-08-17, still standing.
- **`src/app/api/**` stays excluded from coverage** — hides the cron route and the new static `.md` route, the two largest pieces of new logic this month, from the report meant to catch exactly that. First flagged 2026-08-17, still standing.
- **`og/route.tsx`'s font loader was untestable by construction** — module-private loader, non-injectable `fetch`, zero coverage on the three throw paths. First flagged 2026-08-14, resolved (moot) 2026-08-15 by deleting the route.
- **Root layout's `generateMetadata` had no `openGraph`/`twitter` assertion** — half the metadata fix (the layout half, serving `/`, `not-found`, and admin) had no regression guard. First flagged 2026-08-13, resolved 2026-08-13.
- **Guides/guide-topics admin edit pages had no test files at all** — two of four pages that gained the session guard had zero wiring coverage. First flagged 2026-08-13, resolved 2026-08-13.

## Follow-ups

### Resolved during the month

- **2026-08-12 → 2026-08-12** — Admin metadata auth gap + `proxy.ts` comment: closed same day via `adminEditMetadata` + `verifySession()`; three more same-review items (`STORE_HOSTNAMES` seam, hero-row overflow, discontinued repeated CTA) also same-day. [source](code-reviews/2026-08-12.md)
- **2026-08-13 → 2026-08-13** — Imageless-page OG fallback, discontinued-project JSON-LD offers, `buildPageMetadata` bypass enforcement, admin bypass logging at both call sites, and four missing-test items: all resolved same day. [source](code-reviews/2026-08-13.md)
- **2026-08-14 → 2026-08-15** — 43 of 45 findings resolved in one pass (merged as `f868863`): empty-string `og:image` resolving to the site root, `projectJsonLd`'s untyped `.trim()` crash risk, `/og` route's missing caching contract, `adminEditMetadata`'s positional-argument tag confusion, unbounded font fetches with no timeout, and nine missing-test items among them. [source](code-reviews/2026-08-14.md)
- **2026-08-17 → 2026-08-17** — Three documentation-only items same day: duplicated cron comment block, `adminMetadata.ts` doc pointing at the wrong contract-test file, `guides.ts` orphaned docblock. [source](code-reviews/2026-08-17.md)

### Still standing at month-end

- **`verifySession()` not wrapped in React `cache()`** — first flagged 2026-08-13, explicitly withdrawn rather than fixed (gain judged not worth changing auth-path behavior on an unverified `cache()` scope); do not re-raise without a measured cost. [source](code-reviews/2026-08-13.md)
- **`og-card.png`'s stable URL won't propagate a regenerated card** — first flagged 2026-08-14, deliberately deferred as not worth a build step. [source](code-reviews/2026-08-14.md)
- **Palette hand-copied from `globals.css` into the card generator, no drift detection** — first flagged 2026-08-13, still open. [source](code-reviews/2026-08-13.md)
- **Cron reliability and routing findings from the 2026-08-17 review** — `Promise.all` making the cron all-or-nothing, an unguarded `page=0`/`02`/`007` redirect into a dead-end 404, an unbounded `/p/2..10000` crawl surface, and the `WINDOW_HOURS`/`vercel.json` invariant enforced only by prose, among a dozen others. First flagged 2026-08-17 (the month's most recent review), none yet addressed. [source](code-reviews/2026-08-17.md)

## One-off findings

- **2026-08-12 — `isStoreUrl` vs `APP_BUCKETS`** — two independent "is this a shipping app" definitions (CTA keys off link hostname, JSON-LD off `project.bucket`); review concluded no action needed yet. [source](code-reviews/2026-08-12.md)
- **2026-08-14 — Duplicate `getSessionSecret` export** — `auth.ts` and `env.ts` both exported a same-named function returning different types (`Uint8Array` vs `string`); pre-existing, surfaced and fixed via rename to `getSessionSecretKey`. [source](code-reviews/2026-08-14.md)
- **2026-08-17 — Generated Prisma client is stale** — `src/generated/prisma` still carries the removed `@@index([slug])` in its inline schema snapshot, pending `yarn db:push`. [source](code-reviews/2026-08-17.md)
- **2026-08-17 — Scope drift mid-review** — commit `61929b8` landed while review agents were running; folded in as a same-day follow-up pass (Review 2) rather than excluded. [source](code-reviews/2026-08-17.md)
- **2026-08-17 — Uncommitted, unreviewed changes in the working tree** — `src/app/blog/[section]/page.tsx`, `archive/page.tsx`, and both `projects` `loading.tsx` deletions were present but covered by no commit in scope. [source](code-reviews/2026-08-17.md)

<!-- end -->
