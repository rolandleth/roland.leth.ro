# Code Review Monthly Rollup — 2026-08

_Generated 2026-08-31._

Reviews: 9 across 8 days (`2026-08-17.md` contains two passes — Review 1 and the follow-up Review 2).

## Recurring flags

- **Admin page-body session guard — narrows one instance, misses a sibling, every time** — flagged 2026-08-12, 13, 14, 17, 18, 21, 22, 23. `code-reviews/2026-08-21.md` is the strongest instance: `adminEditMetadata` guarded only the `<title>` on all four `[id]/edit` pages and the dashboard root, so an unauthenticated request still rendered the page body — fixed by extending `requireAdminPageSession` to all seven data-reading admin pages. Resolved 2026-08-22 with a real await-aware contract test, but the guard mechanism itself was hit again on 2026-08-23 (see "still standing" below) via the `stripComments` helper backing that test. [strongest source](code-reviews/2026-08-21.md)
- **Docblock/comment drift on load-bearing invariants** — flagged in nearly every pass: the `src/proxy.ts` matcher comment overstating `requireAdmin`'s coverage (08-12), stale caching-contract claims on the `/og` route (08-14), five separate stale-doc findings in one pass (08-17), CLAUDE.md documenting `WINDOW_HOURS=49` after the code moved to 50 (08-18), and `makeBlogPageCache`'s docblock overstating "agree by construction" (08-21, 08-22). Each is resolved on sight; the pattern itself never stops recurring. [strongest source](code-reviews/2026-08-17.md)
- **Source-text/regex test guards ship with blind spots** — the `adminPageContract` walker matched on presence, not behavior (08-17, rewritten); a `generateMetadata`-detection regex missed valid export forms twice in the same session (08-21); a `searchParams`-absence assertion used a nested-brace regex that stopped at the first `}` (08-21); the shared `stripComments` extracted to fix these (08-22) was itself found to cause false negatives in two of the three guards it backs (08-23), still unresolved at month end. [strongest source](code-reviews/2026-08-23.md)
- **OG/social-card generation robustness** — flagged 08-13 (font-CDN dependency, 8-digit-hex render bug) and heavily 08-14 (unbounded WASM bundle for a 404 endpoint, no caching contract, `NODE_ENV`-only production gate, untestable font loader). Resolved 2026-08-15 by moving the generator to a build script; palette-drift detection and CI-wiring for `--check` remain open at month end. [strongest source](code-reviews/2026-08-14.md)
- **Cron/revalidation window and cache-tag correctness** — `WINDOW_HOURS` unguarded against the actual `vercel.json` schedule, `Promise.all` making the cron all-or-nothing, missing DB indices, and `MAX_PAGE` never checked against a second caller were all flagged 08-17/08-18 and resolved same-range. A subtler variant — `makeBlogPageCache` reading `getSectionPageCount` from inside its own `unstable_cache` callback, so a warm entry can serve a stale `totalPages` — was documented, not fixed, 08-22. [strongest source](code-reviews/2026-08-17.md)

## Test coverage gaps

- **Contract/guard tests asserting source text instead of behavior** — the class first flagged 2026-08-17 (`adminPageContract`), still not fully closed: `stripComments` (extracted 08-22) has a demonstrated false-pass path on the `searchParams` guard and the await guard, and has no test of its own. Status at month end: open. [source](code-reviews/2026-08-23.md)
- **No CI pipeline exists in this repo at all** — `.github/workflows` is absent, so the test/lint/typecheck gate (and `yarn og:card --check`) runs only when someone remembers to run it locally; first flagged 2026-08-18, confirmed still true 08-21. Status: open. [source](code-reviews/2026-08-18.md)
- **Legal/privacy page prose has zero test coverage** — `src/app/privacy/page.tsx` changed twice this month (08-21, 08-22) with no assertion added, by design; the Apr→Aug drift this month's session fixed was invisible for five weeks for exactly that reason. Status: open, procedural trigger only (revisit on a change to the login route or `env.ts`). [source](code-reviews/2026-08-21.md)
- **`AdminPagination`'s "every tab renders it unconditionally" condition is unenforced** — `GuidesTab` and `ProjectsTab` have no test files at all; a tab that early-returns its empty state above the pager would silently lose the out-of-range redirect. First and only flagged 2026-08-23. Status: open. [source](code-reviews/2026-08-23.md)
- **`errorDetails` non-serializable-property handling** — the happy path got five new test cases 08-22 (BigInt, cycle, function, mixed, Date); the `name`/`message`/`stack` bypass of that same filter has no test as of 08-23. Status: open. [source](code-reviews/2026-08-23.md)

## Follow-ups

### Resolved during the month

- **2026-08-12 → 2026-08-12** — Admin metadata auth-gap comment overstatement and the underlying unauthenticated read across all four admin edit pages: closed same day. [source](code-reviews/2026-08-12.md)
- **2026-08-13 → 2026-08-13** — OG image empty-page fallback, discontinued-project JSON-LD offers, and admin bypass logging with per-page tags: same-day fixes. [source](code-reviews/2026-08-13.md)
- **2026-08-14 → 2026-08-15** — 43 of 45 findings resolved: `/og` route replaced by a build script, an empty-string image-fallback bug fixed, `adminEditMetadata` moved to an options-object signature with a tag const-map, font-fetch timeouts added, image descriptors single-sourced. Two items deliberately deferred (the stable `og-card.png` URL, palette drift vs `globals.css`). [source](code-reviews/2026-08-14.md)
- **2026-08-17 (+ Review 2) → 2026-08-17** — Nearly all findings resolved same day: cron switched to `Promise.allSettled` with per-branch revalidation, `WINDOW_HOURS` 49→50, `MAX_PAGE` 10,000→30, two missing DB indices added, `adminPageContract` rewritten behaviorally, cron tag-bracket convention unified. Two items explicitly declined ("Not doing"): an ESLint rule that can't follow a value through a helper or spread, and wiring `--check` into CI ahead of font-vendoring. [source](code-reviews/2026-08-17.md)
- **2026-08-18 → 2026-08-21** — `MAX_PAGE`'s missed admin caller closed via a dedicated `parseAdminPageParam`, an `archive/page.test.tsx` added for the highest-value gap in the range, 16 `sonarjs/parameterized-tests` lint errors fixed, two CLAUDE.md drifts corrected, `sectionTag` single-sourced across four inline literals, `BlogPostList`'s empty-page guard restored after re-derivation. [source](code-reviews/2026-08-18.md)
- **2026-08-21 → 2026-08-21/22** — The month's largest security fix: `requireAdminPageSession` extended to all seven data-reading admin pages, a closed `AdminPageTag` union replacing hand-typed strings, `MAX_SAFE_ADMIN_PAGE` bound added, `AdminPagination` centralizing the out-of-range redirect, `errorDetails` widened to preserve Prisma's `code`/`meta`/`clientVersion`. [source](code-reviews/2026-08-21.md)
- **2026-08-22 → 2026-08-22** — Await-guard contract test added and verified to fail on a missing `await`; `stripComments` extracted; `errorDetails` made genuinely JSON-safe via `isJsonSerializable`; `AdminPagination`'s Suspense dependency documented. [source](code-reviews/2026-08-22.md)

### Still standing at month-end

- **`stripComments` false-negative risk** on the `searchParams`-absence guard and the admin await guard (only the `cache()` presence check in `p/[page]/page.test.tsx` fails safely, as documented) — first flagged 2026-08-23; a single-alternation regex fix is proposed but not applied. [source](code-reviews/2026-08-23.md)
- **`errorDetails`: `name`/`message`/`stack` bypass `isJsonSerializable`** on an unenforced "always a string" assumption, reintroducing the throw-while-reporting-a-failure the filter exists to prevent — first flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`errorDetails` drops non-serializable properties with no trace** — a dropped `rowId` is indistinguishable from one that never existed; a `droppedKeys` field is proposed, not added — first flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`adminPageContract`'s await-guard shares the presence-guard's allowlist exemption** — a future guarded page on `PAGES_WITH_NO_BODY_DATA_READ` would get no await enforcement; low severity — first flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`getGuideTopicBySlug` hub-tag gap** — a guide coming due reaches `/guides` and its own page but not its topic hub; deliberately deferred (YAGNI, no `/guides/topics` route exists yet) — first flagged 2026-08-17 (Review 2). [source](code-reviews/2026-08-17.md)
- **No CI pipeline in the repo** — first flagged 2026-08-18. [source](code-reviews/2026-08-18.md)
- **`yarn og:card --check` not wired into CI**, blocked on vendoring Google Fonts locally so an upstream font revision can't fail an unrelated build — first flagged 2026-08-13/17. [source](code-reviews/2026-08-17.md)
- **`og-card.png`'s stable URL means a regenerated card won't propagate** to already-scraped social previews — deliberately deferred, first flagged 2026-08-14. [source](code-reviews/2026-08-14.md)
- **Legal/privacy page prose has no test coverage** — first flagged 2026-08-21. [source](code-reviews/2026-08-21.md)
- **`next-env.d.ts` perpetual churn** (commit-vs-`.gitignore` undecided, since Next rewrites its two import lines depending on whether `dev` or `build` ran last) — first flagged 2026-08-22. [source](code-reviews/2026-08-22.md)

## One-off findings

- **2026-08-12 — `isStoreUrl` vs `APP_BUCKETS`** — two independent "is this a shipping app" checks (CTA hostname vs JSON-LD bucket), authored separately with no third consumer yet to force alignment. [source](code-reviews/2026-08-12.md)
- **2026-08-14 — `/og` bundled Satori+resvg WASM into a production route that only ever served a 404 string** — moving the generator to `scripts/` removed the bundle, the function, and its cold-start cost outright. [source](code-reviews/2026-08-14.md)
- **2026-08-17 — `cronAuth.ts`'s `isAuthorized` had zero test coverage**, and its one real byte-comparison branch was unreachable by every existing fixture (all differed in length from the test secret). One same-length fixture closed it and surfaced that the Headers API strips trailing whitespace per RFC 9110, silently authorizing a header with a trailing newline. [source](code-reviews/2026-08-17.md)
- **2026-08-18 — `MAX_PAGE` dropped 10,000→30 without checking its second caller** — the admin dashboard's pagination clamped silently to page 30 instead of 404ing, the exact asymmetry the blog route's build-time throw was meant to prevent. [source](code-reviews/2026-08-18.md)
- **2026-08-21 — `BlogPostList`'s empty-page guard was deleted on an "agree by construction" claim**, then restored after re-deriving that unpublish/delete — not a post becoming due — can still make two independently-generated cache entries diverge. [source](code-reviews/2026-08-21.md)
