# Code Review Monthly Rollup — 2026-08

_Generated 2026-08-24._

Reviews: 9 across 8 days (`2026-08-17.md` contains two review passes).

## Recurring flags

- **Docblocks and comments drifting from what the code actually does** — the single most repeated flag of the month, appearing in nearly every review: `code-reviews/2026-08-13.md`, `2026-08-14.md`, `2026-08-17.md` (posts.ts, legacyRoutes, MAX_PAGE, isFutureDatetime), `2026-08-18.md` (CLAUDE.md's `WINDOW_HOURS`/`CRON_SECRET` drift), `2026-08-21.md` (six separate stale-docblock findings in one pass), and `2026-08-22.md`/`2026-08-23.md` (`errorDetails`' "JSON-safe" claim). Each instance was fixed on sight, but new ones kept appearing at the same rate, suggesting the pattern itself — not any one file — is the standing risk. Still present at month end. [strongest source](code-reviews/2026-08-21.md)
- **Tests that assert on source text or a mock and cannot fail for the case they exist to catch** — flagged 7+ times: the OG route's negative-idiom trap (`2026-08-14.md`), `adminPageContract.test.ts`'s pure source-text matching (`2026-08-17.md`), vacuous `searchParams`/`legacyRoutes` assertions (`2026-08-18.md`), a truncating regex and a vacuous JSX-mock assertion caught by mutation-testing (`2026-08-21.md`), the admin await-guard still not enforcing `await` (`2026-08-22.md`), and finally `stripComments` itself shown to have a false-negative path that silently defeats two of the three guard tests it backs (`2026-08-23.md`, still open at month end). Each fix held once shipped; the class as a whole never fully closed. [strongest source](code-reviews/2026-08-23.md)
- **Admin second-layer auth guards that log but don't stop rendering** — `2026-08-12.md` and `2026-08-14.md` established the guard pattern; `2026-08-18.md` found the four `[id]/edit` pages' `generateMetadata` guard logged-and-fell-through rather than blocking the page body; `2026-08-21.md` extended real stopping guards from 2 to 7 admin pages (including the dashboard root); `2026-08-22.md` found the contract test enforcing it could pass an unawaited call. Resolved by month end, but took five review passes to close fully. [strongest source](code-reviews/2026-08-21.md)
- **Cache-tag and count invariants living in prose across files rather than being enforced** — `WINDOW_HOURS ≥ 2× cron interval` relation (`2026-08-17.md`), `blog-${section}` as a repeated inline literal (`2026-08-18.md`/`2026-08-21.md`), `feedTag` crossing a module boundary the wrong direction (`2026-08-21.md`, moved again `2026-08-22.md`), and two independently-generated cache entries answering the same "how many pages" question (`2026-08-18.md`/`2026-08-21.md`, still imperfect at `2026-08-22.md`). Each instance was single-sourced when found; the pattern of a new one surfacing per pass continued through month end. [strongest source](code-reviews/2026-08-18.md)

## Test coverage gaps

- **Legal/privacy page prose has no test coverage at all** — `src/app/privacy/page.tsx` and siblings carry factual claims about data handling with zero assertions; a false claim stood for 5 weeks undetected for exactly this reason. First flagged in `dev-journal/2026-08-21.md`, still standing per `code-reviews/2026-08-22.md`'s notes. Status at month end: open, deliberately (a prose test is judged mostly noise; the cheaper trigger is watching changes to the auth/env files that feed the claims). [source](code-reviews/2026-08-22.md)
- **`src/test/sourceText.ts`'s `stripComments` has no test of its own**, despite backing three guard tests and being shown buggy the same day it was scrutinized. First flagged 2026-08-23, still open at month end (last day of data). [source](code-reviews/2026-08-23.md)
- **`GuidesTab`/`ProjectsTab` have no test files at all**, and `AdminPagination`'s docblock names "every tab renders the pager unconditionally" as a load-bearing condition nothing enforces — a tab that early-returns on an empty list silently loses the out-of-range redirect. First flagged 2026-08-23, still open at month end. [source](code-reviews/2026-08-23.md)
- **No CI exists in this repo** (`.github/workflows` absent) — test/lint/typecheck run only when someone remembers locally, which is how the `sonarjs/parameterized-tests` lint gate stayed red long enough that "is lint clean?" stopped being answerable. First flagged 2026-08-18, resolved for that specific gate 2026-08-21, but the structural gap (no CI to catch the next one) is unchanged at month end. [source](code-reviews/2026-08-18.md)

## Follow-ups

### Resolved during the month

- **2026-08-12 → 2026-08-12** — Admin metadata auth gap, `STORE_HOSTNAMES` hardcoding, hero-row overflow, missing tests on `isStoreUrl`/CTA ordering/`storeLink` determinism: all 7 items resolved same day. [source](code-reviews/2026-08-12.md)
- **2026-08-13 → 2026-08-13** — Missing default OG image, discontinued-project JSON-LD still asserting a live price, `buildPageMetadata` bypass risk, admin bypass observability gaps, missing tests on the layout/OG-image spread: all resolved same day. [source](code-reviews/2026-08-13.md)
- **2026-08-14 → 2026-08-15** — 43 of 45 findings resolved across six commits (empty-string image fallback bug, untyped `.trim()` crash risk, `/og` route deleted in favor of a build script, admin tag typing, font-error handling, ~12 missing-test gaps). Two deliberate deferrals carried to watch-outs: `og-card.png`'s stable URL never propagating a refresh, and palette drift between `globals.css` and the card generator. One resolved item (`bypassId` request-scope correlation) shipped with an unverified premise, flagged as an open thread rather than a clean close. [source](code-reviews/2026-08-14.md)
- **2026-08-17 (Review 1+2) → 2026-08-17** — Nearly all findings resolved same day: cron `Promise.all` → `allSettled`, `?page=` redirect accepting `0`/`02`/`007` into a dead end, `MAX_PAGE` cut 10,000 → 30 with a real page-count check, unbounded `findMany` on the due-content queries, missing indexes, ~14 missing-test gaps. Two explicit "not doing" deferrals: an ESLint rule that would need real scope analysis to close a refactor-shaped evasion, and font vendoring as the honest fix for `--check`'s CI-blocking Google Fonts dependency. One item stayed open both passes: tag propagation from `unstable_cache` onto a prerendered route entry is asserted only in prose and needs a real deploy to verify. [source](code-reviews/2026-08-17.md)
- **2026-08-18 → 2026-08-21** — Admin pagination's missed second call site (`MAX_PAGE` drop from 10,000→30 not accounted for on the admin dashboard), the archive route's undertested `dynamicParams` guard (the highest-value gap that pass), the 16-error red lint gate, two CLAUDE.md documentation drifts, `blog-${section}` tag single-sourcing, and ~10 more missing-test gaps: all resolved. Two items stayed open: the `getGuideTopicBySlug` hub-tag gap (deliberately deferred, YAGNI — no hub route exists) and two Prisma indexes unpushed to the live DB (user confirmed pushed outside this session's flow on 2026-08-21, not independently re-verified). [source](code-reviews/2026-08-18.md)
- **2026-08-21 → 2026-08-21** — All 24 findings resolved same/next day: the four-edit-page + dashboard auth gap (the pass's headline fix), the misdirecting `requireAdminPageSession` docblock, `parseAdminPageParam`'s missing upper bound, the clock split between `findMany` and its count, `errorDetails` narrowed then correctly widened, and 9 missing-test gaps. [source](code-reviews/2026-08-21.md)
- **2026-08-22 → 2026-08-22** — All 8 findings resolved same day: the await-guard contract test gained a real assertion, `errorDetails` made genuinely JSON-safe via `isJsonSerializable`, `AdminPagination`'s two unstated load-bearing conditions documented, `feedTag` moved to its proper single-source location, and the gallery-grouping test rewritten to actually assert grouping. [source](code-reviews/2026-08-22.md)

### Still standing at month-end

- **`stripComments`'s false-negative path defeats two of its three guard tests** — a stray `/*` in prose (the file's own docblock contains one) makes the `searchParams`-absence check and the admin await-guard both silently pass against code that should fail them. First flagged 2026-08-23; fix identified (a single alternation regex) but not yet applied. [source](code-reviews/2026-08-23.md)
- **`errorDetails`' `name`/`message`/`stack` bypass the new `isJsonSerializable` filter** on the assumption they're strings by construction — a subclass assigning a non-string `message`, or a foreign-realm error, reintroduces the exact throw-while-logging the filter exists to prevent. First flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`errorDetails` drops filtered properties with no trace** — a log line missing a field is indistinguishable from an error that never carried one; a `droppedKeys` marker was proposed but not implemented. First flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`pagesRequiringBodyGuard`'s allowlist exempts pages from the await check, not just the presence check** — correct today (no exempted page calls the guard), but a page gaining a guard while staying on the allowlist would get no await enforcement. First flagged 2026-08-23. [source](code-reviews/2026-08-23.md)
- **`getGuideTopicBySlug` hub-tag gap** — a guide coming due reaches `/guides` and its own page but not its topic hub; the cron busts neither of the hub's cache tags. Deliberately unfixed (YAGNI, confirmed with the user) since no `/guides/topics` route exists yet. First flagged 2026-08-17. [source](code-reviews/2026-08-17.md)
- **No CI wired in this repo** — the test/lint/typecheck gate runs only when someone remembers locally; `yarn og:card --check` specifically can't be wired in as-is without first vendoring fonts, since an upstream Google Fonts revision would fail the build for a reason unrelated to the author's change. First flagged 2026-08-18. [source](code-reviews/2026-08-18.md)

## One-off findings

- **2026-08-12 — `ProjectContent` hero row overflowed at 375pt with a single link**, worse than the review's own prediction of 3+ links; measured live and fixed same day. [source](code-reviews/2026-08-12.md)
- **2026-08-14 — `og/route.tsx` had four remote font fetches with no `AbortSignal`, timeout, or cancellation** — a hung CDN connection would hang the whole route indefinitely with no error. [source](code-reviews/2026-08-14.md)
- **2026-08-17 — A crawler walking `/blog/tech/p/2…10000` could trigger ~10,000 billed on-demand renders** — `MAX_PAGE` bounded only an in-memory wrapper cache, not the actual query cost or disk cache entries. [source](code-reviews/2026-08-17.md)
- **2026-08-17 — `CRON_SECRET`'s equality check silently authorized a value with trailing whitespace** — the Headers API strips optional whitespace per RFC 9110 before the comparison ever runs; caught while writing a same-length negative fixture for an untested byte-comparison branch. [source](code-reviews/2026-08-17.md)
- **2026-08-18 — Dropping `MAX_PAGE` from 10,000 to 30 silently degraded a second, unaudited caller** — the admin dashboard's own pagination used the same clamped value with no round-trip check, so `?page=31` would render page 30 with no error and no build guard. [source](code-reviews/2026-08-18.md)
- **2026-08-21 — `parseAdminPageParam` had no upper bound**, letting `skip` approach the 32-bit integer boundary Postgres/pg can bind; the realistic impact was a self-inflicted 500 from a hand-edited admin URL, since the surface is authenticated and single-user. [source](code-reviews/2026-08-21.md)
- **2026-08-22 — A presentational pager (`AdminPagination`) performing `redirect()` had two unstated load-bearing conditions** (every tab must render it unconditionally; no Suspense boundary may flush above `/admin` first) that were true only by accident of the existing code shape. [source](code-reviews/2026-08-22.md)

<!-- end -->
