# Code Review — 2026-04-28

## Issues and Observations

- **`.claude/hooks/auto-allow.jq` — `grep_like_flags_re` allows any short-flag letter in the `rg` pipe-stage** — flags like `-l` (list filenames), `-c` (count), `-q` (quiet) are all permitted. None are dangerous in a read-only pipe context, but the comment only explains why non-numeric-value flags are safe; it doesn't address mode-changing flags. Low risk; worth a note in the comment for future reviewers.

- **`.claude/hooks/auto-allow.jq` — trailing `grep_like_flags_re` on the search command is asymmetric with `rg` in pipes** — the top-level `rg|grep` arm allows trailing flags after log paths; the pipe-stage `rg` arm does not (pattern is last). This is intentionally conservative, but the asymmetry is undocumented and may confuse future edits.

## Missing Tests

- ~~**`.claude/hooks/auto-allow.jq`** — no behavioral tests exist for the JQ filter. Given this file controls which commands skip permission prompts, a test suite covering: (a) each allowed command shape, (b) boundary cases like `sort -o`, `rg -e pattern`, and multi-stage pipes, and (c) known-bad injections (`;`, `&&`-chained dangerous commands) would significantly raise confidence on future expansions. This is an ongoing gap, not introduced today.~~ — won't-fix. Hooks/jq are part of the harness, not the application; this repo's test runner (Vitest) doesn't shell out to `jq`, and adding a jq-based test harness purely for this single file isn't worth the maintenance surface. Risk stays bounded by the conservative grammar (allowlisted chars, `\A`/`\z` anchors, numeric-only flag values).

## Risk Assessment

**Low.** The single source code change expands the pipe-chain allowlist in the auto-allow hook (`wc`, `sort`, `rg` added alongside existing `head`/`tail`). The expansion is carefully bounded: `sort` flag set explicitly excludes file-writing (`-o`) and path-taking (`-T`/`-t`) flags; `rg` in pipes reuses `grep_like_flags_re` whose numeric-only value rule prevents non-numeric-arg flags (`-e`, `-f`, `-g`) from matching. No application code changed. The rest of the commit is journal/review file updates.

## Notes

No open threads or watch-outs in the 2026-04-26 dev-journal were affected by today's changes. The two watch-outs (per-request Zod parse in `env.ts`, module-load bcrypt cost in `auth.ts`) remain open and are not touched here.

Orchestration mode: inline (1 file changed in application code, ≤500 LOC delta).
