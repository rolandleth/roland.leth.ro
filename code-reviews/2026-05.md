# Code Review Monthly Rollup — 2026-05

_Generated 2026-05-17._

Reviews: 10 across 10 days (2026-05-01, 2026-05-07, 2026-05-09, 2026-05-10, 2026-05-11, 2026-05-12, 2026-05-13, 2026-05-14, 2026-05-15, 2026-05-16). Two full-codebase reviews this month (`codebase-2026-04-24.md` driving 2026-05-07, `codebase-2026-05-11.md` driving three sessions on 2026-05-11). Daily-diff reviews drove the other days; the implementation sweeps that closed each review's findings landed in the same session.

## Recurring flags

- **`clientBucketKey` falls back to `"unknown"` bucket** — rate-limit degrades to effectively global on any deploy without `x-forwarded-for` (bare-metal, curl, misconfigured CDN). First flagged 2026-05-07. No test covers the missing/multi-value/IPv6/empty-string header cases. 2026-05-12 review re-flagged with sharper detail (off-Vercel a client can rotate the header to bypass limits — documented as intentional in journal). 2026-05-15 review added a related concern: `clientBucketKey` is computed twice per failed login (HMAC-SHA256 × 2 where one suffices) — perf-only flag, still standing. [strongest source](code-reviews/2026-05-07.md), [2026-05-15](code-reviews/2026-05-15.md)

- **`hasKeyword` / `platformBucket` word-boundary regex** — `\b` anchors at letter/digit edges; `react-native`, `.net`, `c++` break. First flagged 2026-05-07; re-flagged across the month. No test covers these cases. Still standing. [source](code-reviews/2026-05-07.md)

- **Future-dated content / cache-snapshot staleness** — flagged in three forms across the month: (a) 2026-05-12 — "three copies of `capture now inside the cached fn` in `getPostBySlug`, `getAllPublishedPostSlugs`, `legacySlug.cachedLookup`, only one of which is documented"; (b) 2026-05-15 — the read-time filter (2026-05-14 reversal) is duplicated across 5+ call sites with no shared helper; any future query that forgets the filter silently leaks scheduled posts via the cache; (c) 2026-05-15 — `getPostsBySection` page 1 vs page>1 boundary can show the same scheduled post on both pages if it crosses `now()` mid-session. 2026-05-17 resolved the "shared helper" leg via `isFutureDatetime(datetime, now)` in `format.ts`; the page-1-vs-page>1 boundary and the cross-call-site lift to a single `now` per request remain open. [2026-05-12](code-reviews/2026-05-12.md), [2026-05-15](code-reviews/2026-05-15.md)

- **Audit log shape vs durable sink tension** — flagged repeatedly. 2026-05-12 — `tag: string` accepts typos at the call site (resolved 2026-05-12 via `AdminAuditTag` closed union; further tightened 2026-05-13 to a const-array derivation). 2026-05-12 — `previousSlug` audit semantics asymmetric between posts and projects PUT (resolved 2026-05-12, lifted to `const isRenamed` on 2026-05-13). 2026-05-12 + recurring — `console.info` is the audit sink, ~1h retention; documented as `DO NOT PROPOSE` in 2026-05-12 journal (durable sink deferred until external aggregation lands). 2026-05-16 — bulk batch correlation needed (resolved 2026-05-17 via `batchId: string | null` field stamped with `randomUUID()` on bulk emissions, `null` everywhere else). [strongest source](code-reviews/2026-05-12.md)

- **`vi.mock` factory mocks + `vi.resetAllMocks` interaction** — repeated lesson, surfaced twice this month. 2026-05-11 sitemap test had `currentDatetimeString` mock cleared by `resetAllMocks` in `beforeEach`; fix was to rebind in `beforeEach`. 2026-05-15 bulk route test re-encountered the same pattern. Now part of the "mock around `currentDatetimeString`" pattern across the codebase. [2026-05-11](code-reviews/2026-05-11.md)

- **About-page outbound links and copy quality** — `rel="noopener noreferrer"` absent (flagged 2026-05-09, resolved 2026-05-10 for bio links); anonymized `https://db.com` "big corporations" link reads inconsistent with named employers (flagged 2026-05-09, 2026-05-10, 2026-05-11 — still present); missing `{" "}` space before Deutsche Bank link introduced 2026-05-11 (regression). [strongest source](code-reviews/2026-05-11.md)

- **Inline / silent failure patterns** — recurring across files. 2026-05-12: `setError("Failed to save")` in `IsFeaturedToggle` catch (inconsistent with sibling `ProjectSortOrderInput`); `setField("sortOrder", ... ? Number(raw) : 0)` silent coercion to 0; `useOptimisticMutation` silent supersession with no log signal. 2026-05-15: silent under-pad in cached page 1 (no log when `filtered.length < PAGE_SIZE`). 2026-05-16: `PostPublishedToggle.error not logged`, `bulk skip reasons not logged server-side`, `cron route no success log`. All of the toggle/observability set resolved 2026-05-17 via `useOptimisticMutation` `console.error` in both failure branches + cron success log + bulk `emitSkipSummary` + `inserting` breadcrumb. [strongest source](code-reviews/2026-05-16.md)

## Test coverage gaps

- **Rate-limit fail-open + secret-set + AND-gate paths untested** — Redis unavailability branch; `ratelimit && ipHashSecret` AND-gate; `{ key: <hash> }` log branch. First flagged 2026-05-07, sharpened 2026-05-15. Still standing. [source](code-reviews/2026-05-07.md), [2026-05-15](code-reviews/2026-05-15.md)
- **`sanitizeFilename()` path traversal, null bytes, Windows separators** — no test for `../../`, `\x00`, `\`; non-ASCII stripping behavior not locked. First flagged 2026-05-07; still standing. (Adjacent to the 2026-05-17 `FILENAME_REGEX` tightening, which has tests for path separators and control chars in the bulk parser specifically.) [source](code-reviews/2026-05-07.md)
- **`clientBucketKey` `"unknown"` fallback** — no test for missing/multi-value/IPv6/empty-string `x-forwarded-for`. First flagged 2026-05-07; still standing. [source](code-reviews/2026-05-07.md)
- **Error boundary digest negative assertions narrower than committed contract** — `not.toContain(error.digest)` (28-char literal); a truncated digest leak (e.g. first 8 chars) would pass. Prior review recommended `not.toMatch(/[a-f0-9]{20,}/)`. Flagged 2026-05-11; still standing. [source](code-reviews/2026-05-11.md)
- **`describeZodIssues` all-empty-paths fallback + bounded line + segment-dedupe pin** — flagged 2026-05-12; the de-dupe behavior added 2026-05-13 has no test (`Array.from(new Set(rawSegments))`) so a refactor dropping the Set passes CI. 2026-05-14 review added the request to pin it; still standing. [source](code-reviews/2026-05-12.md), [2026-05-14](code-reviews/2026-05-14.md)
- **`IP_HASH_SECRET` minimum length / whitespace-only validation** — `getIpHashSecret` accepts any non-empty string; a 1-byte secret passes. HMAC-SHA256 keys should be ≥32 bytes; a typo'd short secret silently re-opens the rainbow-table vector the secret closed. First flagged 2026-05-15; still standing. [source](code-reviews/2026-05-15.md)
- **`getPostsBySection` page 2 with scheduled post crossing `now`** — duplicate-appearance-across-pages unverified. First flagged 2026-05-15; still standing. [source](code-reviews/2026-05-15.md)
- **`datetime === now` exact-equal boundary** — `<= now` predicate tested at far future and historical; not at strict equality. First flagged 2026-05-15; still standing. [source](code-reviews/2026-05-15.md)
- **Feed `take: LIMIT + futureCount` padding** — asserted in `posts.test.ts` but not mirrored for the feed cache. First flagged 2026-05-15; still standing. [source](code-reviews/2026-05-15.md)
- **`/api/admin/keepalive` proxy.ts gating** — route assumes `proxy.ts` enforces session auth on `/api/admin/*`. No integration test hits the route without a session cookie; a future matcher regression would silently open the endpoint. First flagged 2026-05-15; still standing. [source](code-reviews/2026-05-15.md)
- **`PostsTab.tsx` scheduled marker (4 quadrants)** — first flagged 2026-05-16. **Resolved 2026-05-17** via new `PostsTab.test.tsx` pinning all four quadrants (published+future, published+past, draft+future, draft+past). [source](code-reviews/2026-05-16.md)
- **Optimistic-toggle revert baseline regression** — first flagged 2026-05-16 (stale `initialPublished` after parent re-render). **Resolved 2026-05-17** via `BooleanFlagToggle.test.tsx` "reverts to the value at click-time (not the original `initial` prop)". [source](code-reviews/2026-05-16.md)
- **Optimistic-toggle disabled during in-flight + bulk race path + zero-create side effects + cross-section uniqueness scope + bulk parser empty-slug edge + bulk parser Unicode title + keepalive dedicated unit test** — all first flagged 2026-05-16. **Resolved 2026-05-17** via additions to `BooleanFlagToggle.test.tsx`, `bulk/route.test.ts`, `bulkImportParser.test.ts`, new `lib/keepalive.test.ts`. [source](code-reviews/2026-05-16.md)

## Follow-ups

### Resolved during the month

- **2026-05-01 — `vi.doUnmock` promoted to `afterEach`** — previously inside test bodies; a failing assertion would leak the mock into subsequent tests. [source](code-reviews/2026-05-01.md)
- **2026-05-07 → in-session — `handleLogout` error surfacing, postDatetime regex rejection cases, loginSchema.email transform test, upload 500 `requestId` assertion, filterAdminEvents tests, revalidateProject double-call test, IsFeaturedToggle rapid double-toggle test, ClientAnalytics test, `/api/admin` bare-path 401** — all closed in same-session phase pass. [source](code-reviews/2026-05-07.md)
- **2026-05-09 → in-session — `<Link>` → `<a>` for non-route hrefs, `ErrorMessage.tsx` lint suppression, `globals.css` duplicate `.prose a` rule, Error boundaries added + RTL tested, `error.digest` dropped** — all closed same session. [source](code-reviews/2026-05-09.md)
- **2026-05-09 → 2026-05-10 — Outbound `<a>` links `rel="noopener noreferrer"`** — added to all 9 bio external links and Twitter contact link. [source](code-reviews/2026-05-10.md)
- **2026-05-10 → in-session — Error boundary digest negative assertions** — added "does not render the error digest" case to both suites (flagged as narrower than ideal on 2026-05-11). [source](code-reviews/2026-05-10.md)
- **2026-05-11 → in-session — `datetime <= now` cascade across `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams`, `legacySlug.cachedLookup`; magic-byte `detectImageMime` sniff; structured `auditLog` helper; `parseJsonBody` paths-only logging; `<time dateTime>` ISO 8601 fix; carousel APG pattern + padded-button hit regions; skip-link wrapper; `useSyncExternalStore` ThemeProvider** — three full-codebase reviews + three implementation passes (24 commits) closed every actionable finding. [source](code-reviews/2026-05-11.md)
- **2026-05-12 → in-session — `<main>` hoist to root layout (14 pages); `Post.slug` index in schema; posts PUT escalated to Serializable; `useOptimisticMutation` extracted at the 2nd consumer; `AdminAuditTag` closed union; `previousSlug` audit semantics unified across posts/projects; `readErrorMessage` extracted to `lib/`; `postDatetimeToISO` returns `string | undefined`** — daily review → 7+3 phase pass (12 commits). [source](code-reviews/2026-05-12.md)
- **2026-05-13 → in-session — `PrivacyPageLayout.tsx` `<main>` cleanup (missed by Phase 2 hoist); `previousSlug` DRY lift in projects PUT; `MutateResult` discriminant (`failure` vs `superseded`) + unmount-after-resolve race fix; `AdminAuditTag` derived from const array; `metadata.ts` dev-only double-brand throw; `apiErrors.describeZodIssues` dedupes segments; `sanitizeLogString` for upload route log-injection guard; `ProjectForm.handleSubmit` re-snaps from `sortOrderText`** — daily review → 7-phase pass (7 commits). Phase 4 test (unmount-after-non-ok-resolve) exposed a real bug — see Notes. [source](code-reviews/2026-05-13.md)
- **2026-05-14 → in-session — Upstash cron daily; HMAC-SHA256 IP pseudonymization with `IP_HASH_SECRET`; `KEEPALIVE_KEY` centralized to `lib/keepalive.ts`; `POST /api/admin/keepalive` + "Run keepalive" button; `prefetch={false}` on per-row edit links; read-time `datetime <= now` filter with padded caches (reverses the 2026-05-11 "capture `now` inside cached fn" decision)** — two-session day; review's findings closed in the same batch. [source](code-reviews/2026-05-14.md)
- **2026-05-14 → 2026-05-15 — `writeKeepalive(redis): KeepaliveResult` extraction** — closes the 2026-05-14 watch-out "two SET sites might drift on logging/error-shape" — the drift had already materialized by 2026-05-15 review (cron emitted no success line, admin emitted `console.info`; error payloads diverged). [source](code-reviews/2026-05-15.md)
- **2026-05-15 → in-session — bulk markdown import (`/admin/posts/bulk` + `POST /api/admin/posts/bulk`); `[api:admin:posts:BULK]` audit tag; shared `bulkImportParser`; inline `· Scheduled` marker; `PostPublishedToggle` widget** — closed in same session. [source](code-reviews/2026-05-15.md)
- **2026-05-16 → 2026-05-17 — Every issue except the explicitly-deferred `Promise.all(file.text())` perf item.** Resolved via 6-commit phase split: (a) `BooleanFlagToggle` extraction with revert-baseline + visible-label + failure-logging fixes (commit `33297e2`); (b) `isFutureDatetime(datetime, now)` helper + scheduled-marker test (commit `2b6e52c`); (c) `AdminAuditPayload.batchId: string | null` cross-cutting type bump (commit `c445446`); (d) `FILENAME_REGEX` regex tightening + `BULK_MAX_FILES` export + `PostBulkImport` composite-key + truncation banner (commit `6cf98ba`); (e) bulk route hardening: silent-drop reconcile, batchId emission, `prepareBatch` + `emitSkipSummary` helpers, parallel `Map<slug, filename>` so DB-shaped `InsertRow` can't leak, prepared-slug breadcrumb log, skip-reason summary log (commit `183a1b5`); (f) `getKeepaliveRedis()` lazy memoization + `KeepaliveResult.error` narrowed to `Error` + cron success log (commit `658ad40`). 14 specific items checked off in the source review file. [source](code-reviews/2026-05-16.md)

### Still standing at month-end

- **`clientBucketKey` `"unknown"` fallback** — no test for missing/multi-value/IPv6/empty-string `x-forwarded-for`. First flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **`hasKeyword` `\b` wrong for hyphenated/punctuated keywords** — `react-native`, `.net`, `c++` uncovered; no test. First flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **Rate-limit fail-open path untested** — Redis unavailability branch. First flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **`sanitizeFilename()` path traversal/null-byte/Windows-separator tests missing** — first flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **Projects concurrent PUT serializable storm** — two concurrent PUTs on different projects targeting the same `sortOrder` can both retry under serializable isolation; confirm client surfaces 409 or 503 rather than hanging. First flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **`src/proxy.ts` auth gate drift risk** — discrete equality/prefix checks; adding a protected API root requires touching a disjunction. Partially mitigated 2026-05-17 (`/api/upload` consolidated under `/api/admin/`). First flagged 2026-05-07. [source](code-reviews/2026-05-07.md)
- **`https://db.com` anonymized link** — inconsistent with surrounding named employers. Still present after multiple rewrites. [source](code-reviews/2026-05-11.md)
- **`AboutContent.tsx` missing `{" "}` before Deutsche Bank `<a>`** — rendered DOM will be `likeDeutsche Bank`; introduced in 2026-05-11 commit. [source](code-reviews/2026-05-11.md)
- **Error boundary digest negative assertions narrower than committed contract** — `not.toContain` vs recommended `not.toMatch(/[a-f0-9]{20,}/)`. First flagged 2026-05-11. [source](code-reviews/2026-05-11.md)
- **Sitemap future-dated posts** — superseded by 2026-05-14 read-time-filter pivot; verify before closing. Open thread from 2026-05-07. [source](code-reviews/2026-05-07.md)
- **Admin auth single source** — middleware, layout, and `generateMetadata` each enforce gating independently. Open thread from 2026-05-07. [source](code-reviews/2026-05-07.md)
- **`describeZodIssues` not exported (mirror login-route pattern)** — comment claims to mirror but the helper is module-private; the second call site already drifts. First flagged 2026-05-12. [source](code-reviews/2026-05-12.md)
- **`useOptimisticMutation` silent supersession** — stale non-ok responses and stale throws return `{ ok: false }` with no log. If the server starts returning 500s only on superseded requests, the admin gets no signal. Tag-only dev-warn would be cheap. First flagged 2026-05-13. [source](code-reviews/2026-05-13.md)
- **`PresetOrFreeformInput` English-only `aria-label` suffix** — `"(preset)"` / `"(custom)"` hard-coded; minor since file is otherwise i18n-clean. First flagged 2026-05-13. [source](code-reviews/2026-05-13.md)
- **`AdminNav.handleLogout` button flicker between push schedule and `finally` reset** — speculative ("probably invisible"). First flagged 2026-05-13. [source](code-reviews/2026-05-13.md)
- **Apply `Post.slug` index via `yarn db:push`** — schema committed 2026-05-12; DB not migrated. Still pending. [source](code-reviews/2026-05-12.md), [2026-05-13](code-reviews/2026-05-13.md), [2026-05-15](code-reviews/2026-05-15.md)
- **`sanitizeLogString` C0/DEL gap** — covers `\r\n\t\0` only; `\v`, `\f`, `\x1b` ESC, `\x7f` DEL still pass through. ANSI escape could re-paint a tailing terminal. First flagged 2026-05-14. [source](code-reviews/2026-05-14.md)
- **`ProjectForm.handleSubmit` / `sortOrderText` desync** — `setSortOrderText(String(sortOrder))` doesn't mirror `setField("sortOrder", sortOrder)`; current invariant carries correctness but a future change to either source breaks the lockstep silently. First flagged 2026-05-14. [source](code-reviews/2026-05-14.md)
- **`ProjectForm` digit-only regex duplicated 3× (`/^\d+$/`)** — `onChange`/`onBlur`/`handleSubmit`. Extract `snapSortOrder(text, fallback)`. First flagged 2026-05-14. [source](code-reviews/2026-05-14.md)
- **`buildPageMetadata` brand check case-sensitive** — `title.includes("Roland Leth")` lets `"roland leth"` slip past. First flagged 2026-05-14. [source](code-reviews/2026-05-14.md)
- **Split publish contract (DB-layer + read-time)** — `publishedWhere(section, now)` and `post.datetime <= now` duplicated across 5+ call sites; any future query forgetting the read-time filter silently leaks scheduled posts. 2026-05-17 partially mitigated via `isFutureDatetime(datetime, now)` helper used in 2 sites, but the read-time filter remains duplicated. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`getPostsBySection` page 1 vs page>1 boundary** — scheduled post can appear on both pages if it crosses `now()` mid-session. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`getPostsBySection` multiple `now` reads per request** — three `currentDatetimeString()` calls per page-1 request; across a minute boundary, `totalPages` (count-driven) can include a row the read-time filter excludes. Hoist one `now` at the entry point. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`getPostBySlug` future bodies in cache** — `unstable_cache` stores the full `PostDetail` (including `body`) for future-dated rows the user can't see yet. Minor cache bloat; exposes draft content to anyone with cache-dump access. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`IP_HASH_SECRET` minimum length** — `getIpHashSecret` accepts any non-empty string. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`clientBucketKey` computed twice per failed login** — once for the limiter, again for the warn log. Two HMAC-SHA256 calls where one suffices. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`slice(0, 22)` magic constant in login route** — 22 base64url chars (132 bits) is fine, but undocumented. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **Cache-padding pattern duplicated between `posts.ts` and `feed/[section]/route.ts`** — `take: LIMIT + futureCount`. Rule of three says extract at the third caller; flag set. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **`take: PAGE_SIZE + futureCount` padding sizing assumption** — single-author volume assumption baked in. Bulk-import of N future-dated posts inflates every cached page-1 payload linearly. Add `Math.min(futureCount, MAX_PAD)`. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **Silent under-pad in cached page 1** — when a scheduled post is added between cache populate and request, `futureCount` underestimates and the read-time filter can shrink the page below `PAGE_SIZE` with no log. First flagged 2026-05-15. [source](code-reviews/2026-05-15.md)
- **Bulk import `Promise.all(file.text())` reads all 50 files into memory at once on the client** — ~5MB worst case. "Fine today" per the 2026-05-16 review's own framing. Revisit if `BULK_MAX_FILES` is ever raised. [source](code-reviews/2026-05-16.md)

## One-off findings

- **2026-05-01 — Proxy redirect `toContain("/privacy")` over-broad** — `toMatch(/\/privacy$/)` would be non-trivially falsifiable; still using `toContain` for consistency with other redirect tests. [source](code-reviews/2026-05-01.md)
- **2026-05-01 — Privacy redirect query-string passthrough untested** — `/privacy-policy?ref=foo` → `/privacy?ref=foo` not asserted; behavior is correct, just uncovered. [source](code-reviews/2026-05-01.md)
- **2026-05-07 — `redis fail-open` `console.warn` not tagged distinctly** — sustained Redis outage means unlimited login attempts with no operator-visible signal beyond log volume. [source](code-reviews/2026-05-07.md)
- **2026-05-11 — "Over the past year" vs "In the past year"** — minor voice regression in the about-page rewrite. [source](code-reviews/2026-05-11.md)
- **2026-05-12 — `ProjectSectionCarousel.tsx` empty-`images` guard silent** — defensive `return null` is silent; dev-only `console.warn` would surface the parent-gate regression the guard is meant to compensate for. **Resolved 2026-05-13** via dev-only `console.warn` gated on `NODE_ENV !== "production"`. [source](code-reviews/2026-05-12.md)
- **2026-05-12 — `PostCard.tsx` + `PostContent.tsx` — `postDatetimeToISO` throw context** — when the throw bubbles into a server component, only Next's default error boundary sees it; no `{ postId, datetime }` is logged. Wrap with try/catch that logs at warn. Resolved by 2026-05-12's switch to `postDatetimeToISO` returning `string | undefined` instead of throwing. [source](code-reviews/2026-05-12.md)
- **2026-05-12 — `ThemeProvider.tsx` SSR snapshot vs cookie-set class** — `getServerColorSchemeSnapshot` always returns `false`; on a system-dark user with cookie-set class, the SSR snapshot disagrees and `applyTheme`'s effect can briefly flip light→dark on hydration. Inline boot script ostensibly suppresses; verify. Still standing. [source](code-reviews/2026-05-12.md)
- **2026-05-12 — `ALLOWED_UPLOAD_MIMES` and `detectImageMime` independently maintained** — adding a MIME to allowlist without matching magic-byte branch turns upload into hard 415. Derive both from one source or assert at module load. Still standing. [source](code-reviews/2026-05-12.md)
- **2026-05-12 — `createBoundedWrapperCache.get` single-flight assumption** — if not de-duped, two concurrent first-accesses for `(section, slug)` build two wrappers. Worth confirming. Still standing. [source](code-reviews/2026-05-12.md)
- **2026-05-12 — `<time dateTime>` was emitting `yyyy-MM-dd-HHmm`** — load-bearing correctness bug, missed by two prior reviews. Resolved same session via routing both `PostCard` and `PostContent` through `postDatetimeToISO`. Added `<time dateTime>` to the attribute-level audit-target list. [source](code-reviews/2026-05-12.md)
- **2026-05-14 — `formatZodIssues` numeric-zero segment** — new test covers `null`/`undefined`/`""` rejections but not the `0` case (array-index paths like `["sections", 0, "title"]`). Implementation handles it; regression would silently drop index `0`. Still standing. [source](code-reviews/2026-05-14.md)
- **2026-05-14 — Verified two pointed analyzer claims wrong** — (a) the `P2034` test's audit-log negative assertion is NOT vacuous (`auditLog` emits `${tag} success`, src/lib/auditLog.ts:49); (b) the ProjectForm "stale value" test's `expect(sortOrder.value).toBe("5")` pins post-fix visual snap-back. Both findings dropped. Lesson: verify analyzer claims by reading the actual files before promoting them to the rollup. [source](code-reviews/2026-05-14.md)
- **2026-05-15 — Posts PUT 500 under serialization conflict** — escalated to Serializable in 2026-05-12; `serialization_failure → 500` with no retry loop, also not client-retriable. Acknowledged trade. [source](code-reviews/2026-05-13.md)
- **2026-05-15 — Module-load warn for missing `IP_HASH_SECRET`** — emits only on cold start; warm invocations never re-emit. Consider once-per-N-minute lazy warn at the rate-limit gate. Still standing. [source](code-reviews/2026-05-15.md)
- **2026-05-16 — Prompt-injection hygiene on bulk admin filenames** — admin-supplied filenames flow verbatim into `auditLog` payloads + result panel. React escapes for DOM; audit log is attacker-influenced text in a trusted-looking position if those lines ever feed an LLM-backed analyzer. Markdown body relies on existing `react-markdown` HTML-stripping (same posture as single-post create). 2026-05-17 `FILENAME_REGEX` tightening (rejects path separators + ASCII control chars) closed the highest-risk subset; markdown body unchanged. [source](code-reviews/2026-05-16.md)
