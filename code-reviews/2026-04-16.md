# Code Review — 2026-04-16

> Commits reviewed: `165ff80` — "Minor improvements and tests" (2026-04-15)

---

## Summary

Single commit with targeted bug fixes and test additions across the admin project form. All changes are scoped and purposeful; no new behaviour introduced beyond what was needed to fix the bugs.

---

## What's well-done

- `_key` as a stable React key is the right call. The UUID is assigned at every entry point (init from `initialData`, `addLink`) and stripped before submission with a clear comment. No magic index keys remain in `LinkManager`.
- The `spannedBuckets` guard in `PlatformPicker` correctly handles the edge case without over-engineering — just discards the keywords if they span buckets and falls through to freeform.
- `ROLE_OPTIONS` moved to module level matches the existing `LINK_LABEL_OPTIONS` pattern in `LinkManager.tsx`.
- The three new `platforms.test.ts` cases are precise and match the specific logic they're guarding.

---

## Issues found and fixed (2026-04-16)

### 1. `LinkItem` interface was duplicated ✅ fixed

**Files:** [`src/components/admin/LinkManager.tsx`](src/components/admin/LinkManager.tsx), [`src/components/admin/ProjectForm.tsx`](src/components/admin/ProjectForm.tsx)

Both files independently defined an identical `LinkItem` interface. `LinkItem` is now exported from `LinkManager.tsx` and imported in `ProjectForm.tsx`, removing the duplication.

---

### 2. No test for `PlatformPicker`'s cross-bucket initialisation guard ✅ fixed

**File:** [`src/components/admin/PlatformPicker.test.tsx`](src/components/admin/PlatformPicker.test.tsx) _(new)_

Added four component tests covering the two initialisation paths:

- Cross-bucket (`"iOS, React"`): freeform input receives the full value; no bucket is locked out.
- Same-bucket (`"iOS, iPad"`): freeform input is empty and disabled; keywords from other buckets are disabled.

---

### 3. `crypto.randomUUID()` environment availability ✅ verified, no change needed

`ProjectForm.test.tsx` already renders `ProjectForm` (which calls `crypto.randomUUID()` in its `useState` initialiser) and passes. Both `happy-dom` and Node 18+ expose `crypto.randomUUID()` natively, so no polyfill or guard is needed.

---

### 4. `vercel.json` cron schedule change

**File:** [`vercel.json`](vercel.json)

Changed from `0 0 1,15 * *` (2× monthly) to `0 0 1,11,21 * *` (3× monthly, roughly every 10 days). No risk — just pings the `/api/cron/ping` endpoint more frequently, presumably to keep the DB connection alive on the free tier.

---

## Risk assessment

**Low.** All changes are admin-only UI fixes. No public-facing routes, no schema changes, no auth paths touched.
