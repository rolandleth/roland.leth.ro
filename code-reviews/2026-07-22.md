# Code Review — 2026-07-22

## Risk Assessment

**Low.** The last 24h is entirely the `fix/code-review-2026-07-21` branch (merged to `release` at `babd0a6`), which resolved the findings from `code-reviews/2026-07-21.md`. All twelve source/test files are the admin-panel + IndexNow-key work; no schema, auth boundary, or data-flow plumbing widened. Each behavioural change is paired with a behaviour-asserting test, and every change traces to a documented decision in `dev-journal/2026-07-21.md`. Reviewed the fixes for regressions and half-fixes; none found. Load-bearing consistency point verified: the `getIndexNowKey()` trim now applies to **both** readers — the submit route (`src/app/api/admin/indexnow/route.ts:71`) and the served key file (`src/app/indexnow-key.txt/route.ts:25`) — so the `/indexnow-key.txt` content and the submitted `key` stay byte-identical and IndexNow won't 403 on a mismatch. `ErrorMessage` carries `role="alert"`, so the `aria-controls` split (polite outcome region + assertive error region, both always-mounted) is sound and the IDREF no longer dangles.

## Notes

- **Orchestration mode: inline.** 16 files / 483 insertions, but file count is inflated by 4 docs (journal, history, lookup-map, prior review) and the code delta is well under the 500-LOC inline threshold and tightly cohesive (admin panels/hooks + auth/content env helpers). Single inline pass; no gatherer/analyzer subagents. Technically >10 files, so noting the deviation.
- **Prior review resolved.** These commits are the fixes for `code-reviews/2026-07-21.md`; that file's findings (superseded-failure observability, per-batch error detail on `logResult`, `INDEXNOW_KEY` trim, `aria-controls` dual-region wiring, shared `adminOutcomeClass`, and the test-gap groups) are addressed here. Test suite reported 2003 green in the session journal.
- **Deliberate, not re-raised** (per `dev-journal/2026-07-21.md` Watch-outs): middleware as sole auth boundary for the 12 admin routes (Next 16.2.10, not exposed to CVE-2025-29927; revisit on a route with irreversible side effects or a Next major); no IndexNow submit throttle (single-operator scale); `describeKeyProblem` user-facing-copy coupling (intentional, four-site); residual outcome-markup duplication across both panels (left per the shared-class-over-wrapper-component preference; revisit only if a third panel arrives).
- **Observability nuance (context, not a finding):** the superseded-request-failed path in `useAdminAction.ts:98` logs at `console.debug`, which browsers suppress unless the console level is set to Verbose. This is the deliberate tradeoff — `debug` avoids spamming the console for a benign superseded request while keeping the failure diagnosable — and is the correct level given the alternative (`warn`) would surface noise under rapid re-submits. No action needed.
- **Open threads carried** (from the journal, not raised by these commits): set `INDEXNOW_KEY` in Vercel and confirm `/indexnow-key.txt` is live before the first submit; decide what to do with the still-untracked `code-reviews/2026-07-19.md`.
