# Code Review — 2026-05-07

13 commits, 54 files, 1 160 insertions / 179 deletions. Covers: API reliability batch, admin UX/concurrency hardening, test infrastructure expansion, accessibility and performance polish.

---

## Issues and Observations

- **src/app/api/auth/login/route.ts — `clientBucketKey()`**: Falls back to literal `"unknown"` when `x-forwarded-for` is absent, collapsing all header-less requests into one shared bucket — defeats per-IP rate limiting in any environment without a proxy setting that header (direct Vercel hits during development, curl, misconfigured CDN).
- ~~**src/components/admin/AdminNav.tsx — `handleLogout`**~~: Fixed — 401 now redirects directly; other non-ok statuses still show retry message.
- **src/lib/platforms.ts — `hasKeyword`**: `\b…\b` is wrong for keywords with leading/trailing non-word characters (`react-native`, `.net`, `c++`). Word boundaries anchor at letter/digit edges — for hyphenated or punctuated keywords, use lookarounds keyed on whitespace/string boundaries instead.
- ~~**src/lib/analytics.ts — `filterAdminEvents` URL fallback**~~: Fixed — `#` is now stripped from the fallback pathname so `/admin#section` is correctly classified.

---

## Architecture & Coupling

- ~~**src/lib/projects.ts — `revalidateProject`**~~: Fixed — PUT now reads the old slug before the transaction and calls `revalidateProject` twice when a name change produces a new slug.
- **src/proxy.ts — auth gate**: Three discrete equality/prefix checks are open-coded. Adding a new protected API root means touching a disjunction rather than appending to an array — drift risk.

---

## Observability

- **src/app/api/auth/login/route.ts — Redis fail-open**: Correct posture, but the `console.warn` is not tagged distinctly enough to alert on. A sustained Redis outage means unlimited login attempts with no operator-visible signal beyond log volume.
- **src/app/api/auth/login/route.ts — schema validation log at `console.warn`**: Brute-force probing of the login endpoint won't surface in error dashboards that filter on `console.error`. Confirm this downgrade is intentional given the security context.
- ~~**src/app/api/upload/route.ts — upload 500 path**~~: Fixed — 500 response now includes `requestId` for log correlation.
- **src/lib/auth.ts — JWT error split**: Verified sound — the type guard falls back to `error.name` for jose errors that don't carry `.code`, so all jose failure modes land in the correct log level.
- ~~**src/components/admin/ProjectSortOrderInput.tsx — failure path**~~: Fixed — `readErrorMessage` exported from `useAdminResource` and reused; HTTP status now included in displayed error. Network rejection errors propagate `err.message` directly.

---

## Concurrency & Thread Safety

- ~~**src/app/api/admin/posts/[id]/route.ts — cross-section cache bust**~~: Comment added explaining the intentional two-statement design — worst case is a 5-minute cache lag, not a data-integrity issue.
- **src/app/api/admin/projects/[id]/route.ts — serializable storm**: Two concurrent PUTs on different projects targeting the same `sortOrder` can both retry under serializable isolation. Confirm there is a retry/abort policy and that the client surfaces 409 or 503 rather than hanging or silently losing one write.
- ~~**src/components/admin/IsFeaturedToggle.tsx — rapid toggle**~~: Rapid double-toggle test added; abort + optimistic-revert order verified — disabled state clears on settle.

---

## Missing Tests

- **src/app/api/auth/login/route.ts — `clientBucketKey()`**: Untested. Needs cases for missing header, multi-value `x-forwarded-for` (`"1.2.3.4, 5.6.7.8"` — which IP wins?), IPv6, empty string.
- **src/app/api/auth/login/route.ts — rate-limit fail-open**: No test for Redis unavailability path — this is the branch most likely to regress silently into fail-closed (everyone locked out) or fail-loud (500s) on a refactor.
- ~~**src/lib/schemas.ts — `postDatetime` regex**~~: Structural rejection cases added (`"garbage"`, `"2025-01-01"`, `"2025-1-1-0000"`, etc.); semantic out-of-range values excluded with a comment (regex is format-only).
- ~~**src/lib/schemas.ts — `loginSchema.email` transform**~~: Uppercase-email lowercase test added.
- ~~**src/app/api/admin/* — 500 response shape**~~: `requestId` regex assertion added to the upload 500 test.
- **src/app/api/upload/route.ts — `sanitizeFilename()`**: Needs explicit cases for path traversal (`../../`), null bytes, Windows separators (`\`), and the non-ASCII stripping behavior so silent-mutation regressions are caught.
- **src/app/api/upload/route.ts — Content-Length precheck**: No test asserting that `formData()` is *not* called on oversized uploads; the precheck could be reordered in a refactor with tests still passing.
- **src/lib/platforms.ts — `hasKeyword`**: No test for hyphenated (`react-native`) or punctuated (`.net`, `c++`) keywords — the word-boundary correctness risk is unverified by any existing case.
- ~~**src/lib/analytics.ts — `filterAdminEvents`**~~: Tests added for `/admin#section` relative URL (filtered) and `/administrator`-prefixed path (not filtered).
- ~~**src/lib/projects.ts — `revalidateProject` double-call**~~: Test added asserting both old and new slug tags are invalidated on a slug-changing PUT.
- ~~**src/components/admin/IsFeaturedToggle.tsx — rapid double-toggle**~~: Abort + state reconciliation covered.
- ~~**src/app/ClientAnalytics.tsx**~~: Test added asserting both `SpeedInsights` and `Analytics` receive `beforeSend={filterAdminEvents}`.
- ~~**src/components/home/LandingBackground.tsx**~~: Test added asserting `data-blob-bg` is present on blob elements.
- ~~**src/app/(protected)/projects/[id]/edit/page.test.tsx**~~: Tightened to `include: projectInclude` (exact shape).
- ~~**src/proxy.test.ts**~~: Negative test for `/api/adminx` added; bare `/api/admin` path added to the 401 array.

---

## Risk Assessment

**Medium.** This is a substantial batch covering security-adjacent plumbing (rate limiting, auth, upload, cross-section cache invalidation) alongside admin UX polish and test infrastructure. The high-level design is sound and the concurrency patterns (serializable transactions, AbortController, mount guards) are correctly applied. The main elevated risk is `clientBucketKey` sharing one `"unknown"` bucket for header-less requests — a deployment without `x-forwarded-for` loses per-IP bucketing entirely and the current test suite wouldn't catch it. The `handleLogout` 401-vs-500 distinction is a low-severity UX correctness issue. Nothing landed that directly breaks existing contracts, but the missing rejection-case tests for `postDatetime`, `sanitizeFilename`, and `clientBucketKey` leave the new validation logic without regression anchors.

---

## Notes

No dev-journal entries found for 2026-05-04 through 2026-05-07. Three findings were struck after author review: `ProjectSortOrderInput` NaN (guard already present on the lines immediately following `parseInt`); `useAdminResource` JSON.stringify cycle guard (payload is always a plain object from form state — defensive code for an impossible scenario); `sortOrder` clamp returning 400 (intentional: concurrent delete between render and submit is a benign race, clamping is more forgiving than a forced refresh). `Project.sortOrder` lacking `@@unique` is a known ceiling: Prisma cannot express `DEFERRABLE INITIALLY DEFERRED`, so serializable isolation is the documented workaround. `sanitizeFilename` non-ASCII stripping is pre-existing and intentional (UUID prefix carries uniqueness). `hasKeyword` `\b` limitation is accurate but speculative — no current keyword has hyphen/punctuation edges.

Orchestration: split mode, 3 gatherer → analyzer pairs, no synth-pass subagent (≤5 partitions — main consolidated directly).
