# Code Review — 2026-06-18

## Issues and Observations

- **next.config.ts — `headers()`** — The second header block (`source: "/.well-known/atproto-did"`) sits inside the function and re-runs on every `next.config.ts` reload, but is itself static. Fine as-is; only worth noting that if more per-path overrides accumulate, hoisting the override array out of `headers()` would mirror how `securityHeaders` is composed once per call rather than per-rule. Not a defect today.
- **next.config.ts — header-rule ordering** — `source: "/(.*)"` and `source: "/.well-known/atproto-did"` both match the DID request and merge headers. The new `Content-Type` doesn't collide with anything in `securityHeaders`, so the intended override holds. A future rule that also sets `Content-Type` on `/(.*)` (none currently) would silently win or lose depending on Next.js merge order — worth a mental flag, not a fix.
- **public/.well-known/atproto-did** — Plain DID payload, correct per AT Protocol spec (path is `atproto-did`, not the earlier `atproto` from `764caf1` — corrected via `R100` rename in `89e3a43`). No trailing newline visible; Bluesky's verifier trims, so harmless.
- **src/app/about/AboutContent.tsx — `contactLinks`** — Label corrected `BlueSky` → `Bluesky` (official brand). Cosmetic, no risk.

## Observability

- **next.config.ts — silent MIME mismatch path** — If the file is ever renamed back without updating the header rule (or vice versa), the request 404s or reverts to octet-stream + `nosniff` block with no signal beyond Bluesky's verifier failing. Low likelihood; the explanatory comment at lines 66–68 is the mitigation.

## Risk Assessment

**Low.** Two commits, both narrowly scoped to Bluesky/AT Protocol handle verification: a static `.well-known/atproto-did` payload, a single per-path `Content-Type` override to bypass the site-wide `nosniff`, and a brand-capitalisation fix in the About contact list. No load-bearing plumbing touched; no contracts changed; the middleware (`src/proxy.ts`) passes `.well-known/*` through unmodified to Next.js static serving so no interaction risk with the legacy-slug catch-all. Remaining 5 files in the diff are journal/review/history markdown — no behaviour.

## Notes

- Mode: `inline` (8 files, ~294 LOC delta, of which 5 are dev-journal/code-reviews markdown).
- No relevant `Open Threads` or `Watch-outs` in `dev-journal/2026-06-13.md` or `dev-journal/2026-06.md` — the active threads are all about the importer/blob-sync work, unrelated to this change.
- The first commit `764caf1` published the file as `/.well-known/atproto`; the spec-correct path is `/.well-known/atproto-did`. The follow-up commit `89e3a43` (rename + header rule) is what makes verification actually work; anyone bisecting between the two would see a broken intermediate state. Not an issue post-merge.
- No tests added — appropriate: a static asset, a declarative header rule, and a string-literal label fix have no testable behaviour beyond what Next.js itself owns.
