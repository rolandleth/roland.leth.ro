# Code Review — 2026-06-24

## Issues and Observations

- **[src/app/terms/continuum/page.tsx — Overview section]** — Platform scope narrowed from "Mac and iOS app" to "Mac app". Confirm the product side matches: if the iOS app is removed from the App Store / scope, the projects portfolio and any marketing surfaces should reflect the same. If iOS is still planned, a legal doc that excludes it now creates a re-revision cost later.
- **[src/app/terms/continuum/page.tsx — Pricing & Subscriptions]** — Free-tier copy dropped "encrypted local backup and restore", and Pro dropped "richer export"; Pro gained "unlimited signal and entry types" (replacing "your own"). Verify the in-app entitlement matrix matches — terms that promise less than the app delivers are harmless, terms that promise more than the app delivers are not. The Pro list also no longer mentions export at all; if Pro still includes any export, calling that out preserves buyer expectation.
- **[src/app/terms/continuum/page.tsx — Overview, `<strong>` wrap]** — Wrapping the entire Apple EULA sentence (~3 lines) in `<strong>` is heavier than typical inline emphasis; the visual weight may compete with section headings. Consider scoping `<strong>` to the conflict-of-laws clause ("where it conflicts with these terms, it governs") only, which is the actually load-bearing line.

## Notes

- Single-commit window (`5584e0f`); only one source file touched (`src/app/terms/continuum/page.tsx`) — the rest of the diff is dev-journal / code-review / monthly-rollup churn from the prior wrap-up.
- `lastUpdated` correctly bumped to June 24, 2026, consistent with the 2026-06-13 convention of dating legal pages only on substantive revisions.
- No interaction with open threads (`continuumLegalLinks.ts` union type, `LegalPageLayout` naming) or watch-outs in `dev-journal/2026-06.md`.
- Orchestration: inline (1 file, <50 LOC delta).

## Risk Assessment

**Low.** Content-only edit to a legal page; no logic, routing, data, or contract changes. The substantive risk is alignment: terms must match what the app actually does (platform scope, free vs. Pro feature split). Worth a quick cross-check against the current Continuum build's entitlements and the Continuum project entry on this site before the next public link.
