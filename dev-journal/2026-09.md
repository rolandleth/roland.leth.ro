# Dev Journal Monthly Rollup — 2026-09

_Generated 2026-09-01._

Sessions: 1 across 1 day. Source files: `2026-09-01.md` × 1.

## Themes

- **Hardening the scheduled-post notice shipped on 2026-08-31** — the whole month so far is follow-up on one feature. A daily review found six issues in the day-old code; five were fixed the same session and one was withdrawn as already-mitigated. The substantive one collapsed four post-lookup accessors into a single `resolvePost` returning a discriminated union, which removed a publish-boundary race rather than only tidying the API. [source](dev-journal/2026-09-01.md)

## Decisions (chronological)

- **2026-09-01 — One resolver instead of two lookups** — `resolvePost` in `src/lib/db/posts.ts` returns `live` / `scheduled` / `missing` from one row fetch and one `currentDatetimeString()`. Rejected: documenting the race and leaving it; threading an optional `now` through both public signatures. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — `isFutureDatetime` carries the comparison** — the hand-rolled `<=` / `>` had been bypassing an existing helper whose docblock already argued for a single captured `now`. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — Deleted `loadPost`, `getScheduledPost`, `loadScheduledPost`** — all lost their last production caller once both detail routes read `loadPostResolution`. `getPostBySlug` stays because `detailCacheMiss.test.ts` exercises the cache layer through it. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — Scheduled metadata routes through `buildPageMetadata`** — a hand-rolled `{ title, robots }` emits no `alternates`, so the page inherited the layout's tech-feed default and contradicted the section feed the notice itself renders on `/blog/life/*`. Rejected: patching in a lone `alternates` block. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — Scheduled `.md` stub gets real frontmatter** — `buildScheduledPostMarkdownFile` shares `postFrontmatterLines` with the live export and appends `scheduled: true`; `X-Robots-Tag` reaches search crawlers only, and an agent fetching the URL sees the body alone. [source](dev-journal/2026-09-01.md)

## Tradeoffs

- **2026-09-01 — Four fewer exports, more test churn** — gained one way to ask the question (a surviving `loadPost` would let a future caller reintroduce the split verdict); accepted a larger diff on day-old code. Reversibility: easy. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — Scheduled pages now carry an OG card** — a shared scheduled link previews with the post's title instead of the site-generic card, which widens what a pre-publish URL discloses to a link unfurler. Consistent with the title tease accepted 2026-08-31. Reversibility: easy. [source](dev-journal/2026-09-01.md)

## Incidents / Bugs

- **2026-09-01 — `max-w-m` in `ScheduledPostNotice`** — a pure visual-tuning commit (`b6ca1da`) turned `max-w-md` into `max-w-m`; Tailwind v4 resolves `max-w-*` against `--container-*` and drops an unknown name silently, so the body copy ran to the parent's `max-w-4xl`. Lint and `tsc` are both blind to it. [source](dev-journal/2026-09-01.md)
- **2026-09-01 — Two clock reads at the publish boundary** — `getPostBySlug` and `getScheduledPost` each read the clock, so a post crossing its `datetime` between them answered no to both and pinned a 404 at the moment it went live. Sub-millisecond window; the next cron run healed it, so the ceiling was the old behaviour on one post. Fixed by the single-read resolver. [source](dev-journal/2026-09-01.md)

## Environment / Config Changes

- **2026-09-01** — Added `src/components/blog/ScheduledPostNotice.test.tsx`, the last component in `src/components/blog/` without a test. [source](dev-journal/2026-09-01.md)
- **2026-09-01** — `formatCalendarDay` added as a private helper in `src/lib/utils/format.ts`; `formatDate` and `formatDayMonth` now differ only by their `Intl.DateTimeFormatOptions`. [source](dev-journal/2026-09-01.md)

## Learnings

- **A review finding can be already-mitigated one layer up** — an observability finding was withdrawn after reading the cron, which already logs a heartbeat on its quiet path for that exact purpose. A pinned prerendered page runs no code and cannot report on itself. Read the adjacent mechanism before writing up a gap. [source](dev-journal/2026-09-01.md)
- **Check the helpers before proposing a fix** — `isFutureDatetime` already existed with a docblock arguing for the single captured `now` the race fix needed. [source](dev-journal/2026-09-01.md)
- **"I don't really get it" means the explanation was wrong, not the reader** — the pass that landed was a timeline with a concrete post date and both comparisons written out. Lead with the failure, not the mechanism. [source](dev-journal/2026-09-01.md)

## Still open at month-end

### Open Threads

_None. Both threads carried into 2026-09-01 were closed the same day — the blog series import (carried from `2026-08-31.md`) and the end-to-end check of a scheduled post's page and `.md` twin. See [2026-09-01](dev-journal/2026-09-01.md)._

### Watch-outs & Tech Debt

- **Publish-day pre-cron window** — a scheduled post's notice can still be serving ON its publish date until the daily cron busts it. Mitigation is scheduling practice: date posts `-00xx`. Revisit if a sub-daily cron cadence lands. [first flagged: 2026-08-31]
- **Visual tuning has no test that can fail** — an unknown Tailwind class name emits nothing and errors nowhere. Revisit if class-name typos recur; an `eslint-plugin-tailwindcss`-style check is the only mechanical guard. [first flagged: 2026-09-01]
- **Pre-existing uncommitted dev-journal/code-review edits** (`2026-08.md`, `_history*.md`) predate 2026-08-31 — left uncommitted deliberately. [first flagged: 2026-08-31]
