# Dev Journal – 2026-05-14

## Open Threads

- [ ] Set `IP_HASH_SECRET` in Vercel env (Production at minimum; mirror to Preview/Dev if rate-limiting is wanted there). Without it the limiter silently disables — warn line emitted at module load.
- [ ] Verify production: log into `/admin`, click "Run keepalive", confirm `keepalive:last` appears in Upstash data browser with the timestamp shown in the success line.
- [ ] After first daily cron tick (next 00:00 UTC post-deploy), confirm Upstash idle warnings stop arriving.
- [ ] If `keepalive:last` doesn't update on the cron schedule, check Vercel dashboard → Crons tab for last invocation status — Hobby crons can be auto-paused after project inactivity.

## Watch-outs & Tech Debt

- **IP_HASH_SECRET unset on a Redis-configured deploy** — rate-limiter falls open (warn at module load: `[api:auth:login] IP_HASH_SECRET not configured, rate limiting disabled`). Trigger: if `getRedisConfig()` returns non-null while `getIpHashSecret()` returns null. Visible in Vercel function logs.
- **Vercel Hobby cron auto-pause** — crons can be paused after project inactivity even with the schedule in place. Trigger to revisit: Upstash idle warnings reappear or `keepalive:last` stops updating despite the daily schedule.
- **`keepalive:last` has no TTL** — intentional (overwritten daily, no PII), but if the cron stops running the stale timestamp lingers. Trigger: a future "is the cron alive" alert should compare `keepalive:last` ISO against now, not check key existence.

## Session 1

### Decisions

- **Cron schedule → daily (`0 0 * * *`)** — chose daily over every-5-day (`0 0 */5 * *`). Rationale: removes future risk if Upstash tightens its idle threshold; cost is one trivial SET/day. Previous `0 0 1,11,21 * *` had 10-day gaps that exceed Upstash's ~7-day free-tier idle window — the root cause of recurring inactivity emails. History: started at `1,15` (15-day), tightened to `1,11,21` (10-day), now daily.
- **HMAC-pseudonymize IPs (not plain SHA-256)** — chose `HMAC-SHA256(IP_HASH_SECRET, ip)` truncated to 22 base64url chars. Plain SHA-256 is one-way mathematically but reversible in practice: IPv4 keyspace is ~4B, brute-forceable on a laptop in minutes. HMAC with a server-held secret blocks the rainbow-table shortcut.
- **Fail-open when `IP_HASH_SECRET` is missing on a Redis-configured deploy** — chose to disable rate-limiting (with warn at module load) over falling back to plain-IP keys. Rationale: regressing to plain IP would silently undo the GDPR posture improvement; admin lockout from a config gap is the only other failure mode and matches the existing no-Redis fail-open pattern.
- **No env var for `KEEPALIVE_KEY`** — chose hardcoded constant in `src/lib/keepalive.ts`. Rationale: zero operational value to making it configurable; an env-driven key is a future foot-gun ("which env had the old key?"). Centralized so cron and admin endpoints can't drift.
- **Manual admin trigger as a parallel endpoint, not bearer-shared cron** — chose `POST /api/admin/keepalive` (session-gated by `proxy.ts`) over expanding the cron's auth check to accept sessions. Rationale: keeps each route's auth concern single-purpose; KEEPALIVE_KEY is the only shared surface.
- **`prefetch={false}` only on per-row edit links** — kept default prefetch on `New post`/`New project` (cheap blank-form RSC) and tab links. Rationale: per-row edit-link prefetches were the actual DB-hit source on dashboard load; top-of-nav links don't query.

### Tradeoffs

- **Daily cron vs sparser schedule** — gained robustness to future Upstash policy shifts, accepted ~30 extra SETs/month (negligible); reversibility: easy (`vercel.json` one-liner).
- **Prefetch off on edit links** — gained: dashboard load no longer triggers N parallel RSC fetches with N Prisma queries; accepted: brief loading state on click instead of warm cache; reversibility: easy.
- **Helper extraction for keepalive** — only the constant moved to `src/lib/keepalive.ts`, not a full `runKeepalive()` function. Each route keeps its own Redis client + try/catch (5-line block). Avoids premature abstraction at the cost of allowing the two SET sites to drift on logging/error-shape; the constant is the only thing that must not desync. Reversibility: easy.
- **HMAC truncation to 22 chars (132 bits)** — kept full collision resistance for bucket purposes (132 bits is plenty for ~1 admin's worth of IPs); accepted: shorter audit-log strings; reversibility: trivial (change `.slice(22)` value).

### Incidents / Bugs

- **Upstash idle warnings persisted after two schedule tightenings** — root cause: `0 0 1,11,21 * *` has 10-11 day gaps; Upstash free-tier idle threshold is ~7 days. Fix: schedule → daily. Regression risk: zero — if Upstash tightens further, daily still works.
- **Admin dashboard fires N background RSC requests on every load** — root cause: Next.js App Router `<Link>` prefetches on viewport entry; PostsTab and ProjectsTab render plain `<Link>` for each row's edit, each prefetch runs the edit page's server component which hits Prisma. Fix: `prefetch={false}` on the per-row edit `<Link>`s. Regression risk: negligible — click-to-edit path is unchanged, only the prefetch warming is gone.

### Environment / Config Changes

- `vercel.json`: cron schedule `0 0 1,11,21 * *` → `0 0 * * *`.
- New env var: `IP_HASH_SECRET` (HMAC secret for IP pseudonymization). Generate with `openssl rand -base64 32`. Required at Production for rate-limiting to be on; route fails open with a warn line when absent.
- New file: `src/lib/keepalive.ts` — exports `KEEPALIVE_KEY = "keepalive:last"`.
- New route: `POST /api/admin/keepalive` — session-gated by `proxy.ts` (matches `/api/admin/*`), performs same SET as cron, returns `{ ok, value }`.
- New UI: "Run keepalive" button in `AdminNav` (between "New project" and "Logout"), with success line `Keepalive OK — wrote <iso>` rendered below the nav.

### What did I, Claude Code, or my agents learn from this

- **Vercel cron is independent of app traffic** — serverless functions don't have a "sleep" state like Heroku free dynos; the cron scheduler invokes the endpoint on a cold function regardless of traffic. But Hobby crons themselves can be auto-paused after project inactivity — distinct failure mode from Upstash idle detection.
- **Upstash idle detector excludes `PING`** — already documented in `route.ts` comment, but worth surfacing: any keepalive must use a real data command. SET of an observable key (`keepalive:last`) doubles as a "last successful run" marker visible in the data browser.
- **`@upstash/ratelimit` handles TTL automatically** — `PEXPIRE currentKey, window*2+1000` in [`node_modules/@upstash/ratelimit/dist/index.mjs:246`](node_modules/@upstash/ratelimit/dist/index.mjs:246). For `slidingWindow(5, "15 m")`, each bucket key gets ~30min TTL. No manual purge needed; GDPR retention is library-default.
- **IPv4 plain-hash reversibility** — keyspace (~4B) is small enough that `SHA-256(ip)` is brute-forceable in minutes on a laptop. HMAC with a server-held secret is the cheap fix; the secret doesn't enable reversal (HMAC is one-way), it blocks rainbow-table precomputation.
- **GDPR posture for rate-limiting IPs** — IP IS personal data (CJEU *Breyer* C-582/14). Rate-limiting falls under legitimate interest (Art 6(1)(f)). Short library-default TTL + HMAC pseudonymization + privacy-policy mention covers the four pillars (purpose, retention, transparency, minimization).
- **App Router `<Link>` prefetches on viewport entry, not just hover** — a list of N edit links on a dashboard fires N background RSC fetches, each running its target page's server code. `prefetch={false}` per-link is the lever; there's no global "no prefetch on list pages" setting.

### Notes

- IP_HASH_SECRET schema in `src/lib/env.ts`: optional string, accessed via `getIpHashSecret(): string | null`. Mirrors `getCronSecret` shape.
- The rate-limit gate at `src/app/api/auth/login/route.ts:42` requires **both** Redis and secret — `if (ratelimit && ipHashSecret)`. Module-load warn ensures missing secret is discoverable.
- `clientBucketKey(request, secret)` takes the secret as parameter (not a module closure) so TS narrowing works at call sites without assertion gymnastics.
- Invalid-credentials log omits the `key` field entirely when no secret is configured (rather than logging `null` or plain IP) — kept PII out of the audit log for the no-secret deploy path. Test at `src/app/api/auth/login/route.test.ts` asserts the empty-object shape.
- New endpoint emits `console.info("[api:admin:keepalive] success", { value })` — distinguishes manual triggers from scheduled cron in Vercel logs (cron route doesn't emit a success line today; if it ever does, both lines together answer "who wrote this timestamp").
- Code review for today (`code-reviews/2026-05-14.md`) was generated from the morning's Phases 1-7 batch; none of its findings overlap with this session's work.

<!-- end -->
