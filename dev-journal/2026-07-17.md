# Dev Journal – 2026-07-17

## Open Threads

- [x] Implement the guides section per `dev-journal/_guides-plan.md` (fresh session; phases 1–6, commit per phase). **Resolved 2026-07-17 (Session 2)**: all six phases built on the `guides` branch, plus filename-derived `publishedAt`, read-time scheduling, and the prose-disclosure model.
- [ ] Run `yarn db:push` to create the `Guide` + `GuideTopic` tables — nothing works until then. Then `yarn db:import-guides ../blog/guides --dry-run`, review, and import for real.
- [ ] Resolve the Reckon price mismatch: `scripts/imports/reckon/project.json` `offers` says `4.00`, but the stated price is `3.99` (guides no longer carry a number). Fix the manifest before the next `yarn db:import-projects`.
- [ ] Commit the guides content in the blog repo (`blog/guides/making-better-decisions/` — hub `index.md` + four dated guides) as part of that repo's in-flight posts/guides migration.
- [ ] Re-run `yarn db:import-projects` to apply the manifest paragraphing — confirm no admin-UI edits landed since the last import first (importer replaces projects wholesale, so it clobbers them).
- [x] Create the global `write-guide` skill (via `skill-creator`) after the first four Reckon guides ship — it needs shipped exemplars, like `write-blog-post` has. **Resolved 2026-07-17 (Session 2)**: written at `~/.claude/skills/write-guide/`, mirroring `write-blog-post`; the four shipped guides are its exemplars.
- [ ] Teach `app-copy-to-project` (global skill) to author section descriptions as multi-paragraph (`\n\n`) so future manifests don't regress to walls of text.

## Watch-outs & Tech Debt

- **FKs to `Project` are a trap** — `import-projects.ts` replaces a project wholesale (`deleteMany` + `create` in a transaction), so any model referencing `Project` by FK either cascade-deletes on re-import or blocks the importer. Guides deliberately reference by `projectSlug` string. Revisit if the project importer ever moves to upsert-in-place.
- **Guide `publishedAt` has two write paths** — the importer reads it from the filename date and re-syncs on every `--overwrite`; the admin stamps it once on first publish and never rewrites (`resolvePublishedAt`). Trigger to revisit: if a guide's date ever looks wrong, check which path last wrote it. The importer wins on re-import, so a filename rename silently moves the date.
- **Guides don't infer `published` from date, and null `publishedAt` = live** — unlike posts (past→draft, future→published), an imported guide is always `published: true`; a future date only *schedules* it via the read-time `isScheduledGuide` filter. A null `publishedAt` is treated as live, never scheduled — deliberate, so a dateless admin row can't vanish. Watch when adding admin-created guides that skip the filename path.
- **`offers` is JSON-LD-only, not rendered** — a project's price appears in `SoftwareApplication` structured data and (for Reckon) a pricing FAQ, but nothing renders `offers` visually on the project page. Don't assume adding/editing `offers` changes the visible page.

## Session 1

### Decisions

- **Guides live at top-level `/guides/:slug`, not `/blog/guides/:slug`** — blog is a dated, subscribed, frozen-after-publish stream (sitemap marks posts `changeFrequency: "never"`); guides are maintained SEO pages iterated from Search Console data. Different lifecycle → different route; `SECTIONS` stays `["tech", "life"]`.
- **Section named "Guides"** (rejected: articles, notes, writing, misc, freeform, longform, learning-hub, library) — the path token is a one-word promise to search/social clickers; elasticity trades against promise. Only the token is permanent; display labels can be renamed anytime.
- **New `Guide` entity over `Post` reuse** — Post carries legacy guides shouldn't inherit (string `datetime`, section, feed/archive/search, frozen lifecycle); reuse would add a "not guides" filter to every post query.
- **`GuideTopic` grouping now, optional per guide** — hub-and-spoke SEO cluster; the hub carries the customizable copy shown on project pages. First topic: "Making better decisions" (*making*, not *taking* — matches search phrasing).
- **Project reference by slug, not FK; nullable** — see Watch-outs for the FK trap; nullable so future non-product pieces fit (no CTA, no project page, still indexed).
- **All-markdown authoring, no JSON manifest** — guides/topics are prose plus flat scalars (projects needed JSON for structured non-prose data). Importer mirrors the posts script (plan-based, idempotent, `--dry-run`/`--overwrite`), not the projects one.
- **Frontmatter: explicit `slug:` required, `published` DB-only, hand-rolled parser extended** (no YAML dependency) — slugs are SEO-load-bearing so chosen, not derived; `drafts/` folder is the draft mechanism; strict importer validation (unknown keys, duplicate keys, non-int `sortOrder`) closes the parser's silent failure modes. Filename date prefix kept for disk sorting only — importer ignores filenames.
- **Project-page guides block below content, above FAQ** (mirrors `ProjectFaq`), auto-associated — not in the hero `links` pill row (accent CTAs, `target="_blank"`; wrong register). Manifests stay marketing-copy-only.

### Incidents / Bugs

- **Cited Linear as filing essays under "guides" — wrong** — verification showed Linear uses "Method" for that content; Stripe's guides do hold essays/reports. Corrected in place; decision unaffected but the claim shrank from two examples to one.

### What did I, Claude Code, or my agents learn from this

- **Check importer write semantics before designing relations** — the delete-then-create in `import-projects.ts` would have silently nuked guides via a cascade FK; caught only by reading the script before finalizing the schema.
- **Project manifests support markdown paragraphs end-to-end** — the wall-of-text descriptions were an authoring artifact of `app-copy-to-project`, not a pipeline limit; `\n\n` in the JSON renders as real paragraphs.
- **The frontmatter parser's real restrictions** — single-line values, block anchored to byte zero, symmetric-quote stripping; the dangerous modes with many fields (typo'd optional keys, duplicate keys) are validation gaps to close in the importer, not parser bugs.
- **Verify examples before citing them** — the Linear claim was pattern-matched, not known; the user's "Really?" was the only thing that caught it.

### Notes

- The full guides design (schema, frontmatter spec, importer semantics, SEO checklist, phases) is in `dev-journal/_guides-plan.md` — build-ready for the implementation session.
- Paragraphed both `scripts/imports/*/project.json` section descriptions (8 `\n\n` splits, wording untouched — the copy already passed validation). Files are gitignored; the site only changes after re-import.
- Today's code-review file (`code-reviews/2026-07-17.md`) concerns post-ingestion scripts untouched this session — no items resolved.

## Session 2

Built the whole guides section end to end on the `guides` branch (10 commits, 1729 tests). Six planned phases plus three refinement passes: filename-derived dates, the prose-disclosure model, and the disclosure standardization + topic-hub split.

### Decisions

- **Topic membership by directory, not a `topic:` key** — a `guides/<topic>/index.md` declares the topic (its own `slug:` authoritative, directory name decorative); every other file in the folder is a guide in it, and guides carry no `topic:` key. Chose this over the plan's flat `topics/` + `topic:` frontmatter because directory names *are* valid topic slugs and it makes membership single-source. `index.md` (not `_topic.md`) since date-prefixed siblings sort it last anyway.
- **`publishedAt` from the filename date, like posts** — reversed the plan's "stamp at import time." Stamping used the wall clock, so a re-import into a fresh DB re-aged every guide. A date has none of the `:`/`/`/accent problems that keep titles in frontmatter, so the filename owns it (chose authored-date over import-time; the admin path still stamps-once since it has no file).
- **UTC date parse, separate from `postDatetimeToISO`** — new `datetimeToUtcDate`; the post helper builds a *local* Date, which is fine for a stored string formatted locally on both sides but wrong for a real `DateTime` column (a local `2026-07-13-0000` in UTC+2 persists as the 12th). Guide date pipeline is UTC end to end (`formatDateValue` also UTC-pinned).
- **Scheduling guard, read-time** — future `publishedAt` = scheduled; filtered on cached rows at read time (never in-query, never inside the cached fn) so a guide auto-surfaces the first request after its date, no cron. Null `publishedAt` is never scheduled (safe reading of a missing date is *live*).
- **Product link is authored prose, not a card** — deleted the `GuideProjectCta` component. A rendered "RELATED PROJECT" card never says *who made the thing*, so it can't be a disclosure; in-content prose is also the stronger internal link. Rejected a `disclosure` DB/frontmatter field too — the four disclosures are all different and contextual, so there's nothing to share. Link target is `/projects/<slug>` (indexable, passes equity), never the App Store.
- **Importer warns when a project is named but never linked** — recovers the "always present" guarantee the card gave, without a widget: `parseGuideFiles` warns (never skips) when a guide/hub declares `project:` but its body has no `/projects/<slug>` link.
- **No exact price in guide bodies** — dropped "$3.99" from all four + hub; kept "one-time, no subscription" (durable objection-remover), led disclosures with the *mechanism* (does the scoring so the habit sticks). Keeps four SEO pages evergreen; price lives in the project `offers` JSON-LD + a pricing FAQ.
- **Topic-hub disclosure below the guide list via a body split** — `splitTopicHubBody` splits the hub body on its *last* `---`: framing renders above the list, disclosure below. The `---` is both split point and visual separator; keeps the disclosure authored prose and out of the mid-page "in your face" slot.
- **Built `write-guide` now, not deferred** — the plan said defer until exemplars exist; they now do, so it shipped this session.

### Tradeoffs

- **In-memory grouping in `getGuidesOverview`** — one cached payload, grouped in JS; `simplified:` with a ceiling noted (~hundreds of guides) before splitting into scoped queries. Reversibility: easy.
- **Prose disclosure over a field** — loses the guaranteed-present card, bought back by the importer warning. Accepted because per-page contextual copy has no reuse to DRY.

### Incidents / Bugs

- **Duplicate `offers` JSON key in the Reckon manifest** — added an `offers` block not realizing one already existed at line 86; JSON's last-key-wins masked it. Caught by parsing the manifest and seeing `price: 4.00` (mine was `3.99`). Reverted; manifest untouched. Surfaced a real data mismatch (see Notes).

### Environment / Config Changes

- New Prisma models `Guide` + `GuideTopic` in `prisma/schema.prisma`; client regenerated. **Not pushed** — `yarn db:push` is an open thread.
- New `yarn db:import-guides` script + `src/lib/import/guideImport.ts` core.

### What did I, Claude Code, or my agents learn from this

- **The `rg` auto-allow hook is scoped to the project tree** — grepping `~/.claude/` (e.g. the skill file) is rejected; use `Read` there. Also: chaining `rg <log>` after a `&&` in a Bash line, or any `cat`, trips the grammar — run them as separate calls.
- **`postDatetimeToISO` builds a *local* Date** — fine for the string-datetime posts, a latent day-shift bug for anything writing a real `DateTime`. Reach for a UTC constructor when the value lands in a DB column, not the nearest-looking existing helper.
- **A guarantee a component gave can move to the importer** — killing the CTA card lost "a link is always present"; a parse-time warning recovered it without re-adding UI. Deleting chrome doesn't have to mean dropping the invariant it enforced.

### Notes

- **Reckon price mismatch to resolve**: manifest `offers` = `4.00 USD`; user states `3.99`. Guides no longer carry a number, so the figure now lives only in the manifest + App Store. Fix `scripts/imports/reckon/project.json` before the next project import (open thread).
- The four guides + hub parse clean (0 skipped, 0 warnings) and the hub splits correctly (verified via a throwaway parse script, since the DB isn't reachable locally — `db:push` pending). Couldn't render the actual pages; worth eyeballing the hub's rule/spacing after import.
- Content lives in the *blog* repo (`blog/guides/`), which is mid-migration (200+ staged post deletions, `guides/`+`posts/` untracked). Left its git alone; the guide edits are uncommitted there for the user to fold in (open thread).

<!-- end -->
