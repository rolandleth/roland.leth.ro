# Dev Journal – 2026-05-12

## Open Threads

## Watch-outs & Tech Debt

- **Durable audit sink — DO NOT PROPOSE.** The `auditLog` `console.info` sink with ~1h Vercel Hobby retention is the deliberate trade. A durable persistence layer (DB table, external aggregator, Blob append) has been pitched in past code reviews and surfaces again whenever the audit log comes up. Same call as the `error.digest` defer on 2026-05-09 — re-introduce paired with external aggregation, not before. Future code reviews and agents should not re-flag this; if the question comes up, the answer is "no, by design." Revisit only if multi-admin or compliance/regulatory needs land.

## Session

Followed the 2026-05-12 daily code review with a 7-phase implementation pass (correctness, a11y, doc/regex, `readErrorMessage` extraction to `lib/`, audit-tag typing + `previousSlug` semantics unification, observability, tests). Then a deferred-items pass: Post `slug` index, posts PUT escalated to Serializable, `useOptimisticMutation` hook extraction. Audit-sink durability was deferred indefinitely.
