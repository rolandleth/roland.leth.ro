# Rolling History — last 90 days (through 2026-08-31)

## Themes

- **Admin auth: from single gate to defense in depth** — The quarter's most persistent thread. 2026-07-20 flagged (and deliberately deferred) that Next's middleware was the sole authorization boundary for all 12 admin routes; 2026-07-26 converted the watch-out into a real incident when the matcher's extension-exclusion regex turned out unanchored (any path merely *containing* an excluded extension's substring, e.g. `.json` containing `.js`, skipped the gate) — fixed with `requireAdmin()` added to all 21 handlers plus a 32-char floor on `SESSION_SECRET`. August reopened the same class from the page-render side: 2026-08-12 closed an unauthenticated DB read in edit-page `generateMetadata` via a new `adminEditMetadata` helper; 2026-08-21 found — via an external review — that the helper guarded only the `<title>`, not the page body, on the dashboard root and four edit pages, and extended a real session guard to all six; 2026-08-22 then found the contract test proving that guard couldn't itself fail (it string-matched the guard call, which an unawaited/floating-promise call also satisfies). Every fix narrowed the gap; every follow-up review found the fix's own blind spot. [source](dev-journal/2026-07.md) [source](dev-journal/2026-08-12.md) [source](dev-journal/2026-08-21.md) [source](dev-journal/2026-08-22.md)
- **Project import tooling and JSON-LD/structured-data hardening** — June built the manifest-driven importer (`scripts/import-projects.ts` + per-project `project.json` + the `app-copy-to-project` skill) for Reckon and Continuum, over parsing raw marketing markdown or driving the admin HTTP API; direct Prisma + direct Blob writes with deterministic, later content-addressed, blob keys made re-imports idempotent. The same window built a full project FAQ feature and SEO/JSON-LD layer (offers, applicationCategory, FAQPage/SoftwareApplication), which then needed its own security pass: 2026-06-29 built `safeJsonLdString` escaping, closed a multi-currency `AggregateOffer` mislabeling, and by session 7 consolidated every JSON-LD embed through one `<JsonLdScript>` chokepoint with a lint rule banning `dangerouslySetInnerHTML` everywhere else. August's OG-card rework (see below) extended the same "make the structured data honest" instinct to social cards. [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-08-13.md)
- **Static rendering push, cache precision, and the soft-404 incident** — 2026-07-07 split coarse `blog-{section}`/`projects` cache tags into per-slug detail tags plus `*-pages` aggregates (fixing 8 unnecessary ISR regenerations per single edit) and cut Vercel CPU by removing `cookies()`/`headers()` dynamic-rendering forcers. 2026-08-15 pushed further: legacy redirects/rewrites moved into `next.config.ts`'s routing layer, the middleware matcher narrowed to admin-only, blog pagination became path-based, and hourly `revalidate=3600` busts were replaced by a due-check cron. 2026-08-17 found the static push's blind spot: a `loading.tsx` Suspense boundary lets React commit a 200 and stream before `notFound()` can run, so three routes were silently serving soft 404s in production — fixed by route-grouping list skeletons, deleting detail-page ones, and having the cron bust per-item detail tags instead of only list tags. [source](dev-journal/2026-07.md) [source](dev-journal/2026-08-15.md) [source](dev-journal/2026-08-17.md)
- **Reporting honesty: "success" that didn't reflect what was verified** — A recurring July bug class, three independent instances of the same shape. The revalidate endpoint reported requested slugs as "revalidated" without checking they matched a real row, and discarded partial progress on a mid-batch throw; the identical defect reappeared independently in IndexNow's submit-count reporting (`"submitted: N"` even when every batch 403'd), closed by splitting `attempted`/`accepted`. [source](dev-journal/2026-07.md)
- **Gallery/lightbox gesture engineering** — 2026-06-26 misdiagnosed a lightbox "corner halo" twice (backdrop opacity, then `backdrop-filter` fringe) before finding the real cause: galleries never rounded the visible, letterboxed image, only its container — fixed via aspect-true `next/image` sizing. 2026-06-29 built swipe/drag navigation via Framer, then removed all navigation animations per user direction, then added back/forward animation suppression through the Navigation API's `currententrychange` event (rejecting a `history.pushState` monkey-patch). July's continuous cross-section gallery repeated the pattern: a swipe-vs-tap suppression flag set inside framer-motion's `onDragEnd` fired a frame too late (`frame.postRender`, after the browser's own click), fixed by moving to raw `pointerdown`/`pointerup` capture; the same session traced an "accelerated" cross-section swipe to a React `key` remounting the whole carousel and resetting its `MotionValue`s. [source](dev-journal/2026-06-26.md) [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07.md)
- **Test-quality discipline: verify a guard can actually fail** — The quarter's clearest cross-cutting learning, recurring in June's JSON-LD work, July's slug-tooling review, and repeatedly in August (source-text contract tests, vacuous JSX-construction assertions, the floating-promise-blind contract test). The standing fix is always the same: deliberately break the thing a new test claims to guard and confirm it goes red before trusting it. [source](dev-journal/2026-07.md) [source](dev-journal/2026-08-21.md) [source](dev-journal/2026-08-22.md)
- **Sandbox/local-verification ceiling** — A constant environmental constraint all quarter: the sandbox can't reach Google Fonts (`next build` fails at font optimization), can't bind a listening socket (`tsx`, local Postgres auth), and can't push over SSH. The response stayed consistent — verify via `tsc` + full suite + lint on touched files, treat build/browser verification as deploy-time or preview-panel concerns — and 2026-07-24 partly closed the gap by adding `.claude/launch.json` so the integrated preview panel can launch a dev server the sandbox can't bind directly. [source](dev-journal/2026-07.md) [source](dev-journal/2026-08-15.md)
- **Legal/privacy page accuracy** — 2026-06-08 lifted repeated privacy boilerplate ("Last update"/"Contact") into a shared `PrivacyPageLayout` with a `contactPronoun` prop, and established that legal-page `lastUpdated` dates track substantive text changes only, never presentation refactors. 2026-08-21 found and fixed a real accuracy gap this convention hadn't caught: the root privacy page's blanket "I do not collect personally identifiable information" claim was false once the login rate limiter's IP processing was accounted for; narrowed the claim rather than adding disclosure paragraphs around it. [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-08-21.md)

## Decisions of note

- **2026-06-08 — Manifest-driven project importer** — built `scripts/import-projects.ts` + per-project `project.json` + the `app-copy-to-project` skill, over parsing raw marketing markdown (fragile, no stable schema) or driving the admin HTTP API (needs a running server + auth, wrong shape for a controlled import). [source](dev-journal/2026-06-08.md)
- **2026-06-13 — Content-addressed blob keys widened 48-bit → 64-bit with a byte-size assert** — the dedupe contract is "same key ⇒ same bytes"; a collision would silently serve wrong content. Paired with prune-after-import to stop monotonic growth on the 1GB Blob free tier. [source](dev-journal/2026-06-13.md)
- **2026-06-29 (session 7) — Single `<JsonLdScript>` chokepoint for all JSON-LD embeds** — replaced three hand-written `<script>` blocks; lint rule switched from banning `JSON.stringify` inside the attribute to banning the `dangerouslySetInnerHTML` attribute itself everywhere else, closing an aliased-serialization gap. [source](dev-journal/2026-06-29.md)
- **2026-07-07 — Per-slug cache tags replace coarse section tags** — `blog-{section}`/`projects` split into detail tags plus `*-pages` aggregates; added an admin RevalidatePanel since import-script writes bypass Next's cache API entirely. [source](dev-journal/2026-07.md)
- **2026-07-17 — Guides feature built end-to-end in one day**, including a `CacheMissError`-throw-on-miss pattern across all four detail lookups after a prod stale-404 traced to `unstable_cache` durably caching `null`. [source](dev-journal/2026-07.md)
- **2026-07-26 — `requireAdmin()` added as a second lock to all 21 admin handlers**, plus a 32-char floor on `SESSION_SECRET` in the accessor (not the zod schema, to avoid taking every DB-backed page down over a session-only problem). [source](dev-journal/2026-07.md)
- **2026-08-15 — Legacy routing and scheduled-content revalidation moved off middleware and off unconditional hourly busts** — `next.config.ts` redirects/rewrites resolve with zero function invocations; a due-check cron replaces `revalidate=3600` on the feed, archive, and sitemap. [source](dev-journal/2026-08-15.md)
- **2026-08-21 — `requireAdminPageSession` extended to all six data-reading admin pages** after a review found `adminEditMetadata` guarded only the page title, not the body render. [source](dev-journal/2026-08-21.md)

## Incidents of note

- **2026-06-26 — Lightbox "corner halo" misdiagnosed twice** (backdrop opacity, then `backdrop-filter` fringe) before finding the real cause: galleries never rounded the visible, letterboxed image. [source](dev-journal/2026-06-26.md)
- **2026-07-08 — Swipe-vs-tap fix from the prior day didn't hold** — framer-motion's `onDragEnd` fires a frame after the browser's click (`frame.postRender`), so a flag set inside it can't suppress the click it targets; fixed via raw pointer capture. [source](dev-journal/2026-07.md)
- **2026-07-17 — Prod stale-404**: a published, listed post 404'd on its own detail page because `unstable_cache` had durably cached `null` from before the row existed. Fixed with throw-on-miss across all four detail lookups. [source](dev-journal/2026-07.md)
- **2026-07-26 — Admin auth gate skippable via a dotted path** — the matcher's extension-exclusion was unanchored (`.json` contains `.js`); not exploitable in practice since admin `[id]` routes reject non-integer segments first, but fixed with a second lock regardless. [source](dev-journal/2026-07.md)
- **2026-08-13 — Two Satori/OG-card bugs**: 8-digit hex colors rendered near-solid instead of fading (Satori doesn't parse `#rrggbbaa`), and the route exported `size`/`contentType`, valid for the file-convention form but not a route handler — caught only by reading, since the build-time validation that would flag it runs during `next build`, not `tsc`. [source](dev-journal/2026-08-13.md)
- **2026-08-17 — Soft 404s live in production across three routes** — a `loading.tsx` Suspense boundary let React commit a 200 and stream the shell before `notFound()` could set the status; regression risk rated high and invisible, hence a filesystem-level guard test. [source](dev-journal/2026-08-17.md)
- **2026-08-21 — Second-layer admin defense gap found mid-session by an external review**: `adminEditMetadata` guarded only the page `<title>`, not the body, on the dashboard root and four edit pages. [source](dev-journal/2026-08-21.md)
- **2026-08-24 — Reference-style markdown links rendered as literal brackets in 11 tech-post blog-list excerpts** — truncation cut before the link-reference definitions authored at the post's end; fixed by appending extracted definitions to the truncated preview. [source](dev-journal/2026-08-24.md)

## Timeline

- **2026-06-08** — Reckon and Continuum imported as live portfolio projects via the new importer; Continuum's accent color taken from its light-mode `AccentColor`. [source](dev-journal/2026-06-08.md)
- **2026-06-08** — Blob-reuse-by-key+size added to the importer to avoid redundant uploads when testing a local run before a prod run against the same Blob store. [source](dev-journal/2026-06-08.md)
- **2026-06-10** — Login rate limiter degrades to a global bucket (not fail-open) when `IP_HASH_SECRET` is unset, accepting a bounded admin-lockout DoS over no limiting at all. [source](dev-journal/2026-06-10.md)
- **2026-06-13** — Featured project cards stretch to equal height via a `flex h-full flex-col` chain rather than a fixed height. [source](dev-journal/2026-06-13.md)
- **2026-06-28** — Added `cardImage`/`ogImage` project fields and a full FAQ feature rendered as an always-mounted, `inert`-collapsed accordion so answers stay crawlable and accessible at once. [source](dev-journal/2026-06-28.md)
- **2026-06-29** — Added `robots.ts` and a DB-backed `llms.txt` route (project list can't be a static file without drifting), plus `BlogPosting` JSON-LD for blog posts, closing the gap where structured data had been projects-only. [source](dev-journal/2026-06-29.md)
- **2026-08-12** — Fixed a project hero row overflowing 375pt viewports by stacking below the `sm` breakpoint. [source](dev-journal/2026-08-12.md)
- **2026-08-13** — Discontinued projects keep `schema:Discontinued` on JSON-LD offer nodes instead of dropping `offers` outright, preserving historical pricing without asserting current availability. [source](dev-journal/2026-08-13.md)
- **2026-08-21** — Rewrote the privacy page's PII claim to a narrower true statement; declined rotating `IP_HASH_SECRET` as ineffective given Vercel's per-deployment env snapshotting; added global reduced-motion support via Framer's `MotionConfig` plus `jsx-a11y` lint rules. [source](dev-journal/2026-08-21.md)
- **2026-08-22** — Closed a JSON-safety hole in `errorDetails` introduced by the prior day's own remediation (spreading every own-enumerable Error property could throw on a non-serializable field). [source](dev-journal/2026-08-22.md)
- **2026-08-24** — Made blog-excerpt truncation code-block-aware so a cut can no longer land inside an unclosed fence. [source](dev-journal/2026-08-24.md)

<!-- end -->
