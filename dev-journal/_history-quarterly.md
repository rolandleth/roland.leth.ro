# Rolling History — last 90 days (through 2026-05-15)

## Themes

- **The `/simplify` sweep arc (2026-04-16 → 2026-04-17)** — 2026-04-16 opened with targeted code-health wins: `projectInclude` lib extraction, `capitalizeSection` helper, `AnimatedCard` shared component, `searchPosts` pushed to Prisma `OR` filter, `MarkdownEditor` via shared `markdownToReact`, `SectionManager` stable `_key: crypto.randomUUID()`, error surfacing on `IsFeaturedToggle` / `ProjectSortOrderInput`, `ProjectForm` `isDeleting` gate, `LoanCalculatorClient` `parseNum`. 2026-04-17 Session 1 went wide: 6 parallel agents, 10 commits, 96 files. Session 2 absorbed the review: Prisma `Section` enum replacing `as Section` casts (propagated through `PostListItem` / `PostDetail` / `PostArchiveItem` / `PostSearchResult`), `PresetOrFreeformInput` derived-mode fix (eliminated mirror-state bug), `MarkdownEditor` dropped redundant `setTimeout` and gained real `.catch`, schema `.max()` bounds on title/body/summary/name. Shared test infra: `src/test/fixtures.ts` (`makePost`, `makeProjectListItem`, `TEST_SECRET`) and `src/test/mocks/nextCache.ts` (identity-passthrough `unstable_cache`, `revalidateTag` spy). [source](dev-journal/2026-04.md)

- **Legacy-redirect architecture rewrite (2026-04-17 Session 3)** — the `/api/legacy-redirect/[slug]` route + middleware rewrite were replaced by a root-level catch-all page (`src/app/[slug]/page.tsx`) using `permanentRedirect(308)` on hit, `notFound()` on miss. Root cause: API route's `notFound()` returns raw JSON, not the styled 404 page; the redirect looked like a UX regression to visitors hitting broken backlinks. Side effects: `KNOWN_ROUTES` allowlist dissolved as a class-of-bug (static-over-dynamic App Router dispatch means no allowlist to maintain), `proxy.ts` cognitive-complexity lint warning dropped back under 15. [source](dev-journal/2026-04.md)

- **Second review pass → targeted fixes (2026-04-17 Session 4)** — `ProjectSortOrderInput` 1-indexed UI over 0-indexed DB (`419595f`): rendered raw `sortOrder` clamped to `[1, totalCount]` caused typing `0` + tab to silently swap top project. Translate at component boundary: `initialSortOrder + 1` on render, `-1` before PUT. Defence-in-depth: Zod `.min(0)` across project/section/image/link schemas; `ProjectForm` input `min={0}`. `defaultProto` port-heuristic removed (`5994371`) — `example.com:8443` was silently getting `http` (fix: only `localhost`/`127.0.0.1` prefix triggers downgrade). Atom feed `<updated>` uses `Math.max` across entry `updatedAt` (`0b0ebbe`) per RFC 4287 intent. `navLinks` hoisted to `src/lib/navigation.ts` with `as const` (`245869b`). `revalidatePostSection` test asserts `"posts"` tag + `toHaveBeenCalledTimes(3)` (`60b5a3c`) so removing the sitemap-busting tag breaks the suite. Project `sortOrder` single-writer invariant documented inline on PUT `$transaction` (`2c2199e`) rather than `@@unique` constraint (would need `DEFERRABLE INITIALLY DEFERRED`, unexpressible in Prisma DSL) or `SERIALIZABLE` (escalation triggered only when a second admin account is added). [source](dev-journal/2026-04.md)

- **`unstable_cache` discipline (2026-04-20)** — production 500 on `/api/feed/:section` (`Cannot read properties of undefined (reading 'getTime')`) surfaced the core bug: `unstable_cache` JSON-serializes `DateTime` → string on cache hits, so `post.updatedAt.getTime()` explodes only after the first revalidation. Classic "works locally, 500s in prod after first revalidation" trap. Fix: normalize to ISO inside the cached function so handler contract becomes "strings in, strings out". Same investigation found three call sites rebuilding the wrapper per call (Next.js logs each as "new cache registered" with the function body as the identifier). Resolutions differ per site: `lookupLegacySlug` lifted to module scope (dropped dead `legacy-redirect-${slug}` tag after grep-confirming no `revalidateTag` call sites existed); `getPostBySlug` / `getProjectBySlug` use `Map<slug, wrapper>` memoization because the per-slug tags in `projects.ts` **are** revalidated by admin routes on edit/delete. [source](dev-journal/2026-04.md)

- **Atom feed content quality (2026-04-20 → 2026-04-21)** — `excerptFromBody` emitted raw markdown in `<summary>`. Chose AST walk (`unified` + `remark-parse` + `remark-gfm`, all already in the dep graph) over regex (fragile for nested markdown) or `strip-markdown` package (new dependency). Block nodes (`paragraph`, `heading`, `blockquote`, `listItem`) get trailing `\n` so siblings don't run together; fenced code blocks skipped entirely; inline code kept. 04-21 follow-up extracted image/imageReference alt text via `Nodes` typed import (replaced local `MdastNode` shim + double-cast); added `escapeCdata` for `]]>` terminator collision. Key insight: `unified().parse()` is synchronous, so `stripMarkdown` doesn't need to be async. [source](dev-journal/2026-04.md)

- **Security & input hardening arc (2026-04-26 → 2026-05-14)** — Quarter-spanning theme. April 26 batch: hex-format Zod check on `ADMIN_HASH_PASSWORD` only (not `SESSION_SECRET` — `TextEncoder().encode()`, not `Buffer.from(..., "hex")`); typed `EnvConfigError` with `code`/`varName` fields; module-load dummy `bcrypt.hashSync` for timing-defense in no-credentials path (~80ms cold start); Redis config centralized through `getRedisConfig()` (not `Redis.fromEnv()` — single source of truth for env-var names); `crypto.timingSafeEqual` via `isAuthorized` helper for cron Bearer; login success `console.info` audit log added. Per-call `safeParse` deliberately deferred — memoization would break `vi.stubEnv` testability contract. [source](dev-journal/2026-04.md). May 7 added per-IP login rate-limit (`clientBucketKey` keyed on leftmost `x-forwarded-for`); fail-open on Upstash error so a Redis blip doesn't lock the only public auth entry; `ALLOW_UPLOADS === "true"` env flag replaced `NODE_ENV !== "production"` gating. [source](dev-journal/2026-05-07.md). May 11 added magic-byte `detectImageMime` sniff (PNG/JPEG/WEBP/GIF/AVIF `ftyp` brands; SVG deliberately excluded for XSS shape) — read 12 bytes from already-buffered formData, near-free; `parseJsonBody` schema-validation logs `path` only, never values, with a test pinning "submitted value never appears in log calls". [source](dev-journal/2026-05-11.md). May 13 added `sanitizeLogString` (strips CR/LF/TAB/NUL + clamps to 200 chars) before logging the multipart parser's `error.message`, closing a log-injection vector. [source](dev-journal/2026-05-13.md). May 14 culminated in GDPR-grade IP pseudonymization: `IP_HASH_SECRET` env var + `getIpHashSecret()` accessor; `clientBucketKey` HMAC-SHA256s the IP and truncates to 22 base64url chars before using it as the Upstash bucket key. Rate-limit gate widened to require both Redis and the secret (fail-open if either missing, warn at module load on secret-missing-while-Redis-set). Plain SHA-256 was rejected because IPv4's ~4B keyspace is brute-forceable on a laptop in minutes; HMAC with a server-held secret blocks rainbow-table reversal. Library-default `PEXPIRE window*2+1000` (~30min for `slidingWindow(5, "15 m")`) covers retention without manual purge. Under GDPR (CJEU *Breyer* C-582/14) IP is personal data; rate-limiting falls under legitimate interest, but the four pillars (purpose, retention, transparency, minimization) require pseudonymization. [source](dev-journal/2026-05-14.md)

- **Codebase-review remediation arc (2026-05-07 → 2026-05-13)** — Established the daily-review → multi-phase-pass → commit-per-phase cadence. May 7 single-day session worked the `codebase-2026-05-07.md` full-codebase review across 14 phases (17 commits, 721 → 840 tests). Highlights: `parseIntId` strict `^-?\d+$`, `parsePageParam` clamp at 10k, NFKD `createSlug` (kills latent `"   " → "---"` bug), even-length-hex `ADMIN_HASH_PASSWORD`, word-boundary `platformBucket`, `loginSchema.email` lowercase/trim transform, `searchPosts` term-length floor, `siteBase` `VERCEL_URL` fallback, anchored `filterAdminEvents`. Schema: `postCreateSchema.datetime` regex at write time; projects POST `sortOrder` `Math.min(sortOrder, count)` clamp. API: `parseJsonBody` extracted into `apiErrors.ts` and applied to all four admin write handlers; posts PUT reads previous section before update for cross-section cache bust; upload `ALLOW_UPLOADS` flag + Content-Length precheck; login per-IP `clientBucketKey` + try/catch fail-open on Upstash. Observability: `verifyToken` demoted `ERR_JWT_EXPIRED` to `console.info`; `respondInternalError` returns 12-char request id in body and log; `handlePrismaError` warns on 404 with optional tag; `useAdminResource.readErrorMessage` appends `(HTTP NNN)`. Concurrency: `AbortController` + cleanup on `IsFeaturedToggle`, `ProjectSortOrderInput`, `useAdminResource`. DRY: `revalidateProject(slug)` helper, `/api/admin` exact-match gate, `ClientAnalytics` merge. Refactor: `(protected)/page.tsx` split into `PostsTab` + `ProjectsTab` + `AdminPagination` with `parseTab`/`buildAdminPageUrl` in `src/lib/adminPageUrl.ts`. `<ErrorMessage>` extracted across 7 admin error surfaces (drops contradictory `aria-live="polite"` on `role="alert"`). `LandingBackground` reduced-motion via `[data-blob-bg]` CSS rule. [source](dev-journal/2026-05-07.md). May 11 stretched the cadence to three full-codebase reviews + three implementation passes in one day (24 commits, 903 tests) — Session 1 cascaded `datetime <= now` filters; Session 2 unified the audit log shape and extracted `isAbortError`; Session 3 closed the carousel APG pattern + skip-link wrapper + `<time dateTime>` ISO 8601 bug missed by two prior reviews. [source](dev-journal/2026-05-11.md). May 12 ran 7+3 phases (12 commits, 980 tests) including the `<main>` hoist + posts PUT escalation to Serializable. [source](dev-journal/2026-05-12.md). May 13's 7-phase pass closed every actionable review finding — and exposed a real bug (`useOptimisticMutation` unmount-after-resolve race) via a test added during implementation. Generalizable lesson: a test premise that "should pass with the current implementation" sometimes uncovers an actual implementation gap, not a bad test. [source](dev-journal/2026-05-13.md)

- **Admin write contract: unified audit log + abort-aware optimistic mutations (2026-05-07 → 2026-05-15)** — May 7 introduced `AbortController` on every admin mutation (`useRef` + `useEffect` cleanup) and extracted `<ErrorMessage>` (7 callers). [source](dev-journal/2026-05-07.md). May 11 Session 2 added the typed `auditLog(tag, payload)` helper with a fixed 6-key shape (`id, slug, section, sortOrder, previousSection, previousSlug`); Session 3 tightened `AdminAuditPayload` from optional `?` to required `T | null` so silent-null regressions in posts/projects PUT surface at tsc instead of in logs; `isAbortError` helper extracted (5 sites, `DOMException` + `Error` shape). [source](dev-journal/2026-05-11.md). May 12 typed `AdminAuditTag` as a closed union, unified `previousSlug` audit semantics across posts/projects PUT to "the slug renamed, here's what it was" (not "name was edited"), and extracted `useOptimisticMutation` at the 2nd consumer (`IsFeaturedToggle` + `ProjectSortOrderInput` collapse to ~30 LOC each). [source](dev-journal/2026-05-12.md). May 13 derived `AdminAuditTag` from `const ADMIN_AUDIT_TAGS = [...] as const` (single edit site for new admin write surfaces, runtime list greppable); widened `MutateResult` to `{ ok: true } | { ok: false; reason: "failure" | "superseded" }`; nulled `abortRef.current` in the hook's cleanup to close an unmount-after-resolve race a new test exposed. [source](dev-journal/2026-05-13.md). May 15 added `[api:admin:posts:BULK]` to the closed tag union for the new bulk-import endpoint — same 6-key payload, one line per created row. [source](dev-journal/2026-05-15.md)

- **Future-dated content + cache invariants — and the May 14 reversal** — May 11 Session 1 cascaded `datetime <= now` filter across `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams`, `legacySlug.cachedLookup`. `findUnique` → `findFirst` migration required because compound-unique `(section, slug)` can't take filter clauses (every test mock updated). Captured `now` inside cached fns so each entry was "valid for this `now`-snapshot, evicted on mutation bus or organic expire" — explicit tradeoff: future-dated published posts didn't auto-surface at their scheduled time. [source](dev-journal/2026-05-11.md). May 12 added `@@index([slug])` to `Post` (legacy-slug lookup was seq-scanning); escalated posts PUT to Serializable to close the cross-section cache-invalidation race. [source](dev-journal/2026-05-12.md). May 13 clarified that `serialization_failure → 500` is also not client-retriable. [source](dev-journal/2026-05-13.md). May 14 Session 2 reversed the May 11 "capture `now` inside the cached fn" decision: cache stores rows time-independently, the read-time filter applies fresh `now` per request, and paginated caches pad with `LIMIT + futureCount` so the filter can strip futures and still slice a full page. A future-dated published post auto-surfaces the first request after its `datetime` passes — no cache bust, no cron. Bounded-cost tradeoff: padded rows that all crossed their date sit in cache (~100KB per section worst case) until a mutation rebuilds. Tests using `"not-a-date"` to exercise downstream rendering fallbacks broke and exposed an unreachable-in-production code path; switched to `"2020-bad-format"` (past-lex, fails the regex) so the test now reflects real behaviour. [source](dev-journal/2026-05-14.md)

- **A11y + structural cleanup (2026-05-11 → 2026-05-13)** — May 11 added the carousel APG pattern (`role="group"`, `aria-roledescription="carousel"`, `aria-live="polite"` on the slide region), padded-button hit regions for dot indicators (WCAG AAA, replacing the overlap-prone `before:-m-2.5` trick), and a skip-link wrapping `{children}` in a single `#main-content` wrapper. `<time dateTime>` corrected to ISO 8601 (had been emitting the internal `yyyy-MM-dd-HHmm`, missed by two prior reviews — added to attribute-level audit targets). [source](dev-journal/2026-05-11.md). May 12 hoisted `<main>` from per-page wrappers into the root layout (one landmark per document) — 14 pages converted to `<div>`/`<section>`. `PresetOrFreeformInput` got differentiated `aria-label` suffixes for its dual labels. [source](dev-journal/2026-05-12.md). May 13's review caught that `PrivacyPageLayout.tsx` had been missed by the hoist — still rendered `<main>`, producing two landmarks on `/privacy` and `/privacy/body-tracking`; fixed alongside a landmark-structure test. [source](dev-journal/2026-05-13.md). The watch-out written on May 12 ("code-review on any new top-level page") is what surfaced the May 13 catch.

- **Error boundary + non-route-link decisions (2026-05-09)** — `<Link>` → `<a>` reverted for all non-route hrefs on the about page (9 bio links + 3 `contactLinks`): `next/link` for `mailto:`, external URLs, and static assets pulls a client component and has undocumented prefetch behavior on non-route URLs. `error.digest` dropped from both `error.tsx` and `global-error.tsx` — Vercel Hobby ~1h runtime-log retention means the matching log is gone before a user reports the hash; re-introduce paired with external aggregation (same defer rationale that later applied to `auditLog` on May 12). RTL tests added for both error boundaries. `errorMessages.ts` deleted (2 stable strings don't justify a shared module). `ClientAnalytics` moved to `src/components/`. [source](dev-journal/2026-05-09.md)

- **Upstash keepalive triage + admin infra (2026-05-14 → 15)** — Recurring Upstash idle warnings traced to the cron schedule: `0 0 1,11,21 * *` had 10-11 day gaps but the free-tier idle threshold is ~7 days. History: `1,15` (15-day) → `1,11,21` (10-day) → `0 0 * * *` (daily). Handler uses `redis.set(KEEPALIVE_KEY, iso)` not `PING` (Upstash excludes PING from idle detection). `KEEPALIVE_KEY = "keepalive:last"` centralized to `src/lib/keepalive.ts` so cron + new admin trigger can't drift. New `POST /api/admin/keepalive` endpoint (session-gated by `proxy.ts`'s `/api/admin/*` match) drives a manual "Run keepalive" button in `AdminNav`. Separate from the cron's bearer-auth route on purpose: keeps each route's auth concern single-purpose; only the key constant is shared. Same session resolved an unrelated dashboard amplification: `<Link>` in App Router prefetches on viewport entry, not just hover — every visible row's edit link was firing a background RSC fetch with a Prisma query. `prefetch={false}` applied to per-row edit links in `PostsTab`/`ProjectsTab`; kept defaults on top-of-nav links. [source](dev-journal/2026-05-14.md). May 15 closed the May 14 watch-out about the two SET sites drifting — the drift had already happened (cron had no success line, admin emitted `console.info`; error payloads differed). Extracted `writeKeepalive(redis): KeepaliveResult` (discriminated `{ ok: true; value } | { ok: false; error }`) into `src/lib/keepalive.ts`; both routes consume it and own their own log tag + status code + response shape (cron 200 on no-Redis, admin 503; both 502 on `redis.set` reject). The helper centralizes the WRITE; callers own the policy. [source](dev-journal/2026-05-15.md)

- **Admin posts list ergonomics + bulk import (2026-05-15)** — Inline `· Scheduled` marker (in `text-accent`) on rows where `published && datetime > now`, coexisting with `· Draft` for future drafts. New `PostPublishedToggle` mirroring `IsFeaturedToggle` reuses the existing posts PUT (no new endpoint, no schema change). Bulk markdown import: filename `yyyy-MM-dd[-HHmm]-Title with spaces.md` parsed by a single shared `src/lib/bulkImportParser.ts` (consumed by both server route and client preview, returns discriminated `{ ok, ... } | { ok: false, reason }`); section picked once per batch; `published` derived per-file from datetime (future → published so the May 14 auto-surface logic kicks in, past → draft for review). Server pre-queries colliding slugs in the section, dedupes within the batch, then `createManyAndReturn` with `skipDuplicates`, single `revalidatePostSection`, one `[api:admin:posts:BULK]` audit line per created row. Schema cap: 50 files / 100KB each (worst-case ~5MB body). UI returns 200 even on partial success with a per-file `skipped` reason list. [source](dev-journal/2026-05-15.md)

## Decisions of note

- **2026-04-16 — `_key` for list-row identity** — `crypto.randomUUID()` on `LinkItem`/`SectionItem`, used as React key, stripped before API submission. Alternatives rejected: parallel state, module-level counter. Reason: `LinkRow` initialises dropdown/freeform state from props once; index keys caused reorder-drift. [source](dev-journal/2026-04.md)
- **2026-04-17 — 6 parallel agents for Session 1** — independent buckets dispatched in parallel rather than sequential sweeps. Worked because subtask boundaries were genuinely independent; landed 10 commits / 96 files. [source](dev-journal/2026-04.md)
- **2026-04-17 — Prisma `Section` enum over string casts** — `section String` → `section Section`. Accepted cost: `yarn db:push` as deploy gate. Deploying the new client before the schema push causes runtime type mismatches. [source](dev-journal/2026-04.md)
- **2026-04-17 — Catch-all page over API-route legacy redirect** — `src/app/[slug]/page.tsx` with `permanentRedirect`/`notFound`. App Router's static-over-dynamic dispatch dissolves `KNOWN_ROUTES` as a class-of-bug; `notFound()` from a page renders the styled 404. [source](dev-journal/2026-04.md)
- **2026-04-17 — `ProjectSortOrderInput` 1-indexed UI** — translate at component boundary (`value+1` render, `-1` before PUT). Alternatives rejected: shift clamp to `[0, totalCount-1]` (surprising for a position input); do nothing (future-fragile). [source](dev-journal/2026-04.md)
- **2026-04-17 — Document `sortOrder` single-writer invariant** — inline comment on PUT transaction over `@@unique` (Prisma DSL can't express `DEFERRABLE INITIALLY DEFERRED` cleanly), test-only, or `SERIALIZABLE` isolation (escalation triggered only when second admin account is added). [source](dev-journal/2026-04.md)
- **2026-04-20 — Normalize `updatedAt` to ISO inside cached fn** — at cache boundary, not at each read site. Handler contract: "strings in, strings out". One conversion site instead of two. [source](dev-journal/2026-04.md)
- **2026-04-20 — Map-memoized wrappers for slug lookups** — `Map<slug, wrapper>` over lift-with-args or coarsen-to-shared-tag. Per-slug tags in `projects.ts` are revalidated by admin routes on edit/delete — must be preserved. [source](dev-journal/2026-04.md)
- **2026-04-20 — `stripMarkdown` via AST walk** — `unified` + `remark-parse` + `remark-gfm` over regex or `strip-markdown` package. No new deps; ~15 lines. [source](dev-journal/2026-04.md)
- **2026-04-26 — Hex-format check on `ADMIN_HASH_PASSWORD` only** — `^[0-9a-f]*$`; `SESSION_SECRET` consumed via `TextEncoder().encode()` (any string valid). [source](dev-journal/2026-04.md)
- **2026-04-26 — Module-load dummy bcrypt** — `bcrypt.hashSync("dummy", 10)` at import time; ~80ms cold start; predictable over lazy/cached-in-closure. [source](dev-journal/2026-04.md)
- **2026-04-26 — Per-call `safeParse` deliberately deferred** — memoization would break `vi.stubEnv` testability contract; cost negligible on protected-request path. [source](dev-journal/2026-04.md)
- **2026-05-07 — Slim `generateMetadata` rejected** — kept original `loadPostForAdmin`/`loadProjectForAdmin` shared-via-`React.cache()` design over a slim title-only fetch. Slimming would cost a second DB query without a defense-in-depth gain (middleware is the load-bearing gate; both run in the same request). [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Per-IP login rate-limit single bucket** — replaced `"global"` with `clientBucketKey` keyed on leftmost `x-forwarded-for`. Rejected the per-IP + global hybrid: it reintroduces the original DOS-lockout vector at higher attacker volume. Watch-out: `"unknown"` fallback collapses bare-metal deploys into one bucket. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Login `ratelimit.limit` fail-open on Upstash error** — try/catch around the limiter; locking the only public auth entry during an Upstash blip is worse than briefly skipping rate limiting. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Strict `parseIntId` regex** — `^-?\d+$` test before parse. `"3.7"` and `"12abc"` now return `null`. Trailing-garbage IDs landing on real records is worse than failing fast. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `createSlug` Unicode normalization** — NFKD-decompose, strip combining marks, fold dash variants, collapse repeated/leading/trailing hyphens. Pinned `createSlug("   ") === ""` (was `"---"`, latent bug). Inline rewrite over a `slugify` dependency. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `platformBucket` word-boundary regex** — `\b<keyword>\b` per keyword. Comma-split + `Set.has` was rejected because it broke multi-word keywords inside longer phrases (`"menu bar app" → "Mac"` regression). [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `ALLOW_UPLOADS` env flag (lazy-read)** — replaced `NODE_ENV !== "production"` gating with explicit flag, read inside the handler so `vi.stubEnv` works. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Cross-section cache bust reads previous section first** — posts PUT `findUnique`s the prior `section` before the update, then invalidates both old and new tags only when they differ. Avoids double-busting on no-op edits. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `projects` POST sortOrder clamp** — `Math.min(sortOrder, count)` before the shift. Admin UI can drift past `count` after a concurrent delete; clamping is more forgiving than 400. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — JWT `ERR_JWT_EXPIRED` → `console.info`** — only routine failure; signature failures stay at error so secret-rotation/tampering spikes remain visible. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Request id (12-char hex) in `respondInternalError`** — body + log; the app should be debuggable on a self-host (Vercel's platform request id is platform-only). [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Drop `aria-live="polite"` on `role="alert"`** — `role="alert"` already implies live-region; specifying both is contradictory and inconsistently handled by ATs. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Implicit label/input nesting in `LoanCalculatorInput`** — over `useId` + `htmlFor`. Avoids forcing `"use client"` and a Next.js diagnostic about non-serializable function props. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `ProjectContent` `<style>` injection NOT scoped** — finding withdrawn after re-reading: the `--color-header-accent` variable is consumed by the global header (an ancestor of `ProjectContent`), so scoping to a wrapper would break that consumer. [source](dev-journal/2026-05-07.md)
- **2026-05-09 — Revert `<Link>` → `<a>` for non-route hrefs** — applied to all 9 bio links and 3 `contactLinks` (`mailto:`, external, `/resume.pdf` with `download`). `next/link` adds nothing for these; the static-asset case is the worst (Next's prefetch logic isn't documented to skip non-route URLs). Alternative: custom `<ExternalLink>` wrapper — rejected as over-engineering for a single page. [source](dev-journal/2026-05-09.md)
- **2026-05-09 — Drop `error.digest` rendering** — without external error aggregation, the digest is opaque to users and uncorrelatable for the developer (Vercel Hobby retains runtime logs ~1h; by the time a user reports a hash, the matching log is gone). Re-introduce paired with Sentry/Logflare/Axiom. [source](dev-journal/2026-05-09.md)
- **2026-05-11 — `getPostBySlug` `findUnique` → `findFirst`** — adding the `published: true` + `datetime <= now` filter requires non-unique-constraint query shape. Required updating every test mock from `findUnique` to `findFirst`. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Cron 401 instead of 500 on missing `CRON_SECRET`** — pre-auth callers must not learn the server is unconfigured. The server-side `console.error` log preserves operator visibility. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Magic-byte sniff for upload, accept-list `image/avif` brands** — `ftyp` at offset 4-7 plus any of `avif`/`avis`/`mif1` at 8-11. Inline detector ~60 lines; rejected `file-type` package as overkill for 5 image formats. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Audit-log payload structured, not a string** — `{ id, slug, section, previousSection }`. Future log aggregation (Logflare/Axiom) parses structured payloads natively. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `parseJsonBody` logs schema-validation paths only** — locked with a test that pins "submitted value never appears in log calls" so a future "add more context to the log" PR can't accidentally leak credentials. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `createSlug` dash class extension paired with schema `.refine`** — extended `[\s.‐-―−­]+` to include U+2212 minus + U+00AD soft hyphen; the schema refine alone wasn't enough (`createSlug("−−−") === "−−−"` under the old regex). Paired both changes in one commit so they're atomic. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Posts PUT wrapped in `prisma.$transaction` at READ COMMITTED** — left at default isolation; project PUT is Serializable because of the sortOrder shift, posts PUT has no shift. (Reversed on May 12 — see below.) [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `useSyncExternalStore` for `ThemeProvider`** — chose over a simpler `[]`-deps `useEffect` patch. React 18's canonical hook for binding to external mutable sources (`window.matchMedia`); removes local state and eliminates the stale-after-resubscribe class. Honest tradeoff: the simpler patch was probably the right minimal call for a personal blog. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Skip-link wraps `{children}` in a `#main-content` div, not every page's `<main>`** — one wrapper covers every route without touching ~15 page files. (Generalized by the May 12 `<main>` hoist.) [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `AdminAuditPayload` optional `?` → required `T | null`** — call sites must now pass every key (with `null` for inapplicable fields), so a future caller dropping a relevant change surfaces at tsc instead of emitting silent `null`. The silent-null regression already happened twice this cycle (posts PUT `previousSlug`, projects PUT `sortOrder`). [source](dev-journal/2026-05-11.md)
- **2026-05-12 — `postDatetimeToISO` throw → `string | undefined`** — schema regex is the load-bearing gate against bad writes; legacy DB rows are the only path to bad input at runtime. Returning `undefined` (not `null`) drops `?? undefined` at 3 of 4 callers. CLAUDE.md "prefer `null`" applies to object shapes, not function returns feeding React props. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — Hoist `<main>` to root layout** — pages now render plain wrappers; layout owns the single `<main>` with the skip-link target. Layout-owned matches App Router convention; multiple `<main>` per document is invalid HTML5. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — Posts PUT escalated to Serializable** — closes the cross-section cache-invalidation race. Single-admin volume makes `serialization_failure` essentially impossible. No retry loop because `auditLog` would double-fire on retry. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `useOptimisticMutation` extracted at the 2nd consumer** — earlier pass deferred to the third consumer; tackled today as part of the deferred-items follow-up. Hook owns abort plumbing + `isAbortError` + supersession guards; callers own the optimistic value commit and `onRevert`. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `Post.slug` index added** — `legacySlug.lookupLegacySlug` queries by `slug` alone; the composite `(section, slug)` unique can't be used for the slug-only prefix. `@@index([slug])` is essentially free. (Schema committed; needs `yarn db:push` to apply — open thread carried 2026-05-12 → 13 → ?.) [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `previousSlug` audit semantics unified** — projects PUT was emitting `previousSlug` whenever `name` was updated (even if the slug didn't change); aligned both posts and projects to "the slug renamed, here's what it was" — `previousSlug` is now strictly a rename signal. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — Audit-sink durability deferred (`DO NOT PROPOSE`)** — the structured 6-key payload + typed `AdminAuditTag` were sized for a future log aggregator that's now explicitly not coming. The shape still helps grep during the ~1h Vercel retention window; durable persistence pitched repeatedly in code reviews and surfaces again whenever the audit log comes up. Same call as the `error.digest` defer on 2026-05-09 — re-introduce paired with external aggregation, not before. [source](dev-journal/2026-05-12.md)
- **2026-05-13 — `useOptimisticMutation` cleanup nulls `abortRef.current`** — the unmount-after-resolve test exposed that the existing `controller !== abortRef.current` supersession guard was satisfied by post-unmount continuations (ref still held the aborted controller). Nulling in cleanup makes the guard correct for every post-unmount path, not just the abort-rejection one. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `MutateResult` widened to `{ ok: true } | { ok: false; reason: "failure" | "superseded" }`** — discriminant landed at 2 callers, not 3. Both still destructure just `ok`; the seam is invisible to existing consumers. Rule of three doesn't apply when the abstraction extension is invisible to existing callers. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `AdminAuditTag` derived from `const ADMIN_AUDIT_TAGS = [...] as const`** — single edit site when adding a new admin write surface; the runtime list is greppable too. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — Drop the `void` operator from fire-and-forget `mutate()` calls inside `act()`** — `sonarjs/void-use` and `@typescript-eslint/no-floating-promises` directly contradict on the pattern; verified empirically that `no-floating-promises` isn't enabled, so dropping `void` is the cleanest resolution. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `metadata.ts` dev-only double-brand throw** — `process.env.NODE_ENV !== "production"` gates the throw; relies on local dev or CI exercising the offending caller before deploy. Watch-out: if a regression ever ships, escalate to always-on. [source](dev-journal/2026-05-13.md)
- **2026-05-14 — Cron schedule → daily (`0 0 * * *`)** — daily over every-5-day. Removes future risk if Upstash tightens its idle threshold; cost is one trivial SET/day. Previous `0 0 1,11,21 * *` had 10-day gaps that exceed Upstash's ~7-day free-tier idle window. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — HMAC-pseudonymize IPs (not plain SHA-256)** — `HMAC-SHA256(IP_HASH_SECRET, ip)` truncated to 22 base64url chars (132 bits — plenty for a single-admin's worth of buckets). Plain SHA-256 is reversible in practice; HMAC with a server-held secret blocks the rainbow-table shortcut. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Fail-open when `IP_HASH_SECRET` is missing on a Redis-configured deploy** — disable rate-limiting (with warn at module load) over falling back to plain-IP keys. Regressing to plain IP would silently undo the GDPR posture improvement. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Manual admin keepalive as a parallel endpoint, not bearer-shared with cron** — `POST /api/admin/keepalive` (session-gated by `proxy.ts`) over expanding the cron's auth check to accept sessions. Each route's auth concern stays single-purpose; `KEEPALIVE_KEY` is the only shared surface. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Time-based scheduled publishing without cron (Session 2)** — switched from "cache captures `now` at fill time" to "cache stores rows regardless of datetime; filter `datetime <= now` at read time." A future-dated published post auto-surfaces the first request after the boundary. Granularity is request-driven, not minute-accurate; fine for a blog. Alternatives rejected: Vercel Cron hitting a revalidate endpoint (overkill); dropping caches entirely (gives up cache benefits across all reads). [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Padded bounded caches over a single full-list cache** — paginated reads (page 1 of section list, feed) fill with `LIMIT + futureCount` rows so the read-time filter can strip futures and still slice a full page. Padding wins because the cache stays bounded by PAGE_SIZE in the common case (futureCount=0); the alternative ("backfill on read when futures leak") is strictly worse (extra request-time DB hit). [source](dev-journal/2026-05-14.md)
- **2026-05-15 — Bulk-import section picked once per batch; `published` derived per-file from datetime** — section selector lives on the bulk page; future-dated posts auto-publish (the May 14 auto-surface logic picks them up at their `datetime`), past-dated posts saved as drafts. Alternatives rejected for section: filename suffix or per-file picker (more UI for no current need). Alternatives for `published`: blanket draft (doesn't match the intent) or blanket published (ships unreviewed past content). [source](dev-journal/2026-05-15.md)
- **2026-05-15 — Bulk import: pre-query slug collisions, then `createManyAndReturn` with `skipDuplicates`** — one round-trip to detect existing slugs in the same `(section, slug)` unique scope, single `createManyAndReturn` insert, single `revalidatePostSection`, one audit line per created row under `[api:admin:posts:BULK]`. Per-row `create` in a loop rejected (N round-trips, N revalidates). Skip-the-pre-query rejected (`skipDuplicates` alone loses per-file "skipped because" reporting). [source](dev-journal/2026-05-15.md)
- **2026-05-15 — `writeKeepalive(redis): KeepaliveResult` extraction** — discriminated `{ ok: true; value } | { ok: false; error }` shared by `/api/cron/ping` and `/api/admin/keepalive`. Each caller owns its own log tag, status code, and response shape (cron 200 on no-Redis, admin 503 — those policies aren't shared, the WRITE is). Returns the original `error` (not stringified) so callers preserve a usable stack via `console.error`. Alternative considered: a single `keepalive(redis, opts)` taking log-tag + status-code maps — rejected as premature configuration; today there are exactly 2 callers and their differences are real, not accidental. Closes the 2026-05-14 watch-out. [source](dev-journal/2026-05-15.md)

## Incidents of note

- **2026-04-17 — `ProjectSortOrderInput` indexing bug** — root cause: UI rendered raw 0-indexed DB `sortOrder` in a `[1, totalCount]`-clamped input; typing `0` + tab silently swapped top project. Resolution: 1-indexed UI translation. Regression risk: test asserts clamp behavior; Zod `.min(0)` defence-in-depth. [source](dev-journal/2026-04.md)
- **2026-04-17 — Legacy-redirect raw JSON 404** — root cause: `notFound()` from an API route returns JSON, not styled 404 page. Resolution: move lookup to catch-all page. Side effect: `KNOWN_ROUTES` allowlist deleted entirely. [source](dev-journal/2026-04.md)
- **2026-04-17 — Parallel Round 1 agents collided in shared working tree** — Agent 1a saw Agent 1b's uncommitted edits and tried `git stash`. Root cause: parallel agents share the checkout. Fix: serial rounds, commit-between. [source](dev-journal/2026-04.md)
- **2026-04-17 — `defaultProto` port-heuristic silent downgrade** — root cause: `host.includes(":")` treated "has a port" as "is dev"; `example.com:8443` silently got `http`. Resolution: only `localhost`/`127.0.0.1` prefix. [source](dev-journal/2026-04.md)
- **2026-04-17 — Feed `<updated>` used publish time, not modified time** — root cause: feed-level `<updated>` took `posts[0].datetime`; edits bumped `updatedAt` but not `datetime`, so feed readers saw stale timestamp and skipped refetch. Resolution: `Math.max` across entry `updatedAt`. [source](dev-journal/2026-04.md)
- **2026-04-20 — 500 on `/api/feed/tech`: `updatedAt.getTime` on undefined** — root cause: `unstable_cache` JSON-serializes `DateTime` → string on cache hit. Fix: ISO normalization inside cached fn. [source](dev-journal/2026-04.md)
- **2026-04-20 — Revalidation log spam with function body as key** — root cause: `unstable_cache(...)` rebuilt per call; Next.js logged each rebuild as new cache registration. Fix: lift to module scope or Map-memoize. [source](dev-journal/2026-04.md)
- **2026-04-21 — Atom `<summary>` contained raw markdown / `markdownToHtml` test inverted** — `excerptFromBody` sliced raw body; fix: `stripMarkdown` AST walk before slice. Adjacent: assumed `markdownToHtml` was passthrough; actual: `remark-rehype` drops raw HTML by default. Test inverted to assert the drop. [source](dev-journal/2026-04.md)
- **2026-04-26 — Hex regex mistakenly applied to `SESSION_SECRET`** — CLAUDE.md said "hex-encoded" but actual usage is `TextEncoder().encode()`. Caught by user correction; reverted; CLAUDE.md updated to "any string." [source](dev-journal/2026-04.md)
- **2026-05-07 — Edit-page tests had never run** — root cause: `vitest.config.ts` glob excluded `src/app/**/*.test.tsx`. Two test files silently skipped; one asserted a `findUnique({ select: { title: true } })` shape the implementation never used and would have failed if the runner picked it up. Fix: widen include glob; rewrite tests to assert on returned JSX element's `props` (server components don't invoke children synchronously). [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `vi.mock` factory hoisting** — root cause: `const mockX = vi.fn(...)` referenced inside `vi.mock("...", () => ({ default: mockX }))` failed because the factory hoists above the variable. Fix: inline the mock implementation in the factory. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Token-based `platformBucket` regression** — root cause: first fix used `,`-split tokens with `Set.has` exact-match, breaking `"menu bar app" → "Mac"`. Fix: word-boundary regex per keyword. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `AdminNav` redirected on logout failure** — root cause: 401/500/network failure silently `router.push('/admin/login')` while session cookie may still be alive. Fix: surface `<ErrorMessage>`, block redirect, user retries. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `upload` route env stubbing** — root cause: read `process.env.ALLOW_UPLOADS` at module load; `vi.stubEnv` couldn't change a value already evaluated. Fix: read lazily inside the handler. [source](dev-journal/2026-05-07.md)
- **2026-05-11 — U+2212 minus + U+00AD soft hyphen passed `createSlug` unchanged** — the existing regex `[\s.‐-―]+` covers U+2010-U+2015 but not U+2212 or U+00AD. Schema refine didn't catch them (produced non-empty slugs like `"−−−"`). Fix: extended dash class to `[\s.‐-―−­]+`; paired schema refine now rejects them. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `vi.resetAllMocks()` cleared `currentDatetimeString` mock in `sitemap.test.ts`** — after adding `datetime: { lte: now }` to `getAllPublishedPostSlugs`, the cached fn called the mocked fn; `resetAllMocks` in `beforeEach` cleared the `mockReturnValue`. Fix: rebind in `beforeEach`. (Same shape recurred 2026-05-15 in the bulk-route test — same fix.) [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `<time dateTime>` was emitting `yyyy-MM-dd-HHmm` (the project's internal format), not ISO 8601** — both prior reviews missed it. SR readers, search engines, and Schema.org consumers all parse wrong. Fix: route `PostCard` and `PostContent` through `postDatetimeToISO`. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `readErrorMessage` × Zod-array regression** — `parseJsonBody` returns `{ error: parsed.error.issues }` (a `ZodIssue[]`) but `readErrorMessage` typed `data.error` as `string` and string-concatenated it, coercing arrays to `[object Object],[object Object]`. tsc didn't catch it because `await response.json()` returns `any`. Fix: `Array.isArray` branch with `formatZodIssues`. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `legacySlug.cachedLookup` missed in Phase 2's filter cascade** — `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams` got the `datetime <= now` filter; the legacy-slug lookup still filtered `published: true` only. A future-dated published post matching a legacy URL would 308-redirect to a canonical page that 404s on the same filter. Fix: same filter pattern added; test pins the `where` shape. [source](dev-journal/2026-05-11.md)
- **2026-05-13 — Pre-existing `<main>` regression in `PrivacyPageLayout.tsx`** — file wasn't touched in May 12's 11-commit Phase 2 hoist; it kept rendering its own `<main>` inside the now-hoisted root `<main>`. Both `/privacy` and `/privacy/body-tracking` had two landmarks per document. Root cause: rg-only sweep during the hoist would have caught it; the actual sweep missed it. The May 12 watch-out ("code-review on any new top-level page") is what surfaced it. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — Unmount-after-resolve race in `useOptimisticMutation`** — cleanup aborted the controller but didn't null `abortRef.current`, so a buffered non-ok response (or a mock that ignores abort) could pass the `controller !== abortRef.current` guard and run `onRevert`/`setError` on an unmounted component. Surfaced by a test added the same session — the test premise was sound, the implementation gap was real. Fix: null the ref in cleanup. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — ESLint rule contradiction: `sonarjs/void-use` vs `void promise` idiom** — `yarn lint` failed on 6 sites using `void result.current.mutate(...)`. Resolved by dropping the operator entirely after confirming `no-floating-promises` isn't currently enabled. [source](dev-journal/2026-05-13.md)
- **2026-05-14 — Upstash idle warnings persisted after two schedule tightenings** — root cause: `0 0 1,11,21 * *` has 10-11 day gaps; Upstash free-tier idle threshold is ~7 days. Fix: schedule → daily. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Admin dashboard fires N background RSC requests on every load** — root cause: Next.js App Router `<Link>` prefetches on viewport entry; PostsTab and ProjectsTab render plain `<Link>` for each row's edit, each prefetch runs the edit page's server component which hits Prisma. Fix: `prefetch={false}` on per-row edit `<Link>`s. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — `malformed datetime` tests in feed + sitemap broke after the cache refactor** — root cause: tests used `datetime: "not-a-date"` to exercise downstream rendering fallbacks; they passed only because the mocked `prisma.post.findMany` ignored the where-clause. In real Postgres those rows would have been filtered out by `datetime <= now` and never reached the renderer — the tests were exercising an unreachable production code path. Fix: switch test datetime to `"2020-bad-format"` (past-lex, fails the regex). Lesson: don't trust a test that passes because the mock skipped a filter. [source](dev-journal/2026-05-14.md)

## Timeline

- **2026-04-16** — `vercel.json` cron 2×/month → 3×/month (`0 0 1,11,21 * *`) for DB keepalive on free tier (later reverted to daily on May 14). [source](dev-journal/2026-04.md)
- **2026-04-17** — Upload route test coverage: every HTTP response code (403, 400×2, 413, 415×2, 200×2, 500) + `sanitizeFilename` path-traversal + all-stripped. [source](dev-journal/2026-04.md)
- **2026-04-17** — `PrivacyLink` deleted; `eslint.config.mjs` Prisma path fixed; `Typewriter` rewrite for React Compiler lint. [source](dev-journal/2026-04.md)
- **2026-04-18** — `lookupLegacySlug` gains `published: true`; `defaultProto` exported with 7 cases. [source](dev-journal/2026-04.md)
- **2026-04-19** — `defaultProto` exact-match-after-strip fix; `siteBase` 4 tests. [source](dev-journal/2026-04.md)
- **2026-04-20** — Feed 500 + three wrapper-per-call fixes across `legacySlug.ts` / `posts.ts` / `projects.ts`. [source](dev-journal/2026-04.md)
- **2026-04-20** — `stripMarkdown` AST walk; feed `<summary>` no longer emits raw markdown; 10 new tests. [source](dev-journal/2026-04.md)
- **2026-04-20** — `ADMIN_PASSWORD_HASH` → `ADMIN_HASH_PASSWORD` env var rename (code-side only; Vercel config update required to avoid silent admin lockout). [source](dev-journal/2026-04.md)
- **2026-04-20** — `FooterYear` `react-hooks/set-state-in-effect` watch-out resolved: dropped the effect, `suppressHydrationWarning` on stale caches. [source](dev-journal/2026-04.md)
- **2026-05-07** — Codebase-review remediation across 14 phases / 17 commits / 721 → 840 tests. Input validation, schema hardening, API reliability (`parseJsonBody`, cross-section bust, `ALLOW_UPLOADS`, login per-IP), observability, `AbortController` everywhere admin mutates, `revalidateProject`, `/api/admin` exact-match, `ClientAnalytics` merge, `<ErrorMessage>` extraction, `(protected)/page.tsx` split. [source](dev-journal/2026-05-07.md)
- **2026-05-09** — `<Link>`→`<a>` revert for non-route hrefs; `error.digest` dropped from error boundaries; RTL tests for `error.tsx`/`global-error.tsx`; `ClientAnalytics` to `src/components/`; `errorMessages.ts` deleted. [source](dev-journal/2026-05-09.md)
- **2026-05-11** — Three full-codebase reviews + three implementation passes in one day (24 commits, 903 tests): `datetime <= now` cascade + `findUnique→findFirst` migration; magic-byte `detectImageMime` sniff; structured `auditLog` helper with required `T | null` shape; `parseJsonBody` paths-only logging; `isAbortError` extracted; `<time dateTime>` corrected to ISO 8601; carousel APG pattern + padded-button hit regions; skip-link wrapper; `ThemeProvider` to `useSyncExternalStore`. [source](dev-journal/2026-05-11.md)
- **2026-05-12** — Daily review → 7+3 phases (12 commits, 980 tests): `<main>` hoist from 14 pages to root layout; `Post.slug` index in schema; posts PUT escalated to Serializable; `useOptimisticMutation` extracted at the 2nd consumer; `AdminAuditTag` closed union; `previousSlug` audit semantics unified across posts/projects; `readErrorMessage` extracted to `lib/` at the 5th caller; `postDatetimeToISO` returns `string | undefined`. [source](dev-journal/2026-05-12.md)
- **2026-05-13** — Daily review → 7-phase pass (7 commits, 998 tests): `PrivacyPageLayout` `<main>` cleanup; `previousSlug` DRY lift in projects PUT; `MutateResult` discriminant + unmount-after-resolve race fix; `AdminAuditTag` derived from const array; `metadata.ts` dev-only double-brand throw; `apiErrors.describeZodIssues` dedupes segments; `sanitizeLogString` for upload route log-injection guard; `ProjectForm.handleSubmit` re-snaps from `sortOrderText`. [source](dev-journal/2026-05-13.md)
- **2026-05-14** — Two sessions. (1) Upstash keepalive triage + GDPR-grade IP pseudonymization + dashboard prefetch fix: cron `0 0 1,11,21 * *` → `0 0 * * *`; `IP_HASH_SECRET` env var, HMAC-SHA256 bucket keys for login rate-limiter; `KEEPALIVE_KEY` centralized to `src/lib/keepalive.ts`; `POST /api/admin/keepalive` + "Run keepalive" button in `AdminNav`; `prefetch={false}` on per-row edit links. (2) Time-based scheduled publishing without cron: read-time `datetime <= now` filter applied to cached rows; paginated caches pad with `LIMIT + futureCount`; `totalPages` moved to request-time count; reverses the May 11 "capture `now` inside cached fn" decision. [source](dev-journal/2026-05-14.md)
- **2026-05-15** — Admin posts list: inline `· Scheduled` marker, `PostPublishedToggle` (mirrors `IsFeaturedToggle`, reuses existing PUT), bulk `.md` import (`/admin/posts/bulk`) backed by `POST /api/admin/posts/bulk` with `createManyAndReturn` + per-file skipped reasons; new `[api:admin:posts:BULK]` audit tag; shared filename parser `src/lib/bulkImportParser.ts` consumed by server + client preview. Session 2: extracted `writeKeepalive(redis): KeepaliveResult` shared by `/api/cron/ping` + `/api/admin/keepalive`, closing the 2026-05-14 drift watch-out. [source](dev-journal/2026-05-15.md)
