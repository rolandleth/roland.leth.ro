# Dev Journal – 2026-06-10

## Open Threads

- [x] Add a test for the per-IP rate-limit path (Redis + `IP_HASH_SECRET` both set) asserting the bucket key is the HMAC'd IP, not `"global"`. **Resolved 2026-06-10**: added a per-IP `describe` block in `src/app/api/auth/login/route.ratelimit.test.ts` (6 tests) — HMAC keying via an `expectedKey` helper mirroring `clientBucketKey`, leftmost-`x-forwarded-for` derivation, headerless `"unknown"` fallback, per-IP isolation (distinct IPs → distinct buckets), no module-load warn when the secret is set, and the pseudonymized key in the invalid-credentials log. `loadRoute`/`makeRequest` were parametrized to select path + forwarded header.

## Watch-outs & Tech Debt

- **Global-bucket lockout DoS** — when `IP_HASH_SECRET` is unset but Redis is configured, the login limiter degrades to one shared bucket; a botnet doing 5 failed attempts/15min locks the legitimate admin out for 15min. Accepted tradeoff (see Session 1). Trigger to revisit: if the admin reports unexpected 429s on `/api/auth/login`, first confirm `IP_HASH_SECRET` is set in the deploy env.

## Session 1

### Decisions

- **Missing `IP_HASH_SECRET` → global-bucket fallback, not fail-open** — `src/app/api/auth/login/route.ts` now builds the `Ratelimit` whenever Redis (`getRedisConfig()`) is present, regardless of the HMAC secret. The secret controls only granularity via the new `bucketKey()` helper: HMAC'd-IP key when set, constant `globalBucketKey` (`"global"`) when not. Chose global bucket over the two alternatives — fail-open (prior behavior: no limiter at all) and plain-IP keys (GDPR regression, IPv4 keyspace is brute-forceable). Some rate limiting beats none; the global key carries no IP, so no GDPR concern.

### Tradeoffs

- **Re-accepted the admin-lockout DoS** — per-IP keying had deliberately replaced a global bucket precisely to avoid a botnet locking the admin out; the fallback reintroduces that for the no-secret case only. User explicitly accepted: a 15min lockout is tolerable and "screams something is wrong" (i.e. surfaces a missed `IP_HASH_SECRET` on deploy). Reversibility: easy — gated on one config var; correct deploys that set the secret keep per-IP isolation and are unaffected.

### Environment / Config Changes

- No new env vars. Behavioral change: the login limiter is now active whenever both `KV_REST_API_TOKEN` and `KV_REST_API_URL` are set, even if `IP_HASH_SECRET` is absent. The module-load warning text changed from "rate limiting disabled" to "...falls back to a global bucket". `getIpHashSecret` doc in `src/lib/auth/env.ts` updated to match (null degrades granularity, never disables the limiter).

### What did I, Claude Code, or my agents learn from this

- **Testing the route's Redis-present path needs module-load env control** — the route constructs `Ratelimit` at module load from env read at that moment, so `route.test.ts` (which loads under `.env.test` with no KV vars) can only reach the `ratelimit === null` branch. The Redis paths require a separate file that does `vi.resetModules()` → `vi.stubEnv(...)` → dynamic `import("./route")` per test. Mocks for `@upstash/redis`/`@upstash/ratelimit` must be **classes** (constructed with `new`); arrow-function `vi.fn(() => ({}))` throws "is not a constructor". An empty mock class trips eslint `no-extraneous-class` — give it a throwaway instance field. Static `Ratelimit.slidingWindow` is attached via `Object.assign(class {...}, { slidingWindow })`.

### Notes

- Left the invalid-credentials warn log untouched (it omits the bucket key when no secret is set). In global mode the key is the constant `"global"`, so logging it adds no correlation value over the rate-limit-exceeded warn that already logs it. User confirmed fine.

## Session 2

### Decisions

- **Committed inherited working-tree changes** — at the user's explicit "commit everything" instruction, committed changes that predated this session and weren't authored in-conversation: the content-addressed importer refactor, the Continuum DPIA/responsible-use/terms routes, and `app-copy-to-project` skill screenshot-set guidance. Rather than commit blind, verified each type-checks, lints, and passes its tests, and scanned the diff + new files for secrets first (only hit was the literal `secret.png` in a path-traversal doc comment). Committed in focused per-concern commits.

### Notes

- **Inherited importer change** (`src/lib/import/projectImport.ts`, `scripts/import-projects.ts`): replaces the `(key, byte size)` blob-reuse heuristic with **content-addressed keys** — `projects/<slug>/[dirs/]<sha256[:12]>-<filename>`. A changed image now hashes to a brand-new URL the CDN/`next/image` have never cached (clean miss), instead of overwriting a stable URL the edge keeps serving stale. Identical bytes resolve to the same key and are reused, so re-imports stay idempotent. `blobKeyFor` gained a `contentHash` param; `sanitizeRelativeSegments` was extracted; `listExistingBlobs` simplified to `key → url`. Resolves the 2026-06-09 review's "reuse heuristic" finding and defuses the "warn-and-overwrite" hazard (overwrite no longer serves stale bytes).
- **Inherited Continuum legal routes**: DPIA and responsible-use go through `PrivacyPageLayout`; `terms/continuum` deliberately does not (its boilerplate would mislabel terms as a "privacy policy"), composing `PrivacySection` + `PageGlow` instead. Resolves the 2026-06-09 "untracked sibling routes" watch-out.

<!-- end -->
