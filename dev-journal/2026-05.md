# Dev Journal Monthly Rollup — 2026-05

_Generated 2026-05-17._

Sessions: 13 across 8 days. Source files: `2026-05-07.md`, `2026-05-09.md`, `2026-05-11.md`, `2026-05-12.md`, `2026-05-13.md`, `2026-05-14.md`, `2026-05-15.md`, `2026-05-17.md`.

## Themes

- **Code-review-driven implementation cadence** — The pattern established 2026-05-07 (one daily review → multi-phase implementation pass → commit-per-phase) became the month's primary mode of work. 2026-05-07 ran 17 commits (721→840 tests); 2026-05-11 ran three full-codebase reviews and three implementation passes (24 commits, 980 tests); 2026-05-12 ran 7+3 phases (12 commits, 980 tests); 2026-05-13 ran 7 phases (998 tests) — and exposed a real unmount-after-resolve race in `useOptimisticMutation` via a test that was meant to *pin* existing behavior; 2026-05-17 ran 6 phase-aligned commits closing every actionable item in `code-reviews/2026-05-16.md`. The discipline that landed inside this cadence: each commit green at boundary (tsc + lint + tests), intermediate-state authoring for files that span two phases (`PostsTab.tsx`, `bulk/route.ts` — backup to `/tmp` and restore). [2026-05-07](dev-journal/2026-05-07.md), [2026-05-11](dev-journal/2026-05-11.md), [2026-05-13](dev-journal/2026-05-13.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Time-based scheduled publishing** — Evolved through three reversals. 2026-05-11 Session 1: `datetime <= now` filter cascaded across `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams`, `legacySlug.cachedLookup` — with `now` captured *inside* each cached fn so a future-dated post stays invisible until mutation-bus expire. Documented tradeoff: scheduled posts don't auto-surface at their datetime. 2026-05-14 Session 2 reversed: cache stores rows time-independently; read-time filter applies fresh `now` per request; paginated caches pad with `LIMIT + futureCount` so the filter can strip futures and still slice a full page. A future-dated published post now auto-surfaces the first request after `datetime` passes — no cron, no manual revalidate. 2026-05-15 added the inline `· Scheduled` marker in `PostsTab` for `published && datetime > now` rows. 2026-05-17 centralized the lexicographic compare invariant in `isFutureDatetime(datetime, now)` (required `now` arg — avoids the `vi.mock` intra-module-reference gotcha). [2026-05-11](dev-journal/2026-05-11.md), [2026-05-14](dev-journal/2026-05-14.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Audit log shape evolution** — 2026-05-11 Session 2 unified the admin write surfaces under `auditLog(tag, AdminAuditPayload)` with a fixed 6-key payload (`id, slug, section, sortOrder, previousSection, previousSlug`). Session 3 tightened optional `?` → required `T | null` so a future caller dropping a relevant field (the way posts PUT had dropped `previousSlug` and projects PUT had dropped `sortOrder` earlier in the cycle) surfaces at tsc instead of silently emitting `null`. 2026-05-12 closed `tag: string` → `AdminAuditTag` typed union to catch typos at the call site. 2026-05-13 derived the union from `const ADMIN_AUDIT_TAGS = [...] as const` — single edit site for new admin write surfaces, runtime list now greppable. 2026-05-15 added `[api:admin:posts:BULK]` tag for the new bulk import endpoint. 2026-05-17 added `batchId: string | null` field — `null` on single-row handlers, a per-request `randomUUID()` on bulk so a 50-file run is grep-collapsible in the ~1h Vercel log window. The 2026-05-12 watch-out "durable audit sink — DO NOT PROPOSE" was deliberately recorded so future code reviews stop re-pitching it. [2026-05-11](dev-journal/2026-05-11.md), [2026-05-13](dev-journal/2026-05-13.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Optimistic-mutation hook lifecycle** — 2026-05-11 Session 1 first noted the duplication between `IsFeaturedToggle` and `ProjectSortOrderInput`; kept inline for the moment. 2026-05-12 extracted `useOptimisticMutation` at the 2nd consumer (not the 3rd — the deferred-items pass made it the right time). 2026-05-13 widened `MutateResult` to `{ ok: true } | { ok: false; reason: "failure" | "superseded" }` AND fixed a real unmount-after-resolve race the new test exposed: cleanup aborted the controller but didn't null `abortRef.current`, so a buffered non-ok response could pass the `controller !== abortRef.current` guard on an unmounted component. Fix: null the ref in cleanup. 2026-05-17 collapsed `PostPublishedToggle` + `IsFeaturedToggle` into a shared `BooleanFlagToggle` — the genuine reusable shape — and fixed a revert-baseline bug both shared: `onRevert` captured the *first-render* `initial` prop, so after a successful toggle + `router.refresh()` re-rendered the parent with the new server value, a subsequent failed toggle reverted to the now-stale baseline. Capture `const prev = isOn` BEFORE the optimistic `setIsOn(next)`. Both failure branches (non-ok, rejection) now `console.error` so flaky-network failures leave a devtools breadcrumb. [2026-05-12](dev-journal/2026-05-12.md), [2026-05-13](dev-journal/2026-05-13.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Upstash keepalive saga** — 2026-05-14 traced recurring Upstash idle warnings to `0 0 1,11,21 * *` (10-11 day gaps exceeding the ~7-day free-tier idle window — earlier `1,15` 15-day → `1,11,21` 10-day → finally daily `0 0 * * *`). Added a session-gated `POST /api/admin/keepalive` parallel to the bearer-auth cron + "Run keepalive" button in `AdminNav`. Centralized only the `KEEPALIVE_KEY` constant; each route kept its own write logic. 2026-05-15 confirmed the prediction of that day's watch-out ("two SET sites might drift on logging/error-shape") — the drift had already happened by review time (cron emitted no success line, admin emitted `console.info`; error payloads diverged). Extracted `writeKeepalive(redis): KeepaliveResult` shared by both routes; each caller owns its own log tag + status code + response shape. 2026-05-17 centralized the *client construction* via lazy memoized `getKeepaliveRedis(): Redis | null` (three-state cache `undefined / null / Redis`); narrowed `KeepaliveResult.error` from `unknown` to `Error` and wraps non-`Error` throws at the helper boundary; cron route now emits `[api:cron:ping] success` mirroring the admin route. [2026-05-14](dev-journal/2026-05-14.md), [2026-05-15](dev-journal/2026-05-15.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Admin posts bulk import** — 2026-05-15 introduced `/admin/posts/bulk` page + `POST /api/admin/posts/bulk` backed by a shared `src/lib/bulkImportParser.ts` (filename `yyyy-MM-dd[-HHmm]-Title.md` → discriminated `{ ok, datetime, title } | { ok: false, reason }` consumed by both server and client preview). Pre-query slug collisions in section, dedupe within batch, `createManyAndReturn` with `skipDuplicates`, single `revalidatePostSection`, one `[api:admin:posts:BULK]` audit line per created row. Per-file auto-publish: future-dated → published (auto-surface logic kicks in at datetime), past-dated → draft. 50-file cap, 100KB per file. 2026-05-17 hardened: reconciled `createManyAndReturn` silent drops via post-insert slug-set diff (admin no longer sees "created N" with no name for the dropped file); added `randomUUID()` batchId stamped on every audit line + `[api:admin:posts:BULK] inserting` breadcrumb + `[api:admin:posts:BULK] skipped` reasons-by-type summary; tightened `FILENAME_REGEX` title group to `[^/\\\x00-\x1f\x7f]+` (blocks path separators and ASCII control chars at parse time before they reach the stored title or audit payload); moved originating filename to a parallel `Map<slug, filename>` (kept off the DB-shaped `InsertRow` so a future non-DB field can't leak to Prisma); `PostBulkImport.tsx` surfaces an amber "Selected M, importing first N" banner on truncation and uses composite list keys (`${name}-${index}-${lastModified}`). [2026-05-15](dev-journal/2026-05-15.md), [2026-05-17](dev-journal/2026-05-17.md)

- **Security & input hardening** — 2026-05-07 introduced per-IP login rate-limit + `ALLOW_UPLOADS` env flag. 2026-05-11 added magic-byte `detectImageMime` sniff to the upload route + `parseJsonBody` paths-only logging. 2026-05-13 added `sanitizeLogString` (strips CR/LF/TAB/NUL + clamps to 200 chars) to the upload route's multipart-parser error logging, closing a log-injection vector where attacker-controlled boundary bytes could forge fake log lines. 2026-05-14 introduced `IP_HASH_SECRET` env var + `getIpHashSecret()` accessor; `clientBucketKey` now HMAC-SHA256s the IP and truncates to 22 base64url chars before passing to Upstash. Rationale (per *Breyer* C-582/14): IP is personal data under GDPR; plain SHA-256 is reversible in practice (IPv4 keyspace ~4B); HMAC with a server-held secret blocks rainbow-table reversal. Rate-limit gate now requires both Redis and secret to be non-null; fail-open if either missing. 2026-05-17 tightened `FILENAME_REGEX` title group + moved `/api/upload` → `/api/admin/upload` so it's auth-gated by the generic `startsWith("/api/admin/")` proxy gate instead of a per-path exception (one namespace, one gate; future sub-paths auth-gate by default). [2026-05-07](dev-journal/2026-05-07.md), [2026-05-11](dev-journal/2026-05-11.md), [2026-05-13](dev-journal/2026-05-13.md), [2026-05-14](dev-journal/2026-05-14.md), [2026-05-17](dev-journal/2026-05-17.md)

- **A11y + structural cleanup** — 2026-05-11 added the carousel APG pattern (`role="group"`, `aria-roledescription="carousel"`, `aria-live="polite"`), padded-button hit regions for dot indicators (WCAG AAA, replacing the overlap-prone `before:-m-2.5` trick from earlier in the day), and a skip-link wrapping `{children}` in a `#main-content` wrapper. `<time dateTime>` corrected to ISO 8601 (had been emitting the internal `yyyy-MM-dd-HHmm`, missed by two prior reviews — added to attribute-level audit targets). 2026-05-12 hoisted `<main>` from per-page wrappers into the root layout (one landmark per document); 14 pages converted to plain `<div>`/`<section>`. `PresetOrFreeformInput` got differentiated `aria-label` suffixes for its dual labels. 2026-05-13 caught `PrivacyPageLayout.tsx` — missed by the hoist sweep, still rendering its own `<main>`, producing two landmarks on `/privacy` and `/privacy/body-tracking`; fixed alongside a landmark-structure test. The 2026-05-12 watch-out's explicit trigger ("code-review on any new top-level page") fired on a pre-existing page, validating the value of concrete trigger conditions over vague "be careful" notes. [2026-05-11](dev-journal/2026-05-11.md), [2026-05-12](dev-journal/2026-05-12.md), [2026-05-13](dev-journal/2026-05-13.md)

- **Additive cleanup + revert pass (2026-05-09)** — Followed up on the 2026-05-09 daily code review. Reverted `<Link>` → `<a>` for all non-route hrefs on the about page; dropped `error.digest` rendering from both error boundaries (without external log aggregation it's user-uncorrelatable on Vercel Hobby's ~1h retention); deleted `errorMessages.ts` (2 stable strings don't justify a shared module); added RTL tests for both error boundaries; `ClientAnalytics` moved to `src/components/`. [2026-05-09](dev-journal/2026-05-09.md)

## Decisions (chronological — one-offs not absorbed by themes)

- **2026-05-07 — `generateMetadata` / page body share one `loadPostForAdmin` call via React `cache()`** — introducing a title-only fetch would cost a second DB query per request and lose the dedupe win; middleware is the load-bearing gate anyway. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Status-code suffix on surfaced errors** — `useAdminResource.readErrorMessage` appends `(HTTP NNN)`; "Request failed" with no status is a dead-end when DevTools aren't open. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Strict `parseIntId` regex** — `^-?\d+$` before parse; `"3.7"` and `"12abc"` now return `null`. Trailing-garbage IDs landing on real records is worse than failing fast at the URL boundary. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `createSlug` Unicode normalization** — NFKD-decompose, strip combining marks, fold dash variants, collapse repeated/leading/trailing hyphens; pinned `createSlug("   ") === ""`. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `platformBucket` word-boundary regex** — replaced `lower.includes(keyword)` with `\b<keyword>\b`. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Cross-section cache invalidation reads previous section first** — posts PUT `findUnique` for prior `section` before update, then invalidates both old and new section tags on change. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Page assembly split** — `(protected)/page.tsx` → `PostsTab` + `ProjectsTab` + `AdminPagination` + centralized `parseTab` + `buildAdminPageUrl` in `src/lib/adminPageUrl.ts`. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `<ErrorMessage>` extraction** — single component covers 7 callers, two sizes (`sm`/`md`). [source](dev-journal/2026-05-07.md)
- **2026-05-09 — Revert `<Link>` → `<a>` for non-route hrefs** — `next/link` for absolute URLs falls back to `<a>` but pulls a client component and drops prefetch silently. [source](dev-journal/2026-05-09.md)
- **2026-05-09 — Drop `error.digest` rendering** — Vercel Hobby's ~1h log retention means the digest is user-uncorrelatable; re-introduce paired with external aggregation. [source](dev-journal/2026-05-09.md)
- **2026-05-11 — `getPostBySlug` switch from `findUnique` to `findFirst`** — adding `published: true` + `datetime <= now` filter requires non-unique-constraint shape; every test mock updated. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Cron 401 instead of 500 on missing `CRON_SECRET`** — pre-auth callers must not learn the server is unconfigured; server-side `console.error` preserves operator visibility. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `parseJsonBody` logs schema-validation paths only, never values** — pinned with a test that "submitted value never appears in log calls". [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `useSyncExternalStore` for `ThemeProvider`** — chose over the simpler `[]`-deps `useEffect` patch; honest tradeoff acknowledged (simpler patch would have worked). [source](dev-journal/2026-05-11.md)
- **2026-05-12 — `postDatetimeToISO` throw → `string | undefined`** — schema regex is the load-bearing gate against bad writes; `undefined` drops `?? undefined` boilerplate at 3 React/Next call sites. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `readErrorMessage` extracted to `lib/`** — at the 5th caller; non-hook callers no longer pull a `"use client"` module just for a pure helper. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `Post.slug` index added** — `legacySlug.lookupLegacySlug` queries by `slug` alone; composite `(section, slug)` unique can't drive the slug-only lookup. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — Posts PUT escalated to Serializable isolation** — closes the cross-section cache-invalidation race; `serialization_failure → 500` is also not client-retriable (Prisma doesn't auto-retry; admin must re-issue manually). [source](dev-journal/2026-05-12.md)
- **2026-05-13 — `useOptimisticMutation.cleanup` nulls `abortRef.current`** — closes the unmount-after-resolve race the new test exposed (existing supersession guard was satisfied by stale ref). [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `apiErrors.describeZodIssues` de-duplicates segments before slicing** — pathological payloads with 30× the same `invalid_type` empty-paths were log-spamming with no signal. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `metadata.ts` dev-only throw on `"Roland Leth"` substring** — guards against double-branding against the layout's `"%s | Roland Leth"` template; dev/test only, production behavior unchanged. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — `ProjectForm.handleSubmit` re-snaps from `sortOrderText`** — submit can fire before blur (Enter inside another input, Save click while focused), pre-fix shipped stale committed value. [source](dev-journal/2026-05-13.md)
- **2026-05-14 — Vercel cron daily** — `0 0 * * *` over every-5-day; removes future risk if Upstash tightens the idle threshold. History: `1,15` → `1,11,21` → daily. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — `prefetch={false}` on per-row edit links** — App Router prefetches every visible `<Link>` on viewport entry; dashboard load was firing N background RSC fetches with N Prisma queries. Top-level "New post"/"New project" links keep default prefetch (cheap blank-form RSC). [source](dev-journal/2026-05-14.md)
- **2026-05-17 — `BULK_MAX_FILES` exported from `schemas.ts`** — client and server share one cap. `PostBulkImport.tsx` imports it; surfaces "Selected M, importing first N" on truncation instead of dropping silently; uses composite list key `${name}-${index}-${lastModified}`. [source](dev-journal/2026-05-17.md)

## Tradeoffs

- **2026-05-07 — `AbortController` everywhere admin-mutates** — gained: in-flight PUTs don't outlive unmount; cost: every admin component carries extra `useRef` + `useEffect` cleanup. Reversibility: easy. [source](dev-journal/2026-05-07.md)
- **2026-05-11 — Filtering future-dated posts everywhere broke "scheduled publish"** — gained: drafts and future-dated posts no longer leak via canonical URL / sitemap / generateStaticParams. Lost: scheduled posts don't auto-surface at their time. Reversed 2026-05-14 Session 2. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — Audit logs without external aggregation** — gained: structured payloads greppable in Vercel's ~1h log window. Cost: no signal once logs roll over. Same tradeoff as the 2026-05-09 `error.digest` decision. [source](dev-journal/2026-05-11.md)
- **2026-05-12 — `<main>` hoist** — gained: skip link targets the actual landmark; one `<main>` per document, App Router convention. Cost: 15-file mechanical change. Reversibility: easy. [source](dev-journal/2026-05-12.md)
- **2026-05-12 — `useOptimisticMutation` extracted at the 2nd consumer (not 3rd)** — gained: ~80 LOC removed, supersession-guard concentrated. Cost: abstraction may shape wrongly for the 3rd consumer. Mitigation: hook returns primitives. Reversibility: moderate. [source](dev-journal/2026-05-12.md)
- **2026-05-13 — `MutateResult` widened to discriminant** — gained: future-proof failure-vs-supersession contract. Cost: code-shape change for behavior neither caller exercises today. Reversibility: easy. [source](dev-journal/2026-05-13.md)
- **2026-05-14 — Helper extraction for keepalive constant only (not the write)** — gained: avoided premature abstraction. Accepted: allowed the two SET sites to drift on logging/error-shape. Predicted in journal; drift materialized within 24h. Reversibility: easy (and was reversed). [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Read-time filter over cached `now`** — gained: scheduled posts auto-surface the first request after `datetime` passes, no cron / no manual revalidate. Cost: padded caches may carry dead rows until next mutation (~100KB worst case per section, bounded). [source](dev-journal/2026-05-14.md)
- **2026-05-17 — Six-commit phase split** — gained: bisect-friendly, each commit a coherent unit. Cost: intermediate-state authoring for `PostsTab.tsx` and `bulk/route.ts` (working-tree edit + `/tmp` backup of the full bulk-route rewrite, restored later). Reversibility: trivial. [source](dev-journal/2026-05-17.md)
- **2026-05-17 — `BooleanFlagToggle` call sites pass 4 props instead of 2** — gained: bug fix concentrated, missing-label UX gap closed in same change. Cost: 2 extra prop lines per call site. Reversibility: easy. [source](dev-journal/2026-05-17.md)

## Incidents / Bugs

- **2026-05-07 — Edit-page tests had never run** — `vitest.config.ts` glob excluded `src/app/**/*.test.tsx`; two existing tests had been silently skipped. Fix: widen glob; rewrite tests to assert on element `props`. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `vi.mock` factory hoisting** — `const mockPostForm = vi.fn(...)` referenced inside a `vi.mock` factory failed because the factory hoists above the `const`. Fix: inline `function MockPostForm() { return null }`. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — `platformBucket` comma-split regression** — `Set.has` exact-match broke multi-word keywords inside longer phrases. Fix: word-boundary regex per keyword. [source](dev-journal/2026-05-07.md)
- **2026-05-07 — Upload route stubbing path** — `process.env.ALLOW_UPLOADS` read at module load; `vi.stubEnv` couldn't change it. Fix: read lazily inside the handler. [source](dev-journal/2026-05-07.md)
- **2026-05-11 — U+2212 minus and soft hyphen passed `createSlug` unchanged** — schema refine in Phase 8 didn't catch these because they produced non-empty slugs like `"−−−"`. Fix: extended dash class to `[\s.‐-―−­]+`. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `vi.resetAllMocks()` cleared the `currentDatetimeString` mock** — set in `vi.mock` factory via `mockReturnValue`; `resetAllMocks` in `beforeEach` cleared it. Fix: rebind in `beforeEach`. Re-encountered 2026-05-15 with the bulk route test — same fix. [source](dev-journal/2026-05-11.md), [2026-05-15](dev-journal/2026-05-15.md)
- **2026-05-11 — `legacySlug.cachedLookup` missed in Phase 2** — added `datetime <= now` filter to `getPostBySlug` etc but the legacy-slug lookup still filtered `published: true` only; future-dated published post matching a legacy URL would 308-redirect to a canonical page that 404s. Fix: same filter pattern. [source](dev-journal/2026-05-11.md)
- **2026-05-11 — `readErrorMessage` × Zod-array regression** — `parseJsonBody` returned `{ error: ZodIssue[] }` but `readErrorMessage` typed `data.error` as `string` and coerced arrays to `[object Object],[object Object]`. tsc didn't catch because `await response.json()` is `any`. Fix: `Array.isArray` branch with `formatZodIssues` helper. [source](dev-journal/2026-05-11.md)
- **2026-05-12 — `<time dateTime>` was emitting `yyyy-MM-dd-HHmm`** — prior reviews missed it; SR readers and Schema.org consumers all parsed wrong. Fix: route both `PostCard` and `PostContent` through `postDatetimeToISO`. Added `<time dateTime>` to attribute-level audit targets. [source](dev-journal/2026-05-11.md)
- **2026-05-13 — Pre-existing `<main>` regression in `PrivacyPageLayout.tsx`** — file wasn't touched in the 11-commit Phase 2 hoist; kept rendering its own `<main>` inside the now-hoisted root `<main>`. Both `/privacy` and `/privacy/body-tracking` had two landmarks per document. Fix: convert to `<div>`. Watch-out's explicit trigger from 2026-05-12 fired correctly. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — Unmount-after-resolve race in `useOptimisticMutation`** — cleanup aborted the controller but didn't null `abortRef.current`; a buffered non-ok response could pass the `controller !== abortRef.current` guard on an unmounted component. Surfaced by a test premised on "should pass with the current implementation". Fix: null the ref in cleanup. [source](dev-journal/2026-05-13.md)
- **2026-05-13 — ESLint rule contradiction: `sonarjs/void-use` vs `void promise`** — 6 sites used `void result.current.mutate(...)`; `sonarjs/void-use` rejects the operator outright. Verified `no-floating-promises` isn't enabled; dropped `void`. [source](dev-journal/2026-05-13.md)
- **2026-05-14 — Upstash idle warnings persisted after two schedule tightenings** — `0 0 1,11,21 * *` has 10-11 day gaps; Upstash free-tier idle threshold ~7 days. Fix: schedule → daily. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — Admin dashboard fires N background RSC requests on every load** — `<Link>` prefetches on viewport entry; PostsTab/ProjectsTab edit links each fire a server-component fetch hitting Prisma. Fix: `prefetch={false}` per-row. [source](dev-journal/2026-05-14.md)
- **2026-05-14 — `malformed datetime` tests in feed + sitemap broke after the read-time-filter refactor** — old tests used `datetime: "not-a-date"` to exercise downstream rendering fallbacks; mocked `findMany` ignored the where-clause so the tests passed but exercised an unreachable code path in production. Fix: switched test datetime to `"2020-bad-format"` (past-lex, fails the regex). [source](dev-journal/2026-05-14.md)
- **2026-05-17 — Optimistic-toggle revert used stale `initialPublished`/`initialIsFeatured`** — `onRevert` captured the *original* first-render prop; after a successful toggle + `router.refresh()` re-rendered the parent with the new server value, a subsequent failed toggle reverted to the now-stale baseline. Fix: capture `const prev = isOn` before optimistic set; collapsed both toggles into `BooleanFlagToggle`. [source](dev-journal/2026-05-17.md)
- **2026-05-17 — Bulk route `skipDuplicates: true` silently ate concurrent-write losers** — response reported `created: <returned>` without naming the dropped file. Fix: post-insert slug-set diff pushes losers into `skipped` with explicit reason. [source](dev-journal/2026-05-17.md)

## Environment / Config Changes

- **2026-05-07** — `vitest.config.ts` include broadened to `src/**/*.test.tsx` (was `src/components/**/*.test.tsx`). [source](dev-journal/2026-05-07.md)
- **2026-05-07** — `src/test/setup.ts` now spies `console.warn` and `console.info` in addition to `console.error`. [source](dev-journal/2026-05-07.md)
- **2026-05-07** — New env var: `ALLOW_UPLOADS` (must equal `"true"` to enable `/api/upload`). [source](dev-journal/2026-05-07.md)
- **2026-05-11** — `posts.test.ts` mock factory: added `findFirst: vi.fn()`. `posts/[id]/route.test.ts`: added `$transaction: vi.fn()`. `upload/route.test.ts`: `pngFile` helper now prefixes the magic header. [source](dev-journal/2026-05-11.md)
- **2026-05-12** — `prisma/schema.prisma`: added `@@index([slug])` to `Post`. Run `yarn db:push` to apply. New files: `src/lib/readErrorMessage.ts` (+ test) — moved from `src/components/admin/useAdminResource.ts`. `src/lib/useOptimisticMutation.ts` (+ test). [source](dev-journal/2026-05-12.md)
- **2026-05-13** — New: `src/components/privacy/PrivacyPageLayout.test.tsx`. `src/lib/auditLog.ts` added `ADMIN_AUDIT_TAGS` const array. `src/lib/useOptimisticMutation.ts` exports `MutateResult` type. New helper: `sanitizeLogString` in `src/app/api/upload/route.ts`. [source](dev-journal/2026-05-13.md)
- **2026-05-14** — `vercel.json`: cron schedule → `0 0 * * *`. New env var: `IP_HASH_SECRET` (HMAC secret for IP pseudonymization). New file: `src/lib/keepalive.ts` (exports `KEEPALIVE_KEY`). New route: `POST /api/admin/keepalive`. New UI: "Run keepalive" button in `AdminNav`. [source](dev-journal/2026-05-14.md)
- **2026-05-15** — New audit tag `[api:admin:posts:BULK]` added to `ADMIN_AUDIT_TAGS`. New file: `src/lib/bulkImportParser.ts` + test. New route: `POST /api/admin/posts/bulk`. New page: `/admin/posts/bulk`. `src/lib/keepalive.ts` now exports `writeKeepalive(redis): KeepaliveResult`. [source](dev-journal/2026-05-15.md)
- **2026-05-17** — `src/app/api/upload/` moved to `src/app/api/admin/upload/` (via `git mv`); client at `ImageUpload.tsx` posts to `/api/admin/upload`. `src/lib/keepalive.ts` now exports `getKeepaliveRedis(): Redis | null` (lazy memoized). `AdminAuditPayload` extended with required `batchId: string | null`. `BULK_MAX_FILES` exported from `src/lib/schemas.ts`. New: `BooleanFlagToggle.tsx` + test (replaces `PostPublishedToggle` + `IsFeaturedToggle`). New: `PostsTab.test.tsx`, `keepalive.test.ts`. `format.ts` exports `isFutureDatetime(datetime, now)`. [source](dev-journal/2026-05-17.md)

## Learnings

- **Server components don't invoke their child components synchronously** — `<PostForm initialData={...} />` is `React.createElement(PostForm, props)`, not a render. Assert on element `props`, not `vi.fn()` mock calls. [source](dev-journal/2026-05-07.md)
- **`vi.mock` factories hoist above top-level `const`** — referencing a `vi.fn()` declared at module scope inside a factory fails with a hoisting error. Inline the mock or use `vi.hoisted`. [source](dev-journal/2026-05-07.md)
- **`vi.resetAllMocks` clears `mockReturnValue` set in `vi.mock` factories** — rebind in `beforeEach` after `resetAllMocks`. Hit twice this month (sitemap + bulk route). [source](dev-journal/2026-05-11.md), [source](dev-journal/2026-05-15.md)
- **`vi.mock` with `...actual` doesn't rewrite intra-module references** — if helper `foo` calls another export `bar` of the same module, mocking `bar` does NOT redirect `foo`'s internal call; the `...actual` exports retain lexical bindings to the real `bar`. Mitigation: design helpers to take dependencies as explicit args (this is why `isFutureDatetime(datetime, now)` has `now` required), or mock the consuming function. [source](dev-journal/2026-05-17.md)
- **`role="alert"` already implies assertive live-region** — stacking `aria-live="polite"` is contradictory. [source](dev-journal/2026-05-07.md)
- **Schema-level slug validation catches a class of failures the unique constraint can't articulate** — `min(1)` is necessary but not sufficient. Any derived-field-from-input pattern (slug from title, hash from body) should refine at the schema, not trust the derivation downstream. [source](dev-journal/2026-05-11.md)
- **For unique-constraint filters in cached fetchers**: the original "capture `now` inside the cached fn" approach (2026-05-11) was reversed on 2026-05-14 — push the time-dependent comparison *out* of the cached function and into the wrapper that reads from it; cache becomes time-independent, wrapper handles fresh `now` per request. [source](dev-journal/2026-05-14.md)
- **Don't trust a test that passes because the mock skipped a filter** — when refactoring a filter from DB-side to JS-side, audit tests that depend on the filter being a no-op. [source](dev-journal/2026-05-14.md)
- **A test you wrote can expose a real implementation gap** — the unmount-after-resolve test in 2026-05-13 was meant to pin behavior assumed correct; instead it surfaced a real cleanup bug. When a test premise that "should pass with the current implementation" fails, first hypothesis should be "implementation has a gap," not "test is wrong." [source](dev-journal/2026-05-13.md)
- **Concrete trigger conditions in watch-outs do their job** — the 2026-05-12 watch-out about new pages adding their own `<main>` had an explicit trigger; the 2026-05-13 review fired on a *pre-existing* page the hoist sweep missed. Will keep writing watch-outs with explicit triggers, not just "revisit later." [source](dev-journal/2026-05-13.md)
- **Discriminant unions land cheap when callers destructure just the shared discriminator** — `{ ok: true } | { ok: false; reason: ... }` widened `useOptimisticMutation`'s return; both existing callers continue to destructure just `ok` (`boolean` on the union). Rule of three doesn't apply when the abstraction extension is invisible to existing consumers. [source](dev-journal/2026-05-13.md)
- **App Router `<Link>` prefetches on viewport entry, not just hover** — N edit links on a dashboard fires N background RSC fetches, each running its target page's server code. `prefetch={false}` per-link is the lever. [source](dev-journal/2026-05-14.md)
- **`@upstash/ratelimit` handles TTL automatically** — `PEXPIRE currentKey, window*2+1000`. For `slidingWindow(5, "15 m")`, each bucket gets ~30min TTL. GDPR retention is library-default. [source](dev-journal/2026-05-14.md)
- **IPv4 plain-hash reversibility** — keyspace ~4B; `SHA-256(ip)` is brute-forceable in minutes. HMAC with a server-held secret is the cheap fix; secret doesn't enable reversal (HMAC is one-way), blocks rainbow-table precomputation. [source](dev-journal/2026-05-14.md)
- **`null` vs `undefined` is a context call, not a global rule** — CLAUDE.md prefers `null` checks in TS, but that's about object shapes (intentional absence in a record). Function return values feeding React props or Next `Metadata` fields should return `undefined` — those types are written `T | undefined`, drops `?? undefined` boilerplate. [source](dev-journal/2026-05-12.md)
- **Single `<main>` per document is the App Router convention** — multiple `<main>` is invalid HTML5; layout-owned `<main>` with pages rendering plain wrappers is the structural norm. [source](dev-journal/2026-05-12.md)
- **`sonarjs/cognitive-complexity` (limit 15)** — bites when concentrating route-handler logic. Extract per-file loops and side-effect emitters into named helpers; the rule passes and readability improves. [source](dev-journal/2026-05-17.md)
- **Required typed fields propagate cleanly through tsc** — adding `batchId: string | null` to `AdminAuditPayload` surfaced as type errors on every one of the 7 single-row callers, making "did I miss a site?" trivial. Worth using proactively when extending shared payload shapes. [source](dev-journal/2026-05-17.md)
- **Phase-aligned commits require intermediate-state authoring for files that span phases** — `PostsTab.tsx` participated in commits 1 and 2; `bulk/route.ts` participated in commits 3 and 5. The cleanest path: stage commit N's version of the file, commit, then edit to commit N+1's version. Backup-to-`/tmp` is the failsafe when reverting via `git checkout HEAD --` between phases. [source](dev-journal/2026-05-17.md)

## Still open at month-end

### Open Threads

(Latest snapshot — 2026-05-17 — has no listed open threads. Earlier-month open threads worth tracking even though they don't appear in the latest daily:)

- [ ] **Apply `Post.slug` index** — `prisma/schema.prisma` 2026-05-12 added `@@index([slug])`; needs `yarn db:push`. Last seen as Open Thread on 2026-05-13. [dev-journal/2026-05-12.md](dev-journal/2026-05-12.md)
- [ ] **Set `IP_HASH_SECRET` in Vercel env** — rate-limiter silently fails-open without it (warn at module load). Last seen as Open Thread on 2026-05-14. [dev-journal/2026-05-14.md](dev-journal/2026-05-14.md)
- [ ] **Verify production keepalive flow** — log into `/admin`, click "Run keepalive", confirm `keepalive:last` updates in Upstash. Last seen 2026-05-14. [dev-journal/2026-05-14.md](dev-journal/2026-05-14.md)
- [ ] **Confirm cron stops Upstash idle warnings** — after first daily cron tick post-deploy. Last seen 2026-05-14. [dev-journal/2026-05-14.md](dev-journal/2026-05-14.md)
- [ ] **Sitemap future-dated posts** — superseded by 2026-05-14 read-time-filter pivot; verify before closing. [dev-journal/2026-05-07.md](dev-journal/2026-05-07.md)
- [ ] **Admin auth single source** — middleware (`src/proxy.ts`), `(protected)/layout.tsx`, and `generateMetadata` each enforce admin gating independently. [dev-journal/2026-05-07.md](dev-journal/2026-05-07.md)

### Watch-outs & Tech Debt

- **`vi.mock` with `...actual` doesn't rewrite intra-module references** — helper `foo` calling another export `bar` of the same module is NOT redirected by `vi.mock("M", { ...actual, bar: vi.fn() })`. Mitigation: pass dependencies as explicit args, or mock the consumer. [first flagged: 2026-05-17]
- **Auto-allow hook blocks paths containing `/opt`** (case-insensitive substring match) — affected the natural `OptimisticBooleanToggle.tsx` naming, forced rename to `BooleanFlagToggle.tsx`. [first flagged: 2026-05-17]
- **Bulk import body-size budget** — schema caps each file at 100KB and the batch at 50, worst-case payload ~5MB. Vercel App Router's default JSON body limit hasn't been exercised that hard. Revisit if a real bulk run hits 413. [first flagged: 2026-05-15]
- **Bulk import filename ambiguity** — title is captured verbatim from the filename; a title that starts with `HHmm-` would be eaten by the optional time group. Acceptable today. [first flagged: 2026-05-15]
- **IP_HASH_SECRET unset on a Redis-configured deploy** — rate-limiter falls open silently (warn at module load only). [first flagged: 2026-05-14]
- **Vercel Hobby cron auto-pause** — crons can be paused after project inactivity even with the schedule in place. [first flagged: 2026-05-14]
- **`keepalive:last` has no TTL** — intentional, but if the cron stops running, the stale timestamp lingers. A future alert should compare the ISO against `now`, not check key existence. [first flagged: 2026-05-14]
- **Durable audit sink — DO NOT PROPOSE** — the `auditLog` `console.info` sink with ~1h Vercel Hobby retention is a deliberate trade. Re-introduce paired with external aggregation, not before. Future code reviews should treat this as a closed decision. [first flagged: 2026-05-12]
- **`auditLog` complexity vs current value** — the structured 6-key (now 7-key) payload was sized for a future aggregator. The shape still helps grep, but is over-engineered for single-admin use. Don't extend without re-evaluating the carrying cost. [first flagged: 2026-05-12]
- **Posts PUT 500 under serialization conflict** — escalated to Serializable; `serialization_failure → 500` with no retry loop (also not client-retriable; admin must re-issue manually). At single-admin volumes, conflicts are essentially impossible. Revisit if multi-admin lands. [first flagged: 2026-05-12]
- **Single `<main>` lives in root layout** — `src/app/layout.tsx` owns the document `<main>`; pages render plain wrappers. New pages dropping their own `<main>` produce two landmarks. Trigger: code-review on any new top-level page. (Fired correctly on 2026-05-13 for `PrivacyPageLayout.tsx`.) [first flagged: 2026-05-12]
- **`postDatetimeToISO` returns `string | undefined`** — `null` was rejected as boilerplate. Schema regex is the load-bearing gate; `undefined` covers legacy DB rows. Trigger: 5th caller wanting explicit `null` semantics. [first flagged: 2026-05-12]
- **ESLint rule conflict: `sonarjs/void-use` vs `void promise`** — write fire-and-forget Promises inside `act()` as bare statements, not `void X`. [first flagged: 2026-05-13]
- **`useOptimisticMutation` MutateResult discriminant is unconsumed today** — both callers still just destructure `ok`. Collapse trigger: a third caller that doesn't differentiate either; keep trigger: a caller that skips a toast on supersession. [first flagged: 2026-05-13]
- **`metadata.ts` double-brand dev guard is dev/test-only** — `next build` won't catch a pre-branded title. Escalate to an always-on throw if a regression ships. [first flagged: 2026-05-13]
- **Future-dated published post auto-surface (reversed 2026-05-14)** — read-time filter now applies fresh `now` per request; padded caches may carry dead rows until next mutation. ~100KB worst case per section, bounded. [first flagged: 2026-05-11, reversed: 2026-05-14]
- **`parseJsonBody` 400 response widens silently** — schema-validation 400s return `{ error: ZodIssue[] }` while every other handler returns `{ error: string }`. `readErrorMessage` handles both, but `await response.json()` is typed `any`. Revisit on a third response-error shape or a typed API client. [first flagged: 2026-05-11]
- **`<Link>`-for-external-URL pattern** — `next/link` for absolute / `mailto:` / static-asset hrefs adds client component overhead. Prefer `<a className="link">` for any non-route href. [first flagged: 2026-05-09]
- **`error.digest` removed from both error boundaries** — re-introduce paired with external error aggregation (Sentry / Logflare / Axiom / Vercel Pro+). [first flagged: 2026-05-09]
- **Per-IP login rate-limit bucket** — `clientBucketKey` falls back to `"unknown"` when `x-forwarded-for` is absent; on a bare-metal deploy without a reverse proxy, every attempt hits one shared bucket. [first flagged: 2026-05-07]
- **`src/proxy.ts` auth gate** — discrete equality/prefix checks for protected API roots; adding a new protected root means touching a disjunction. (Partially mitigated 2026-05-17 — `/api/upload` consolidated under `/api/admin/`.) [first flagged: 2026-05-07]
