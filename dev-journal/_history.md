# Rolling History — last 30 days (through 2026-04-26)

## Themes

- **The `/simplify` sweep arc (2026-04-16 → 2026-04-17)** — opened 2026-04-16 with targeted code-health wins (`projectInclude` extracted, `capitalizeSection`, `AnimatedCard`, `searchPosts` pushed to Prisma `OR`, `MarkdownEditor` via shared `markdownToReact`, `SectionManager` stable `_key: crypto.randomUUID()`, error surfacing on `IsFeaturedToggle`/`ProjectSortOrderInput`, `ProjectForm` `isDeleting`, `LoanCalculatorClient` `parseNum`); then went wide on 2026-04-17 Session 1 with 6 parallel agents across 10 commits / 96 files. Session 2 absorbed the review: Prisma `Section` enum (replacing `as Section` casts), `PresetOrFreeformInput` derived-mode, `MarkdownEditor` lost its redundant `setTimeout` and gained a real `.catch`, schema `.max()` bounds, shared fixtures (`src/test/fixtures.ts`, `src/test/mocks/nextCache.ts`). [source](dev-journal/2026-04-16.md), [source](dev-journal/2026-04-17.md)
- **Legacy-redirect architecture rewrite (2026-04-17 Session 3)** — replaced the `/api/legacy-redirect/[slug]` + middleware rewrite with a root-level catch-all page (`src/app/[slug]/page.tsx`) using `permanentRedirect(308)` on hit and `notFound()` on miss. `KNOWN_ROUTES` allowlist and the middleware rewrite branch were deleted; static-over-dynamic dispatch handles known routes. Side effect: `proxy.ts` cognitive-complexity lint dropped under threshold. Root cause: API route's `notFound()` returns raw JSON, not the styled 404 page. [source](dev-journal/2026-04-17.md)
- **Second review pass → targeted fixes (2026-04-17 Session 4)** — `ProjectSortOrderInput` 1-indexed UI / 0-indexed DB translation (`419595f`); Zod `.min(0)` across create/update/sections/links/images; `ProjectForm` input `min={0}`. `defaultProto` port-heuristic removed (`5994371`) — `example.com:8443` was silently getting `http`. Feed `<updated>` uses `Math.max` across `updatedAt` (`0b0ebbe`) per RFC 4287. `navLinks` hoisted to `src/lib/navigation.ts` with `as const` (`245869b`). `revalidatePostSection` test asserts `"posts"` tag + `toHaveBeenCalledTimes(3)` (`60b5a3c`). Project `sortOrder` single-writer invariant documented inline (`2c2199e`) rather than adding `@@unique` (would need `DEFERRABLE INITIALLY DEFERRED`, unexpressible in Prisma DSL). [source](dev-journal/2026-04-17.md)
- **`unstable_cache` wrapper-per-call anti-pattern (2026-04-20 Session 1)** — production 500 on `/api/feed/:section` (`Cannot read properties of undefined (reading 'getTime')`) surfaced that `unstable_cache` JSON-serializes `Date` → string on cache hits; fresh returns `Date`, cached returns string. Fix: normalize to ISO at cache boundary. Same investigation found three call sites rebuilding the wrapper per call. `lookupLegacySlug` lifted to module scope (dropped dead `legacy-redirect-${slug}` tag — grep-confirmed); `getPostBySlug`/`getProjectBySlug` use `Map<slug, wrapper>` because the per-slug tags in `projects.ts` **are** revalidated by admin routes. [source](dev-journal/2026-04-20.md)
- **Atom feed content quality (2026-04-20 Session 2 → 2026-04-21)** — `excerptFromBody` emitted raw markdown in `<summary>`. Picked AST walk over regex or `strip-markdown` package: `unified` + `remark-parse` + `remark-gfm` already in deps; `textOnlyProcessor` module-level, matching `markdownProcessor`. Block nodes trailing `\n`; fenced code skipped; inline code kept. `unified().parse()` is synchronous (only `.run()` is async). Follow-on 2026-04-21 closed the four review findings: `extractText` now returns `node.alt` for `image`/`imageReference` (silent drop of alt-text in summaries); feed route gained an `escapeCdata` helper (`]]>` → `]]]]><![CDATA[>` split) so authored HTML can't terminate the `<content>` CDATA section; `MdastNode` shim replaced by typed `Nodes` import from `mdast`; new tests cover image alt, CDATA escape, and the raw-HTML-drop contract of `markdownToHtml`. [source](dev-journal/2026-04-20.md), [source](dev-journal/2026-04-21.md)
- **Auth + env hardening pass (2026-04-26)** — daily code review surfaced 9 findings; 8 actioned, 1 deferred (per-request `safeParse(process.env)` left as-is). `env.ts` gained a typed `EnvConfigError` subclass (`code: "ENV_MISSING"` + `varName`) and a hex regex on `ADMIN_HASH_PASSWORD` only — initial mistake added it to `SESSION_SECRET` too based on a misleading CLAUDE.md note ("hex-encoded"); actual usage is `TextEncoder().encode()`, no hex parsing. CLAUDE.md updated to "any string". `verifyCredentials` precomputes a dummy bcrypt at module load and runs `bcrypt.compare` against it on the no-credentials path so the timing-defense contract holds whether or not creds are configured. Cron Bearer compare via `crypto.timingSafeEqual` (with same-length dummy compare on length mismatch) inside an `isAuthorized` helper. Both auth/cron routes now pass the resolved Redis config object to `new Redis({...})` instead of `Redis.fromEnv()` (single source of truth in `env.ts`). Login adds `console.info` audit log on success. Two pre-existing UI bugs surfaced in the same review and fixed: `ProjectContent.tsx:77` interpolated nullable `accentColor` instead of the `accent` fallback into `color-mix()` (invalid CSS on no-accent projects); `ProjectForm` `Number(e.target.value)` produced `NaN` on partial-entry/cleared `sortOrder` — fixed with `Number.isFinite` guard. [source](dev-journal/2026-04-26.md)

## Timeline

- **2026-04-16** — `vercel.json` cron: `0 0 1,15 * *` → `0 0 1,11,21 * *` (DB keepalive on free tier). [source](dev-journal/2026-04-16.md)
- **2026-04-16** — `unstable_cache` mock in tests replaced by `src/test/mocks/nextCache.ts` identity-passthrough + `revalidateTag` spy. [source](dev-journal/2026-04-16.md)
- **2026-04-17** — Prisma `Section` enum migration requires `yarn db:push` as deploy gate. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — `FadeIn` tag union widened to `keyof typeof motion`; one bounded `as React.ElementType` cast inside component body. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — Upload route tests thorough: every HTTP response code (403, 400×2, 413, 415×2, 200×2, 500) as its own test; `sanitizeFilename` covers path-traversal and all-stripped inputs. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — `PrivacyLink` deleted (Session 5 drive-by). [source](dev-journal/2026-04-17.md)
- **2026-04-18** — `lookupLegacySlug` gains `published: true` filter; `defaultProto` exported with 7 test cases; upload route inline comment for all-stripped key edge case. [source](dev-journal/2026-04-17.md)
- **2026-04-19** — `defaultProto` false-positive on `localhost`-prefix hostnames (`localhostapp.example.com`) fixed via exact-match after `host.split(":")[0]`; `siteBase` gains 4 tests. [source](dev-journal/2026-04-17.md)
- **2026-04-20** — Feed 500 fixed via ISO normalization inside cached fn; three wrapper-per-call anti-patterns resolved across `legacySlug.ts`/`posts.ts`/`projects.ts`. [source](dev-journal/2026-04-20.md)
- **2026-04-20** — `stripMarkdown` via `unified` AST walk added; `excerptFromBody` passes bodies through it before slicing. 10 new tests in `markdown.test.ts`. [source](dev-journal/2026-04-20.md)
- **2026-04-20** — `ADMIN_PASSWORD_HASH` → `ADMIN_HASH_PASSWORD` env var rename (code-side only; Vercel config update required). [source](dev-journal/2026-04-20.md)
- **2026-04-20** — `FooterYear` `react-hooks/set-state-in-effect` watch-out resolved in commit `aa583a7` (dropped the effect, `suppressHydrationWarning` on stale caches). [source](dev-journal/2026-04-20.md)
- **2026-04-21** — Discovered `remark-rehype` drops raw HTML by default (no `allowDangerousHtml`) — `markdownToHtml` is implicitly safer than assumed; test now locks the drop, not the passthrough. [source](dev-journal/2026-04-21.md)
- **2026-04-21** — mdast walk in `stripMarkdown` switched from local `MdastNode` shim + `as unknown as` cast to typed `Nodes` import from `mdast`; narrowing via `"value" in node` / `"children" in node`. [source](dev-journal/2026-04-21.md)
- **2026-04-26** — `vi.fn().mockImplementation(() => instance)` is not a constructor — Redis mock now uses class literal (`class { ping = vi.fn().mockResolvedValue(...) }`). [source](dev-journal/2026-04-26.md)
- **2026-04-26** — CLAUDE.md `SESSION_SECRET` env-var note updated from "JWT signing secret (hex-encoded)" to "(any string)" to match `TextEncoder().encode()` consumer. [source](dev-journal/2026-04-26.md)
