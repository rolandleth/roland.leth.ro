# Rolling History — last 30 days (through 2026-06-15)

## Themes

- **Project platform restructure** (May 20–21) — `Project.platform: String` → `PlatformBucket` enum + `PlatformTag[]` array; Postgres enum types; `compactLabel` with Fullstack rule (family-set union of `WEB_FRONTEND_TAGS` / `WEB_BACKEND_TAGS`, `Next.js` cross-listed in both); schema coherence `superRefine` validates bucket/tag compatibility via `projectFields` map (shared between create and update schemas); two-phase deployment accepted (brief broken window). `@default([])` on `platformTags` required; detail page 500 from null `platformTags` fixed. Picker UX: dropped hidden required `readOnly` input; `role="status" aria-live="polite"` hint for pruned tags; `getProjectsGalleryCached()` + `getProjectsForAdmin()` split for distinct caching needs; 6 of 10 review findings were false positives. [source](dev-journal/2026-05-20.md) [source](dev-journal/2026-05-21.md)

- **`Post.summary` required field** (May 18) — `Post.summary: String NOT NULL`; `deriveSummary` strips markdown via AST, truncates at `SUMMARY_MAX_CHARS = 160` on word boundary, hard-slice fallback; edit-route resolution logic (authored → keep; cleared → re-derive; body-changed + untouched → re-derive; unchanged → skip). [source](dev-journal/2026-05-18.md)

- **`src/lib/` reorganization + test infrastructure** (May 18–22) — 6-bucket layout (db / auth / api / content / client / utils), single commit `8d8fa8b`, 140 files changed, 220 import paths rewritten; session lockout from `cd src/lib` hook relative-path issue (lesson: hooks use project-root-relative paths). `setupUser({delay:null})` cuts bcrypt wait; `AnimatePresence mode="sync"` global mock; ~47% test time reduction. `nextCacheSpyFactory` + module-load snapshot to pin cache-wrap set; `mockPickerConfig.autoFill: "both" | "bucket-only" | "off"`. [source](dev-journal/2026-05-18.md) [source](dev-journal/2026-05-22.md)

- **Manifest-driven project importer** (June) — `scripts/import-projects.ts` + per-project `project.json`; idempotent re-run via content-addressed blob keys (`SHA256[:64-bit]`); `blobSync.ts` extracted with `BlobStore` DI interface (testable shell); orphan pruning post-import (gated by `--no-prune`); blob listing paginated with `hasMore`/cursor loop; bounded-concurrent uploads (4 parallel). Key evolution: path-based keys (06-08) → `SHA256[:12]` content-addressed (06-10) → 64-bit width + byte-size backstop (06-13). [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-06-10.md) [source](dev-journal/2026-06-13.md)

- **Continuum legal pages cluster** (June) — `PrivacyPageLayout` DRY refactor (06-08); Continuum DPIA + responsible-use pages added (06-10); `LegalPageLayout` generalized from `PrivacyPageLayout` with `documentKind: "privacy" | "terms"` param (06-13); `AppleLegalLink` component extracted with `target="_blank" rel="noopener noreferrer"` convention; `continuumLegalLinks.ts` cross-links the four Continuum legal pages in a "Related" section. Legal dates kept at true substantive-revision dates (not bumped on layout-only changes). [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-06-13.md)

- **Per-IP auth rate limiting hardening** (June) — `IP_HASH_SECRET` HMAC added 06-10: `clientBucketKey` HMACs the leftmost XFF IP (no PII logged); `globalBucketKey` fallback when secret absent. Keyspace partitioned 06-13 (`ip:<hash>` vs `"global"` literal; `rl:login` prefix non-shared). Log level split: 429 → `info`, fail-open → `error`. Bucket key hoisted to outer scope (eliminates double HMAC per failed login). Five new tests added 06-13. [source](dev-journal/2026-06-10.md) [source](dev-journal/2026-06-13.md)

## Timeline

- **2026-05-17** — `/api/upload` → `/api/admin/upload`; `BooleanFlagToggle` collapses `PostPublishedToggle` + `IsFeaturedToggle`; `isFutureDatetime(datetime, now)` with required `now` (vi.mock intra-module reference gotcha); `AdminAuditPayload.batchId: string | null` required; lazy `getKeepaliveRedis()`; `prepareBatch` + `emitSkipSummary` extracted; optimistic-toggle revert bug fix (capture `const prev = isOn` before optimistic set). [source](dev-journal/2026-05-17.md)
- **2026-05-18** — `Post.summary: String NOT NULL`; `deriveSummary` (AST strip, 160-char word-boundary truncation); `setupUser({delay:null})`; `AnimatePresence mode="sync"` global mock; `src/lib/` 6-bucket (140 files, 220 imports, commit `8d8fa8b`); happy-dom `<form>` Proxy identity bug (diagnosed, not fixed). [source](dev-journal/2026-05-18.md)
- **2026-05-20** — `PlatformBucket` enum + `PlatformTag[]`; Postgres enums; `compactLabel` Fullstack family-set rule; `@default([])` on `platformTags`; two-phase deployment. [source](dev-journal/2026-05-20.md)
- **2026-05-21** — Coherence `superRefine` (bucket/tag); picker UX; `getProjectsGalleryCached`/`getProjectsForAdmin` split; 6 of 10 review findings false positives. [source](dev-journal/2026-05-21.md)
- **2026-05-22** — `nextCacheSpyFactory` + module-load snapshot; `mockPickerConfig.autoFill: "both" | "bucket-only" | "off"`. [source](dev-journal/2026-05-22.md)
- **2026-06-08** — `scripts/import-projects.ts` + `project.json`; `PrivacyPageLayout` DRY refactor; `scripts/imports/` gitignore narrowed; `app-copy-to-project` skill. [source](dev-journal/2026-06-08.md)
- **2026-06-10** — Content-addressed blob keys (`SHA256[:12]`); `IP_HASH_SECRET` HMAC per-IP rate limit; Continuum DPIA + responsible-use pages. [source](dev-journal/2026-06-10.md)
- **2026-06-13** — `blobSync.ts` with `BlobStore` DI; orphan pruning + `--no-prune`; blob pagination; content-hash widened 48→64-bit; `LegalPageLayout` generalized; `AppleLegalLink` standardized; keyspace partition; log level split; HMAC double-call eliminated; `FeaturedProjectCard` equal-height via flexbox; Twitter → Bluesky contact link. [source](dev-journal/2026-06-13.md)
