# Rolling History — last 30 days (through 2026-05-18)

## Themes

- **Simplify/restructure sweep (April)** — April 20: feed 500 regression fixed — `unstable_cache` boundary was wrapping non-JSON-serializable data; pattern locked: serialize before caching ([2026-04-20](dev-journal/2026-04-20.md)). April 21: `stripMarkdown` improvements ([2026-04-21](dev-journal/2026-04-21.md)).

- **Security & input hardening** — Timing-safe bearer token comparison + env validation tightened ([2026-04-26](dev-journal/2026-04-26.md)). May 7: per-IP login rate-limit replaced global bucket — `clientBucketKey` keyed on leftmost `x-forwarded-for` IP; fail-open on Upstash/Redis error so a Redis blip doesn't lock the only public auth entry out; `ALLOW_UPLOADS === "true"` env flag replaced `NODE_ENV !== "production"` gating ([2026-05-07](dev-journal/2026-05-07.md)). May 11: magic-byte image-MIME sniff via `detectImageMime` (PNG/JPEG/WEBP/GIF/AVIF `ftyp` brands accepted; SVG deliberately excluded for XSS shape) — read 12 bytes from already-buffered formData, near-free; `parseJsonBody` schema-validation logs `path` only, never values, with a test pinning "submitted value never appears in log calls" ([2026-05-11](dev-journal/2026-05-11.md)). May 13: `sanitizeLogString` strips CR/LF/TAB/NUL + clamps to 200 chars before logging the multipart parser's `error.message`, closing a log-injection vector ([2026-05-13](dev-journal/2026-05-13.md)). May 14: `IP_HASH_SECRET` env var + `getIpHashSecret()` accessor; `clientBucketKey` now HMAC-SHA256s the IP and truncates to 22 base64url chars before using it as the Upstash bucket key. Rate-limit gate widened to require both Redis and the secret (fail-open if either missing, warn at module load on secret-missing-while-Redis-set). Rationale: under GDPR (CJEU *Breyer* C-582/14) IP is personal data; rate-limiting falls under legitimate interest, but plain SHA-256 of an IP is reversible in practice (IPv4 keyspace ~4B is brute-forceable), so HMAC with a server-held secret blocks rainbow-table reversal. Library-default `PEXPIRE window*2+1000` (~30min for `slidingWindow(5, "15 m")`) covers retention without manual purge ([2026-05-14](dev-journal/2026-05-14.md)). May 17 moved `/api/upload` under `/api/admin/upload`, dropping the `pathname === "/api/upload"` exact-match branch in `src/proxy.ts` — the route is now gated by the generic `startsWith("/api/admin/")` admin gate alongside posts/projects/keepalive. One namespace, one gate; future sub-paths under the upload route auth-gate by default rather than falling through. Log tag renamed `[api:upload:POST]` → `[api:admin:upload:POST]` to match the path-shape convention used elsewhere in `src/app/api/` ([2026-05-17](dev-journal/2026-05-17.md)).

- **Admin write contract: unified audit log + abort-aware optimistic mutations** — May 7 introduced `AbortController` on every admin mutation (`useRef` + `useEffect` cleanup) and extracted `<ErrorMessage>` (7 callers) ([2026-05-07](dev-journal/2026-05-07.md)). May 11 Session 2 added the typed `auditLog(tag, payload)` helper with a fixed 6-key shape (`id, slug, section, sortOrder, previousSection, previousSlug`); Session 3 tightened `AdminAuditPayload` from optional `?` to required `T | null` so silent-null regressions in posts/projects PUT surface at tsc instead of in logs; `isAbortError` helper extracted (5 sites, `DOMException` + `Error` shape) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 typed `AdminAuditTag` as a closed union, unified `previousSlug` audit semantics across posts/projects PUT to "the slug renamed, here's what it was" (not "name was edited"), and extracted `useOptimisticMutation` at the 2nd consumer (`IsFeaturedToggle` + `ProjectSortOrderInput` collapse to ~30 LOC each) ([2026-05-12](dev-journal/2026-05-12.md)). May 13 derived `AdminAuditTag` from a `const ADMIN_AUDIT_TAGS = [...] as const` array (single edit site for new admin resources, runtime list now greppable); widened `MutateResult` to `{ ok: true } | { ok: false; reason: "failure" | "superseded" }`; nulled `abortRef.current` in the hook's cleanup to close an unmount-after-resolve race a new test exposed ([2026-05-13](dev-journal/2026-05-13.md)). May 15 added the `[api:admin:posts:BULK]` tag for the new bulk-import endpoint — same 6-key payload, one line per created row ([2026-05-15](dev-journal/2026-05-15.md)). May 17 Session 2 collapsed `PostPublishedToggle` + `IsFeaturedToggle` into a shared `BooleanFlagToggle` after the 2026-05-16 review surfaced a revert-baseline bug both shared: `onRevert` captured the *first-render* `initial` prop, so after a successful toggle + `router.refresh()` re-rendered the parent with the new server value, a subsequent failed toggle reverted to the now-stale baseline. Fix: capture `const prev = isOn` BEFORE the optimistic `setIsOn(next)`. Both `useOptimisticMutation` failure branches (non-ok, rejection) now `console.error`. Same session added required `batchId: string | null` to `AdminAuditPayload` — `null` on the 7 single-row handlers, a per-request `randomUUID()` on `[api:admin:posts:BULK]` so a 50-file run is grep-collapsible in the ~1h Vercel log retention window ([2026-05-17](dev-journal/2026-05-17.md)).

- **Future-dated content + cache invariants** — May 11 Session 1 cascaded `datetime <= now` filter across `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams`, `legacySlug.cachedLookup`. `findUnique` → `findFirst` migration required because compound-unique `(section, slug)` can't take filter clauses (every test mock updated). Captured `now` inside cached fns so each entry is "valid for this `now`-snapshot, evicted on mutation bus or organic expire" — tradeoff: future-dated published posts don't auto-surface at their scheduled time (revisit if time-based scheduling becomes a feature) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 added `@@index([slug])` to `Post` (legacy-slug lookup was seq-scanning `posts`); escalated posts PUT to Serializable isolation to close the cross-section cache-invalidation race ([2026-05-12](dev-journal/2026-05-12.md)). May 13 clarified in the route doc that the resulting `serialization_failure → 500` is also not client-retriable (Prisma doesn't auto-retry; admin must retry manually) ([2026-05-13](dev-journal/2026-05-13.md)). May 14 Session 2 reversed the prior "capture `now` inside the cached fn" decision: cache stores rows time-independently, the read-time filter applies fresh `now` per request, and paginated caches pad with `LIMIT + futureCount` so the filter can strip futures and still slice a full page. A future-dated published post now auto-surfaces the first request after its `datetime` passes — no cache bust, no cron. Tradeoff: padded rows that have all crossed their date sit in cache until a mutation rebuilds; documented as bounded (~100KB per section worst case) ([2026-05-14](dev-journal/2026-05-14.md)). May 17 centralized the lexicographic compare invariant in `isFutureDatetime(datetime, now)` in `format.ts`, used by the bulk route's auto-publish logic and `PostsTab`'s scheduled marker. Required `now` arg (rather than defaulting to `currentDatetimeString()`) avoids the `vi.mock` intra-module-reference gotcha — if a helper calls another export of the same module, mocking the second export doesn't redirect the internal call ([2026-05-17](dev-journal/2026-05-17.md)).

- **A11y + structural cleanup** — May 11 added the carousel APG pattern (`role="group"`, `aria-roledescription="carousel"`, `aria-live="polite"` on the slide region), padded-button hit regions for dot indicators (WCAG AAA, replacing the overlap-prone `before:-m-2.5` trick), and a skip-link wrapping `{children}` in a single `#main-content` wrapper. `<time dateTime>` corrected to ISO 8601 (had been emitting the internal `yyyy-MM-dd-HHmm`, missed by two prior reviews — added to attribute-level audit targets) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 hoisted `<main>` from per-page wrappers into the root layout (one landmark per document) — 14 pages converted to `<div>`/`<section>`. `PresetOrFreeformInput` got differentiated `aria-label` suffixes for its dual labels ([2026-05-12](dev-journal/2026-05-12.md)). May 13's review caught that `PrivacyPageLayout.tsx` had been missed by the hoist — still rendered `<main>`, producing two landmarks on `/privacy` and `/privacy/body-tracking`; fixed alongside a landmark-structure test ([2026-05-13](dev-journal/2026-05-13.md)).

- **Code-review-driven phase commits cadence** — May 7's 17-commit batch (`codebase-2026-05-07.md`) established the pattern: daily review surfaces 8–15 findings; same-session multi-phase implementation pass closes each one; commits per phase, not at the end ([2026-05-07](dev-journal/2026-05-07.md)). May 11 ran three full-codebase reviews and three implementation passes in one day (24 commits across the day) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 ran 7+3 phases (12 commits) over a daily review ([2026-05-12](dev-journal/2026-05-12.md)). May 13's 7-phase pass closed every actionable review finding — and exposed a real bug (`useOptimisticMutation` unmount-after-resolve race) via a test added during implementation: generalizable lesson, a test premise that "should pass with the current implementation" sometimes uncovers an actual implementation gap, not a bad test ([2026-05-13](dev-journal/2026-05-13.md)). May 17 closed every actionable item in `code-reviews/2026-05-16.md` via 6 phase-aligned commits (`33297e2..658ad40`): toggle extraction → `isFutureDatetime` helper → audit `batchId` type bump → bulk parser hardening → bulk route hardening → keepalive Redis centralization. Required intermediate-state authoring for files that span two phases (`PostsTab.tsx`, `bulk/route.ts`) — backup-to-`/tmp` was the failsafe when reverting via `git checkout HEAD --` between commits ([2026-05-17](dev-journal/2026-05-17.md)).

- **Error boundary decisions (May 9)** — `<Link>` → `<a>` reverted for all non-route hrefs on the about page (9 bio links + 3 `contactLinks`): `next/link` for `mailto:`, external URLs, and static assets pulls a client component and has undocumented prefetch behavior on non-route URLs ([2026-05-09](dev-journal/2026-05-09.md)). `error.digest` dropped from both `error.tsx` and `global-error.tsx` — Vercel Hobby ~1h runtime-log retention means the matching log is gone before a user reports the hash; re-introduce paired with external aggregation. RTL tests added for both error boundaries. `errorMessages.ts` deleted (2 stable strings don't justify a shared module). `ClientAnalytics` moved to `src/components/`.

- **Upstash keepalive + admin infra (May 14 → 17)** — Recurring Upstash idle warnings traced to the cron schedule: `0 0 1,11,21 * *` had 10-11 day gaps but the free-tier idle threshold is ~7 days. History: `1,15` (15-day) → `1,11,21` (10-day) → `0 0 * * *` (daily). The handler already uses `redis.set(KEEPALIVE_KEY, iso)` not `PING` (Upstash excludes PING from idle detection — already noted in route comment). `KEEPALIVE_KEY = "keepalive:last"` centralized to `src/lib/keepalive.ts` so cron + new admin trigger can't drift. New `POST /api/admin/keepalive` endpoint (session-gated by `proxy.ts`'s `/api/admin/*` match) drives a manual "Run keepalive" button in `AdminNav`. Separate from the cron's bearer-auth route on purpose: keeps each route's auth concern single-purpose; only the key constant is shared. Also resolved an unrelated dashboard amplification: `<Link>` in App Router prefetches on viewport entry, not just hover — every visible row's edit link was firing a background RSC fetch with a Prisma query. `prefetch={false}` applied to per-row edit links in `PostsTab.tsx`/`ProjectsTab.tsx`; kept defaults on `New post`/`New project` and tab links ([2026-05-14](dev-journal/2026-05-14.md)). May 15 closed the 2026-05-14 watch-out about the two SET sites drifting: the drift had already happened (cron emitted no success line, admin emitted `console.info`; error payloads differed). Extracted `writeKeepalive(redis): KeepaliveResult` (discriminated `{ ok: true; value } | { ok: false; error }`) into `src/lib/keepalive.ts`; both routes consume it and own their own log tag + status code + response shape ([2026-05-15](dev-journal/2026-05-15.md)). May 17 centralized the *client construction* via lazy memoized `getKeepaliveRedis(): Redis | null` (three-state cache `undefined / null / Redis` so an unconfigured deploy doesn't re-read env per request); narrowed `KeepaliveResult.error` from `unknown` to `Error` and wraps non-`Error` throws at the helper boundary via `new Error(String(error))`; cron route now emits `[api:cron:ping] success` info line on every successful write so "no cron success in N hours" alerting can be a log grep instead of an Upstash scrape ([2026-05-17](dev-journal/2026-05-17.md)).

- **Admin posts list ergonomics + bulk import (May 15 → 18)** — Inline `· Scheduled` marker (in `text-accent`) on rows where `published && datetime > now`, coexisting with `· Draft` for future drafts; new `PostPublishedToggle` mirroring `IsFeaturedToggle` reuses the existing posts PUT (no new endpoint, no schema change). Bulk markdown import: filename `yyyy-MM-dd[-HHmm]-Title with spaces.md` parsed by a single shared `src/lib/bulkImportParser.ts` (consumed by both server route and client preview, returns discriminated `{ ok, ... } | { ok: false, reason }`); section picked once per batch; `published` derived per-file from datetime (future → published so the May 14 auto-surface logic kicks in, past → draft for review). Server pre-queries colliding slugs in the section, dedupes within the batch, then `createManyAndReturn` with `skipDuplicates`, single `revalidatePostSection`, one `[api:admin:posts:BULK]` audit line per created row. Schema cap: 50 files / 100KB each (worst case ~5MB). UI returns 200 even on partial success with a per-file `skipped` reason list. Lesson re-confirmed (third time this month): `vi.resetAllMocks()` clears factory-set return values — the bulk route test's `currentDatetimeString` mock had to be re-set in `beforeEach` ([2026-05-15](dev-journal/2026-05-15.md)). May 17 hardened the bulk surface following the 2026-05-16 review: post-insert slug-set diff pushes `skipDuplicates`-dropped rows into `skipped` with reason `"Skipped at insert (concurrent write)"` (admin no longer sees "created N" with no name for the dropped file); `randomUUID()` batchId stamped on every audit line; `[api:admin:posts:BULK] inserting` breadcrumb before the DB call so a subsequent 500 still tells us which slugs were in-flight; `[api:admin:posts:BULK] skipped` summary log emits `reasonsByType` on any non-empty `skipped`; `FILENAME_REGEX` title group tightened to `[^/\\\x00-\x1f\x7f]+` (blocks path separators and ASCII control chars at parse time before they reach the stored title or audit-log payload); originating filename moved to a parallel `Map<slug, filename>` so the DB-shaped `InsertRow` type can't accidentally leak fields to Prisma. `PostBulkImport.tsx` now surfaces an amber "Selected M, importing first N" banner on truncation and uses composite list keys `${name}-${index}-${lastModified}` so two files with the same `name` from different sources don't share a React row ([2026-05-17](dev-journal/2026-05-17.md)). May 18 closed the "bulk import leaves `summary` empty" gap surfaced by the user — but the right fix was wider than bulk. `Post.summary` flipped from `String?` → `String NOT NULL` in `prisma/schema.prisma`; new `deriveSummary(body)` helper in `src/lib/markdown.ts` strips markdown via existing `stripMarkdown`, truncates at the last whitespace ≤ 160 chars (Google snippet width), appends `…`, hard-slice fallback for whitespace-less bodies. `SUMMARY_MAX_CHARS = 160` exported and tied to `postCreateSchema.summary.max(160)` (was 300 — wishful, beyond consumer display width). Write-time invariant "summary is never empty" applied at all three entry points: create auto-derives when blank; bulk always derives (no per-file input); edit runs initial-vs-current comparison inside the existing Serializable txn (`findUnique` select extended with `body` + `summary`) — authored summary kept, untouched + body changed re-derives, cleared re-derives. Form already sends `summary: state.summary || undefined` ([PostForm.tsx:82](src/components/admin/PostForm.tsx#L82)) so cleared and untouched-when-empty look identical to the server, which is fine under "never empty." Read-time fallbacks deleted (`?? undefined` in blog metadata; `?? stripMarkdown(...).slice(0, 300)` + `FEED_SUMMARY_MAX_CHARS` constant in the feed) once the column type narrowed to `string`. Lesson: "in practice never fires" ≠ "dead code" — a null-guard stays load-bearing until the type itself narrows ([2026-05-18](dev-journal/2026-05-18.md)).

## Timeline

- **2026-04-20** — Feed 500 fix; `unstable_cache` JSON-serialization pattern locked (serialize before caching). [source](dev-journal/2026-04-20.md)
- **2026-04-21** — `stripMarkdown` improvements. [source](dev-journal/2026-04-21.md)
- **2026-04-26** — Timing-safe bearer token comparison; env validation. [source](dev-journal/2026-04-26.md)
- **2026-05-07** — 17 commits (721→840 tests): per-IP rate-limit, `ALLOW_UPLOADS` lazy env flag, strict `parseIntId`, `platformBucket` word-boundary regex, `createSlug` NFKD normalization, cross-section cache invalidation, `<ErrorMessage>` extraction, admin `AbortController` pattern, vitest glob broadened, `console.warn`/`console.info` spying added. [source](dev-journal/2026-05-07.md)
- **2026-05-09** — `<Link>`→`<a>` revert for non-route hrefs; `error.digest` dropped from error boundaries; RTL tests for `error.tsx`/`global-error.tsx`; `ClientAnalytics` to `src/components/`; `errorMessages.ts` deleted. [source](dev-journal/2026-05-09.md)
- **2026-05-11** — Three full-codebase reviews + three implementation passes in one day (24 commits, 903 tests): `datetime <= now` cascade + `findUnique→findFirst` migration; magic-byte `detectImageMime` sniff; structured `auditLog` helper with required `T | null` shape; `parseJsonBody` paths-only logging; `isAbortError` extracted; `<time dateTime>` corrected to ISO 8601; carousel APG pattern + padded-button hit regions; skip-link wrapper; `ThemeProvider` to `useSyncExternalStore`. [source](dev-journal/2026-05-11.md)
- **2026-05-12** — Daily review → 7+3 phases (12 commits, 980 tests): `<main>` hoist from 14 pages to root layout; `Post.slug` index in schema; posts PUT escalated to Serializable; `useOptimisticMutation` extracted at the 2nd consumer; `AdminAuditTag` closed union; `previousSlug` audit semantics unified across posts/projects; `readErrorMessage` extracted to `lib/` at the 5th caller; `postDatetimeToISO` returns `string | undefined` (dropped `?? undefined` boilerplate at 3 React/Next call sites). [source](dev-journal/2026-05-12.md)
- **2026-05-13** — Daily review → 7-phase pass (7 commits, 998 tests): `PrivacyPageLayout` `<main>` cleanup (missed by Phase 2 hoist); `previousSlug` DRY lift in projects PUT; `MutateResult` discriminant (`failure` vs `superseded`) + unmount-after-resolve race fix exposed by a new test; `AdminAuditTag` derived from const array; `metadata.ts` dev-only double-brand throw; `apiErrors.describeZodIssues` dedupes segments; `sanitizeLogString` for upload route log-injection guard; `ProjectForm.handleSubmit` re-snaps from `sortOrderText` to prevent stale-value submission. [source](dev-journal/2026-05-13.md)
- **2026-05-14** — Two sessions. (1) Upstash keepalive triage + GDPR-grade IP pseudonymization + dashboard prefetch fix: cron `0 0 1,11,21 * *` → `0 0 * * *` (10-day gap exceeded ~7-day Upstash idle threshold); `IP_HASH_SECRET` env var, HMAC-SHA256 bucket keys for login rate-limiter; `KEEPALIVE_KEY` centralized to `src/lib/keepalive.ts`; `POST /api/admin/keepalive` + "Run keepalive" button in `AdminNav` (session-gated, parallel to bearer-auth cron); `prefetch={false}` on per-row edit links in `PostsTab`/`ProjectsTab`. (2) Time-based scheduled publishing without cron: read-time `datetime <= now` filter applied to cached rows; paginated caches pad with `LIMIT + futureCount`; `totalPages` moved to request-time count; reverses the May 11 "capture `now` inside cached fn" decision. [source](dev-journal/2026-05-14.md)
- **2026-05-15** — Admin posts list: inline `· Scheduled` marker, `PostPublishedToggle` (mirrors `IsFeaturedToggle`, reuses existing PUT), bulk `.md` import (`/admin/posts/bulk`) backed by `POST /api/admin/posts/bulk` with `createManyAndReturn` + per-file skipped reasons; new `[api:admin:posts:BULK]` audit tag; shared filename parser `src/lib/bulkImportParser.ts` consumed by server + client preview. Session 2: extracted `writeKeepalive(redis): KeepaliveResult` shared by `/api/cron/ping` + `/api/admin/keepalive`, closing the 2026-05-14 drift watch-out. [source](dev-journal/2026-05-15.md)
- **2026-05-17** — Session 1: `/api/upload` → `/api/admin/upload` via `git mv`; dropped the `pathname === "/api/upload"` exact-match clause in `src/proxy.ts` (route is now covered by the generic `startsWith("/api/admin/")` admin gate); log tag renamed `[api:upload:POST]` → `[api:admin:upload:POST]`. Session 2: closed every actionable finding in `code-reviews/2026-05-16.md` via 6 phase-aligned commits (`33297e2..658ad40`) — `BooleanFlagToggle` extraction (revert-baseline + visible label + `console.error` in `useOptimisticMutation`'s failure branches), `isFutureDatetime(datetime, now)` helper + `PostsTab.test.tsx`, `AdminAuditPayload.batchId` cross-cutting type bump, bulk parser regex tightening + `BULK_MAX_FILES` export + `PostBulkImport` UX (composite list key + truncation banner), bulk route hardening (silent-drop reconcile, batch correlation, observability logs, `PreparedRow.filename` leak fix), keepalive `getKeepaliveRedis()` memoization + `Error`-narrowing + cron success log. Lessons: `vi.mock` with `...actual` doesn't rewire intra-module references; `sonarjs/cognitive-complexity` (limit 15) bites concentrated route handlers — extract loop + side-effect emitters into named helpers. [source](dev-journal/2026-05-17.md)
- **2026-05-18** — `Post.summary` migrated `String?` → `String NOT NULL`; new `deriveSummary` helper in `src/lib/markdown.ts` (strip → 160-char word-boundary truncation with `…`, hard-slice fallback); cap aligned to Google snippet width (300 → 160). Write-time invariant "summary is never empty" applied at create (auto-derive when blank), bulk (always derive — no per-file input channel), edit (initial-vs-current compare inside the existing Serializable txn; authored kept, untouched + body changed re-derives, cleared re-derives). Read-time fallbacks (`?? undefined` in blog post metadata, `?? stripMarkdown(...).slice(0, 300)` + `FEED_SUMMARY_MAX_CHARS` in the feed) deleted once the column type narrowed. [source](dev-journal/2026-05-18.md)
