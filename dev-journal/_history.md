# Rolling History — last 30 days (through 2026-07-18)

## Themes

- **SEO and structured data infrastructure (June)** — FAQ accordion (always-in-DOM + `inert`/`aria-hidden`; `AnimatePresence` conditional-mount excluded from SSR, defeating the SEO goal); JSON-LD layer (`SoftwareApplication`, `FAQPage`, `BlogPosting`); new `Project` fields (`metaTitle`, `keywords`, `offers`, `applicationCategory`, `cardImage`, `ogImage`); `robots.ts` (allow-all incl. AI crawlers; `Disallow: /api/` with `Allow: /api/feed/` carved back); `/llms.txt` dynamic route (reuses `getProjectsGalleryCached()` so it can't drift). Both JSON-LD builders unified onto `siteBase()` param. [source](dev-journal/2026-06-28.md), [source](dev-journal/2026-06-29.md)

- **Gallery UX overhaul (June→July)** — Gallery lifted to `ProjectContent`; portal-mounted lightbox survives section changes; `ProjectSectionCarousel` made controlled; aspect-true sizing fixed the lightbox corner halo. July rebuilt it as a continuous cross-section strip with a hand-rolled pointer/zoom engine (`zoom.ts`), fixing two subtle bugs: framer's `onDragEnd` fires after `click` (swipe opened the lightbox — moved arbitration to raw `pointerdown`/`pointerup`), and a `key` on the panel remounted the carousel mid-swipe (dropped it for stable node identity). [source](dev-journal/2026-06-26.md), [source](dev-journal/2026-07-08.md)

- **Static rendering + cache-precision → stale-404 hardening (July)** — pages flipped dynamic→static by dropping `cookies()` (theme → pre-paint inline script + localStorage) and `headers()` (origin → `getSiteUrl()` from env). Cache tags split coarse→per-slug + a `RevalidatePanel` for script-driven busts. A prod stale-404 (a durably-cached `null` predating the row) then forced the deeper fix: detail misses are never cached (`CacheMissError` thrown, not returned), the revalidate endpoint reports applied/skipped, and detail tags are single-sourced per module. [source](dev-journal/2026-07-07.md), [source](dev-journal/2026-07-17.md)

- **Post slug authority & ingestion-pipeline sharing (July)** — `slug:` frontmatter became the authoritative, self-healing slug for both ingestion paths (over title-derivation, which silently moves a retitled post's URL). `resolveSlug`/`setFrontmatterSlug`/`slugRewrite` back-fill files; `createSlug` idempotency doubles as the canonical-shape check; `.md` export emits `slug:`. `init-post-slugs.ts` added as an idempotent DB→files resync (filename-datetime match, title fallback, refuses ambiguity). The admin bulk route's `prepareBatch` was refactored onto the shared `parsePostFiles`, killing a ~60-line copy that had drifted back to title-derivation — zero test changes. [source](dev-journal/2026-07-16.md)

- **Guides section — built, then reviewed (July)** — the `_guides-plan.md` design shipped end to end on 07-17: `Guide` + `GuideTopic` models, topic membership by directory (`<topic>/index.md`), filename-derived `publishedAt` with a read-time scheduling guard, product disclosure as authored prose (deleted the `GuideProjectCta` card; an importer warning recovers the "always links its project" guarantee), admin CRUD, a markdown importer, and sitemap/llms.txt/footer integration, plus the `write-guide` skill. Project reference is a `projectSlug` string, deliberately not an FK (`import-projects.ts` deletes-then-creates, which would cascade-nuke guides). [source](dev-journal/2026-07-17.md)

- **Code-review-driven quality sweep (07-18)** — acted on the ~39-finding daily review of the guides merge + import/cache/middleware work across 8 phased commits + a follow-up (branch `fix/code-review-2026-07-18`, 1810 → 1892 tests): cache-tag namespace collision (`guide-detail-`/`guide-topic-`), a `useAdminResource` double-submit race, revalidate parent-hub busting + per-resource 207 isolation, admin-form DRY (`@apply` + `useFormState`), a `botProbes` middleware module (inner-segment probe bypass + case-insensitive gate), import-tooling hardening (`atomicWrite` uniquifier/mode, honest `init-post-slugs` status, `errorMessage`/`sortedMarkdownNames`/`wrapNullableDetail`), and a broad test backfill. Also fixed a latent bug found in review: a pure guide folder-move (byte-identical content) was silently skipped, leaving its `topicId` stale. Deferred 2 findings (miss-stampede negative cache; admin-search GIN index) and skipped 6 with reasons (incl. a false-positive P2002 message — slugs are separate per-table `@unique`s). [source](dev-journal/2026-07-18.md)

## Timeline

- **2026-06-26** — Gallery lifted to `ProjectContent`; aspect-true image sizing fix; `useScrollOverflow` test hang fixed (stable `useRef`). [source](dev-journal/2026-06-26.md)
- **2026-06-28** — `cardImage`/`ogImage` fields; FAQ accordion (always-in-DOM + `inert`); JSON-LD (`SoftwareApplication`/`FAQPage`/`BlogPosting`); `ProjectFaq`; `safeJsonLdString`. [source](dev-journal/2026-06-28.md)
- **2026-06-29** — `--color-background-transparent`; carousel measured aspect ratio; back/forward entrance suppression (`NavigationTypeTracker` + Navigation API); `robots.ts`; `/llms.txt`; JSON-LD origin via `siteBase()`. [source](dev-journal/2026-06-29.md)
- **2026-07-05→08** — Static-rendering sweep; `.md` blog route handler; featured grid 3-up; continuous gallery + hand-rolled zoom; per-slug cache tags; framer `onDragEnd` timing + panel-identity fixes; stage aspect tuning. [source](dev-journal/2026-07.md)
- **2026-07-16** — `slug:` frontmatter as source of truth; `resolveSlug`/`setFrontmatterSlug`/`slugRewrite`; `.md` export emits `slug:`; `init-post-slugs.ts` resync; `prepareBatch` onto shared `parsePostFiles`. [source](dev-journal/2026-07-16.md)
- **2026-07-17** — Guides section built end to end (schema, importer, admin CRUD, public pages, `write-guide` skill); prod stale-404 hardening (throw-on-miss, revalidate applied/skipped, single-sourced detail tags); slug-tooling review fixes (byte-canonical drift heal, `matchRow` datetime guard, atomic writes); middleware bot-probe short-circuit + admin-gate over-match fix. [source](dev-journal/2026-07-17.md)
- **2026-07-18** — Acted on the 39-finding guides/import/cache/middleware code review across 8 phased commits + a folder-move follow-up (`fix/code-review-2026-07-18`, 1810→1892 tests, unmerged); fixed a latent pure-folder-move skip. [source](dev-journal/2026-07-18.md)
