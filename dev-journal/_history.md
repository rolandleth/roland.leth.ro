# Rolling History — last 30 days (through 2026-07-08)

## Themes

- **Manifest-driven project importer** — built `scripts/import-projects.ts` + per-project `project.json` + the `app-copy-to-project` skill over three sessions: 2026-06-08 used deterministic path keys; 2026-06-10 switched to content-addressed `SHA256[:12]` keys (same bytes = same key, changed bytes = automatic CDN cache-bust); 2026-06-13 extracted pure `blobSync.ts` (`BlobStore` DI interface, paginated listing, 4 bounded-concurrent uploads, `pruneOrphans` post-import sweep), widened hash to 64-bit, added byte-size assert as collision backstop. 2026-07-05 hardened the write path: documented Serializable no-retry rationale, added distinct `P2034` failure branch (`toFailureResult`), replaced `prisma!` with an explicit null guard. A persistent dual-implementation hazard (`import-projects.ts` keeps its own `writeProject`/`referencedImageUrls` logic separate from `src/lib/import/projectImport.ts`) remains open. [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-06-13.md) [source](dev-journal/2026-07-05.md)

- **Project detail gallery — lightbox, carousel, and swipe** — 2026-06-26: `ProjectSectionCarousel` made controlled with gallery position lifted to `ProjectContent`; `ProjectImageLightbox` extracted (portal, focus-trap, body-scroll-lock, opaque `bg-neutral-950` backdrop); lightbox chevrons walk the whole gallery crossing section boundaries; multi-round corner-halo bug fixed by aspect-true `next/image` sizing (`width={0} height={0}` + `h-auto w-auto max-h-* max-w-* rounded-xl`). 2026-06-29: tablist edge-fades fixed (`transparent` is transparent *black*, smears grey on iOS Safari; replaced with `--color-background-transparent`); carousel stage measures `naturalWidth/Height` on `onLoad` and drives `aspect-ratio`; Framer `drag="x"` swipe extracted to `src/lib/client/swipe.ts`. 2026-07-05: carousel resets to `h-120` fallback on invalid `onLoad` dimensions and on `onError` only (avoids per-swipe reflow on valid transitions); lightbox gained focus/scroll edge-case tests. 2026-07-07 reworked it into one continuous cross-section strip: `flattenSections` flattens every section's images to a single flat index in `ProjectContent`, the active tab follows the landed slide, a shared `GalleryTrack` renders the row; the fixed-height stage letterboxes off-ratio images (dropping the aspect-hug rounding). Lightbox zoom is hand-rolled from Pointer Events (pinch/pan/swipe arbitration on one surface; pure geometry in `src/lib/client/zoom.ts`, thin `useLightboxGestures.ts` hook driving Framer `MotionValue`s) — trackpad zoom splits on `event.ctrlKey` (pinch = ctrl+wheel with its own sensitivity + a per-event cap; plain two-finger scroll pans when zoomed). Swipe-opens-lightbox fixed with a drag-travel guard (`suppressEnlargeRef`, reset on stage pointer-down). In-app per-slide corner rounding (`containedSize`) was built then reverted — corners will come from the source PNGs. 2026-07-08 corrected two gesture bugs the 07-07 rework left behind: (1) the `suppressEnlargeRef` guard set the flag in Framer's `onDragEnd`, which fires a frame late via `frame.postRender` (`VisualElementDragControls.mjs:174`) — after the browser's `pointerup`→`click` — so a swipe still opened the lightbox; re-armed by reading raw pointer travel at `onPointerDownCapture`/`onPointerUpCapture` (both `isPrimary`-guarded), which run before the click. (2) Crossing sections animated a fast multi-slide fly-across because `ProjectContent`'s tabpanel `<div key={activeSection.id}>` remounted the continuous carousel (resetting its `x` `MotionValue` + re-measuring width from 0 → springing from slide 0 across every slide); dropped the key so the strip keeps stable identity and steps one slide. [source](dev-journal/2026-06-26.md) [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07-05.md) [source](dev-journal/2026-07-07.md) [source](dev-journal/2026-07-08.md)

- **SEO and structured data** — 2026-06-28 added `ProjectFaq` model + `faqs` manifest field; FAQ accordion uses always-in-DOM + `inert` (text crawlable, collapsed panels out of a11y tree); `FAQPage` + `SoftwareApplication` JSON-LD with `applicationCategory`, `offers` (single `Offer` or `AggregateOffer` low–high), `metaTitle`, `keywords`. 2026-06-29 closed the blog gap with `buildBlogPostingJsonLd` (`PostDetail.updatedAt` added for `dateModified`); `llms.txt` as a dynamic `route.ts` (DB-backed); `robots.ts` with `/api/` blocked except `/api/feed/`. 2026-07-05 remediated the 2026-06-30 review: hoisted `absoluteImageUrl` to `jsonLd.ts` (relative `ogImage` was emitting invalid `SoftwareApplication.image`); tightened `buildOfferNode` to guard `Number("") === 0`; tightened `JsonLdScript` to `Record | null` (undefined now throws via `safeJsonLdString`); `$`-anchored the CCBot `robots` rule; filtered discontinued projects from `llms.txt`. [source](dev-journal/2026-06-28.md) [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07-05.md)

- **Login rate-limit hardening** — 2026-06-10: `Ratelimit` constructs whenever Redis is present regardless of `IP_HASH_SECRET`; missing secret degrades to a global bucket (`"global"`) rather than failing open; 6 per-IP tests added in a separate `route.ratelimit.test.ts` (required `vi.resetModules()` + dynamic import per test). 2026-06-13: `ip:` keyspace prefix makes the IP and global keys formally disjoint; `bucketKey` hoisted so HMAC runs once per request; log levels split (routine 429 → `info`, fail-open → `error`). [source](dev-journal/2026-06-10.md) [source](dev-journal/2026-06-13.md)

- **Legal/privacy DRY and Continuum legal cluster** — 2026-06-08: `PrivacyPageLayout` gained `lastUpdated`/`contactEmail`/`contactPronoun?` with auto-appended boilerplate; `AppleLegalLink` component centralises the URL across 6 call sites. 2026-06-13: `LegalPageLayout` generalised with `documentKind: "privacy" | "terms"`, optional `relatedLinks`; `continuumLegalLinks.ts` cross-links the four-page cluster; legal dates held at true substantive-revision dates (a layout swap does not bump the date). [source](dev-journal/2026-06-08.md) [source](dev-journal/2026-06-13.md)

- **Back/forward entrance suppression** — `fadeUp`'s `initial → animate` replayed on every `popstate` because Next App Router remounts the route subtree on back/forward. Fixed with `src/lib/client/navigationType.ts` + `NavigationTypeTracker` island in root layout: Navigation API `currententrychange` mirrors the navigation type into a module flag; `fadeUp` returns `initial: false` on back/forward. 2026-07-05 narrowed the event cast to the `'reload' | 'push' | 'replace' | 'traverse'` union and pinned install-once idempotency with a re-mount + StrictMode double-invoke test. [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07-05.md)

## Timeline

- **2026-06-08** — Manifest-driven importer shell: `project.json`, `import-projects.ts`, `app-copy-to-project` skill; `scripts/` gitignore narrowed to `scripts/imports/`; `PrivacyPageLayout` DRY; `AppleLegalLink`; Reckon accent `#B5673F` chosen. [source](dev-journal/2026-06-08.md)
- **2026-06-10** — Importer keys → content-addressed `SHA256[:12]`; rate-limiter active whenever Redis configured; global-bucket fallback; per-IP test file; Continuum DPIA + responsible-use + terms routes committed. [source](dev-journal/2026-06-10.md)
- **2026-06-13** — `blobSync.ts` extracted; hash widened 48→64-bit + byte-size backstop; `pruneOrphans` post-import; `LegalPageLayout` generalised; `ip:` keyspace prefix; HMAC hoisted; log levels split; `FeaturedProjectCard` equal-height via `flex h-full flex-col` + `flex-1` Link chain. [source](dev-journal/2026-06-13.md)
- **2026-06-26** — `ProjectImageLightbox` extracted (portal, opaque backdrop, focus-trap); gallery position lifted to `ProjectContent`; lightbox cross-section nav; corner-halo fixed by aspect-true `next/image` sizing. [source](dev-journal/2026-06-26.md)
- **2026-06-28** — `Project.cardImage` + `Project.ogImage`; `resolveCardImage`/`resolveOgImage` helpers; `ProjectFaq` model; FAQ always-in-DOM + `inert` accordion; `FAQPage` + `SoftwareApplication` JSON-LD; `metaTitle`, `keywords`, `offers`, `applicationCategory`. [source](dev-journal/2026-06-28.md)
- **2026-06-29** — `--color-background-transparent` CSS var; carousel `naturalWidth/Height`-driven `aspect-ratio`; Framer swipe via `src/lib/client/swipe.ts`; `AnimatePresence` transitions stripped; back/forward suppression via Navigation API (`navigationType.ts`, `NavigationTypeTracker`); `robots.ts`; `llms.txt` route handler; `BlogPosting` JSON-LD. [source](dev-journal/2026-06-29.md)
- **2026-07-05** — Remediated `code-reviews/2026-06-30.md` (22/24 findings): JSON-LD image absolutization; `Offer` NaN empty-string guard; `offerCount` dedupe; `JsonLdScript` → `Record | null`; carousel invalid/`onError` reset; CCBot `$`-anchor; `llms.txt` discontinued filter; nav-type union; import `P2034` branch + `prisma` null guard; eslint `Property` selector. Suite 1343 → 1355. [source](dev-journal/2026-07-05.md)
- **2026-07-07** — Gallery reworked into one continuous cross-section strip (`flattenSections`, flat index in `ProjectContent`, shared `GalleryTrack` + `useElementSize`); hand-rolled Pointer-Event lightbox zoom (`src/lib/client/zoom.ts` + `useLightboxGestures.ts`: pinch/pan/swipe, wheel, double-tap), trackpad pinch split on `ctrlKey` with deltaMode normalization + per-event cap; swipe-no-longer-opens-lightbox guard; per-slide corner rounding built then reverted (source PNGs will carry rounding). Suite at 1397. [source](dev-journal/2026-07-07.md)
- **2026-07-08** — Gallery gesture fixes: swipe-opens-lightbox re-fixed via raw pointer travel at `pointerup` (`onPointerDownCapture`/`onPointerUpCapture`, `isPrimary`-guarded) because Framer defers `onDragEnd` past the click; cross-section fly-across fixed by dropping `key={activeSection.id}` on the `ProjectContent` tabpanel so the continuous carousel isn't remounted. Regression tests added (pointer-travel arbitration in `ProjectSectionCarousel.test.tsx`; panel node-identity in `ProjectContent.test.tsx`). [source](dev-journal/2026-07-08.md)
