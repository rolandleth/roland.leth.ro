# Rolling History — last 30 days (through 2026-07-20)

## Themes

- **Gallery/lightbox gesture engineering** — the lightbox corner-halo bug (06-26) traced to the gallery never rounding the *visible* image (`object-contain` letterboxes; container `rounded-xl` misses it), fixed via aspect-true `next/image` sizing. 06-29 replaced backdrop-opacity gradients with a zero-alpha CSS var, added framer-drag swipe, then stripped nearly all navigation animations and suppressed entrance replay on back/forward via the Navigation API's `currententrychange` event. 07-07 rebuilt the gallery as a continuous cross-section strip with hand-rolled pointer-event lightbox zoom (rejecting `@use-gesture/react` and third-party lightboxes), but shipped two framer-motion timing bugs — a swipe-suppression flag set inside `onDragEnd` (deferred a frame past the browser's `click`) and a keyed tabpanel remounting the carousel's `MotionValue`s on cross-section swipes — both corrected 07-08 by arbitrating from raw `pointerdown`/`pointerup` and dropping the remount key. [source](dev-journal/2026-06-26.md) [source](dev-journal/2026-07-07.md) [source](dev-journal/2026-07-08.md)

- **JSON-LD / structured-data hardened to a single chokepoint** — 06-28 built the FAQPage + SoftwareApplication JSON-LD layer (flat `offers` price-points array, `applicationCategory` manifest-driven, an always-in-DOM + `inert` FAQ accordion for crawlability without sacrificing a11y). 06-29 sessions 5-7 added `safeJsonLdString` escaping, extracted a shared `jsonLd.ts`, closed the last raw `JSON.stringify` embed on the blog page, and introduced `JsonLdScript` as the only component allowed `dangerouslySetInnerHTML` — flipping the eslint rule from banning the serializer call to banning the attribute itself, closing an aliased-serialization gap. 07-05 tightened `JsonLdScript`'s prop to reject `undefined` (throws instead of silently rendering nothing) and fixed an Offer NaN guard that let an empty-string price through as `0`. [source](dev-journal/2026-06-28.md) [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07-05.md)

- **Cache/revalidation tag granularity, a prod incident, and hardening** — 07-07 split the coarse `blog-{section}`/`projects` tags into per-slug detail tags (`post-{section}-{slug}`, `project-{slug}`) plus shared `*-pages` tags for "revalidate all", after an ISR dashboard showed 8 blind writes on an unedited page. 07-17 extended the model to guides the same day a prod incident landed — a published post 404'd because `unstable_cache` had durably cached a `null` miss predating the row, and import scripts write via Prisma so can't bust tags — fixed via `CacheMissError` throw-on-miss across all four detail lookups, alongside a still-unresolved asymmetry where one tag bust didn't clear an entry that a broader "all" bust did. 07-18's review then caught a guide/topic tag-namespace collision (`guide-topic-foo` ambiguous between a guide and a topic) and a revalidate endpoint that discarded partial results on a mid-batch failure; both fixed same day with namespaced tags and a per-resource 207 response. [source](dev-journal/2026-07-07.md) [source](dev-journal/2026-07-17.md) [source](dev-journal/2026-07-18.md)

- **Guides section built end-to-end, then hardened** — 07-16 built prerequisite slug-management tooling (`slug:` frontmatter as source of truth, self-healing via `createSlug` idempotency). 07-17 designed and shipped the entire `Guide`/`GuideTopic` feature (top-level `/guides/:slug` routes, directory-based topic membership, prose-only product disclosure replacing a CTA card, read-time scheduling) across 10 commits, the same day a review of 07-16's slug tooling found real gaps (trailing-space drift not healed, a `matchRow` false-positive that could bind a file to the wrong DB row) fixed via a `slugInit.ts` extraction. 07-18's ~39-finding review of the guides merge caught a cache-tag collision, a stale revalidate-panel flash, and a latent bug where moving a guide file between topic folders silently failed to update its `topicId` — all fixed same day across 8 phased commits. [source](dev-journal/2026-07-16.md) [source](dev-journal/2026-07-17.md) [source](dev-journal/2026-07-18.md)

- **Vercel CPU reduction via static rendering** — 07-07 converted most pages from dynamic to static/ISR by moving theme state to localStorage + a pre-paint inline script (removing `cookies()`) and canonical origin to an env-derived `getSiteUrl()` (removing `headers()`). 07-17 picked the thread back up from a Vercel Fluid analytics screenshot (middleware 53.8% of CPU vs. function 46.2%), adding a middleware bot-probe short-circuit so junk requests never reach the function, and fixing a latent admin-gate over-match (`/administrator*` wrongly treated as protected) that the new bot-filter test fixtures surfaced. [source](dev-journal/2026-07-07.md) [source](dev-journal/2026-07-17.md)

- **AI/search discoverability infrastructure** — 06-29 shipped `robots.ts` (allow-all incl. AI-citation crawlers, `/api/` disallowed except `/api/feed/`) and a DB-driven dynamic `/llms.txt` route reusing `getProjectsGalleryCached()`, plus `BlogPosting` JSON-LD closing the projects-only structured-data gap. 07-06 added a markdown export route (`/blog/:section/:slug.md` via a middleware rewrite, since Next dynamic segments can't carry a file extension) discoverable only via `<link rel="alternate" type="text/markdown">`. 07-19 shipped IndexNow submission via a session-gated admin endpoint (guarantees the real prod origin/DB, no localhost-announcement risk), sourcing its URL list from the existing `sitemap()` function and serving the verification key from env at a fixed `/indexnow-key.txt` path. 07-20's scheduled review of that single commit found 36 issues and fixed 34 across six phases — the lib reported attempted URLs as submitted (an all-403 run still claimed success), `isSubmittableOrigin` passed any dotted host despite a comment claiming IP literals were excluded, config gaps returned 400 where the sibling `keepalive` route had established 503, `?dryRun=false` silently *enabled* preview, and `IndexNowPanel` had shipped with no test file while three server-side test files landed beside it. The same session reversed 07-19's env-schema choice: the `INDEXNOW_KEY` regex moved out of the aggregate Zod parse (a typo in an optional var was failing `readEnv()` for login, cron auth and site-URL resolution) into `isValidIndexNowKey`, applied via `describeKeyProblem` at the one route that needs it. [source](dev-journal/2026-06-29.md) [source](dev-journal/2026-07-06.md) [source](dev-journal/2026-07-19.md) [source](dev-journal/2026-07-20.md)

- **Admin panel request scaffolding converged on shared primitives** — 07-18's review DRY'd four admin forms onto `@apply` classes plus a `useFormState` hook rather than a shared `AdminForm` shell, and fixed a `useAdminResource` save/remove race where a stale in-flight response re-enabled the submit button. 07-19 extracted `adminButtonClass` into `controlStyles.ts` when `IndexNowPanel` was added beside `RevalidatePanel`. 07-20 found that extraction had stopped at one string while ~25 lines of fetch/abort/supersede/finally scaffolding were copied verbatim into the second panel, and lifted it into a `useAdminAction` hook adopted by both — `RevalidatePanel`'s nine pre-existing tests passed unchanged, which is the evidence the behaviour was preserved. [source](dev-journal/2026-07-18.md) [source](dev-journal/2026-07-19.md) [source](dev-journal/2026-07-20.md)

## Timeline

- **2026-06-26** — `useScrollOverflow` test hung the vitest worker (~60s) from an infinite render loop caused by a fresh ref each render; fixed the test to use a stable ref and hardened the hook to bail when overflow flags are unchanged. [source](dev-journal/2026-06-26.md)
- **2026-06-28** — Renamed the manifest image field to `cardImage`/`ogImage` to match the `app-copy-to-project` skill's manifest keys exactly; `scripts/import-projects.ts`'s own untested `writeProject`/`referencedImageUrls` had silently dropped the new fields (the tested `projectImport.ts` core wasn't touched), flagged as a recurring hazard for future `Project` fields. [source](dev-journal/2026-06-28.md)
- **2026-07-05** — Carousel resets to the `h-120` fallback only on invalid `onLoad` dimensions or `onError`, not on every image change, avoiding a reflow on every valid-to-valid swipe. [source](dev-journal/2026-07-05.md)
- **2026-07-06** — Featured projects gallery grid widened to 3-up on large screens (`lg:grid-cols-3`); `Image` `sizes` hint updated to match the new 1/2/3-up breakpoints. [source](dev-journal/2026-07-06.md)
- **2026-07-07** — `src/app/api/admin/upload/route.ts` exported test-only helpers, which Next forbids from a route file (caught only by a real build, not tsc/vitest/eslint); moved to `uploadHelpers.ts`. [source](dev-journal/2026-07-07.md)
- **2026-07-08** — Gallery stage switched from a fixed height to `aspect-[1270/760]` + `max-h` caps (kills large mobile letterboxing); rejected storing per-image DB dimensions as disproportionate to the goal. [source](dev-journal/2026-07-08.md)
- **2026-07-16** — Admin bulk-import route's hand-copied gate pipeline had drifted from the import script (re-derived slugs from title, ignoring `slug:`); unified both onto shared `parsePostFiles`. [source](dev-journal/2026-07-16.md)
- **2026-07-17** — Discovered a Reckon price mismatch (manifest `offers` `4.00` vs. the actual `3.99`) while investigating a duplicate `offers` JSON key in the manifest (JSON's last-key-wins had masked the duplicate); manifest fix deferred. [source](dev-journal/2026-07-17.md)
- **2026-07-20** — A review subagent reported a confidently wrong finding — that unauthenticated `/api/admin/*` redirects to the HTML login page — contradicting its own paired gatherer; `src/proxy.ts:135-136` returns 401 JSON for the API arm and redirects only for pages. Caught and dropped before it reached the review file. [source](dev-journal/2026-07-20.md)
- **2026-07-20** — Established that middleware is the sole auth boundary for all 12 admin routes; deferred a shared `requireAdminSession()` refactor, noting vendor guidance against a sole middleware boundary and the CVE-2025-29927 bypass precedent (patched in 14.2.25 / 15.2.3; repo is on Next 16.2.10, so not exposed). [source](dev-journal/2026-07-20.md)

<!-- end -->
