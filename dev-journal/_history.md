# Rolling History — last 30 days (through 2026-06-13)

## Themes

- **Security and audit-log hardening** — HMAC-SHA256 IP pseudonymization for rate-limiting (plain SHA-256 is rainbow-table reversible over ~4B IPv4 keyspace); `IP_HASH_SECRET` gates limiter *granularity*, not whether it runs — originally fail-open on missing secret with warn at module load (05-14), revised 06-10 to fall back to a single global bucket (`bucketKey()` returns constant `"global"`, limiter built whenever Redis is configured) — degrade-not-disable, re-accepting the admin-lockout DoS (botnet 5/15min) as the lesser evil over no limiter. Hardened 06-13 from the 06-11 review: client keys now carry an `ip:` prefix so they're formally disjoint from `"global"` under the shared `rl:login` Redis prefix; `bucketKey` hoisted so the HMAC runs once per request and the limiter/invalid-credentials logs can't drift; log levels split (routine 429 → `info`, fail-open Redis-down → `error`) so dashboards alert by level; invalid-credentials payload always carries the resolved key (never an empty `{}`). `AdminAuditTag` typed closed union from `const ADMIN_AUDIT_TAGS=[…]as const` (05-12–05-13); `AdminAuditPayload.batchId: string|null` for bulk-import log correlation (05-17); `/api/upload` → `/api/admin/upload` single `startsWith("/api/admin/")` gate in `proxy.ts` (05-17); `sanitizeLogString` against log injection (05-13). [source](dev-journal/2026-06-10.md) [source](dev-journal/2026-06-13.md)

- **Time-based scheduled publishing** — Three-reversal arc (05-14): (1) filter inside `unstable_cache` (futures permanently invisible until mutation bust), (2) filter at read time with padded bounded caches (`take: LIMIT + futureCount` at fill, zero overhead at read), (3) `isFutureDatetime(datetime, now)` with required `now` on 05-17 (avoids `vi.mock` intra-module reference trap; lets bulk callers capture one `now` per batch). Unbounded caches (archive, sitemap) drop futures at the wrapper; single-row caches (`getPostBySlug`) return `null` if `datetime > now`. `totalPages` moved to request-time count. [source](dev-journal/2026-05-14.md) [source](dev-journal/2026-05-17.md)

- **Platform taxonomy refactor** — `Project.platform: String` required three separate derivation algorithms (grouping, display, redundancy); replaced with `bucket: PlatformBucket` + `platformTags: PlatformTag[]` as Postgres enums (05-20); `compactLabel` rules (0→bucket, 1→tag, Web/OpenSource spanning frontend+backend families → Fullstack, all-natural-multi→bucket, else Multiplatform); browser-safe `@generated/prisma/enums` import fixed Turbopack `node:module` crash (05-20); `superRefine` coherence gate shared across create/update schemas; `getProjectsGalleryCached`/`getProjectsForAdmin` split decouples cache vs sort (05-21). [source](dev-journal/2026-05-20.md) [source](dev-journal/2026-05-21.md)

- **Admin UX polish and bulk import** — Bulk import: `bulkImportParser.ts` with `FILENAME_REGEX`, `createManyAndReturn + skipDuplicates`, post-insert slug-set diff for concurrent-write silent-drop reconcile, `[api:admin:posts:BULK]` audit tag, `·Scheduled` suffix on future posts (05-15); `BooleanFlagToggle` collapse from `PostPublishedToggle` + `IsFeaturedToggle`, stale-revert bug fixed (capture `prev = isOn` before optimistic flip, not `initialProp`) (05-17); `useOptimisticMutation` `console.error` in failure branches (05-17). [source](dev-journal/2026-05-15.md) [source](dev-journal/2026-05-17.md)

- **Test infra speedup and `src/lib/` reorg** — `setupUser({ delay: null })` helper + `AnimatePresence mode="sync"` global mock → 47% test suite speedup (8.91s → 4.69s across 79 files) (05-18); `src/lib/` reorganized into 6 buckets: `db/`, `auth/`, `api/`, `content/`, `client/`, `utils/` — 140 files, 220 import rewrites in one atomic commit `8d8fa8b` (05-18). `Post.summary: String NOT NULL` with `deriveSummary` word-boundary truncation and `SUMMARY_MAX_CHARS = 160` (05-18). [source](dev-journal/2026-05-18.md)

- **Manifest-driven project importer** — `scripts/import-projects.ts` with per-project `project.json` manifest; reuses `projectCreateSchema` (powers `--dry-run`) and `projectMappers.ts`/`projectImport.ts`; deliberately omits the upload route's MIME/size checks (trusted first-party files) (06-08). Keys went content-addressed (hash baked into `projects/<slug>/<hash>-<file>`) to fix CDN cache-busting, then 06-13 (from the 06-11 review) closed the resulting failure modes: extracted a pure, dependency-injected `src/lib/import/blobSync.ts` (`listProjectBlobs`, `syncImages`, `pruneOrphans`, `formatBytes`) so pagination/concurrency/prune are unit-testable with in-memory fakes; **prune-after-import** sweeps unreferenced blobs under the project prefix (gated by `--no-prune`); paginated the blob listing; bounded-concurrent uploads (4, fail-fast); widened the hash 48→64-bit with a byte-size assert on reuse as a collision backstop; `listExistingBlobs` distinguishes credential misconfig (rethrow) from transient errors (warn). [source](dev-journal/2026-06.md) [source](dev-journal/2026-06-13.md)

- **Legal / privacy page layout** — `PrivacyPageLayout` (shared shell: `PageGlow` + centered wrapper + auto-rendered Last-update/Contact boilerplate; `<main>`→`<div>` so the document keeps one landmark, 05-13) generalized 06-13 into `LegalPageLayout` with a `documentKind: "privacy" | "terms"` switch driving the boilerplate wording, so `terms/continuum` reuses the shell instead of hand-rolling it. Added `continuumLegalLinks.ts` as the single source for the four-page Continuum legal cluster (privacy / responsible-use / DPIA / terms) and an optional `relatedLinks` "Related" section cross-linking them. Fixed the responsible-use ordered-list double-numbering (`aria-hidden` decorative `01`–`06` markers + `role="list"` to survive WebKit's `list-style:none` semantics strip); standardized `AppleLegalLink` on `target="_blank" rel="noopener noreferrer"`. Legal `lastUpdated` dates kept at each page's true substantive-revision date (presentation refactors don't bump them). [source](dev-journal/2026-06-13.md)

## Timeline

- **2026-05-14** — Upstash keepalive → daily `0 0 * * *`; HMAC-SHA256 IP pseudonymization + admin keepalive endpoint; scheduled publishing via read-time filter + padded bounded caches. [source](dev-journal/2026-05-14.md)
- **2026-05-15** — Bulk import: `bulkImportParser.ts`, `createManyAndReturn + skipDuplicates`, `·Scheduled` suffix; `writeKeepalive` extraction. [source](dev-journal/2026-05-15.md)
- **2026-05-17** — `/api/upload` → `/api/admin/upload`; `BooleanFlagToggle` collapse; `isFutureDatetime(now required)`; `batchId` required; `getKeepaliveRedis()` lazy memoized; stale-revert bug fixed; bulk concurrent-write reconcile. [source](dev-journal/2026-05-17.md)
- **2026-05-18** — `Post.summary NOT NULL` + `deriveSummary`; `setupUser()` + `AnimatePresence mode="sync"` (47% speedup); `src/lib/` 6-bucket reorg `8d8fa8b` (session-lockout incident from `cd src/lib`). [source](dev-journal/2026-05-18.md)
- **2026-05-20** — `Project.platform` → `bucket + platformTags` Postgres enums; `compactLabel` + Fullstack family-set rule; browser-safe `@generated/prisma/enums`; two-phase deployment. [source](dev-journal/2026-05-20.md)
- **2026-05-21** — `superRefine` bucket/tag coherence; picker hidden-input → submit-gate; `getProjectsGalleryCached`/`getProjectsForAdmin` split; for-of single-pass partition. [source](dev-journal/2026-05-21.md)
- **2026-05-22** — `nextCacheSpyFactory` + module-load snapshot test; `mockPickerConfig.autoFill` enum union. [source](dev-journal/2026-05-22.md)
- **2026-06-08** — Manifest-driven project importer; `projectMappers.ts`/`projectImport.ts`; `scripts/` gitignore fix. [source](dev-journal/2026-06.md)
- **2026-06-10** — Login rate-limiter: missing `IP_HASH_SECRET` now degrades to a global bucket instead of disabling the limiter; new `route.ratelimit.test.ts` covers the Redis-present path via `vi.resetModules`+`vi.stubEnv`+class mocks. [source](dev-journal/2026-06-10.md)
- **2026-06-13** — Resolved the 2026-06-11 review across three areas: auth/rate-limit (`ip:`/`global` keyspace partition, hoisted bucket key, 429→info / fail-open→error log split, 5 new tests), importer (extracted testable `blobSync.ts`, prune-after-import for orphaned blobs, paginated list, bounded-concurrent uploads, 64-bit hash + byte-size collision guard), Continuum legal pages (`LegalPageLayout` generalization, cross-linked cluster, list a11y, true-revision dates). 1160 tests green. [source](dev-journal/2026-06-13.md)
