# Rolling History — last 30 days (through 2026-06-29)

## Themes

- **Manifest-driven project importer** — built `scripts/import-projects.ts` + per-project `project.json` + the `app-copy-to-project` skill over three sessions: 2026-06-08 used deterministic path keys with a (key+size) reuse heuristic; 2026-06-10 switched to content-addressed `SHA256[:12]` keys (same bytes = same key, changed bytes = automatic CDN cache-bust); 2026-06-13 extracted pure `blobSync.ts` (`BlobStore` DI interface, paginated listing, 4 bounded-concurrent uploads, `pruneOrphans` post-import sweep), widened hash to 64-bit, added byte-size assert as collision backstop. The `scripts/` gitignore was narrowed from `scripts/` to `scripts/imports/` so the script is trackable. Both Reckon and Continuum imported against prod; a persistent dual-implementation hazard (`scripts/import-projects.ts` keeps its own `writeProject`/`referencedImageUrls` logic separate from the tested `src/lib/import/projectImport.ts`) re-surfaced on 2026-06-28 when `cardImage`/`ogImage` were added. [2026-06-08](dev-journal/2026-06-08.md) [2026-06-13](dev-journal/2026-06-13.md) [2026-06-28](dev-journal/2026-06-28.md)

- **Project detail gallery — lightbox, carousel, and swipe** — 2026-06-26: `ProjectSectionCarousel` made controlled with gallery position lifted to `ProjectContent` (dots/tabs/lightbox share one index and direction); `ProjectImageLightbox` extracted (portal, focus-trap, body-scroll-lock, opaque `bg-neutral-950` backdrop); lightbox chevrons walk the whole gallery crossing section boundaries. Multi-round corner-halo bug root-caused to `object-contain` letterboxing preventing container `rounded-xl` from touching the visible image — fixed by aspect-true `next/image` sizing (`width={0} height={0}` + `h-auto w-auto max-h-* max-w-* rounded-xl`). 2026-06-29: tablist edge-fades faded to `--color-background-transparent` (CSS `transparent` is transparent *black*, smears grey on iOS Safari); carousel stage switched to measuring `naturalWidth/Height` in `onLoad` and driving `aspect-ratio` (capped, `h-120` fallback until measured); Framer `drag="x"` swipe added to both surfaces, routed through `navigateImage`, extracted to `src/lib/client/swipe.ts`. Slide transitions and `AnimatePresence` keying stripped in session 2 (no navigation animations in the gallery). [2026-06-26](dev-journal/2026-06-26.md) [2026-06-29](dev-journal/2026-06-29.md)

- **SEO and structured data** — 2026-06-28 added `ProjectFaq` model + `faqs` manifest field; FAQ accordion uses always-in-DOM + `inert` (text crawlable, collapsed panels out of a11y tree); `FAQPage` + `SoftwareApplication` JSON-LD with `applicationCategory`, `offers` (single `Offer` or `AggregateOffer` low–high), `metaTitle`, `keywords`. 2026-06-29 closed the blog gap with `buildBlogPostingJsonLd` (`PostDetail.updatedAt` added for `dateModified`); `llms.txt` as a dynamic `route.ts` (DB-backed, can't be a static file); `robots.ts` with `/api/` blocked except `/api/feed/` carved back (feed uses a 301 redirect *into* `/api/`). Both JSON-LD builders take `base` from `siteBase()` — removed the short-lived `SITE_ORIGIN` constant. [2026-06-28](dev-journal/2026-06-28.md) [2026-06-29](dev-journal/2026-06-29.md)

- **Login rate-limit hardening** — 2026-06-10: `Ratelimit` constructs whenever Redis is present regardless of `IP_HASH_SECRET`; missing secret degrades to a global bucket (`"global"`) rather than failing open; 6 per-IP tests added in a separate `route.ratelimit.test.ts` (required `vi.resetModules()` + dynamic import per test; Upstash mock classes, not arrow functions). 2026-06-13: `ip:` keyspace prefix makes the IP and global keys formally disjoint; `bucketKey` hoisted so HMAC runs once per request; log levels split (routine 429 → `info`, fail-open → `error`); `invalid-credentials` log always carries resolved bucket key. [2026-06-10](dev-journal/2026-06-10.md) [2026-06-13](dev-journal/2026-06-13.md)

- **Legal/privacy DRY and Continuum legal cluster** — 2026-06-08: `PrivacyPageLayout` gained `lastUpdated`/`contactEmail`/`contactPronoun?` with auto-appended boilerplate; `AppleLegalLink` component centralises the URL across 6 call sites; Continuum "Purchases" privacy copy committed. 2026-06-10 (inherited commit): Continuum DPIA and responsible-use routes added through `PrivacyPageLayout`; `terms/continuum` deliberately bypasses it (boilerplate would mislabel terms as a privacy policy). 2026-06-13: `LegalPageLayout` generalised with `documentKind: "privacy" | "terms"`, optional `relatedLinks`; `continuumLegalLinks.ts` cross-links the four-page cluster; legal dates held at true substantive-revision dates (a layout swap does not bump the date). [2026-06-08](dev-journal/2026-06-08.md) [2026-06-13](dev-journal/2026-06-13.md)

- **Back/forward entrance suppression** — `fadeUp`'s `initial → animate` replayed on every `popstate` because Next App Router remounts the route subtree on back/forward. Fixed with `src/lib/client/navigationType.ts` + `NavigationTypeTracker` island in root layout: Navigation API `currententrychange` mirrors the navigation type into a module flag; `fadeUp` returns `initial: false` on back/forward, jumping to the resolved state. Navigation API chosen over History `pushState`-patching (user rejected monkey-patching globals). Gracefully degrades on Firefox/older Safari (matches prior behaviour). [2026-06-29](dev-journal/2026-06-29.md)

## Timeline

- **2026-06-08** — Manifest-driven importer shell: `project.json`, `import-projects.ts`, `app-copy-to-project` skill, `tsx` devDep + `db:import-projects` script; `scripts/` gitignore narrowed to `scripts/imports/`; `heroImage` defaulted to first section image at import; section titles shortened to 2–3 words + `overflow-x-auto` scrollable tablist; Reckon accent `#B5673F` chosen. [source](dev-journal/2026-06-08.md)
- **2026-06-08** — `PrivacyPageLayout` DRY: `lastUpdated`/`contactEmail`/`contactPronoun?`, auto-appended boilerplate; `AppleLegalLink` component; Continuum "Purchases" privacy copy. [source](dev-journal/2026-06-08.md)
- **2026-06-10** — Importer keys → content-addressed `SHA256[:12]` (CDN cache-bust on content change); rate-limiter active whenever Redis configured; `IP_HASH_SECRET` absent degrades to global bucket; per-IP test file added. [source](dev-journal/2026-06-10.md)
- **2026-06-10** — Continuum DPIA + responsible-use + `terms/continuum` routes committed (inherited). [source](dev-journal/2026-06-10.md)
- **2026-06-13** — `blobSync.ts` extracted; hash widened 48→64-bit + byte-size backstop; `pruneOrphans` post-import; `BlobAccessError` rethrows vs transient warn; `LegalPageLayout` generalised with `documentKind` + `relatedLinks`; `continuumLegalLinks.ts` added. [source](dev-journal/2026-06-13.md)
- **2026-06-13** — `ip:` keyspace prefix + `bucketKey` hoisted per request; log levels split (429 → `info`, fail-open → `error`); `responsible-use` list a11y fixed (`aria-hidden` on decorative markers + `role="list"`). [source](dev-journal/2026-06-13.md)
- **2026-06-13** — `FeaturedProjectCard` equal-height via `flex h-full flex-col` + `flex-1` Link chain; hook migration to shared `~/.claude/hooks/web-auto-allow.jq` (`slug=rlr`). [source](dev-journal/2026-06-13.md)
- **2026-06-26** — `ProjectImageLightbox` extracted (portal, opaque backdrop, focus-trap); `useScrollOverflow` hook + edge-fades widened to `w-20`; gallery position lifted to `ProjectContent`; lightbox chevrons walk all sections with tab `scrollIntoView`; corner-halo fixed by aspect-true `next/image` sizing. [source](dev-journal/2026-06-26.md)
- **2026-06-28** — `Project.cardImage` + `Project.ogImage` added; `resolveCardImage`/`resolveOgImage` helpers sharing `firstSectionImage`; `gallery featuredImage` chain = `cardImage ?? ogImage ?? heroImage ?? first`; import script fixed to write both fields. [source](dev-journal/2026-06-28.md)
- **2026-06-28** — `ProjectFaq` model + relation; FAQ always-in-DOM accordion with `inert` for crawlability; `FAQPage` + `SoftwareApplication` JSON-LD; `metaTitle`, `keywords`, `offers`, `applicationCategory` schema fields; Continuum + Reckon manifests updated; DB push pending. [source](dev-journal/2026-06-28.md)
- **2026-06-29** — Tablist edge-fade black-smear (iOS Safari) fixed via `--color-background-transparent`; carousel stage measures `naturalWidth/Height` on `onLoad` + drives `aspect-ratio`; Framer `drag="x"` swipe on carousel + lightbox via shared `src/lib/client/swipe.ts`. [source](dev-journal/2026-06-29.md)
- **2026-06-29** — Slide `AnimatePresence` transitions and `slideDirection` plumbing stripped; tab-panel `motion.div` replaced with plain `div`; no navigation animations in gallery. [source](dev-journal/2026-06-29.md)
- **2026-06-29** — Back/forward entrance suppression: `navigationType.ts` + `NavigationTypeTracker` island; `fadeUp` returns `initial: false` on `popstate`; Navigation API `currententrychange` over History patching. [source](dev-journal/2026-06-29.md)
- **2026-06-29** — `robots.ts` (allow-all, `/api/` blocked except `/api/feed/`); `llms.txt` dynamic route handler; `BlogPosting` JSON-LD (`buildBlogPostingJsonLd`, `PostDetail.updatedAt`); both JSON-LD builders use `siteBase()`. [source](dev-journal/2026-06-29.md)
