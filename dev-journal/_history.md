# Rolling History — last 30 days (through 2026-05-07)

## Themes

- **Broad simplify / DRY sweep** — 2026-04-16 extracted `capitalizeSection` (5 sites), unified `AnimatedPostCard`/`AnimatedProjectCard` into `AnimatedCard`, pushed `searchPosts` filter to Prisma `OR`, deduped `MarkdownEditor` pipeline. 2026-04-17 Session 1 ran 6 parallel agents across 96 files (10 commits): `React.cache()` for per-request `generateMetadata` + page body dedupe, `Section` tightened from `string` to `"tech" | "life"` union, `postDatetimeToISO` throws on malformed (replaces silent fallback), `Header.tsx` absorbs `SiteChrome.tsx`. Lesson: parallel agents in a shared checkout race on uncommitted state — serial + commit-between for all subsequent rounds. [source](dev-journal/2026-04-17.md)

- **Legacy redirect architectural rethink** — `/api/legacy-redirect/[slug]` replaced by `src/app/[slug]/page.tsx` catch-all (2026-04-17 Session 3). `notFound()` renders `app/not-found.tsx` with full site chrome; `permanentRedirect` is a one-liner; `KNOWN_ROUTES` allowlist dissolved (Next.js static routes are the allowlist natively). `lookupLegacySlug` extracted to `legacySlug.ts`. 14 API-route tests replaced by 7 `legacySlug` + 5 page tests — decoupled from `NextRequest`/`NextResponse` mocking. [source](dev-journal/2026-04-17.md)

- **Atom feed quality** — `defaultProto` dropped the port heuristic: any host other than `localhost`/`127.0.0.1` → `https` (non-Vercel reverse-proxy hosts no longer silently get http feeds). Feed `<updated>` uses `Math.max(updatedAt)` across the 20-entry slice, not `posts[0].updatedAt` (edits to older posts advance the timestamp). 2026-04-20 fixed a 500 on cache hit: `unstable_cache` JSON-serializes `Date` fields — `updatedAt.getTime()` exploded on cache read; normalized to ISO string inside the cached function. `unstable_cache` wrapper-per-call anti-pattern lifted: per-slug `Map<slug, wrapper>` for `getPostBySlug`/`getProjectBySlug`, module-scope for `lookupLegacySlug`. 2026-04-21: `stripMarkdown` via unified AST walk (not regex); image alt text extracted; `escapeCdata` helper for `]]>` collision. [source](dev-journal/2026-04-20.md)

- **Security hardening batch** — 2026-04-26: hex regex `^[0-9a-f]*$` on `ADMIN_HASH_PASSWORD` in Zod schema (fails loud on misconfigured deploys). Typed `EnvConfigError` with `code`/`varName`. Module-load dummy bcrypt (`bcrypt.hashSync("dummy", 10)`) at cold start for null-credentials timing defense (~80ms one-time cost). `crypto.timingSafeEqual` via `isAuthorized` helper for cron Bearer. Both `login/route.ts` and `cron/ping/route.ts` use `new Redis(getRedisConfig())` (replaces `Redis.fromEnv()` bypass). `accentColor` nullable CSS interpolation fixed (`color-mix()` was receiving `undefined`). `Number(e.target.value)` NaN guard on `sortOrder` inputs. Successful login now emits `console.info("[api:auth:login] success")`. [source](dev-journal/2026-04-26.md)

- **Codebase-review remediation arc (2026-05-07)** — single-day session worked the 2026-05-07 full-codebase review across 14 phases (17 commits, 721 → 840 tests). Tightened `parseIntId` to `^-?\d+$` (kills `12abc`→12 hole), clamped `parsePageParam` at 10k, NFKD-normalized `createSlug`, even-length-hex on `ADMIN_HASH_PASSWORD`, word-boundary `platformBucket` (`webhook` no longer matches `web`), `loginSchema.email` `.toLowerCase().trim()` transform, `searchPosts` 2-char minimum, `siteBase` `VERCEL_URL` fallback, anchored `filterAdminEvents`. Schema: `postCreateSchema.datetime` regex enforced at write time; projects POST `sortOrder` clamped to `[0, count]`. API: `parseJsonBody` helper across 6 admin handlers, posts PUT cross-section cache bust, `ALLOW_UPLOADS` env flag + Content-Length precheck on uploads, login per-IP rate-limit + Upstash fail-open. Observability: JWT `ERR_JWT_EXPIRED` → `console.info`, request id in `respondInternalError`, `(HTTP NNN)` suffix on surfaced errors, `handlePrismaError` warn-on-404. Concurrency: `AbortController` on `IsFeaturedToggle`, `ProjectSortOrderInput`, `useAdminResource`. DRY: `revalidateProject(slug)` helper, `/api/admin` exact-match gate, `ClientAnalytics` merge of two Vercel telemetry wrappers. UX/A11y: `AdminNav` blocks redirect on logout failure + surfaces error; `LoanCalculatorInput` nests input in `<label>` for implicit association. Refactor: `(protected)/page.tsx` split into `PostsTab` + `ProjectsTab` + `AdminPagination` with `parseTab`/`buildAdminPageUrl` in `src/lib/adminPageUrl.ts`. Extracted `<ErrorMessage>` covering 7 admin error surfaces. Withdrawn-on-review: `ProjectContent` `<style>` scoping (variable consumed by ancestor header), `Header.tsx` segment-bound rewrite (already correct), `PlatformPicker` derived state (defensive for non-existent scenario). [source](dev-journal/2026-05-07.md)

- **Test infrastructure correctness (2026-05-07)** — `vitest.config.ts` glob excluded `src/app/**/*.test.tsx`: two existing edit-page tests had been silently skipped, one of them asserting a `findUnique({ select: { title: true } })` shape the implementation never used. Glob widened to `src/**/*.test.tsx` (later simplified by user). Tests rewritten to assert on returned JSX element `props` since server components don't invoke their child mocks synchronously (`<X ... />` builds an element, doesn't call `X`). `vi.mock` factory hoisting failure (`mockPostForm` referenced inside hoisted factory) fixed by inlining the mock implementation in the factory. `src/test/setup.ts` extended to spy `console.warn` and `console.info` so log-level assertions are possible. [source](dev-journal/2026-05-07.md)

## Timeline

- **2026-04-16** — `capitalizeSection`, `AnimatedCard`, Prisma OR filter for `searchPosts`, `MarkdownEditor` dedup, `SectionManager` stable keys, error surfacing on toggle/input. [source](dev-journal/2026-04-16.md)
- **2026-04-17** — 6-agent simplify sweep (96 files, 10 commits): `React.cache()`, `Section` closed union, `postDatetimeToISO` throws, `Header.tsx` absorbs `SiteChrome.tsx`. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — 20 review findings fixed: Prisma `Section` DB enum, `PresetOrFreeformInput` fully derived (no mirror state), `legacy-redirect` miss returns JSON 404. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — Catch-all `[slug]/page.tsx` replaces `api/legacy-redirect`; `lookupLegacySlug` extracted; `KNOWN_ROUTES` dissolved. [source](dev-journal/2026-04-17.md)
- **2026-04-17** — `defaultProto` drops port heuristic; feed `<updated>` via `Math.max(updatedAt)`; `navLinks` to `navigation.ts`; Zod `.min(0)` on `sortOrder`; `FadeIn.as` widened to `keyof typeof motion`. [source](dev-journal/2026-04-17.md)
- **2026-04-20** — Feed 500 fixed (`unstable_cache` Date→string serialization); `lookupLegacySlug` lifted to module scope; Map-memoized wrappers for `getPostBySlug`/`getProjectBySlug`. [source](dev-journal/2026-04-20.md)
- **2026-04-20** — `stripMarkdown` via unified AST walk; block-node `\n` for paragraph separation. [source](dev-journal/2026-04-20.md)
- **2026-04-21** — Image alt extraction in `extractText`; `escapeCdata` helper; `Nodes` import replaces local shim; `markdownToHtml` test inverted (drops raw HTML by default). [source](dev-journal/2026-04-21.md)
- **2026-04-26** — 8 of 9 review findings fixed: `EnvConfigError`, hex regex on `ADMIN_HASH_PASSWORD`, module-load dummy bcrypt, `isAuthorized` timing-safe helper, Redis via `getRedisConfig()`, `accentColor` CSS fix, NaN guard on `sortOrder`. [source](dev-journal/2026-04-26.md)
- **2026-05-07** — Codebase-review remediation: input validation, schema hardening, API reliability (`parseJsonBody`, cross-section bust, `ALLOW_UPLOADS`, login per-IP), observability (JWT log levels, request id, status-suffix), `AbortController` on admin mutations, `revalidateProject`, `/api/admin` exact-match, `ClientAnalytics` merge, reduced-motion blobs, `AdminNav` failure surface, `<label>` nesting. [source](dev-journal/2026-05-07.md)
- **2026-05-07** — `(protected)/page.tsx` split into `PostsTab`/`ProjectsTab`/`AdminPagination`; `<ErrorMessage>` extracted across 7 admin call sites. [source](dev-journal/2026-05-07.md)
- **2026-05-07** — Test infrastructure: glob widened to `src/**/*.test.tsx`; edit-page tests fixed (assert on returned JSX `props`); `vi.mock` factory hoisting workaround inlined; setup.ts spies `console.warn`/`console.info`. [source](dev-journal/2026-05-07.md)
