# Rolling History — last 30 days (through 2026-06-08)

## Themes

- **Project-import tooling** — 2026-06-08: built a project-agnostic importer that turns an app's marketing copy + screenshots into a portfolio project — `scripts/import-projects.ts` (runner), a per-project `project.json` manifest, and the project-level `app-copy-to-project` skill that authors the manifest + stages images. Direct Prisma + direct Blob (the `seed-projects.ts` pattern), idempotent via deterministic keys `projects/<slug>/<path>` (`addRandomSuffix:false, allowOverwrite:true`) + transactional delete-by-slug. Reuses `projectCreateSchema` (data-integrity, powers `--dry-run`) and `toSectionCreate`/`toLinkCreate` (extracted to Next-free `src/lib/db/projectMappers.ts`, shared with the admin POST/PUT routes); new slug/key logic in tested `src/lib/import/projectImport.ts`. Deliberately omits the upload route's MIME/size checks — those guard untrusted network uploads; the importer handles trusted first-party files. Added `tsx` + `db:import-projects`; narrowed root `.gitignore` (`scripts/` → `scripts/imports/`) after finding the whole `scripts/` tree was ignored (would've silently dropped the runner). Reckon staged as the first import (`bucket: iOS`, tags `[iOS, iPad]`). [source](dev-journal/2026-06-08.md)

- **`Project.platform` structural rewrite** — 2026-05-20: replaced free-text `Project.platform: String` with `bucket: PlatformBucket` (4 values) + `platformTags: PlatformTag[]` (19 values), eliminating three derivation algorithms in `src/lib/utils/platforms.ts`. `PlatformPicker` → bucket radio + tag chips scoped to `BUCKET_SUGGESTED_TAGS[bucket]` (OpenSource surfaces all). `compactLabel`: in-family multi-tags collapse to bucket label, Web/OpenSource spanning families → "Fullstack", outside-family → "Multiplatform"; `detailLabel` renders the honest stack. Phase 2 (NOT NULL `bucket`, drop `platform`) = `ecc86d1`. 2026-05-21 hardened it: schema-side `refineBucketTagCoherence` (`superRefine`) + duplicate-tag refine on create + update; explicit `<ErrorMessage>` submit gate replacing the picker's hidden `required readOnly` input; "Removed N tag(s)" `aria-live` hint on bucket switch. (This coherence rule is what blocks tagging `macOS` under the iOS bucket — surfaced again in the 2026-06-08 Reckon import.) [source](dev-journal/2026-05-20.md), [source](dev-journal/2026-05-21.md)

- **DB helper boundary clarity** — 2026-05-21: split `getAllProjectsForGallery({ sortDiscontinued })` into `getProjectsGalleryCached()` (public, cached, discontinued-last) and `getProjectsForAdmin()` (admin, uncached). One boolean had coupled cache-use AND ordering. 2026-05-22 pinned the cache contract in tests via `nextCacheSpyFactory` + a module-load `unstable_cache.mock.calls` snapshot asserting the exact `[{projects-gallery}, {all-project-slugs}]` set. [source](dev-journal/2026-05-21.md), [source](dev-journal/2026-05-22.md)

- **`src/lib/` 6-bucket reorganization** — 2026-05-18 (Session 3): `src/lib/` split into `db/`, `auth/`, `api/`, `content/`, `client/`, `utils/`; 140 files, 220 imports, `8d8fa8b`. Residual misassignments flagged: `keepalive`/`boundedCache` placement, `theme.ts` server surface, `auth/env.ts` creating a `db→auth` dep. [source](dev-journal/2026-05-18.md)

- **Post.summary derivation** — 2026-05-18 (Session 1): `Post.summary` → `String NOT NULL`; `deriveSummary(body)` strips markdown via AST, word-boundary truncation at `SUMMARY_MAX_CHARS = 160`, hard-slice fallback; edit-route resolution (authored→keep, cleared/body-changed→re-derive, else skip). [source](dev-journal/2026-05-18.md)

- **Test infrastructure speedup** — 2026-05-18 (Session 2): `setupUser({ delay: null })` project-wide; `AnimatePresence mode="sync"` global mock in `setup.dom.ts`; 21 files migrated; ~47% speedup (8.91s → 4.69s). Happy-dom Proxy identity bug diagnosed, not fixed. [source](dev-journal/2026-05-18.md)

- **Code-review cadence (May)** — daily-review → fix loop in steady state. 2026-05-21: of 10 issues, 6 were false positives after reading the cited code (pattern: always read the cited file before treating an analyzer finding as actionable). 2026-05-22: only 3 surfaced, all real, all closed inline — the cadence is tight enough that each day's delta reviews-and-closes in one follow-up. [source](dev-journal/2026-05-21.md), [source](dev-journal/2026-05-22.md)

## Timeline

- **2026-05-18** — `Post.summary` NOT NULL + `deriveSummary` (S1); `setupUser({ delay: null })` + global `mode="sync"` mock, ~47% test speedup (S2); `src/lib/` 6-bucket reorg 140 files `8d8fa8b`, session-lockout incident (S3). [source](dev-journal/2026-05-18.md)
- **2026-05-20** — `Project.platform` free-text → `bucket` + `platformTags`; `PlatformPicker` bucket-radio + tag-chips; `compactLabel`/`detailLabel`; phase 1 + 2 deployed; S2 broadened Fullstack to family-sets, cross-listed `Next`. [source](dev-journal/2026-05-20.md)
- **2026-05-21** — Schema-side bucket/tag coherence + duplicate-tag refine; explicit `ProjectForm` submit gate; pruned-tags hint; split `getProjectsGalleryCached()` + `getProjectsForAdmin()`. [source](dev-journal/2026-05-21.md)
- **2026-05-22** — Closed review findings: stale comment in `projects.test.ts`; `ProjectForm` empty-tags branch; `getProjectsForAdmin` cache-bypass pinned via `nextCacheSpyFactory` + module-load snapshot. [source](dev-journal/2026-05-22.md)
- **2026-06-08** — Project-import tooling: `scripts/import-projects.ts` + `project.json` manifest + `app-copy-to-project` skill; direct Prisma + Blob, idempotent; `projectMappers.ts`/`projectImport.ts` (tested); dropped importer image security checks; `tsx` + `db:import-projects`; root `.gitignore` narrowed to `scripts/imports/`. [source](dev-journal/2026-06-08.md)
