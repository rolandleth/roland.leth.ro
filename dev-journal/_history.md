# Rolling History — last 30 days (through 2026-08-12)

## Themes

- **Cache/revalidation granularity** — 2026-07-07 split the coarse `blog-{section}`/`projects` tags (caught via an ISR-dashboard tell — 8 blind writes on an unedited page) into per-slug detail tags plus dedicated `*-pages` aggregate tags, and added a session-gated admin `RevalidatePanel` since import scripts write via Prisma and can't call `revalidateTag`. 2026-07-17 session 3 debugged a prod stale-404 (a durably cached `null` in `unstable_cache`, predating the row) into a `CacheMissError` throw-on-miss pattern applied to all four detail lookups (posts, projects, guides, guide topics); a still-unresolved individual-vs-"all" tag-bust asymmetry is logged in `dev-journal/_revalidate-fix-plan.md`. 2026-07-18's guides review caught a `guide-` vs `guide-topic-` tag-namespace collision, fixed by prefixing to `guide-detail-`/`guide-topic-`. 2026-07-24 made the Atom feed route `force-static` with `revalidate=3600` (replacing a hand-set `Cache-Control` header `revalidateTag` could never purge from the CDN) and added the same backstop to the archive and sitemap for scheduled-post visibility. [source](dev-journal/2026-07-17.md) [source](dev-journal/2026-07-24.md)

- **Post-import slug tooling hardening** — 2026-07-16 made `slug:` frontmatter the explicit source of truth (`createSlug` idempotency doubles as the canonical-shape check: unchanged means valid, changed means rewrite), fixed the admin bulk-import route's silent regression to title-derived slugs by sharing `parsePostFiles` with the script, and kept `init-post-slugs.ts` as a resync tool rather than a one-shot throwaway. 2026-07-17 session 4 acted on that day's code review: byte-canonical slug-drift healing (comparing `setFrontmatterSlug`'s own output to the file, not a trim-only string compare), `matchRow` now refuses an ambiguous title-match-at-a-different-datetime bind rather than guessing, plus a `KNOWN_FLAGS` guard and atomic file writes (`writeFileAtomic`). [source](dev-journal/2026-07-16.md) [source](dev-journal/2026-07-17.md)

- **Guides section: build, review, fix-the-fixes** — 2026-07-17 sessions 1-2 built the entire guides feature in one day: a new `Guide`/`GuideTopic` entity (not `Post` reuse, which would inherit a dated/frozen lifecycle guides don't have), top-level `/guides/:slug` routes, project referenced by slug rather than FK (dodging the project importer's delete-then-create cascade trap), topic membership by directory rather than a `topic:` key, and product links as authored prose rather than a `GuideProjectCta` card (a rendered card can't disclose *who made the thing*; an importer warning recovers the "always present" guarantee instead). The same day, a prod stale-404 incident was debugged and fixed (see Cache theme). 2026-07-18 resolved that day's ~39-finding code review in 8 phased commits, deliberately deferring 2 findings (a miss-stampede negative cache, an admin-search GIN index — both premature at current scale) and skipping 6 with recorded reasons, including a false positive on cross-table `Guide`/`GuideTopic` slug uniqueness. [source](dev-journal/2026-07-17.md) [source](dev-journal/2026-07-18.md)

- **IndexNow submission feature** — 2026-07-19 built admin-triggered IndexNow submission: a session-gated dashboard button, not a local script, so `getSiteUrl()` resolves the real prod origin with zero risk of announcing localhost; the verification key is served from `INDEXNOW_KEY` at a fixed `/indexnow-key.txt` path so the served body and the submitted key read the same accessor and can't drift. 2026-07-20's scheduled code review found the lib reported `submitted: N` even when every batch 403'd — split into `attempted`/`accepted` — and fixed `isSubmittableOrigin` to actually reject IP literals as its comment already claimed. 2026-07-21 fixed a double-body-read bug (`response.json()` consumed the `Response` before the error-message fallback re-read it) that had been swallowing real error messages behind a generic "Request failed", and extracted a shared `useAdminAction` hook (not a wrapper component) after ~25 lines of fetch/abort/supersede scaffolding had been copied verbatim into a second admin panel. [source](dev-journal/2026-07-19.md) [source](dev-journal/2026-07-20.md) [source](dev-journal/2026-07-21.md)

- **Admin auth: single gate to defense in depth** — 2026-07-17 session 5 fixed a latent `isAdminPage` over-match (`/administrator`, `/admin-notes` wrongly treated as protected via a bare `startsWith`) while building bot-probe-filter test fixtures. 2026-07-20 flagged, and deliberately deferred, that `src/proxy.ts` middleware is the sole authorization boundary for all 12 admin routes — correct on Next 16.2.10 but against Vercel's own guidance, with the `x-middleware-subrequest` CVE (2025-29927) as precedent. 2026-07-26 turned that watch-out into a real incident: `config.matcher`'s extension-exclusion group was unanchored, so any path merely *containing* an excluded extension's substring (a `.json` path contains `.js`) bypassed the middleware entirely, with no handler re-checking the session as a backstop — unexploitable in practice only because every admin `[id]` route parses its segment as a plain integer, rejecting a dotted path before any DB access. Fixed from both sides: an anchored, explicit-namespace matcher plus a new `requireAdmin()` guard added to all 21 handlers across 13 route modules, alongside a `SESSION_SECRET` 32-character floor in `getSessionSecret()`. 2026-08-12 narrowed the remaining half: `requireAdmin` was confirmed to cover `/api/admin/*` route handlers **only**, so admin *pages* had no equivalent — and all four `[id]/edit` pages read the DB in `generateMetadata`, which runs independently of `admin/(protected)/layout.tsx`. A shared `adminEditMetadata` guard in `src/lib/auth/adminMetadata.ts` now runs `verifySession()` before the loader, and the `src/proxy.ts` matcher comment was rewritten to state the invariant per namespace instead of claiming one that held only for the API side. [source](dev-journal/2026-07-20.md) [source](dev-journal/2026-07-26.md) [source](dev-journal/2026-08-12.md)

## Timeline

- **2026-07-17** — Diagnosed a Vercel Fluid CPU split (middleware 53.8% / function 46.2%) from an analytics screenshot and added a narrow bot-probe short-circuit in middleware (an extension `Set` + segment-boundary path list, not a blanket dotfile rule). [source](dev-journal/2026-07-17.md)
- **2026-07-19** — `.txt` confirmed whitelisted in the bot-probe filter so `/indexnow-key.txt` passes middleware cleanly; `INDEXNOW_KEY` documented as optional (unset 404s the key route, 400s the submit route rather than 500ing). [source](dev-journal/2026-07-19.md)
- **2026-07-20** — A review subagent produced a confidently wrong finding about `proxy.ts`'s admin-API redirect behavior, contradicted by its own paired gatherer; dropped before reaching the review file. [source](dev-journal/2026-07-20.md)
- **2026-07-24** — Fixed feed autodiscovery being entirely absent site-wide (the Express→Next migration had dropped it) and invalid Atom output (missing feed-level `<author>`, dropped `<subtitle>`/`<icon>`/`<rights>`); added `.claude/launch.json` so the app's preview panel can launch a dev server the sandbox can't bind directly. [source](dev-journal/2026-07-24.md)
- **2026-08-12** — Project detail hero row (`ProjectContent.tsx`) was overflowing a 375pt viewport — `scrollWidth` 379 vs `clientWidth` 375, measured on the live site with a single link, from a `justify-between` row with a `shrink-0` links grid and a ~148px `Get on App Store` pill. Now stacks below the `sm` breakpoint. Affects 375pt devices only (iPhone SE, 12/13 mini); 393pt+ had room. [source](dev-journal/2026-08-12.md)
- **2026-08-12** — Discontinued projects stop selling but stay reachable: bare label on the hero storefront pill, no repeated CTA below the content. The rejected alternative kept `Get on`, on the grounds that delisting and discontinuing differ and an unmaintained app can still be installable. `isStoreUrl`'s hardcoded hostname became a `STORE_HOSTNAMES` set (behaviour unchanged — no non-Apple storefront exists in the data). [source](dev-journal/2026-08-12.md)
- **2026-08-12** — Tooling gotcha worth not repeating: `rg -r` is `--replace`, not "recursive". `rg -rn "requireAdmin"` silently rewrote every match in the output to `n`, making a normal Zod schema look like it was bound to a one-letter variable and nearly producing a bogus code-quality finding. Use plain `rg -n` / `rg -l`. [source](dev-journal/2026-08-12.md)

<!-- end -->
