# Rolling History — last 30 days (through 2026-05-13)

## Themes

- **Simplify/restructure sweep (April)** — Two April sessions overhauled routing, data layer, and DRY: April 16 extracted `capitalizeSection` (5 sites), unified `AnimatedPostCard`/`AnimatedProjectCard` into `AnimatedCard`, pushed `searchPosts` filter to Prisma `OR`, deduped `MarkdownEditor` pipeline ([2026-04-16](dev-journal/2026-04-16.md)). April 17 ran 6 parallel agents across 96 files (10 commits): `React.cache()` for per-request `generateMetadata` + page body dedupe, `Section` tightened from string to `"tech" | "life"` union, `postDatetimeToISO` throws on malformed (replaces silent fallback), `Header.tsx` absorbs `SiteChrome.tsx`, catch-all `[slug]` page replacing per-section API-route rewrites. Lesson: parallel agents in a shared checkout race on uncommitted state — serial + commit-between for all subsequent rounds ([2026-04-17](dev-journal/2026-04-17.md)). April 20: feed 500 regression fixed — `unstable_cache` boundary was wrapping non-JSON-serializable data; pattern locked: serialize before caching ([2026-04-20](dev-journal/2026-04-20.md)). April 21: `stripMarkdown` improvements ([2026-04-21](dev-journal/2026-04-21.md)).

- **Security & input hardening** — Timing-safe bearer token comparison + env validation tightened ([2026-04-26](dev-journal/2026-04-26.md)). May 7: per-IP login rate-limit replaced global bucket — `clientBucketKey` keyed on leftmost `x-forwarded-for` IP; fail-open on Upstash/Redis error so a Redis blip doesn't lock the only public auth entry out; `ALLOW_UPLOADS === "true"` env flag replaced `NODE_ENV !== "production"` gating ([2026-05-07](dev-journal/2026-05-07.md)). May 11: magic-byte image-MIME sniff via `detectImageMime` (PNG/JPEG/WEBP/GIF/AVIF `ftyp` brands accepted; SVG deliberately excluded for XSS shape) — read 12 bytes from already-buffered formData, near-free; `parseJsonBody` schema-validation logs `path` only, never values, with a test pinning "submitted value never appears in log calls" ([2026-05-11](dev-journal/2026-05-11.md)). May 13: `sanitizeLogString` strips CR/LF/TAB/NUL + clamps to 200 chars before logging the multipart parser's `error.message`, closing a log-injection vector ([2026-05-13](dev-journal/2026-05-13.md)).

- **Admin write contract: unified audit log + abort-aware optimistic mutations** — May 7 introduced `AbortController` on every admin mutation (`useRef` + `useEffect` cleanup) and extracted `<ErrorMessage>` (7 callers) ([2026-05-07](dev-journal/2026-05-07.md)). May 11 Session 2 added the typed `auditLog(tag, payload)` helper with a fixed 6-key shape (`id, slug, section, sortOrder, previousSection, previousSlug`); Session 3 tightened `AdminAuditPayload` from optional `?` to required `T | null` so silent-null regressions in posts/projects PUT surface at tsc instead of in logs; `isAbortError` helper extracted (5 sites, `DOMException` + `Error` shape) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 typed `AdminAuditTag` as a closed union, unified `previousSlug` audit semantics across posts/projects PUT to "the slug renamed, here's what it was" (not "name was edited"), and extracted `useOptimisticMutation` at the 2nd consumer (`IsFeaturedToggle` + `ProjectSortOrderInput` collapse to ~30 LOC each) ([2026-05-12](dev-journal/2026-05-12.md)). May 13 derived `AdminAuditTag` from a `const ADMIN_AUDIT_TAGS = [...] as const` array (single edit site for new admin resources, runtime list now greppable); widened `MutateResult` to `{ ok: true } | { ok: false; reason: "failure" | "superseded" }`; nulled `abortRef.current` in the hook's cleanup to close an unmount-after-resolve race a new test exposed ([2026-05-13](dev-journal/2026-05-13.md)).

- **Future-dated content + cache invariants** — May 11 Session 1 cascaded `datetime <= now` filter across `getPostBySlug`, `getAllPublishedPostSlugs`, sitemap, `generateStaticParams`, `legacySlug.cachedLookup`. `findUnique` → `findFirst` migration required because compound-unique `(section, slug)` can't take filter clauses (every test mock updated). Captured `now` inside cached fns so each entry is "valid for this `now`-snapshot, evicted on mutation bus or organic expire" — tradeoff: future-dated published posts don't auto-surface at their scheduled time (revisit if time-based scheduling becomes a feature) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 added `@@index([slug])` to `Post` (legacy-slug lookup was seq-scanning `posts`); escalated posts PUT to Serializable isolation to close the cross-section cache-invalidation race ([2026-05-12](dev-journal/2026-05-12.md)). May 13 clarified in the route doc that the resulting `serialization_failure → 500` is also not client-retriable (Prisma doesn't auto-retry; admin must retry manually) ([2026-05-13](dev-journal/2026-05-13.md)).

- **A11y + structural cleanup** — May 11 added the carousel APG pattern (`role="group"`, `aria-roledescription="carousel"`, `aria-live="polite"` on the slide region), padded-button hit regions for dot indicators (WCAG AAA, replacing the overlap-prone `before:-m-2.5` trick), and a skip-link wrapping `{children}` in a single `#main-content` wrapper. `<time dateTime>` corrected to ISO 8601 (had been emitting the internal `yyyy-MM-dd-HHmm`, missed by two prior reviews — added to attribute-level audit targets) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 hoisted `<main>` from per-page wrappers into the root layout (one landmark per document) — 14 pages converted to `<div>`/`<section>`. `PresetOrFreeformInput` got differentiated `aria-label` suffixes for its dual labels ([2026-05-12](dev-journal/2026-05-12.md)). May 13's review caught that `PrivacyPageLayout.tsx` had been missed by the hoist — still rendered `<main>`, producing two landmarks on `/privacy` and `/privacy/body-tracking`; fixed alongside a landmark-structure test ([2026-05-13](dev-journal/2026-05-13.md)).

- **Code-review-driven phase commits cadence** — May 7's 17-commit batch (`codebase-2026-05-07.md`) established the pattern: daily review surfaces 8–15 findings; same-session multi-phase implementation pass closes each one; commits per phase, not at the end ([2026-05-07](dev-journal/2026-05-07.md)). May 11 ran three full-codebase reviews and three implementation passes in one day (24 commits across the day) ([2026-05-11](dev-journal/2026-05-11.md)). May 12 ran 7+3 phases (12 commits) over a daily review ([2026-05-12](dev-journal/2026-05-12.md)). May 13's 7-phase pass closed every actionable review finding — and exposed a real bug (`useOptimisticMutation` unmount-after-resolve race) via a test added during implementation: generalizable lesson, a test premise that "should pass with the current implementation" sometimes uncovers an actual implementation gap, not a bad test ([2026-05-13](dev-journal/2026-05-13.md)).

- **Error boundary decisions (May 9)** — `<Link>` → `<a>` reverted for all non-route hrefs on the about page (9 bio links + 3 `contactLinks`): `next/link` for `mailto:`, external URLs, and static assets pulls a client component and has undocumented prefetch behavior on non-route URLs ([2026-05-09](dev-journal/2026-05-09.md)). `error.digest` dropped from both `error.tsx` and `global-error.tsx` — Vercel Hobby ~1h runtime-log retention means the matching log is gone before a user reports the hash; re-introduce paired with external aggregation. RTL tests added for both error boundaries. `errorMessages.ts` deleted (2 stable strings don't justify a shared module). `ClientAnalytics` moved to `src/components/`.

## Timeline

- **2026-04-16** — Large simplify sweep: `capitalizeSection`, `AnimatedCard` unification, Prisma filter push, `MarkdownEditor` dedup. [source](dev-journal/2026-04-16.md)
- **2026-04-17** — 6-agent parallel pass (96 files, 10 commits): `Section` union type, `React.cache()`, `postDatetimeToISO` throws, catch-all `[slug]` page, `Header.tsx` absorbs `SiteChrome.tsx`. [source](dev-journal/2026-04-17.md)
- **2026-04-20** — Feed 500 fix; `unstable_cache` JSON-serialization pattern locked (serialize before caching). [source](dev-journal/2026-04-20.md)
- **2026-04-21** — `stripMarkdown` improvements. [source](dev-journal/2026-04-21.md)
- **2026-04-26** — Timing-safe bearer token comparison; env validation. [source](dev-journal/2026-04-26.md)
- **2026-05-07** — 17 commits (721→840 tests): per-IP rate-limit, `ALLOW_UPLOADS` lazy env flag, strict `parseIntId`, `platformBucket` word-boundary regex, `createSlug` NFKD normalization, cross-section cache invalidation, `<ErrorMessage>` extraction, admin `AbortController` pattern, vitest glob broadened, `console.warn`/`console.info` spying added. [source](dev-journal/2026-05-07.md)
- **2026-05-09** — `<Link>`→`<a>` revert for non-route hrefs; `error.digest` dropped from error boundaries; RTL tests for `error.tsx`/`global-error.tsx`; `ClientAnalytics` to `src/components/`; `errorMessages.ts` deleted. [source](dev-journal/2026-05-09.md)
- **2026-05-11** — Three full-codebase reviews + three implementation passes in one day (24 commits, 903 tests): `datetime <= now` cascade + `findUnique→findFirst` migration; magic-byte `detectImageMime` sniff; structured `auditLog` helper with required `T | null` shape; `parseJsonBody` paths-only logging; `isAbortError` extracted; `<time dateTime>` corrected to ISO 8601; carousel APG pattern + padded-button hit regions; skip-link wrapper; `ThemeProvider` to `useSyncExternalStore`. [source](dev-journal/2026-05-11.md)
- **2026-05-12** — Daily review → 7+3 phases (12 commits, 980 tests): `<main>` hoist from 14 pages to root layout; `Post.slug` index in schema; posts PUT escalated to Serializable; `useOptimisticMutation` extracted at the 2nd consumer; `AdminAuditTag` closed union; `previousSlug` audit semantics unified across posts/projects; `readErrorMessage` extracted to `lib/` at the 5th caller; `postDatetimeToISO` returns `string | undefined` (dropped `?? undefined` boilerplate at 3 React/Next call sites). [source](dev-journal/2026-05-12.md)
- **2026-05-13** — Daily review → 7-phase pass (7 commits, 998 tests): `PrivacyPageLayout` `<main>` cleanup (missed by Phase 2 hoist); `previousSlug` DRY lift in projects PUT; `MutateResult` discriminant (`failure` vs `superseded`) + unmount-after-resolve race fix exposed by a new test; `AdminAuditTag` derived from const array; `metadata.ts` dev-only double-brand throw; `apiErrors.describeZodIssues` dedupes segments; `sanitizeLogString` for upload route log-injection guard; `ProjectForm.handleSubmit` re-snaps from `sortOrderText` to prevent stale-value submission. [source](dev-journal/2026-05-13.md)
