# Dev Journal – 2026-05-20

## Open Threads

- [x] Run `yarn db:push` against prod to apply the final platform schema (drops `Project.platform`, sets `bucket` NOT NULL). **Resolved 2026-05-20**: pushed.
- [x] Deploy the `ecc86d1` commit immediately after the schema push. Brief broken window between the two is accepted. **Resolved 2026-05-20**: deployed.

## Watch-outs & Tech Debt

- **"Fullstack" alias is the lone editorial special-case in `compactLabel`** — Web/OpenSource buckets check tags against `WEB_FRONTEND_TAGS` + `WEB_BACKEND_TAGS` sets in `src/lib/utils/platforms.ts`; if both sets are hit, returns "Fullstack" (Session 2 broadened from literal-tag check to family-set check, cross-listed `Next` in both families, and extended to the OpenSource bucket). Still the only surviving tag-membership special-case. Revisit when a 2nd "two-tag → single-word" alias is needed; at that point, lift into a data-driven `BUCKET_ALIASES` constant.
- **Backfill scaffolding is gone** — `scripts/backfillBuckets.ts`, the disposable `/admin/migrate-buckets` route, and the `db:backfill-buckets` npm script were all removed pre-merge. If the platform shape ever changes again, rebuild. Revisit when adding another structural migration to `Project`.
- **`projects/[id]` PUT route uses `...rest` spread** — `src/app/api/admin/projects/[id]/route.ts` spreads the Zod-parsed body straight into Prisma's update payload. Works because every Zod field matches a Prisma column name 1:1. Revisit when adding a field whose API name differs from its column name; the spread will silently lose it.

## Session 1

### Decisions

- **Replace `Project.platform: String` with `bucket: PlatformBucket` + `platformTags: PlatformTag[]`** — chose structured storage over keeping free-text with smarter parsing. Rationale: the old system had three separate derivation algorithms (regex word-boundary keyword matching for buckets, comma-split + set membership for display, prefix/substring + case-normalize for redundancy) all reverse-engineering structure from prose. Storage now matches editorial intent; picker no longer reverse-parses its own output.
- **Bucket as Postgres enum, not entity table** — gained exhaustive switch, type safety, and convention parity with the existing `Section` enum. Rejected the entity-table version because (a) the user has added zero buckets in the project's lifetime so "inline-add from picker" was over-engineering, (b) splitting `Section` (enum) and `PlatformBucket` (entity) for two similar editorial taxonomies is its own smell, (c) per-bucket display rules ("Fullstack" for Web) live cleanly in a `switch (bucket)` but require magic-string slug checks against an entity.
- **Tags also as Postgres enum (`PlatformTag[]`), not freeform `String[]`** — same enum-vs-entity rationale, plus tags-as-enum makes the canonicalization concern moot (the enum value IS canonical) and the Fullstack rule becomes pure enum equality instead of fuzzy string matching. Accepted that adding a tag is a schema migration; this is rare and editorial.
- **`bucket` is explicit editorial input, not derived from tag membership** — the prior system used keyword-order in `PLATFORM_BUCKETS` to route OSS-flavor web projects to the Open Source bucket via the `"Web"` tag. New picker has bucket as a separate radio; tags are honest descriptors. Open Source's suggested tags include every tag (since OSS spans platforms); other buckets are scoped to their natural sets.
- **Single project entry for multi-platform-multi-stack apps, primary bucket only** — the user has exactly one such project (mobile-led with fe+be counterpart). Chose single entry with primary bucket (iOS) + tags spanning the full stack, over two entries or a `secondaryBuckets` schema field. Rejected the secondary-buckets affordance: designing for n=1 is over-engineering and gallery-filter discoverability matters less at portfolio scale (~20 projects).
- **Compact-label rules in `compactLabel(bucket, tags)`** — 0 tags → bucket label; 1 tag → tag label; Web + Frontend + Backend → "Fullstack"; multi-tag all within bucket's natural set → bucket label; otherwise "Multiplatform". Extrapolated the natural-set fallback from the user's explicit "iOS + iPad → 'iOS'" guidance. Detail page renders `tags.join(" + ")` directly, no inference.
- **Skipped the disposable `/admin/migrate-buckets` review page on prod** — built it in phase 1, but the backfill produced clean data after one tweak (mapping the legacy `"Web"` tag → `[Frontend, Backend]` for the single OSS-Web project). User went straight from phase 1 to phase 2.
- **Two-phase deployment, with brief broken window accepted** — phase 1 (additive schema + backfill, deployed) → phase 2 (final schema + code rewrite, single commit). User explicitly accepted minutes of broken state between the `db:push` finalize and the code deploy.

### Tradeoffs

- **Enum-locked bucket vs entity-table bucket** — gained type safety, exhaustive switch, ergonomic display-label maps. Accepted that adding a 5th bucket requires `schema.prisma` edit + `db:push` + redeploy (~10 min). Reversibility: moderate — migrating enum → entity later is a one-time copy/swap dance, not exotic but not free.
- **Enum-locked tags vs freeform `String[]`** — gained type safety + canonical-by-construction + no typo-shadow taxonomy. Accepted that adding a tag is a schema migration even if it only describes one project. Reversibility: moderate — same enum → string-array migration if needed.
- **Single-entry-with-tags vs multi-bucket placement for cross-stack projects** — gained simplicity, no double-counting in the gallery. Accepted that the project is "invisible" when scrolling the non-primary bucket section. Reversibility: easy — add a `secondaryBuckets: PlatformBucket[]` field later if discoverability ever matters.
- **`compactLabel` returns bucket label for in-family multi-tags (e.g. [React, Next] → "Web")** — gained consistency with the user's explicit "iOS + iPad → iOS" guidance and a simpler mental model (all-natural collapses, anything outside-family triggers Multiplatform). Old logic returned "Multiplatform" for `"React, Next"`. Reversibility: easy — flip one branch in `compactLabel` if undesired.

### Incidents / Bugs

- **Lint error: `useEffect` inside a `default:` factory member in the `ProjectForm.test.tsx` picker mock** — React Hook rules-of-hooks requires the enclosing function name to start with uppercase. Fixed by hoisting the mock to a named `MockPlatformPicker` function, then `vi.mock("@/components/admin/PlatformPicker", () => ({ default: MockPlatformPicker }))`. Vitest hoists `vi.mock` to before imports but the factory is invoked at module-import time, so the reference resolves.

### Environment / Config Changes

- **`prisma/schema.prisma`**: new `PlatformBucket` enum (`iOS | Mac | Web | OpenSource`) + new `PlatformTag` enum (19 values). `Project.platform: String` removed; `Project.bucket: PlatformBucket` + `Project.platformTags: PlatformTag[]` added.
- **Prod DB**: phase 1 (additive) applied via `yarn db:push` mid-session — `bucket` nullable + `platformTags` empty-default added alongside the still-present `platform: String` NOT NULL. Backfill script (now deleted) populated `bucket`/`platformTags` from `platform` for all rows. Phase 2 (drop `platform`, NOT NULL `bucket`) still pending — Open Threads above.
- **`package.json`**: `db:backfill-buckets` script added in phase 1, removed in phase 2 along with the script file.

### What did I, Claude Code, or my agents learn from this

- **Free-text DB column + multiple derivation algorithms is a strong, recurring smell** — when three different consumers (grouping, display, redundancy) each reverse-engineer structure from the same prose with different algorithms, the storage shape is wrong. If a picker has to reverse-parse its own output to render its UI, that's the signal.
- **Postgres enum arrays work natively with Prisma** — `PlatformTag[]` typed and indexable. No join table needed for descriptive tags that carry no extra attributes (no per-tag sort order, no relations). A join table would have been over-engineered here.
- **Identifier ≠ display label** — Postgres enum identifiers can't have spaces, so `MenuBar` (identifier) ↔ `"Menu bar"` (label) lives in a small `TAG_LABELS: Record<PlatformTag, string>` map. Same pattern as `BUCKET_LABELS` for `OpenSource` → `"Open Source"`. Don't fight Postgres' identifier rules.
- **Migration-window code can target the final schema** — during phase 2, every consumer was rewritten against the final schema shape (non-null `bucket`, no `platform`) even though the live Prisma client still reflected the intermediate state. Persistent stale-client diagnostics all clear together when the user runs `db:push` at the end. Better than a half-migrated codebase carrying transitional nullability.
- **Disposable migration UIs are sometimes worth building and then skipping** — built the `/admin/migrate-buckets` review page expecting the user to need it; backfill came out clean enough that they didn't. Time wasn't wasted — having it available as a safety net let them deploy with confidence. Pattern: build the safety net, plan to delete it.
- **`vi.mock` factories can close over top-level identifiers declared below them** — `vi.mock("…", () => ({ default: MockPlatformPicker }))` works even though `MockPlatformPicker` is declared after `vi.mock` in source order. Vitest invokes the factory at module-import time (post-hoist), so top-level bindings are in scope. Useful for keeping mocked React components named (rules-of-hooks).
- **Don't over-engineer for n=1** — the user's "multi-multiplatform" case is exactly one project. Designing a `secondaryBuckets` schema field for that one project would have added complexity (gallery double-counting decisions, picker UX for multi-select buckets) without payoff. Resist the pull toward symmetric flexibility when the data doesn't demand it.

### Notes

- Full rewrite landed in commit `ecc86d1` ("Restructure project platforms: bucket enum + platformTags array"), 30 files, +1042/-505, after `tsc=0`, `test=0`, `lint=0`.
- `scripts/` is gitignored — `scripts/seed-projects.ts` was updated alongside everything else but the edits are local-only and don't ship.
- Test fixtures touched across many files (every `platform: "iOS"` → `bucket: PlatformBucket.iOS, platformTags: [PlatformTag.iOS]`); the persistent stale-Prisma-client diagnostics during the rewrite were all the same underlying cause (regen needed).

## Session 2

### Decisions

- **Import platform enums from `@/generated/prisma/enums`, not `@/generated/prisma/client`** — Prisma 7 ships an enum-only `enums.ts` (explicitly marked "🟢 You can import this file directly") that has no `node:module` dependency, so it bundles safely for browser/client components. Importing the enums via the main client export dragged the full Prisma runtime into any `"use client"` module that transitively reached `platforms.ts` (`ProjectContent`, `PlatformPicker`, `ProjectForm`) and blew up Turbopack with "the chunking context does not support external modules (request: node:module)". 17 files updated to the new path.
- **Broaden Fullstack rule from literal `[Frontend, Backend]` to family-set membership** — `compactLabel` now fires Fullstack when Web/OpenSource tags hit at least one of `WEB_FRONTEND_TAGS` (`Frontend`, `React`, `Next`) AND at least one of `WEB_BACKEND_TAGS` (`Backend`, `Node`, `Vapor`, `Next`). The original regression: `[Vapor, React]` should be Fullstack (the old keyword-based logic treated it as such) but my literal-tag check missed it. Family-set check restores the original intent without the regex hairball.
- **`Next.js` cross-listed in both frontend and backend families** — pairing it with any other frontend or backend tag triggers Fullstack. Rationale: Next.js spans both halves; user's stack pattern is `[React, Next]` to signal "React UI + Next.js backend". Tradeoff: `[Next, Vapor]` also fires Fullstack (because Next contributes a frontend signal even though Vapor is the explicit backend) — defensible, slightly fuzzy.
- **`detailLabel` separator changed from " + " to ", "** — comma reads as a clean list on the project detail page where there's room. The " + " framing was overly mathematical for what's just an enumeration of stack components.
- **Defensive `?? []` on `compactLabel`/`detailLabel`/`isCompactLabelRedundant`** — Prisma's TS types claim `platformTags: PlatformTag[]` (non-null) but Postgres array columns without an explicit default are nullable at the column level. One stray NULL row would crash the gallery and detail pages with "Cannot read properties of null (reading 'length')". Defensive coercion is belt-and-braces; the real fix is `@default([])` on the column.
- **`@default([])` on `Project.platformTags`** — prevents Postgres from returning NULL for rows inserted without an explicit tag list. Schema-level guarantee; the defensive code is now redundant but kept as second line of defense.

### Tradeoffs

- **Family-set Fullstack rule vs explicit `[Frontend, Backend]` rule** — gained: matches user intent ("React+Vapor is fullstack"), restored original keyword-system behavior. Accepted: an extra layer of indirection in `compactLabel` (family sets vs direct includes). Reversibility: easy — flip back to literal-tag check if the fuzzier rule produces surprises.
- **Cross-listing Next in both families** — gained: user's natural tagging pattern (`[React, Next]`) now fires Fullstack. Accepted: combinations like `[Next, Vapor]` also fire Fullstack, which is technically correct (Next has frontend) but might surprise. Reversibility: easy — remove `Next` from one of the sets.
- **OpenSource bucket also gets the Fullstack rule** — gained: an OSS lib + frontend + backend tagging surfaces as "Fullstack" on lists instead of "Multiplatform", which is more informative when both halves are present. Accepted: the rule now bleeds across two buckets, weakening the per-bucket-rules clarity of the original design. Reversibility: easy — drop the `|| bucket === PlatformBucket.OpenSource` clause.

### Incidents / Bugs

- **`yarn build` failed locally + on Vercel: "the chunking context does not support external modules (request: node:module)"** — root cause: `src/lib/utils/platforms.ts` imported `PlatformBucket`/`PlatformTag` from `@/generated/prisma/client`, which transitively pulls in `PrismaClient` which uses `node:module`. `platforms.ts` is consumed by `ProjectContent.tsx` (a `"use client"` component), so the Prisma runtime got pulled into the browser bundle. Fix: swap to `@/generated/prisma/enums` everywhere. The same fault chain affected `schemas.ts`, `db/projects.ts`, `PlatformPicker.tsx`, `ProjectForm.tsx`, fixtures, and a long tail of test files (17 total).
- **`/admin/posts/bulk` prerender failed with `Cannot read properties of undefined (reading 'length')` on Vercel** — same root cause. The Turbopack chunking failure left some imports resolving to `undefined` at runtime; the first downstream `.length` access crashed the prerender. The error frame was framework-ignored (`at ignore-listed frames`), and the digest gave no app-code location — confirmed it was downstream of the module-graph corruption, not a code bug at the bulk page itself. Disappeared once the Prisma-client/enum import swap landed.
- **Project detail page 500 in browser with `t.length` (minified)** — same shape. Once the prod build succeeded, the detail page render path hit `compactLabel(bucket, platformTags)` and crashed. Two possibilities: stale data with `platformTags === null` (unlikely per user — confirmed no null rows), OR same module-graph corruption residue from the Turbopack run. Either way: defensive `?? []` + schema `@default([])` close both gates.
- **Fullstack rule too narrow on initial rewrite** — first-pass `compactLabel` only fired Fullstack on literal `[Frontend, Backend]`, regressing the original keyword-based logic that included framework pairs (`[React, Vapor]`, `[Frontend, Node]`, etc.). Caught by user testing post-deploy. Fix: family-set membership.

### Environment / Config Changes

- **`prisma/schema.prisma`**: `Project.platformTags` now has `@default([])`. Prod DB needs `yarn db:push` to materialize; no data backfill needed (user confirmed no existing NULL rows).
- **17 files swapped from `@/generated/prisma/client` → `@/generated/prisma/enums`** for `PlatformBucket`/`PlatformTag` imports. Other Prisma imports (`PrismaClient`, `Prisma`, `Post`) stay on the main client export.

### What did I, Claude Code, or my agents learn from this

- **`yarn build` is the load-bearing check when Prisma client meets client components** — `tsc --noEmit`, `vitest`, and `eslint` all passed cleanly with the broken imports because none of them traverse the bundler boundary. They only check types and module resolution. Turbopack is the only checker that actually attempts to chunk for the browser, and that's where `node:module` exploded. Lesson for future Prisma-touching work: run `yarn build` before declaring "all green".
- **Prisma 7's `enums.ts` is the browser-safe path** — when you only need enum values + types, import from `@/generated/prisma/enums`, not `@/generated/prisma/client`. The generated `enums.ts` file is explicitly marked "🟢 You can import this file directly" and skips the runtime client setup. Use this for any enum reference in client components, hooks, or modules that flow into client bundles.
- **Bundler chunking errors can manifest as runtime `.length` crashes** — Turbopack/webpack don't always hard-fail; they sometimes proceed past a chunking error, leaving the affected modules resolving to `undefined` at runtime. The first `.length`/`.map`/`.includes` on the undefined import crashes with a framework-ignored frame. If you see prerender-time TypeErrors with no app-code in the stack, suspect the module graph first, not the render logic.
- **Postgres array columns are NULL-capable even when Prisma's TS types claim non-null** — `PlatformTag[]` in schema.prisma → TypeScript `PlatformTag[]` at the type level, but the underlying column allows NULL unless `@default([])` (or `NOT NULL`) is set. One NULL row is enough to 500 the page that calls `.length` on it. Two-layer fix: schema default + defensive code coercion.
- **Editorial taxonomies grow** — within a single session, the Fullstack rule went from "literal `[Frontend, Backend]`" → "family-set spanning frontend+backend" → "also includes `Next` in both families" → "also applies to OpenSource bucket". Every step was justified, but the trajectory is the kind of thing that explains why "the one editorial special-case" watch-outs grow. Worth periodically checking whether enough of these have accumulated to warrant lifting into a data-driven `BUCKET_ALIASES` constant.

### Notes

- Three small commits this session: `f22abf0` (enum import swap), `80a2996` (Fullstack broadening + defensive null + separator + `@default([])`), and the still-pending wrap-up commit (OpenSource Fullstack extension + journal updates).
- The Google Fonts build errors locally were a network issue unrelated to anything in this session — Vercel reaches Google fine.
- Next 16 + React 19 emit cosmetic key warnings during prerender from internal `<head>`/`<meta>`/`<__next_viewport_boundary__>` renders. They're not from app code, they don't break the build, and they go away with Next patches. Confirmed not worth chasing.

<!-- end -->
