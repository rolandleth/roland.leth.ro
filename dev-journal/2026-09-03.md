# Dev Journal – 2026-09-03

## Open Threads

- [ ] Nothing on this route has been exercised in a running Next runtime — the dev server was declined in session 1 and not started in session 2, so the route resolving at `/blog/:section/:slug/override-preview`, the `force-dynamic` behaviour, the `redirect()`, and now the `console.info` hit line are all argued from types and unit tests, not observed. Hit the URL once on the next local run or deploy and confirm the log line actually appears.
- [x] Decide whether shared preview links should unfurl richer than title-only (pass `description`/`image` through `buildPageMetadata`). **Closed 2026-09-03 (session 2): staying title-only.** See session 2 Notes for the argument that was raised and declined.

## Watch-outs & Tech Debt

- **`force-dynamic` on `override-preview` is load-bearing, not decoration** — it was belt-and-braces while the route read a session cookie; once the auth gate came out, nothing else opts the route out of the full route cache. Remove it and the route freezes on first render and the live-yet verdict sticks, so the preview would keep serving after the post went live instead of redirecting. Now pinned by a test (session 2), so this is a documented invariant rather than a trap. Revisit only if the route gains a real per-request read. (Session 1 stated this watch-out as freezing "both the post body and the verdict" — the body half is wrong, see session 2 Decisions.)
- **Re-adding a session check to `override-preview` is not a one-line change** — the route sits outside the `src/proxy.ts` matcher, so a check in the page body alone leaves the matcher lying about what it covers. Two tests now fail on any session call from the page (session 2), which makes this a deliberate decision to reverse rather than something to drift into. Trigger: if the URL ever shows up in logs from traffic Roland didn't send.
- **`override-preview` is the only public page route not prerendered** — `force-dynamic` plus deliberately-uncached misses (`CacheMissError`) means each probe costs one function invocation and one live Prisma query, with no rate limit on the path. Same cost profile the root-level legacy slug route was deleted for. Inert while nothing advertises the suffix. Trigger: the hit log showing traffic Roland didn't send, or a jump in function invocations.

## Session 1

### Decisions

- **A separate route, not `?overrideSchedule` on the post page** — reading `searchParams` in `/blog/[section]/[slug]` opts the whole prerendered route into dynamic rendering, putting every blog hit on billed compute. Rejected the query param outright rather than reaching for a `has`-query rewrite in `next.config.ts` (which would have preserved the param at zero cost but added routing-layer surface for one person's occasional use).
- **The route is public** — no session gate. The URL itself is the override, so a preview link works for someone with no account. Reversed mid-session: it shipped session-gated first on a safe-default reading of an unanswered question, then came out.
- **307, not 308, on the live redirect** — once `datetime` passes, the preview URL redirects to the canonical post. A permanent redirect would be cached by the browser and break the URL for the next post scheduled at that slug.
- **`resolvePost` split into `resolvePostRow` + a narrowing wrapper** — one clock read and one `fetchPostRow` cache entry still back both, but the teaser narrowing (title/date, never the body) is now a visible step rather than baked into the resolver. `PostRowResolution` carries the full row on both branches; only the preview route reads it.
- **No preview banner** — `PostContent` already renders the post's date under the title, so a future date is the signal. (Roland's call; the first draft had a banner.)
- **Route appended to the post path** rather than a top-level `/override-preview/:section/:slug`, since the starting point is always a post URL you're already looking at.
- **No `BlogPosting` JSON-LD, and `noindex, nofollow`** — the structured data asserts a published article at a canonical URL, and neither holds. No `robots.txt` `Disallow`: that would stop a crawler reading the `noindex` it needs to obey.

### Tradeoffs

- **Public URL means the convention is the secret** — anyone who learns the `/override-preview` suffix can read *any* scheduled post, not just one they were sent. Accepted deliberately; the escape hatch if it's ever abused is to require a search param on top. Reversibility: easy.
- **Scope held to the detail route** — list, archive, search, feed, `llms.txt`, sitemap and the `.md` twin all still filter scheduled posts out, so a post is never surfaced early to someone who didn't ask for this exact URL. Cost: no way to preview how a scheduled post will look *in a list*. Reversibility: easy.

### What did I, Claude Code, or my agents learn from this

- **Mutation-test the guard, not just the suite** — ran both load-bearing tests against a deliberately broken implementation (auth check inverted, then the `live` status compared against a bogus value) and confirmed each goes red. The project's own recurring lesson ("an untested guard is an unverified guard") applies to brand-new tests too, not only inherited ones.
- **A comment's justification can go stale faster than its code** — removing the session check turned `force-dynamic` from a precaution into a requirement without changing the line itself. Three docblocks still claimed a session gate and had to be rewritten. When a decision is reversed, grep for the prose that argued the old one.
- **Answer the question that was asked before the one that seems bigger** — the access question (auth vs public) was posed as load-bearing and got dodged in favour of the route shape; I filled it with a safe default instead of re-asking, and the work was thrown away when the real answer arrived one message later.

### Notes

- `verifySession()` was the right primitive for the discarded gated version, not `requireAdminPageSession` — the latter logs a middleware-bypass security event, and an anonymous hit on a route deliberately outside the `src/proxy.ts` matcher is an ordinary public request, not a bypass. Logging it as one would poison that signal.
- The new route is registered in `ROUTES_REQUIRING_REAL_404` in `src/app/loadingBoundaries.test.ts`: it 404s unknown slugs *and* carries two `redirect()`s, both of which a `loading.tsx` above it would degrade.
- Drafts (`published: false`) still 404 here — `fetchPostRow` filters at the query boundary before the datetime comparison runs, so this overrides the schedule and nothing else. Pinned by a test.

## Session 2

Scheduled daily code review of `108e9c2`, then fixing what it found.

### Decisions

- **The hit log is a tagged `console.info`, not `auditLog`** — `ADMIN_AUDIT_TAGS` and `AdminAuditPayload` are typed to admin *writes*, and every key is required so aggregators can write one parser. Widening either for an anonymous public read would break the shape that docblock promises. Matches the `[blog:p]` / `[ProjectPage]` convention instead.
- **Only the served-body case logs** — a redirect to a live post discloses nothing, so logging it would bury the one event worth watching under crawler traffic. Pinned by a test asserting the redirect path logs nothing.
- **No admin affordance for the preview URL** — flagged in review, then withdrawn: the route was deliberately appended to the post path because you're always already on the post URL when you want a preview, so typing the suffix *is* the workflow. Roland's call, and the review item stays open as a note rather than a task.
- **The `force-dynamic` comment's body claim was wrong and got rewritten** — the flag does not keep the body fresh. The body comes from `fetchPostRow`'s cache entry tagged `postTag(section, slug)`, which `revalidatePost` busts on save, so an edit reaches the route either way. The verdict is the half with no tag to bust, since nothing mutates when a `datetime` merely passes.

### Tradeoffs

- **Mocking two auth modules the route doesn't import** — gains a standing guard that fails the moment a session call is added to the page; costs a mock that looks superfluous to anyone who doesn't read the comment explaining it. Reversibility: easy.

### Incidents / Bugs

- **Three comments still argued for the session gate removed in `108e9c2`** — `loadingBoundaries.test.ts` claimed "two `redirect()`s (to the login page…)" where there is one redirect and a `notFound()`; `posts.test.ts` called the query boundary a supplement to a gate that doesn't exist; the `force-dynamic` comment justified itself partly on an effect it doesn't have. Root cause: session 1's own cleanup pass fixed the source docblocks and missed the test comments. All three rewritten. Regression risk: none, comments only.

### What did I, Claude Code, or my agents learn from this

- **A test can be green for a reason that isn't the guard.** The "serves the body with no session" test passed only because an unmocked auth helper would throw in the test env — an incidental failure mode, not an assertion. Giving the auth mocks a *hostile* default (a session that validates) proved it: with a `verifySession()` call added to the page, the body still rendered and every other test stayed green; only the explicit not-called assertions caught it. When a test's name claims it guards an absence, check what actually fails when the thing is present.
- **Mutation-test each guard separately, not the suite as a whole.** Three mutations, three single-test failures — that one-to-one mapping is what says each guard watches its own invariant rather than one over-broad test covering for the rest.
- **Downgrade your own finding when the design already answered it.** The admin-affordance item was written as if it undercut the feature's purpose; the journal from session 1 already recorded why the appended-path shape makes it a non-issue. Reading the prior session's rationale before writing the finding, not after being asked, would have caught it.

### Notes

- `code-reviews/2026-09-03.md` carries the full review plus a resolution section. Six of eight items closed in-session; the two left `[ ]` are deliberate (the admin affordance above, and the enumeration cost now tracked as a watch-out).
- The review ran inline rather than split — 5 source files and 433 LOC, under both orchestration thresholds.
- **Preview-link unfurl thread closed: staying title-only.** Worth recording the argument that lost, so it isn't re-derived. `override-preview` inherited its metadata shape from the scheduled-notice page, where title-only is a *privacy* decision — that page is public and prerendered, so its summary would leak an unpublished post's content. That reason does not transfer: the preview route already renders the full body, so withholding `description`/`image` from the card protects nothing, and the two pages could reasonably diverge. Roland's call is to leave it; a preview link goes to someone who has already been told what it is. Live posts keep passing `description`, `image`, `publishedTime`, `type: "article"` and `markdownPath`; both scheduled surfaces pass only `title`, `path`, `feed`, and `buildPageMetadata` supplies `defaultOgImage`, so the card is plain rather than empty.

<!-- end -->
