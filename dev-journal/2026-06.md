# Dev Journal Monthly Rollup — 2026-06

_Generated 2026-06-08._

Sessions: 1 across 1 day. Source files: `2026-06-08.md` × 1.

## Themes

- **Project-import tooling** — built a project-agnostic importer that turns an app's marketing copy + screenshots into a portfolio project: `scripts/import-projects.ts` (runner), a per-project `project.json` manifest, and the `app-copy-to-project` skill that authors the manifest + stages images. Direct Prisma + direct Blob (the `seed-projects.ts` pattern), idempotent via deterministic blob keys and delete-by-slug. The design *narrowed* within the session — it began with extracted validation/mapper libs, then got trimmed once the importer was framed as handling *trusted first-party* files, so the upload route's untrusted-input MIME/size checks were dropped as ceremony. [source](dev-journal/2026-06-08.md)

## Decisions (chronological)

- **2026-06-08 — Manifest-driven importer** — chose a `project.json` + script + skill over parsing raw marketing markdown (no stable schema) or driving the admin HTTP API (needs server/auth/`ALLOW_UPLOADS`, random keys, auto-slug/sortOrder-shift). [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Direct Prisma + direct Blob** — own `PrismaClient` + `put()`, transactional delete-by-slug + create; idempotent deterministic keys `projects/<slug>/<path>`. [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Dropped importer image security checks** — reverted the `uploadValidation` extraction + route change; kept `projectCreateSchema` validation as data-integrity (powers `--dry-run`), not security. [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Kept `projectMappers` + `projectImport`** — mappers are shared by the admin POST/PUT routes and tested; `projectImport` holds the new tested slug/key/ref logic. Relocated mapper tests to `projectMappers.test.ts`. [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Project-level skill** — `app-copy-to-project` lives in the repo's `.claude/skills/`, reads the live schema contract instead of hardcoding it. [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Reckon platform + accent** — `bucket: iOS`, tags `[iOS, iPad]` (coherence rule blocks `macOS` under iOS); accent terracotta `#B5673F` (icon) over the app's slate-blue `AccentColor`, as the more balanced dual-theme value. [source](dev-journal/2026-06-08.md)

## Tradeoffs

- **2026-06-08 — Inlined `sanitizePathSegment`** — small dup of the route's `sanitizeFilename` to keep the importer free of Next-route deps; reversibility: easy. [source](dev-journal/2026-06-08.md)
- **2026-06-08 — Cache staleness after import** — direct-Prisma writes skip `revalidateProject`; new projects need a deploy/revalidation to surface; reversibility: easy. [source](dev-journal/2026-06-08.md)

## Incidents / Bugs

- **2026-06-08 — `scripts/` wholly gitignored** — root `.gitignore` `scripts/` would have silently excluded the new importer (and seeds); narrowed to `scripts/imports/`. Verified importer trackable, staging ignored. [source](dev-journal/2026-06-08.md)

## Environment / Config Changes

- **2026-06-08** — added `tsx` devDep + `db:import-projects` npm script; narrowed root `.gitignore` (`scripts/` → `scripts/imports/`); importer reads `DATABASE_URL` + `BLOB_READ_WRITE_TOKEN` via dotenv. [source](dev-journal/2026-06-08.md)

## Learnings

- **Xcode asset discovery** — app icon at `Assets.xcassets/AppIcon.appiconset/icon_1024.png`, accent at `AccentColor.colorset/Contents.json` (sRGB × 255 → hex); baked into the skill. [source](dev-journal/2026-06-08.md)
- **App Store link from app id** — build `https://apps.apple.com/app/id<id>`; one universal id covers iPhone/iPad/Mac. [source](dev-journal/2026-06-08.md)
- **Custom accents aren't theme-aware** — only the default accent is theme-split; a project's `accentColor` is one value for both themes, so mid-tones are always a contrast compromise. [source](dev-journal/2026-06-08.md)

## Still open at month-end

### Open Threads

- [ ] Run the real Reckon import (`yarn db:import-projects reckon`, prod creds) then redeploy/revalidate.
- [ ] Continuum — stage + author its manifest via `app-copy-to-project`, then import.
- [ ] Confirm Reckon metadata defaults + exact terracotta hex before import.
- [ ] Optional: delete now-redundant `scripts/imports/.gitignore`.

### Watch-outs & Tech Debt

- **Direct-Prisma import skips cache revalidation** — projects won't surface until a deploy/revalidation. [first flagged: 2026-06-08]
- **bucket↔tag coherence vs universal apps** — a universal iOS+Mac app can't tag `macOS` under the iOS bucket. [first flagged: 2026-06-08]
- **Single `accentColor` across themes** — custom accents are one value for light+dark; no mid-tone hits AA 4.5:1 on both backgrounds. [first flagged: 2026-06-08]
