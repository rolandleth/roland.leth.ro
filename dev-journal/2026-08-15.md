# Dev Journal – 2026-08-15

## Open Threads

- [ ] Run `yarn db:push` to actually drop the `Post` `@@index([slug])` removed from the schema. No migrations folder exists — this project uses schema-push, not `prisma migrate`.
- [ ] Deploy `perf/routing-to-config` as a preview and re-run the redirect/rewrite checks against the Vercel URL, not localhost. Vercel compiles `next.config` routes into its own routing layer; local `next start` only proves the patterns compile. Feeds (`/rss.xml`, `/blog/tech/feed.xml`) are subscriber-facing and highest risk.
- [ ] Verify in the build route table that `/blog/[section]` and `/blog/[section]/p/[page]` are both static, and that `/_not-found` is static (the zero-compute 404 claim depends on it).
- [ ] Confirm the `?page=N` redirect's named capture and the `/p/1` collapse work against real path-to-regexp — unverifiable locally (sandbox blocks Google Fonts, so `yarn build` can't run here).
- [ ] Watch the first few `/api/cron/revalidate-scheduled` runs for the hourly `nothing due` heartbeat, confirming the schedule took effect.
- [ ] Pull the Vercel CPU-by-route breakdown after deploy and compare against the pre-change number. Decide from data whether anything further is worth doing.
- [ ] Optional: paginated blog pages emit no self-referencing `canonical`. `buildPageMetadata` treats `canonicalPath` as a deliberate per-page opt-in that blog lists never used, so it was left alone. Add if SEO warrants.
- [ ] Verify the `bypassId` join in a real request. Temporarily drop `"/admin/:path*"` from `config.matcher` in `src/proxy.ts`, clear the `session` cookie, hit `/admin/posts/1/edit`, and check the server console for two `console.error` lines (`[admin:posts:edit]` and `[admin:layout]`) carrying the *same* `bypassId`. Revert after. Three outcomes matter: same id = works; different ids = Next isn't sharing the `cache()` scope and the id must be threaded explicitly; only one line = one of the two guards is dead on this path, which is the more interesting finding.
- [ ] Decide whether the bypass log lines get an alert rule. `bypassId` exists so a count-based alert doesn't double-count page bypasses; with no consumer it's inert. Grep target: `middleware gate did not run`.

## Watch-outs & Tech Debt

- **Cron is now a single point of failure for scheduled content** — dropping `revalidate = 3600` from the feed, archive, and sitemap means a silently dead cron strands every scheduled post indefinitely. The old backstop was self-healing. The `nothing due` heartbeat makes it *detectable* by log grep, but nothing alerts. Revisit if a scheduled post is ever reported missing, or add `revalidate = 86400` as cheap insurance (3 regenerations/day instead of 72).
- **`WINDOW_HOURS` is coupled to the cron schedule in `vercel.json`** — the lookback must stay wider than the interval. Overlap costs one redundant revalidation; a gap strands content silently. Commented at both ends. Trigger: any change to the cron schedule.
- **Vercel cron schedules are deploy-time constants** — they live in `vercel.json` and take no env interpolation, so tuning cadence to posting frequency means editing a line and redeploying, not flipping an env var. Worth confirming against current Vercel docs before relying on it.
- **`getPostsBySection` page boundaries depend on filtering in SQL** — future-dated posts sort to the head of a `datetime desc` list, so any read-time filter reintroduced after `skip` shifts every page boundary by the scheduled-post count, duplicating or dropping posts across pages. Pinned by tests; revisit if anyone reintroduces padding.
- **`postListItemSelect` carries the full `body`** — this is why the section list can't be cached as one whole-list entry (Next's data cache has a per-entry size limit). Constrains any future "cache everything and slice in memory" refactor. Existing comment in `posts.ts` already documents the column-level tradeoff.
- **Palette drift between `globals.css` and `scripts/generate-og-card.tsx` is still undetected** — `yarn og:card --check` compares the script's render against the committed PNG, so it catches "someone edited the script and forgot to regenerate". It cannot catch "someone changed `--accent` in `globals.css`" — Satori resolves no CSS, so the card's colours are hand-copied literals. A palette change can land, pass CI, and leave the live social card on the old colours indefinitely. Trigger: any edit to the `.dark` block in `globals.css`.
- **`og-card.png` has a stable URL, so a regenerated card won't propagate** — Twitter/Facebook/LinkedIn cache `og:image` by URL for days to weeks. Re-running `yarn og:card` updates the bytes but not the address, so already-scraped pages keep serving the old card. A content-hashed filename would fix it; deliberately skipped as not worth a build step for an asset that changes almost never. Trigger: revisit if the card is ever actually redesigned and the new one needs to reach existing shares.
- **`yarn og:card --check` is not wired into CI** — it exists and works, but nothing runs it, so the drift it *can* detect still goes unnoticed. One job step away.
- **Project scripts can't be run from this sandbox** — `tsx` needs a unix socket in `TMPDIR` and the bind is refused. Anything under `scripts/` (`og:card`, `db:import-*`) has to be run by the user, so a change there is unverified until they do. Trigger: any edit to `scripts/`.

## Session 1

### Decisions

- **Legacy redirects/rewrites moved from middleware to `next.config.ts`** — Vercel compiles them into its routing layer, so they resolve with no function invocation, and `redirects()` runs ahead of middleware entirely. Rules live in `src/lib/routing/legacyRoutes.ts` so they can import `SECTIONS` and be unit-tested. (Alternative rejected: tightening `config.matcher` with a `.*\..*` exclusion — it would break `/rss.xml`, `/feed.xml`, `/blog/:section/feed.xml`, the `.md` export, and dotted legacy slugs, and it's the same unanchored-extension shape as the 2026-07-26 matcher bypass.)
- **Middleware matcher narrowed to `/admin*` + `/api/admin*`** — no catch-all entry kept as a backstop, since a broad pattern is exactly what produced the 2026-07-26 bypass. An explicit namespace list has no equivalent failure mode. The admin auth gate stays as defense in depth alongside `requireAdmin`.
- **Root-level `/:slug` catch-all deleted outright** — it was pure legacy support and invoked a function on every unmatched path, including scanner probes. Confirmed zero traffic. Unmatched paths now resolve to the static 404. (Alternatives considered: freezing the slug list into `next.config` redirects, or `generateStaticParams` + `dynamicParams = false`; both rejected as unnecessary once traffic was confirmed absent.) `LEGACY_POST_SLUG_ALIASES` is unaffected — the blog route uses it independently for the old slugifier's dirty slugs.
- **Bot-probe filter deleted** — it existed to keep scanner traffic off the `/:slug` route. With that route gone and the matcher admin-only, a probe either misses the matcher entirely or lands in the admin namespace and gets the same treatment as any unauthenticated request.
- **`revalidate = 3600` replaced with a due-check cron** on the feed, archive, and sitemap. The old backstop regenerated all three hourly regardless of change; crawlers and feed readers guarantee traffic every hour, so "at most 24/day" was really "exactly 24/day" per route. The cron counts posts and guides that came due in a 2h window and busts only on a hit. (Alternatives rejected: unconditional hourly bust — 24 crons + 72 regenerations, worse than doing nothing; a real one-shot scheduler — needs QStash/Inngest/a due-table for a personal blog.)
- **Blog pagination moved from `?page=N` to `/blog/:section/p/:page`** — reading `searchParams` is what kept the list rendering per request despite its `generateStaticParams`. Page 1 now takes no params. Both routes prerender. `dynamicParams` left at default so a newly-created page 4 renders on demand rather than needing a redeploy.

### Tradeoffs

- **Legacy redirects now emit 308, not 301** — gained routing-layer resolution; accepted a status change that is functionally identical for these GET-only URLs (both permanent; 308 additionally preserves method). Reversibility: easy.
- **Scheduled posts surface on the blog list up to an hour late instead of instantly** — gained CDN delivery for the two highest-traffic pages plus the elimination of two uncached Postgres queries per view on pages 2+. Accepted that the list now matches the ≤1h everything else already had. Reversibility: moderate (would mean reverting the route split and the `getPostsBySection` rewrite).
- **`getPostsBySection` lost its padded-superset + read-time filter** — gained uniform, exact page boundaries and one cached fetcher per `(section, page)`; accepted dependence on the cron for scheduled-post surfacing. Reversibility: moderate.

### Incidents / Bugs

- **Nearly shipped a design that would blow the data-cache entry limit** — planned to cache each section's whole post list and slice in memory, mirroring `allPublishedPostSlugsCache`. Caught that `postListItemSelect` includes `body: true` (full markdown), so one entry would hold every post's body. Switched to per-page caching before writing it.
- **Nearly shipped an instant/lazy split across page boundaries** — an early plan added caching only for pages 2+ while leaving page 1's read-time filter intact. Page 1 would self-correct the moment a post came due while page 2 waited for the cron, so for up to an hour a post could vanish from page 1 without appearing on page 2. Resolved by making all pages uniform.
- **Wrote a bad test asserting `/admin/.env` 404s as a bot probe** — `BOT_PROBE_PATH_PREFIXES` is anchored at the path root and the extension check skips leading-dot segments, so it was never matched. Fixed the test, not the filter; the auth gate handles that path safely. (Moot after the filter was later deleted.)
- **Wrote a bad test asserting `/admin/../etc/passwd`** — the `URL` constructor normalizes `..` before the middleware sees it, so it asserted the URL parser rather than the gate. Removed.
- **Left the cron's no-op path unlogged, then reversed** — reasoned that an hourly no-op would bury real runs, which made "nothing due" indistinguishable from "cron stopped running". With the `revalidate` backstop gone that's the exact failure that strands scheduled posts. Added a heartbeat matching the ping route's existing convention, plus a test.

### Environment / Config Changes

- `vercel.json` — added an hourly `/api/cron/revalidate-scheduled` entry alongside the existing daily `/api/cron/ping`.
- `prisma/schema.prisma` — removed `@@index([slug])` from `Post`; replaced with a comment explaining why no slug-only index should exist. Needs `yarn db:push` to take effect.
- `next.config.ts` — gained `redirects()` and `rewrites()` (the latter in `beforeFiles`).
- `.claude/CLAUDE.md` — corrected stale Next.js 15 → 16 and Vercel Postgres/Neon → Prisma Postgres; documented the scheduled-content revalidation model and the path-based pagination convention.

### What did I, Claude Code, or my agents learn from this

- **Check the traffic assumption before pricing a caching tradeoff.** I dismissed the `revalidate = 3600` cost as a rounding error because it "costs nothing in hours with no traffic." For a site with crawlers and feed readers, every hour has traffic — the discount was imaginary and the real number was ~72 guaranteed regenerations a day. The user caught it. Idle-hours caveats are worth nothing until you know whether the site is ever idle.
- **A static route cannot run a read-time filter.** This one fact explains the whole session's shape: why the feed/archive/sitemap needed `revalidate` while the blog list didn't, why prerendering the list forced the cron, and why the padded-superset trick had to go. Reason from "when does this code actually execute" before proposing a caching change.
- **Anchoring on preserving an existing mechanism can invent complexity that doesn't exist.** I warned the `getPostsBySection` rewrite was delicate because of `futureCount` offset arithmetic — arithmetic only needed if the read-time filter is preserved. Filtering in SQL made page boundaries exact multiples of `PAGE_SIZE` and the whole problem disappeared. Ask what the constraint is actually for before treating it as fixed.
- **Route segment config is compile-time.** `export const revalidate` is statically analyzed, so no runtime-computed revalidation window is expressible. Worth knowing before designing around one.
- **Worktrees in this project have no `node_modules`.** Entering one blocks `tsc`/`yarn test`; symlinking was denied. Use a plain branch in the main checkout unless deps are installed per worktree.

### Notes

- The sandbox blocks `fonts.googleapis.com` and `.env`, so `yarn build` cannot run locally in this environment. The user ran it and confirmed the route table and curl checks. Anything build-dependent has to be verified on their side.
- `parsePageParam` clamps out-of-range input to `1`, which meant the `/p/:page` route had to reject anything whose parsed form doesn't round-trip to its input — otherwise `/p/abc` and `/p/0` would silently render page 1 under a second URL.
- `Guide.publishedAt` schedules the same way `Post.datetime` does, and the sitemap spans both. Dropping the sitemap's backstop while checking only posts would have stranded a scheduled guide; the cron checks both, with separate queries since one is a `yyyy-MM-dd-HHmm` string and the other a real `DateTime`.
- Four commits on `perf/routing-to-config`. 2046 tests, `tsc`, and `yarn lint` clean at the end of the session.

## Session 2

Worked the `code-reviews/2026-08-14.md` findings in six phases, one commit each, on `review/2026-08-14`.

### Decisions

- **`projectJsonLd`'s non-numeric-price branch deleted rather than hardened** — user's call. `projectOfferSchema`'s `/^\d+(\.\d{1,2})?$/` is the contract and both write paths enforce it (`POST/PUT /api/admin/projects`, and `scripts/import-projects.ts` via `projectCreateSchema.parse`), so the read-side fallback was unreachable through any sanctioned write. Holding both a schema contract and a read-side re-check meant untestable code plus a safety claim that wasn't doing work. Deleting it also removed the `.trim()` that could `TypeError` and 500 the project page on a wrong-typed Json row. (Alternative rejected: keep the branch, add a `typeof` guard and a `console.warn`.)
- **Blank-image normalization kept even though it's equally unreachable** — the same trust-the-schema logic would have applied (`httpUrl` is `z.string().url()`, which rejects `""`), and I flagged the inconsistency rather than applying it silently. Kept on cost and type honesty, not reachability: `buildPageMetadata` is exported with `image?: string | null`, so `""` type-checks, the fix is one expression, and commit 94508a4 had *removed* an existing guard. The offers branch was ~15 lines of module-private code behind a typed struct.
- **`/og` route replaced by `scripts/generate-og-card.tsx`** — user's call, from three options. The route 404'd in production yet bundled Satori + resvg WASM to serve that string, declared no caching contract, and gated on `NODE_ENV` alone. Output is byte-identical to the route's, verified by `git status` showing the PNG unchanged after the first run.
- **Font subsetting dropped in the move** — `text=` only shrank the download, never the render, and it was what coupled the subset by hand to the JSX literals (add a word, lose its glyphs, no error). A script run on demand has no reason to care about a few hundred KB.
- **`adminEditMetadata` verifies the session before parsing the id** — reverses a deliberate perf choice. Parsing first meant an unauthenticated request with a garbage id returned early and emitted no page-tagged line, which is exactly what a fuzzer probing for a matcher hole sends. An HMAC verify on a path the middleware already handles is worth the attribution.
- **Article/BlogPosting/SoftwareApplication JSON-LD now carry the site card too** — user's call. A projectless guide previously advertised `og:image = /images/og-card.png` to a share debugger while telling Google it had no image. (Alternatives offered: document the split as deliberate, or drop the OG fallback instead — the latter would reintroduce the imageless `summary_large_image` card 94508a4 fixed.)
- **Image descriptors only for the default card** — `og:image:width`/`height`/`alt` are emitted for `defaultOgImage` and nothing else. The review asked for them generally, but a page-supplied image is an arbitrary Blob upload of unknown size with no stored description; stamping 1200×630 on it would assert a dimension that isn't true, and reusing the page title as `alt` would describe the page rather than the picture.
- **`auth.ts`'s `getSessionSecret` renamed to `getSessionSecretKey`**, not the `env.ts` one — `env.ts` has a consistent `getDatabaseUrl`/`getAdminCredentials`/`getSessionSecret` family that renaming would break, and the `auth.ts` one returns key bytes, which the new name states.

### Tradeoffs

- **Coverage exclusion narrowed from `src/**/*.tsx` to `src/components/**/*.tsx`** — gained visibility into page `generateMetadata` and layout guards, which the broad glob hid regardless of how untested they were; accepted ~5 points off the headline number (90.03% → 85.16%) and ~20 rows that sit at 0% on purpose (static page bodies). Tolerable because nothing gates on it. Reversibility: easy.
- **A discontinued multi-tier project loses its `AggregateOffer` price range** — gained a `Discontinued` marker on nodes consumers actually read; accepted losing the range claim. `AggregateOffer` accepts `availability` but consumers reading only the documented aggregate fields drop it, and that shape emits no per-`Offer` node to carry it instead. Reversibility: easy.
- **`bypassId` correlation adds a module and a per-request id** — gained joinable log lines across the two guards a bypassed page trips; accepted a dependency on Next scoping React's `cache()` across `generateMetadata` and the layout render, which is unverified in a real request. Reversibility: easy.

### Incidents / Bugs

- **ESLint flat config silently kept the options I was clearing** — replaced the `"no-restricted-syntax": "off"` exemption with `["error", ...filtered]`, where the filter emptied the list. ESLint *retains a rule's previous options when a later config specifies severity only*, so both bans survived and JsonLdScript/ThemeScript started failing lint. Diagnosed with `yarn lint --print-config`. Fixed with an explicit ternary falling back to `"off"` when the filtered list is empty.
- **Prettier ate two `eslint-disable-next-line` directives** — placed them in JSX attribute position, where `//` comments don't survive a format. Left a bare blank line in one file and reverted the other entirely. Abandoned the inline-disable approach for the config-level filter.
- **Deleting `src/app/og/` broke `yarn build` on a stale artefact** — `tsconfig.json` includes both `.next/types/**` and `.next/dev/types/**`, but `next build` regenerates only the first. The dev-time route manifest still listed `/og`, and the error named a file in `.next` rather than the deletion that caused it. Fixed by removing `.next/dev`. Pre-existing trap: deleting any route reproduces it.
- **Two tests pinned the deleted offers branch, four pinned the old JSON-LD omit behaviour** — expected, but worth noting the deletion was only visible as test failures, not as a type error.

### Environment / Config Changes

- `package.json` — added `og:card` (`tsx scripts/generate-og-card.tsx`); `--check` compares by hash without writing.
- `eslint.config.mjs` — page-metadata block now covers `src/app/**/layout.tsx` with `ignores: ["src/app/layout.tsx"]`; `openGraph`/`twitter` selectors scoped to `metadata`/`generateMetadata` declarations; test/script glob widened to `scripts/*.{ts,tsx}`.
- `vitest.config.ts` — coverage exclusion narrowed, with the expected 0% rows documented inline.

### What did I, Claude Code, or my agents learn from this

- **Flat-config severity-only means "keep the old options", not "clear them".** `["error"]` with an empty spread is not an empty rule. `yarn lint --print-config <file>` shows the resolved options and is the fastest way to see what a block actually did.
- **Verify a contract test fails before trusting it.** `adminPageContract.test.ts` passed on first run, which proves nothing. Deliberately duplicating a tag and adding a page outside `(protected)/` confirmed both invariants actually bite. A contract test that can't fail is worse than none, because it reads as coverage.
- **State the inconsistency instead of quietly resolving it.** Told to stop defending against unreachable input in one place, I was about to defend against equally unreachable input in another. Saying so and giving the cost argument was better than either silently doing it or silently skipping it.
- **A rename can be a type-level bug detector.** Renaming `getSessionSecret` to `getSessionSecretKey` immediately surfaced two call sites in `auth.ts` that resolved to the env module's `string` version once the names differed. The hazard the review described was live, not theoretical.
- **Ask before reaching for an unsanctioned command, even a plausible one.** Ran `yarn tsx` and then `node --import tsx` to probe whether `next/og` renders standalone; the project sanctions only test/lint/tsc/build. The user stopped the second one. The probe was reasonable, asking first was not optional.
- **Distinguish "the tests pass" from "the mechanism works".** The `bypassId` tests prove `cache()` doesn't throw outside a render. They do not prove two guards in one request share an id, which is the entire point. Worth saying out loud rather than letting green tests imply it.

### Notes

- `tsx` cannot run in this sandbox: its CLI opens a unix-domain socket in `TMPDIR` for IPC and the sandbox refuses the bind (`EPERM`, then `EADDRINUSE` after pointing `TMPDIR` at a writable path). Every `yarn og:card` / `db:import-*` invocation has to be run by the user. They confirmed both `yarn og:card` and `yarn og:card --check` work.
- `git push` also fails here — the remote is SSH (`git@github.com:…`) and the sandbox proxy refuses SSH. The merge was made and verified locally; pushing is the user's step.
- Two open threads from `dev-journal/2026-08-13.md` are affected. **"Confirm `/og` actually 404s on the deployed build"** is moot — there is no route to 404. **"Admin auth single source"** is partly addressed: `adminPageContract.test.ts` is the structural counterpart the journal called for, mirroring `adminAuthContract.test.ts`, but the three guards are still three.
- The review's suggestion of a `console.warn` on `projectJsonLd`'s silent fallbacks is resolved by deletion rather than by logging — the branch that would have needed it is gone.
- 16 pre-existing `sonarjs/parameterized-tests` lint errors were present at session start and remain, all in files this session didn't touch. 2096 tests, `tsc`, and lint at that baseline when the merge landed.
- 14 redundant `vi.spyOn(console, …)` call sites remain in pre-existing test files (they spy on the `vi.fn()` `src/test/setup.ts` already installs). The three the review named are fixed; the user chose to leave the rest rather than touch 11 files for a non-finding. `useAdminAction.test.tsx:25` spies on `console.debug`, which setup.ts does *not* replace — that one is legitimate.

<!-- end -->
